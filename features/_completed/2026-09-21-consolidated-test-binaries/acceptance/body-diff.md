# Acceptance 5: source changes by kind

Generated by `acceptance/body-diff.py`. Old file at the package's base
revision against the new module at `HEAD`. See the script header for the
classification rules.

| Package | Base | Modules | Byte-identical | Structural lines | Comment lines | Other lines | Modules with other lines |
|---|---|---:|---:|---:|---:|---:|---:|
| `claudine-cli` | `9621882ae` | 139 | 3 | 523 | 74 | 302 | 9 |
| `darkmatter` | `048e44f7a` | 74 | 59 | 22 | 10 | 20 | 6 |
| `darkmatter-cli` | `cb9a3d38b` | 53 | 1 | 113 | 20 | 46 | 1 |
| `biscuit-terminal` | `beca6c368` | 38 | 9 | 67 | 6 | 0 | 0 |

## Other lines, by module

### `claudine-cli`: `claudine/cli/tests/compose_caller_file_provenance.rs` → `claudine/cli/tests/l1/compose_caller_file_provenance.rs`

```diff
- write(&router, include_str!("../../../prompts/implement.md"));
+ write(&router, include_str!("../../../../prompts/implement.md"));
- include_str!("../../../prompts/_implement/implement-suggestions.md"),
+ include_str!("../../../../prompts/_implement/implement-suggestions.md"),
- include_str!("../../../prompts/_no_formatting.md"),
+ include_str!("../../../../prompts/_no_formatting.md"),
- include_str!("../../../prompts/_os.md"),
+ include_str!("../../../../prompts/_os.md"),
- include_str!("fixtures/shipped_implement_route/_implement/implement-plan.md"),
+ include_str!("../fixtures/shipped_implement_route/_implement/implement-plan.md"),
- write(&router, include_str!("../../../prompts/implement.md"));
+ write(&router, include_str!("../../../../prompts/implement.md"));
- write(&router, include_str!("../../../prompts/review.md"));
+ write(&router, include_str!("../../../../prompts/review.md"));
- include_str!("../../../prompts/review.md"),
+ include_str!("../../../../prompts/review.md"),
- write(&router, include_str!("../../../prompts/review.md"));
+ write(&router, include_str!("../../../../prompts/review.md"));
- write(&router, include_str!("../../../prompts/review.md"));
+ write(&router, include_str!("../../../../prompts/review.md"));
```

### `claudine-cli`: `claudine/cli/tests/dispatch_inventory.rs` → `claudine/cli/tests/l1/dispatch_inventory.rs`

```diff
- const REGEN_COMMAND: &str =
- "CLAUDINE_UPDATE_INVENTORY=1 cargo nextest run -p claudine-cli --test dispatch_inventory";
+ const REGEN_COMMAND: &str = "CLAUDINE_UPDATE_INVENTORY=1 just test-cli dispatch_inventory:: (in claudine/)";
- tool: "claudine-cli/tests/dispatch_inventory.rs",
+ tool: "claudine-cli/tests/l1/dispatch_inventory.rs",
- GUARD_ALLOWLIST (cli/tests/dispatch_inventory.rs). Migrate it to a ProviderInfo \
+ GUARD_ALLOWLIST (cli/tests/l1/dispatch_inventory.rs). Migrate it to a ProviderInfo \
```

### `claudine-cli`: `claudine/cli/tests/error_guards.rs` → `claudine/cli/tests/l1/error_guards.rs`

```diff
- const ALLOWLIST_FILE: &str = "cli/tests/error_guards/transport-allow.toml";
+ const ALLOWLIST_FILE: &str = "cli/tests/l1/error_guards/transport-allow.toml";
- const BOXED_ALLOWLIST_FILE: &str = "cli/tests/error_guards/boxed-diagnostic-allow.toml";
+ const BOXED_ALLOWLIST_FILE: &str = "cli/tests/l1/error_guards/boxed-diagnostic-allow.toml";
```

### `claudine-cli`: `claudine/cli/tests/sequence_schema.rs` → `claudine/cli/tests/l1/sequence_schema.rs`

```diff
- fs::write(&target, include_str!("../../../prompts/review.md")).unwrap();
+ fs::write(&target, include_str!("../../../../prompts/review.md")).unwrap();
```

### `claudine-cli`: `claudine/cli/tests/shipped_prompt_contract.rs` → `claudine/cli/tests/l1/shipped_prompt_contract.rs`

```diff
- "fixtures/nested_span_regression/review-spec-inline.md"
+ "../fixtures/nested_span_regression/review-spec-inline.md"
- include_str!("fixtures/shipped_implement_route/_implement/implement-plan.md"),
+ include_str!("../fixtures/shipped_implement_route/_implement/implement-plan.md"),
```

### `claudine-cli`: `claudine/cli/tests/shipped_prompt_route_drift.rs` → `claudine/cli/tests/l1/shipped_prompt_route_drift.rs`

```diff
- \x20 CLAUDINE_UPDATE_SHIPPED_PROMPT_HASHES=1 cargo nextest run -p claudine-cli \
- --test shipped_prompt_route_drift"
+ \x20 CLAUDINE_UPDATE_SHIPPED_PROMPT_HASHES=1 just test-cli \
+ shipped_prompt_route_drift:: (in claudine/)"
```

### `claudine-cli`: `claudine/cli/tests/spawn_site_guard.rs` → `claudine/cli/tests/l1/spawn_site_guard.rs`

```diff
- cli/tests/spawn_site_guard.rs with a one-line reason.\nUnlisted sites:\n{}",
+ cli/tests/l1/spawn_site_guard.rs with a one-line reason.\nUnlisted sites:\n{}",
- cli/tests/spawn_site_guard.rs with a one-line reason.\nEscapes:\n{}",
+ cli/tests/l1/spawn_site_guard.rs with a one-line reason.\nEscapes:\n{}",
- "wrap_basics.rs",
- "sequence_cli.rs",
- "wrap_ctrl_c_windows.rs",
+ "l1/wrap_basics.rs",
+ "l1/sequence_cli.rs",
+ "l1/wrap_ctrl_c_windows.rs",
- "level1_dry_run_pty.rs",
- "level1_provided_partial_file_pty.rs",
- "level1_pty_wrapper_summary.rs",
- "level1_schema_prompt_pty.rs",
+ "l1/level1_dry_run_pty.rs",
+ "l1/level1_provided_partial_file_pty.rs",
+ "l1/level1_pty_wrapper_summary.rs",
+ "l1/level1_schema_prompt_pty.rs",
- "wrap_basics.rs",
- "cli_process_fixture.rs",
- "ctx_launch_anchor.rs",
- "propagated_context_fixtures.rs",
+ "l1/wrap_basics.rs",
+ "l1/cli_process_fixture.rs",
+ "l1/ctx_launch_anchor.rs",
+ "l1/propagated_context_fixtures.rs",
- "compose_schema_cli.rs",
- "sequence_cli.rs",
- "loop_cli.rs",
+ "l1/compose_schema_cli.rs",
+ "l1/sequence_cli.rs",
+ "l1/loop_cli.rs",
- "level1_dry_run_pty.rs",
- "level1_provided_partial_file_pty.rs",
- "level1_pty_wrapper_summary.rs",
- "level1_schema_prompt_pty.rs",
+ "l1/level1_dry_run_pty.rs",
+ "l1/level1_provided_partial_file_pty.rs",
+ "l1/level1_pty_wrapper_summary.rs",
+ "l1/level1_schema_prompt_pty.rs",
- assert!(!governed.contains("system_prompt_perf_bench.rs"));
+ assert!(!governed.contains("l1/system_prompt_perf_bench.rs"));
- assert!(!governed.contains("level2_context_capture.rs"));
+ assert!(!governed.contains("level2/level2_context_capture.rs"));
- assert!(!governed.contains("spawn_site_guard.rs"));
+ assert!(!governed.contains("l1/spawn_site_guard.rs"));
```

### `claudine-cli`: `claudine/cli/tests/test_placement.rs` → `claudine/cli/tests/l1/test_placement.rs`

```diff
+ const DATA_DIRECTORIES: &[&str] = &["fixtures", "snapshots"];
+ path: Option<String>,
+ fn path_attribute(attribute: &str) -> Option<String> {
+ let inner = attribute.strip_prefix("#[")?.trim_start();
+ let value = inner.strip_prefix("path")?.trim_start().strip_prefix('=')?;
+ let value = value.trim_start().strip_prefix('"')?;
+ Some(value[..value.find('"')?].to_string())
+ fn module_declarations(source: &str) -> Vec<ModuleDeclaration> {
+ let code = sanitize(source);
+ let mut declarations = Vec::new();
+ let mut index = 0;
+ while index < code.len() {
+ if index > 0 && is_ident(code[index - 1]) {
+ index += 1;
+ let mut cursor = index;
+ let mut path = None;
+ while code.get(cursor) == Some(&b'#') && code.get(cursor + 1) == Some(&b'[') {
+ let Some(close) = matching_delimiter(&code, cursor + 1, b'[') else {
+ path = path.or_else(|| path_attribute(&source[cursor..=close]));
+ cursor = skip_whitespace(&code, close + 1);
+ cursor = skip_visibility(&code, cursor);
+ if code.get(cursor..cursor + 3) == Some(b"mod")
+ && code.get(cursor + 3).is_some_and(u8::is_ascii_whitespace)
+ let name_start = skip_whitespace(&code, cursor + 3);
+ let mut name_end = name_start;
+ while code.get(name_end).is_some_and(|&byte| is_ident(byte)) {
+ name_end += 1;
+ let after = skip_whitespace(&code, name_end);
+ if name_end > name_start && code.get(after) == Some(&b';') {
+ declarations.push(ModuleDeclaration {
+ name: source[name_start..name_end].to_string(),
+ });
+ index = after + 1;
+ index = cursor.max(index + 1);
+ fn normalize_relative(path: &str) -> String {
+ let mut parts: Vec<&str> = Vec::new();
+ for part in path.split('/') {
+ "" | "." => {}
+ ".." => {
+ parts.pop();
+ part => parts.push(part),
+ parts.join("/")
+ fn declared_module_files(file: &str, declaration: &ModuleDeclaration) -> Vec<String> {
+ let (directory, name) = file.rsplit_once('/').unwrap_or(("", file));
+ if let Some(path) = &declaration.path {
+ return vec![normalize_relative(&format!("{directory}/{path}"))];
+ let base = if name == "main.rs" || name == "mod.rs" {
+ directory.to_string()
+ format!("{directory}/{}", name.trim_end_matches(".rs"))
+ vec![
+ normalize_relative(&format!("{base}/{}.rs", declaration.name)),
+ normalize_relative(&format!("{base}/{}/mod.rs", declaration.name)),
+ ]
+ fn layout_violations(manifest: &str, files: &BTreeMap<String, String>) -> Vec<String> {
+ let mut violations = Vec::new();
+ let manifest: toml::Table = match toml::from_str(manifest) {
+ Ok(table) => table,
+ Err(error) => return vec![format!("Cargo.toml does not parse: {error}")],
+ let autotests = manifest
+ .get("package")
+ .and_then(|package| package.get("autotests"))
+ .and_then(toml::Value::as_bool);
+ if autotests != Some(false) {
+ violations.push(
+ "Cargo.toml must set `autotests = false`: every test target is declared \
+ explicitly"
+ .to_string(),
+ );
+ let mut pending = Vec::new();
+ .get("test")
+ .and_then(toml::Value::as_array)
+ .into_iter()
+ .flatten()
+ let name = target.get("name").and_then(toml::Value::as_str).unwrap_or("<unnamed>");
+ match target.get("path").and_then(toml::Value::as_str) {
+ Some(path) if files.contains_key(&normalize_relative(path)) => {
+ pending.push(normalize_relative(path));
+ Some(path) => violations.push(format!("[[test]] {name}: {path} does not exist")),
+ None => violations.push(format!("[[test]] {name}: declare its `path` explicitly")),
+ let mut reached = BTreeSet::new();
+ while let Some(file) = pending.pop() {
+ if !reached.insert(file.clone()) {
+ for declaration in module_declarations(&files[&file]) {
+ if let Some(found) = declared_module_files(&file, &declaration)
+ .into_iter()
+ .find(|candidate| files.contains_key(candidate))
+ pending.push(found);
+ for file in files.keys().filter(|file| !reached.contains(*file)) {
+ let (directory, name) = file.rsplit_once('/').unwrap_or(("", file));
+ violations.push(if directory == "tests" {
+ format!(
+ "{file}: a top-level test file is never compiled; move it into a declared \
+ target's directory and declare it in that target's main.rs"
+ )
+ } else if name == "main.rs" {
+ format!(
+ "{file}: an undeclared test crate root; add it to an existing target, or \
+ declare a [[test]] for a new execution contract"
+ )
+ format!("{file}: no declared test target compiles this module; declare it with `mod`")
+ });
+ fn collect_test_sources(
+ crate_root: &Path,
+ directory: &Path,
+ files: &mut BTreeMap<String, String>,
+ ) -> std::io::Result<()> {
+ for entry in fs::read_dir(directory)? {
+ let path = entry?.path();
+ if path.is_dir() {
+ if !path
+ .file_name()
+ .is_some_and(|name| DATA_DIRECTORIES.iter().any(|data| name == *data))
+ collect_test_sources(crate_root, &path, files)?;
+ } else if path.extension().is_some_and(|extension| extension == "rs") {
+ let relative = normalized(path.strip_prefix(crate_root).unwrap_or(&path));
+ files.insert(relative, fs::read_to_string(&path)?);
+ Ok(())
+ fn every_test_source_is_compiled_by_a_declared_target() {
+ let cli_root = biscuit_test_harness::manifest_dir!();
+ let manifest = fs::read_to_string(cli_root.join("Cargo.toml")).expect("read Cargo.toml");
+ let mut files = BTreeMap::new();
+ collect_test_sources(&cli_root, &cli_root.join("tests"), &mut files).expect("scan tests/");
+ assert!(files.len() > 140, "scanned only {} test source file(s)", files.len());
+ for expected in [
+ "tests/l1/main.rs",
+ "tests/l1/sequence_ctrl_c_windows.rs",
+ "tests/l1/error_guards/source_scan.rs",
+ "tests/level3/level3_linux_sequence_ctrl_c.rs",
+ "tests/common/pty.rs",
+ ] {
+ assert!(files.contains_key(expected), "{expected} was not scanned");
+ let violations = layout_violations(&manifest, &files);
+ assert!(
+ violations.is_empty(),
+ "test sources outside every declared test target:\n{}",
+ violations.join("\n")
+ );
+ fn layout_gate_rejects_stray_roots_and_undeclared_modules() {
+ const MANIFEST: &str = "[package]\nname = \"fixture\"\nautotests = false\n\n\
+ [[test]]\nname = \"l1\"\npath = \"tests/l1/main.rs\"\n";
+ let tree = |extra: &[(&str, &str)]| {
+ let mut files: BTreeMap<String, String> = [
+ (
+ "tests/l1/main.rs",
+ "#[path = \"../common/mod.rs\"]\nmod common;\n\n#[cfg(windows)]\nmod windows_only;\nmod guard;\n",
+ ),
+ ("tests/l1/windows_only.rs", "#[test]\nfn case() {}\n"),
+ ("tests/l1/guard.rs", "mod scan;\n"),
+ ("tests/l1/guard/scan.rs", ""),
+ ("tests/common/mod.rs", "pub(crate) mod pty;\n"),
+ ("tests/common/pty.rs", ""),
+ ]
+ .into_iter()
+ .map(|(path, source)| (path.to_string(), source.to_string()))
+ .collect();
+ for (path, source) in extra {
+ files.insert(path.to_string(), source.to_string());
+ assert_eq!(layout_violations(MANIFEST, &tree(&[])), Vec::<String>::new());
+ let stray = layout_violations(MANIFEST, &tree(&[("tests/wrap_env.rs", "#[test]\nfn a() {}\n")]));
+ assert_eq!(stray.len(), 1);
+ assert!(stray[0].starts_with("tests/wrap_env.rs: a top-level test file"), "{stray:?}");
+ let root = layout_violations(MANIFEST, &tree(&[("tests/level9/main.rs", "")]));
+ assert_eq!(root.len(), 1);
+ assert!(root[0].contains("undeclared test crate root"), "{root:?}");
+ let modules = layout_violations(
+ &tree(&[
+ ("tests/l1/orphan.rs", ""),
+ ("tests/l1/guard/unused.rs", ""),
+ ("tests/l1/quoted.rs", ""),
+ ("tests/common/wrap.rs", "// mod quoted;\nconst S: &str = \"mod quoted;\";\n"),
+ ]),
+ );
+ assert_eq!(
+ modules.iter().map(|v| v.split(':').next().unwrap()).collect::<Vec<_>>(),
+ ["tests/common/wrap.rs", "tests/l1/guard/unused.rs", "tests/l1/orphan.rs", "tests/l1/quoted.rs"]
+ );
+ fn layout_gate_requires_explicit_targets() {
+ let files = BTreeMap::from([("tests/l1/main.rs".to_string(), String::new())]);
+ let autodiscovered = layout_violations(
+ "[package]\nname = \"fixture\"\n\n[[test]]\nname = \"l1\"\npath = \"tests/l1/main.rs\"\n",
+ &files,
+ );
+ assert_eq!(autodiscovered.len(), 1);
+ assert!(autodiscovered[0].contains("autotests = false"));
+ let unpathed = layout_violations(
+ "[package]\nname = \"fixture\"\nautotests = false\n\n[[test]]\nname = \"l1\"\n\n\
+ [[test]]\nname = \"gone\"\npath = \"tests/gone/main.rs\"\n",
+ &files,
+ );
+ assert!(unpathed.iter().any(|v| v == "[[test]] l1: declare its `path` explicitly"), "{unpathed:?}");
+ assert!(unpathed.iter().any(|v| v == "[[test]] gone: tests/gone/main.rs does not exist"), "{unpathed:?}");
+ assert!(unpathed.iter().any(|v| v.starts_with("tests/l1/main.rs: an undeclared test crate root")));
+ fn module_declarations_read_attributes_visibility_and_path() {
+ let source = "//! mod in_docs;\n#[cfg(unix)]\n#[path = \"../common/mod.rs\"]\nmod common;\n\
+ assert_eq!(
+ module_declarations(source),
+ [
+ ModuleDeclaration { name: "common".into(), path: Some("../common/mod.rs".into()) },
+ ModuleDeclaration { name: "helpers".into(), path: None },
+ ]
+ );
+ assert_eq!(
+ declared_module_files("tests/l1/main.rs", &module_declarations("#[path = \"../common/mod.rs\"] mod common;")[0]),
+ ["tests/common/mod.rs"]
+ );
+ assert_eq!(
+ declared_module_files("tests/l1/error_guards.rs", &module_declarations("mod source_scan;")[0]),
+ ["tests/l1/error_guards/source_scan.rs", "tests/l1/error_guards/source_scan/mod.rs"]
+ );
```

### `claudine-cli`: `claudine/cli/tests/wrap_compose_validation.rs` → `claudine/cli/tests/l1/wrap_compose_validation.rs`

```diff
- let review = include_str!("fixtures/nested_span_regression/review-spec-inline.md");
+ let review = include_str!("../fixtures/nested_span_regression/review-spec-inline.md");
- let commit = include_str!("fixtures/nested_span_regression/commit.md");
+ let commit = include_str!("../fixtures/nested_span_regression/commit.md");
- const INCIDENT: &str = include_str!("fixtures/nested_span_regression/review-spec-inline.md");
+ const INCIDENT: &str = include_str!("../fixtures/nested_span_regression/review-spec-inline.md");
```

### `darkmatter`: `darkmatter/lib/tests/as_block_error_registry.rs` → `darkmatter/lib/tests/l1/as_block_error_registry.rs`

```diff
- let registry_source = include_str!("../src/markdown/errors/mod.rs");
+ let registry_source = include_str!("../../src/markdown/errors/mod.rs");
```

### `darkmatter`: `darkmatter/lib/tests/benchmark_fixtures.rs` → `darkmatter/lib/tests/l1/benchmark_fixtures.rs`

```diff
- # --test benchmark_fixtures`. Verified by benchmark_fixtures.rs.\n";
+ # --test l1 benchmark_fixtures::`. Verified by benchmark_fixtures.rs.\n";
```

### `darkmatter`: `darkmatter/lib/tests/inline_document_text.rs` → `darkmatter/lib/tests/l1/inline_document_text.rs`

```diff
- include_str!("../../tests/fixtures/schema-triggers/docs/claudine.md"),
+ include_str!("../../../tests/fixtures/schema-triggers/docs/claudine.md"),
- include_str!("../../tests/fixtures/schema-triggers/docs/inline-compose.md"),
+ include_str!("../../../tests/fixtures/schema-triggers/docs/inline-compose.md"),
- include_str!("../../tests/fixtures/schema-triggers/docs/plain.md"),
+ include_str!("../../../tests/fixtures/schema-triggers/docs/plain.md"),
- include_str!("../../tests/fixtures/schema-triggers/docs/sequence.md"),
+ include_str!("../../../tests/fixtures/schema-triggers/docs/sequence.md"),
```

### `darkmatter`: `darkmatter/lib/tests/predict_conflicts.rs` → `darkmatter/lib/tests/l1/predict_conflicts.rs`

```diff
- "../../docs/schemas/expression-functions.yaml"
+ "../../../docs/schemas/expression-functions.yaml"
```

### `darkmatter`: `darkmatter/lib/tests/schema_phase_validation.rs` → `darkmatter/lib/tests/l1/schema_phase_validation.rs`

```diff
- let text = include_str!("../../../claudine/cli/tests/fixtures/shipped_implement_route/_implement/implement-plan.md");
+ let text = include_str!("../../../../claudine/cli/tests/fixtures/shipped_implement_route/_implement/implement-plan.md");
```

### `darkmatter`: `darkmatter/lib/tests/suggest_constraint_phase1.rs` → `darkmatter/lib/tests/l1/suggest_constraint_phase1.rs`

```diff
- let inline = include_str!("../../dmls/tests/fixtures/suggest_constraint/inline.md");
- let completion = include_str!("../../dmls/tests/fixtures/suggest_constraint/completion.md");
+ let inline = include_str!("../../../dmls/tests/fixtures/suggest_constraint/inline.md");
+ let completion = include_str!("../../../dmls/tests/fixtures/suggest_constraint/completion.md");
```

### `darkmatter-cli`: `darkmatter/cli/tests/spawn_site_guard.rs` → `darkmatter/cli/tests/l1/spawn_site_guard.rs`

```diff
- file: "md_process_fixture.rs",
+ file: "l1/md_process_fixture.rs",
+ if let Some((directory, _)) = relative.split_once('/')
+ && EXCLUDED_PREFIXES
+ .iter()
+ .any(|prefix| prefix.strip_suffix('_') == Some(directory))
- cli/tests/spawn_site_guard.rs naming the specific technical necessity.\n\
+ cli/tests/l1/spawn_site_guard.rs naming the specific technical necessity.\n\
- cli/tests/spawn_site_guard.rs saying which command it targets.\nEscapes:\n{}",
+ cli/tests/l1/spawn_site_guard.rs saying which command it targets.\nEscapes:\n{}",
+ assert!(excluded("level2/level2_errors.rs"));
+ assert!(excluded("level2/harness_integrity.rs"));
+ assert!(excluded("level2/main.rs"));
+ assert!(!excluded("l1/clean.rs"));
+ assert!(!excluded("l1/main.rs"));
+ assert!(!excluded("level2x/clean.rs"));
+ assert!(!excluded("l1/level2/clean.rs"));
- "clean.rs",
- "code_block.rs",
- "schema_validate.rs",
- "schema_validate_baseline.rs",
- "toc.rs",
- "md_process_fixture.rs",
- "spawn_site_guard.rs",
+ "l1/clean.rs",
+ "l1/code_block.rs",
+ "l1/schema_validate.rs",
+ "l1/schema_validate_baseline.rs",
+ "l1/toc.rs",
+ "l1/md_process_fixture.rs",
+ "l1/spawn_site_guard.rs",
- assert!(names.contains("layout_alignment.rs"));
- assert!(!names.contains("level2_errors.rs"));
+ assert!(names.contains("l1/layout_alignment.rs"));
+ assert!(!names.contains("level2/level2_errors.rs"));
+ assert!(!names.contains("level2/harness_integrity.rs"));
- .find(|(file, source)| file == "layout_alignment.rs" && spawn_sites(source).is_empty())
+ .find(|(file, source)| file == "l1/layout_alignment.rs" && spawn_sites(source).is_empty())
- governed.contains("md_process_fixture.rs"),
+ governed.contains("l1/md_process_fixture.rs"),
- assert!(governed.contains("clean.rs"));
+ assert!(governed.contains("l1/clean.rs"));
- assert!(!governed.contains("level2_errors.rs"));
+ assert!(!governed.contains("level2/level2_errors.rs"));
- assert!(!governed.contains("spawn_site_guard.rs"));
+ assert!(!governed.contains("l1/spawn_site_guard.rs"));
```

