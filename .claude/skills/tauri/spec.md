# Convert React to VueJS

This specification is about converting an agent skill on Tauri and maintaining as much of it's ideas while migrating the frontend to use VueJS as the frontend framework instead of
