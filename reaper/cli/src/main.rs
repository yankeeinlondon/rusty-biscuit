//! Reaper CLI entrypoint.
//!
//! Placeholder binary: the user-facing command surface is delivered by the
//! specifications under `reaper/features/`. It exists so the `reaper-cli`
//! workspace member is buildable.

fn main() {
    eprintln!("reaper: not yet implemented; see reaper/features/ for the in-progress design");
    std::process::exit(1);
}
