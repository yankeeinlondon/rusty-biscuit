//! Reaper library.
//!
//! Reaper extracts context from web pages and sites as deterministically as
//! possible, deferring judgment-requiring work to an injected
//! [`biscuit_contract::inference::InferenceAdapter`]. This crate is a
//! placeholder: the implementation is tracked under `reaper/features/` and is
//! delivered by its own specifications. It exists so the workspace is loadable
//! by `cargo metadata` (the documented source of truth for membership).
