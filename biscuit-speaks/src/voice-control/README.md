# Voice Control

There are standards like SSML, LLM-TTS, and EmotionML which allows greater expressiveness to be expressed in the text that is passed through to TTS. This directory tree is a module focused on parsing, stripping, and creating voice control.

NOTE: this has not yet been implemented.
