This folder and sub-folders are all related to interacting with and extending the `rig-core` crate (and it's ecosystem).

When working inside this directory tree, you should always use the `rig` skill!
