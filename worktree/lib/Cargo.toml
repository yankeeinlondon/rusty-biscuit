[package]
name = "worktree"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"

[dependencies]
dirs = "6"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sniff = { path = "../../sniff/lib" }
thiserror = "2.0"
which = "8.0"

[features]
count-git = []

[dev-dependencies]
serial_test = "3"
tempfile = "3"
