$schema:
    Knowledge:
      - "{ state: literal(known), value: number(integer; min(0); required) }(required)"
      - "{ state: literal(unknown), gap: string(required) }"
    Fact: "{ id: string(required), knowledge: Knowledge@this }"
