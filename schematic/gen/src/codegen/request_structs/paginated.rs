//! Paginated request struct generation.
//!
//! Reserved for future shape-specific handling of paginated endpoints.
//! Currently, pagination is handled via the `Paginated` trait in [`crate::codegen::traits`].
