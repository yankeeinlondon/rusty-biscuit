// This code was automatically generated by schematic-gen. Do not edit manually.

use serde::{Deserialize, Serialize};
use crate::shared::{Paginated, RequestParts, SchematicError};
use super::responses::*;
/// Request for `GetRepository` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::GetRepositoryRequest;
///
/// let request = GetRepositoryRequest::new("owner_value", "repo_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetRepositoryRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
}
impl GetRepositoryRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(owner: impl Into<String>, repo: impl Into<String>) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repos/{}/{}", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetRepositoryRequest {
    type Response = RepositoryInfo;
    const ENDPOINT_ID: &'static str = "GetRepository";
}
/// Request for `GetGitTree` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::GetGitTreeRequest;
///
/// let request = GetGitTreeRequest::new("owner_value", "repo_value", "sha_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetGitTreeRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: sha
    pub sha: String,
}
impl GetGitTreeRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        sha: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            sha: sha.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repos/{}/{}/git/trees/{}", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string()), urlencoding::encode(& self.sha
            .to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetGitTreeRequest {
    type Response = GitTreeResponse;
    const ENDPOINT_ID: &'static str = "GetGitTree";
}
/// Request for `GetGitTreeRecursive` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::GetGitTreeRecursiveRequest;
///
/// let request = GetGitTreeRecursiveRequest::new("owner_value", "repo_value", "sha_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetGitTreeRecursiveRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: sha
    pub sha: String,
}
impl GetGitTreeRecursiveRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        sha: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            sha: sha.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repos/{}/{}/git/trees/{}?recursive=true", urlencoding::encode(& self.owner
            .to_string()), urlencoding::encode(& self.repo.to_string()),
            urlencoding::encode(& self.sha.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetGitTreeRecursiveRequest {
    type Response = GitTreeResponse;
    const ENDPOINT_ID: &'static str = "GetGitTreeRecursive";
}
/// Request for `GetRepositoryContentRaw` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::GetRepositoryContentRawRequest;
///
/// let request = GetRepositoryContentRawRequest::new("owner_value", "repo_value", "filepath_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetRepositoryContentRawRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: filepath
    pub filepath: String,
}
impl GetRepositoryContentRawRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        filepath: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            filepath: filepath.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repos/{}/{}/raw/{}", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string()), self.filepath
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetRepositoryContentRawRequest {
    type Response = String;
    const ENDPOINT_ID: &'static str = "GetRepositoryContentRaw";
}
/// Request for `ListPullRequests` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListPullRequestsRequest;
///
/// let request = ListPullRequestsRequest::new("owner_value", "repo_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///     .with_state(/* value */)
///     .with_sort(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListPullRequestsRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
    /// Query parameter: Filter by PR state
    pub state: Option<String>,
    /// Query parameter: Sort order for results
    pub sort: Option<String>,
}
impl ListPullRequestsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(owner: impl Into<String>, repo: impl Into<String>) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            page: None,
            limit: None,
            state: None,
            sort: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Sets the `state` query parameter.
    pub fn with_state(mut self, value: String) -> Self {
        self.state = Some(value);
        self
    }
    /// Sets the `sort` query parameter.
    pub fn with_sort(mut self, value: String) -> Self {
        self.sort = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repos/{}/{}/pulls", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if let Some(ref value) = self.state {
            query_pairs.push(("state", value.to_string()));
        }
        if let Some(ref value) = self.sort {
            query_pairs.push(("sort", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListPullRequestsRequest {
    type Response = Vec<PullRequestSummary>;
    const ENDPOINT_ID: &'static str = "ListPullRequests";
}
/// Request for `ListPullRequestFiles` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListPullRequestFilesRequest;
///
/// let request = ListPullRequestFilesRequest::new("owner_value", "repo_value", "index_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListPullRequestFilesRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: index
    pub index: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
}
impl ListPullRequestFilesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        index: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            index: index.into(),
            page: None,
            limit: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repos/{}/{}/pulls/{}/files", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string()), urlencoding::encode(& self
            .index.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListPullRequestFilesRequest {
    type Response = Vec<PullRequestFile>;
    const ENDPOINT_ID: &'static str = "ListPullRequestFiles";
}
/// Request for `ListIssues` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListIssuesRequest;
///
/// let request = ListIssuesRequest::new("owner_value", "repo_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///     .with_state(/* value */)
///     .with_issue_type(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssuesRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
    /// Query parameter: Filter by issue state
    pub state: Option<String>,
    /// Query parameter: Filter by type (issues, pulls, or all)
    pub issue_type: Option<String>,
}
impl ListIssuesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(owner: impl Into<String>, repo: impl Into<String>) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            page: None,
            limit: None,
            state: None,
            issue_type: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Sets the `state` query parameter.
    pub fn with_state(mut self, value: String) -> Self {
        self.state = Some(value);
        self
    }
    /// Sets the `issue_type` query parameter.
    pub fn with_issue_type(mut self, value: String) -> Self {
        self.issue_type = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repos/{}/{}/issues", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if let Some(ref value) = self.state {
            query_pairs.push(("state", value.to_string()));
        }
        if let Some(ref value) = self.issue_type {
            query_pairs.push(("issue_type", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssuesRequest {
    type Response = Vec<IssueSummary>;
    const ENDPOINT_ID: &'static str = "ListIssues";
}
/// Request for `GetIssue` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::GetIssueRequest;
///
/// let request = GetIssueRequest::new("owner_value", "repo_value", "index_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetIssueRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: index
    pub index: String,
}
impl GetIssueRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        index: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            index: index.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repos/{}/{}/issues/{}", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string()), urlencoding::encode(& self
            .index.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetIssueRequest {
    type Response = IssueSummary;
    const ENDPOINT_ID: &'static str = "GetIssue";
}
/// Request for `ListIssueComments` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListIssueCommentsRequest;
///
/// let request = ListIssueCommentsRequest::new("owner_value", "repo_value", "index_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssueCommentsRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: index
    pub index: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
}
impl ListIssueCommentsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        index: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            index: index.into(),
            page: None,
            limit: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repos/{}/{}/issues/{}/comments", urlencoding::encode(& self.owner
            .to_string()), urlencoding::encode(& self.repo.to_string()),
            urlencoding::encode(& self.index.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssueCommentsRequest {
    type Response = Vec<IssueComment>;
    const ENDPOINT_ID: &'static str = "ListIssueComments";
}
/// Request for `ListIssueTimeline` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListIssueTimelineRequest;
///
/// let request = ListIssueTimelineRequest::new("owner_value", "repo_value", "index_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssueTimelineRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: index
    pub index: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
}
impl ListIssueTimelineRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        index: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            index: index.into(),
            page: None,
            limit: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repos/{}/{}/issues/{}/timeline", urlencoding::encode(& self.owner
            .to_string()), urlencoding::encode(& self.repo.to_string()),
            urlencoding::encode(& self.index.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssueTimelineRequest {
    type Response = Vec<TimelineEvent>;
    const ENDPOINT_ID: &'static str = "ListIssueTimeline";
}
/// Request for `ListTags` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListTagsRequest;
///
/// let request = ListTagsRequest::new("owner_value", "repo_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListTagsRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
}
impl ListTagsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(owner: impl Into<String>, repo: impl Into<String>) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            page: None,
            limit: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repos/{}/{}/tags", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListTagsRequest {
    type Response = Vec<RepoTag>;
    const ENDPOINT_ID: &'static str = "ListTags";
}
/// Request for `ListReleases` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListReleasesRequest;
///
/// let request = ListReleasesRequest::new("owner_value", "repo_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///     .with_draft(/* value */)
///     .with_pre_release(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListReleasesRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
    /// Query parameter: Include draft releases
    pub draft: Option<bool>,
    /// Query parameter: Include pre-releases
    pub pre_release: Option<bool>,
}
impl ListReleasesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(owner: impl Into<String>, repo: impl Into<String>) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            page: None,
            limit: None,
            draft: None,
            pre_release: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Sets the `draft` query parameter.
    pub fn with_draft(mut self, value: bool) -> Self {
        self.draft = Some(value);
        self
    }
    /// Sets the `pre_release` query parameter.
    pub fn with_pre_release(mut self, value: bool) -> Self {
        self.pre_release = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repos/{}/{}/releases", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if let Some(ref value) = self.draft {
            query_pairs.push(("draft", value.to_string()));
        }
        if let Some(ref value) = self.pre_release {
            query_pairs.push(("pre_release", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListReleasesRequest {
    type Response = Vec<Release>;
    const ENDPOINT_ID: &'static str = "ListReleases";
}
/// Request for `GetTagReference` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::GetTagReferenceRequest;
///
/// let request = GetTagReferenceRequest::new("owner_value", "repo_value", "git_ref_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetTagReferenceRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: git_ref
    pub git_ref: String,
}
impl GetTagReferenceRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        git_ref: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            git_ref: git_ref.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repos/{}/{}/git/refs/{}", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string()), urlencoding::encode(& self
            .git_ref.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetTagReferenceRequest {
    type Response = Vec<GitRef>;
    const ENDPOINT_ID: &'static str = "GetTagReference";
}
/// Request for `GetAnnotatedTag` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::GetAnnotatedTagRequest;
///
/// let request = GetAnnotatedTagRequest::new("owner_value", "repo_value", "sha_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetAnnotatedTagRequest {
    /// Path parameter: owner
    pub owner: String,
    /// Path parameter: repo
    pub repo: String,
    /// Path parameter: sha
    pub sha: String,
}
impl GetAnnotatedTagRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        owner: impl Into<String>,
        repo: impl Into<String>,
        sha: impl Into<String>,
    ) -> Self {
        Self {
            owner: owner.into(),
            repo: repo.into(),
            sha: sha.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repos/{}/{}/git/tags/{}", urlencoding::encode(& self.owner.to_string()),
            urlencoding::encode(& self.repo.to_string()), urlencoding::encode(& self.sha
            .to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetAnnotatedTagRequest {
    type Response = AnnotatedTagObject;
    const ENDPOINT_ID: &'static str = "GetAnnotatedTag";
}
/// Request for `ListOrgRepos` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitea::ListOrgReposRequest;
///
/// let request = ListOrgReposRequest::new("org_value")
///     .with_page(/* value */)
///     .with_limit(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListOrgReposRequest {
    /// Path parameter: org
    pub org: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub limit: Option<i64>,
}
impl ListOrgReposRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(org: impl Into<String>) -> Self {
        Self {
            org: org.into(),
            page: None,
            limit: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `limit` query parameter.
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/orgs/{}/repos", urlencoding::encode(& self.org.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.limit {
            query_pairs.push(("limit", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListOrgReposRequest {
    fn from(param: &str) -> Self {
        Self {
            org: param.to_string(),
            page: None,
            limit: None,
        }
    }
}
impl From<String> for ListOrgReposRequest {
    fn from(param: String) -> Self {
        Self {
            org: param,
            page: None,
            limit: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListOrgReposRequest {
    type Response = Vec<RepositoryInfo>;
    const ENDPOINT_ID: &'static str = "ListOrgRepos";
}
impl Paginated for ListPullRequestsRequest {}
impl Paginated for ListPullRequestFilesRequest {}
impl Paginated for ListIssuesRequest {}
impl Paginated for ListIssueCommentsRequest {}
impl Paginated for ListIssueTimelineRequest {}
impl Paginated for ListTagsRequest {}
impl Paginated for ListReleasesRequest {}
impl Paginated for ListOrgReposRequest {}
/// Request enum for Gitea API.
///
/// Each variant wraps a strongly-typed request struct.
pub enum GiteaRequest {
    /// Get repository metadata including default branch
    GetRepository(GetRepositoryRequest),
    /// Get a Git tree (non-recursive, single level)
    GetGitTree(GetGitTreeRequest),
    /// Get a Git tree recursively (may be paginated for large repos)
    GetGitTreeRecursive(GetGitTreeRecursiveRequest),
    /// Get raw file content from repository
    GetRepositoryContentRaw(GetRepositoryContentRawRequest),
    /// List pull requests with metadata
    ListPullRequests(ListPullRequestsRequest),
    /// List files changed in a pull request
    ListPullRequestFiles(ListPullRequestFilesRequest),
    /// List issues (use type=issues to exclude PRs)
    ListIssues(ListIssuesRequest),
    /// Get a single issue by index
    GetIssue(GetIssueRequest),
    /// List comments on an issue
    ListIssueComments(ListIssueCommentsRequest),
    /// List timeline events for an issue
    ListIssueTimeline(ListIssueTimelineRequest),
    /// List repository tags
    ListTags(ListTagsRequest),
    /// List releases (linked to tags via tag_name)
    ListReleases(ListReleasesRequest),
    /// Get tag reference (returns array; check object.type: 'commit' vs 'tag')
    GetTagReference(GetTagReferenceRequest),
    /// Get annotated tag object details (message, tagger)
    GetAnnotatedTag(GetAnnotatedTagRequest),
    /// List repositories for an organization
    ListOrgRepos(ListOrgReposRequest),
}
impl GiteaRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// Delegates to the inner request struct's `into_parts()` method.
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        match self {
            Self::GetRepository(req) => req.into_parts(),
            Self::GetGitTree(req) => req.into_parts(),
            Self::GetGitTreeRecursive(req) => req.into_parts(),
            Self::GetRepositoryContentRaw(req) => req.into_parts(),
            Self::ListPullRequests(req) => req.into_parts(),
            Self::ListPullRequestFiles(req) => req.into_parts(),
            Self::ListIssues(req) => req.into_parts(),
            Self::GetIssue(req) => req.into_parts(),
            Self::ListIssueComments(req) => req.into_parts(),
            Self::ListIssueTimeline(req) => req.into_parts(),
            Self::ListTags(req) => req.into_parts(),
            Self::ListReleases(req) => req.into_parts(),
            Self::GetTagReference(req) => req.into_parts(),
            Self::GetAnnotatedTag(req) => req.into_parts(),
            Self::ListOrgRepos(req) => req.into_parts(),
        }
    }
    /// Returns the endpoint identifier for this request.
    ///
    /// This is used internally for response hook lookup.
    #[must_use]
    pub fn endpoint_id(&self) -> &'static str {
        match self {
            Self::GetRepository(_) => {
                <GetRepositoryRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetGitTree(_) => {
                <GetGitTreeRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetGitTreeRecursive(_) => {
                <GetGitTreeRecursiveRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetRepositoryContentRaw(_) => {
                <GetRepositoryContentRawRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListPullRequests(_) => {
                <ListPullRequestsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListPullRequestFiles(_) => {
                <ListPullRequestFilesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssues(_) => {
                <ListIssuesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetIssue(_) => {
                <GetIssueRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssueComments(_) => {
                <ListIssueCommentsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssueTimeline(_) => {
                <ListIssueTimelineRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListTags(_) => {
                <ListTagsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListReleases(_) => {
                <ListReleasesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetTagReference(_) => {
                <GetTagReferenceRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetAnnotatedTag(_) => {
                <GetAnnotatedTagRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListOrgRepos(_) => {
                <ListOrgReposRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
        }
    }
    /// Returns how this endpoint's response body is decoded.
    ///
    /// The generic `request::<T>()` decodes JSON; it consults this to
    /// reject text, binary, and empty endpoints up front.
    #[must_use]
    pub fn response_kind(&self) -> crate::shared::ResponseKind {
        match self {
            Self::GetRepository(_) => crate::shared::ResponseKind::Json,
            Self::GetGitTree(_) => crate::shared::ResponseKind::Json,
            Self::GetGitTreeRecursive(_) => crate::shared::ResponseKind::Json,
            Self::GetRepositoryContentRaw(_) => crate::shared::ResponseKind::Text,
            Self::ListPullRequests(_) => crate::shared::ResponseKind::Json,
            Self::ListPullRequestFiles(_) => crate::shared::ResponseKind::Json,
            Self::ListIssues(_) => crate::shared::ResponseKind::Json,
            Self::GetIssue(_) => crate::shared::ResponseKind::Json,
            Self::ListIssueComments(_) => crate::shared::ResponseKind::Json,
            Self::ListIssueTimeline(_) => crate::shared::ResponseKind::Json,
            Self::ListTags(_) => crate::shared::ResponseKind::Json,
            Self::ListReleases(_) => crate::shared::ResponseKind::Json,
            Self::GetTagReference(_) => crate::shared::ResponseKind::Json,
            Self::GetAnnotatedTag(_) => crate::shared::ResponseKind::Json,
            Self::ListOrgRepos(_) => crate::shared::ResponseKind::Json,
        }
    }
}
impl From<GetRepositoryRequest> for GiteaRequest {
    fn from(req: GetRepositoryRequest) -> Self {
        Self::GetRepository(req)
    }
}
impl From<GetGitTreeRequest> for GiteaRequest {
    fn from(req: GetGitTreeRequest) -> Self {
        Self::GetGitTree(req)
    }
}
impl From<GetGitTreeRecursiveRequest> for GiteaRequest {
    fn from(req: GetGitTreeRecursiveRequest) -> Self {
        Self::GetGitTreeRecursive(req)
    }
}
impl From<GetRepositoryContentRawRequest> for GiteaRequest {
    fn from(req: GetRepositoryContentRawRequest) -> Self {
        Self::GetRepositoryContentRaw(req)
    }
}
impl From<ListPullRequestsRequest> for GiteaRequest {
    fn from(req: ListPullRequestsRequest) -> Self {
        Self::ListPullRequests(req)
    }
}
impl From<ListPullRequestFilesRequest> for GiteaRequest {
    fn from(req: ListPullRequestFilesRequest) -> Self {
        Self::ListPullRequestFiles(req)
    }
}
impl From<ListIssuesRequest> for GiteaRequest {
    fn from(req: ListIssuesRequest) -> Self {
        Self::ListIssues(req)
    }
}
impl From<GetIssueRequest> for GiteaRequest {
    fn from(req: GetIssueRequest) -> Self {
        Self::GetIssue(req)
    }
}
impl From<ListIssueCommentsRequest> for GiteaRequest {
    fn from(req: ListIssueCommentsRequest) -> Self {
        Self::ListIssueComments(req)
    }
}
impl From<ListIssueTimelineRequest> for GiteaRequest {
    fn from(req: ListIssueTimelineRequest) -> Self {
        Self::ListIssueTimeline(req)
    }
}
impl From<ListTagsRequest> for GiteaRequest {
    fn from(req: ListTagsRequest) -> Self {
        Self::ListTags(req)
    }
}
impl From<ListReleasesRequest> for GiteaRequest {
    fn from(req: ListReleasesRequest) -> Self {
        Self::ListReleases(req)
    }
}
impl From<GetTagReferenceRequest> for GiteaRequest {
    fn from(req: GetTagReferenceRequest) -> Self {
        Self::GetTagReference(req)
    }
}
impl From<GetAnnotatedTagRequest> for GiteaRequest {
    fn from(req: GetAnnotatedTagRequest) -> Self {
        Self::GetAnnotatedTag(req)
    }
}
impl From<ListOrgReposRequest> for GiteaRequest {
    fn from(req: ListOrgReposRequest) -> Self {
        Self::ListOrgRepos(req)
    }
}
