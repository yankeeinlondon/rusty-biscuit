// This code was automatically generated by schematic-gen. Do not edit manually.

use serde::{Deserialize, Serialize};
use crate::shared::{Paginated, RequestParts, SchematicError};
use super::responses::*;
/// Request for `GetRepository` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::GetRepositoryRequest;
///
/// let request = GetRepositoryRequest::new("workspace_value", "repo_slug_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetRepositoryRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
}
impl GetRepositoryRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(workspace: impl Into<String>, repo_slug: impl Into<String>) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repositories/{}/{}", urlencoding::encode(& self.workspace.to_string()),
            urlencoding::encode(& self.repo_slug.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetRepositoryRequest {
    type Response = Repository;
    const ENDPOINT_ID: &'static str = "GetRepository";
}
/// Request for `ListDirectoryContents` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListDirectoryContentsRequest;
///
/// let request = ListDirectoryContentsRequest::new("workspace_value", "repo_slug_value", "commit_value", "path_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListDirectoryContentsRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: commit
    pub commit: String,
    /// Path parameter: path
    pub path: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListDirectoryContentsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        commit: impl Into<String>,
        path: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            commit: commit.into(),
            path: path.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/src/{}/{}", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.commit.to_string()), urlencoding::encode(& self
            .path.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListDirectoryContentsRequest {
    type Response = PaginatedResponse<SourceEntry>;
    const ENDPOINT_ID: &'static str = "ListDirectoryContents";
}
/// Request for `GetFileContentRaw` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::GetFileContentRawRequest;
///
/// let request = GetFileContentRawRequest::new("workspace_value", "repo_slug_value", "commit_value", "path_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetFileContentRawRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: commit
    pub commit: String,
    /// Path parameter: path
    pub path: String,
}
impl GetFileContentRawRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        commit: impl Into<String>,
        path: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            commit: commit.into(),
            path: path.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repositories/{}/{}/src/{}/{}", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.commit.to_string()), urlencoding::encode(& self
            .path.to_string())
        );
        Ok((
            "GET",
            path,
            crate::shared::RequestBody::Empty,
            vec![("Accept".to_string(), "text/plain".to_string())],
        ))
    }
}
impl crate::shared::EndpointSpec for GetFileContentRawRequest {
    type Response = String;
    const ENDPOINT_ID: &'static str = "GetFileContentRaw";
}
/// Request for `ListPullRequests` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListPullRequestsRequest;
///
/// let request = ListPullRequestsRequest::new("workspace_value", "repo_slug_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///     .with_state(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListPullRequestsRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
    /// Query parameter: Filter by state (OPEN, MERGED, DECLINED, SUPERSEDED)
    pub state: Option<String>,
}
impl ListPullRequestsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(workspace: impl Into<String>, repo_slug: impl Into<String>) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            page: None,
            pagelen: None,
            state: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Sets the `state` query parameter.
    pub fn with_state(mut self, value: String) -> Self {
        self.state = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/pullrequests", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if let Some(ref value) = self.state {
            query_pairs.push(("state", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListPullRequestsRequest {
    type Response = PaginatedResponse<PullRequest>;
    const ENDPOINT_ID: &'static str = "ListPullRequests";
}
/// Request for `GetPullRequest` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::GetPullRequestRequest;
///
/// let request = GetPullRequestRequest::new("workspace_value", "repo_slug_value", "id_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetPullRequestRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: id
    pub id: String,
}
impl GetPullRequestRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        id: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            id: id.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repositories/{}/{}/pullrequests/{}", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.id.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetPullRequestRequest {
    type Response = PullRequest;
    const ENDPOINT_ID: &'static str = "GetPullRequest";
}
/// Request for `ListPullRequestComments` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListPullRequestCommentsRequest;
///
/// let request = ListPullRequestCommentsRequest::new("workspace_value", "repo_slug_value", "id_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListPullRequestCommentsRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListPullRequestCommentsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        id: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            id: id.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/pullrequests/{}/comments", urlencoding::encode(& self
            .workspace.to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListPullRequestCommentsRequest {
    type Response = PaginatedResponse<PullRequestComment>;
    const ENDPOINT_ID: &'static str = "ListPullRequestComments";
}
/// Request for `ListIssues` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListIssuesRequest;
///
/// let request = ListIssuesRequest::new("workspace_value", "repo_slug_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssuesRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListIssuesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(workspace: impl Into<String>, repo_slug: impl Into<String>) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/issues", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssuesRequest {
    type Response = PaginatedResponse<Issue>;
    const ENDPOINT_ID: &'static str = "ListIssues";
}
/// Request for `GetIssue` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::GetIssueRequest;
///
/// let request = GetIssueRequest::new("workspace_value", "repo_slug_value", "id_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetIssueRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: id
    pub id: String,
}
impl GetIssueRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        id: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            id: id.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repositories/{}/{}/issues/{}", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.id.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetIssueRequest {
    type Response = Issue;
    const ENDPOINT_ID: &'static str = "GetIssue";
}
/// Request for `ListIssueComments` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListIssueCommentsRequest;
///
/// let request = ListIssueCommentsRequest::new("workspace_value", "repo_slug_value", "id_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssueCommentsRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListIssueCommentsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        id: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            id: id.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/issues/{}/comments", urlencoding::encode(& self
            .workspace.to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssueCommentsRequest {
    type Response = PaginatedResponse<IssueComment>;
    const ENDPOINT_ID: &'static str = "ListIssueComments";
}
/// Request for `ListIssueChanges` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListIssueChangesRequest;
///
/// let request = ListIssueChangesRequest::new("workspace_value", "repo_slug_value", "id_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssueChangesRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListIssueChangesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        id: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            id: id.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/issues/{}/changes", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssueChangesRequest {
    type Response = PaginatedResponse<IssueChange>;
    const ENDPOINT_ID: &'static str = "ListIssueChanges";
}
/// Request for `ListTags` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListTagsRequest;
///
/// let request = ListTagsRequest::new("workspace_value", "repo_slug_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListTagsRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListTagsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(workspace: impl Into<String>, repo_slug: impl Into<String>) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/refs/tags", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListTagsRequest {
    type Response = PaginatedResponse<Tag>;
    const ENDPOINT_ID: &'static str = "ListTags";
}
/// Request for `GetTag` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::GetTagRequest;
///
/// let request = GetTagRequest::new("workspace_value", "repo_slug_value", "name_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetTagRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: name
    pub name: String,
}
impl GetTagRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        name: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            name: name.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repositories/{}/{}/refs/tags/{}", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.name.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetTagRequest {
    type Response = Tag;
    const ENDPOINT_ID: &'static str = "GetTag";
}
/// Request for `ListDownloads` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListDownloadsRequest;
///
/// let request = ListDownloadsRequest::new("workspace_value", "repo_slug_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListDownloadsRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListDownloadsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(workspace: impl Into<String>, repo_slug: impl Into<String>) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}/{}/downloads", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListDownloadsRequest {
    type Response = PaginatedResponse<Download>;
    const ENDPOINT_ID: &'static str = "ListDownloads";
}
/// Request for `GetDownload` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::GetDownloadRequest;
///
/// let request = GetDownloadRequest::new("workspace_value", "repo_slug_value", "filename_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetDownloadRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Path parameter: repo_slug
    pub repo_slug: String,
    /// Path parameter: filename
    pub filename: String,
}
impl GetDownloadRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        workspace: impl Into<String>,
        repo_slug: impl Into<String>,
        filename: impl Into<String>,
    ) -> Self {
        Self {
            workspace: workspace.into(),
            repo_slug: repo_slug.into(),
            filename: filename.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/repositories/{}/{}/downloads/{}", urlencoding::encode(& self.workspace
            .to_string()), urlencoding::encode(& self.repo_slug.to_string()),
            urlencoding::encode(& self.filename.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetDownloadRequest {
    type Response = bytes::Bytes;
    const ENDPOINT_ID: &'static str = "GetDownload";
}
/// Request for `ListWorkspaceRepos` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::bitbucket::ListWorkspaceReposRequest;
///
/// let request = ListWorkspaceReposRequest::new("workspace_value")
///     .with_page(/* value */)
///     .with_pagelen(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListWorkspaceReposRequest {
    /// Path parameter: workspace
    pub workspace: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 50, max: 100)
    pub pagelen: Option<i64>,
}
impl ListWorkspaceReposRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(workspace: impl Into<String>) -> Self {
        Self {
            workspace: workspace.into(),
            page: None,
            pagelen: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `pagelen` query parameter.
    pub fn with_pagelen(mut self, value: i64) -> Self {
        self.pagelen = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/repositories/{}", urlencoding::encode(& self.workspace.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.pagelen {
            query_pairs.push(("pagelen", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListWorkspaceReposRequest {
    fn from(param: &str) -> Self {
        Self {
            workspace: param.to_string(),
            page: None,
            pagelen: None,
        }
    }
}
impl From<String> for ListWorkspaceReposRequest {
    fn from(param: String) -> Self {
        Self {
            workspace: param,
            page: None,
            pagelen: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListWorkspaceReposRequest {
    type Response = PaginatedResponse<Repository>;
    const ENDPOINT_ID: &'static str = "ListWorkspaceRepos";
}
impl Paginated for ListDirectoryContentsRequest {}
impl Paginated for ListPullRequestsRequest {}
impl Paginated for ListPullRequestCommentsRequest {}
impl Paginated for ListIssuesRequest {}
impl Paginated for ListIssueCommentsRequest {}
impl Paginated for ListIssueChangesRequest {}
impl Paginated for ListTagsRequest {}
impl Paginated for ListDownloadsRequest {}
impl Paginated for ListWorkspaceReposRequest {}
/// Request enum for Bitbucket API.
///
/// Each variant wraps a strongly-typed request struct.
pub enum BitbucketRequest {
    /// Get repository metadata including default branch and workspace info
    GetRepository(GetRepositoryRequest),
    /// List directory contents at a specific commit
    ListDirectoryContents(ListDirectoryContentsRequest),
    /// Get raw file content at a specific commit
    GetFileContentRaw(GetFileContentRawRequest),
    /// List pull requests (all states)
    ListPullRequests(ListPullRequestsRequest),
    /// Get a single pull request by ID
    GetPullRequest(GetPullRequestRequest),
    /// List comments on a pull request
    ListPullRequestComments(ListPullRequestCommentsRequest),
    /// List repository issues
    ListIssues(ListIssuesRequest),
    /// Get a single issue by ID
    GetIssue(GetIssueRequest),
    /// List comments on an issue
    ListIssueComments(ListIssueCommentsRequest),
    /// List change history for an issue
    ListIssueChanges(ListIssueChangesRequest),
    /// List repository tags
    ListTags(ListTagsRequest),
    /// Get a single tag by name
    GetTag(GetTagRequest),
    /// List repository downloads (release artifacts)
    ListDownloads(ListDownloadsRequest),
    /// Download a file from repository downloads
    GetDownload(GetDownloadRequest),
    /// List repositories in a workspace
    ListWorkspaceRepos(ListWorkspaceReposRequest),
}
impl BitbucketRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// Delegates to the inner request struct's `into_parts()` method.
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        match self {
            Self::GetRepository(req) => req.into_parts(),
            Self::ListDirectoryContents(req) => req.into_parts(),
            Self::GetFileContentRaw(req) => req.into_parts(),
            Self::ListPullRequests(req) => req.into_parts(),
            Self::GetPullRequest(req) => req.into_parts(),
            Self::ListPullRequestComments(req) => req.into_parts(),
            Self::ListIssues(req) => req.into_parts(),
            Self::GetIssue(req) => req.into_parts(),
            Self::ListIssueComments(req) => req.into_parts(),
            Self::ListIssueChanges(req) => req.into_parts(),
            Self::ListTags(req) => req.into_parts(),
            Self::GetTag(req) => req.into_parts(),
            Self::ListDownloads(req) => req.into_parts(),
            Self::GetDownload(req) => req.into_parts(),
            Self::ListWorkspaceRepos(req) => req.into_parts(),
        }
    }
    /// Returns the endpoint identifier for this request.
    ///
    /// This is used internally for response hook lookup.
    #[must_use]
    pub fn endpoint_id(&self) -> &'static str {
        match self {
            Self::GetRepository(_) => {
                <GetRepositoryRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListDirectoryContents(_) => {
                <ListDirectoryContentsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetFileContentRaw(_) => {
                <GetFileContentRawRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListPullRequests(_) => {
                <ListPullRequestsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetPullRequest(_) => {
                <GetPullRequestRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListPullRequestComments(_) => {
                <ListPullRequestCommentsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssues(_) => {
                <ListIssuesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetIssue(_) => {
                <GetIssueRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssueComments(_) => {
                <ListIssueCommentsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssueChanges(_) => {
                <ListIssueChangesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListTags(_) => {
                <ListTagsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetTag(_) => {
                <GetTagRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListDownloads(_) => {
                <ListDownloadsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetDownload(_) => {
                <GetDownloadRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListWorkspaceRepos(_) => {
                <ListWorkspaceReposRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
        }
    }
    /// Returns how this endpoint's response body is decoded.
    ///
    /// The generic `request::<T>()` decodes JSON; it consults this to
    /// reject text, binary, and empty endpoints up front.
    #[must_use]
    pub fn response_kind(&self) -> crate::shared::ResponseKind {
        match self {
            Self::GetRepository(_) => crate::shared::ResponseKind::Json,
            Self::ListDirectoryContents(_) => crate::shared::ResponseKind::Json,
            Self::GetFileContentRaw(_) => crate::shared::ResponseKind::Text,
            Self::ListPullRequests(_) => crate::shared::ResponseKind::Json,
            Self::GetPullRequest(_) => crate::shared::ResponseKind::Json,
            Self::ListPullRequestComments(_) => crate::shared::ResponseKind::Json,
            Self::ListIssues(_) => crate::shared::ResponseKind::Json,
            Self::GetIssue(_) => crate::shared::ResponseKind::Json,
            Self::ListIssueComments(_) => crate::shared::ResponseKind::Json,
            Self::ListIssueChanges(_) => crate::shared::ResponseKind::Json,
            Self::ListTags(_) => crate::shared::ResponseKind::Json,
            Self::GetTag(_) => crate::shared::ResponseKind::Json,
            Self::ListDownloads(_) => crate::shared::ResponseKind::Json,
            Self::GetDownload(_) => crate::shared::ResponseKind::Binary,
            Self::ListWorkspaceRepos(_) => crate::shared::ResponseKind::Json,
        }
    }
}
impl From<GetRepositoryRequest> for BitbucketRequest {
    fn from(req: GetRepositoryRequest) -> Self {
        Self::GetRepository(req)
    }
}
impl From<ListDirectoryContentsRequest> for BitbucketRequest {
    fn from(req: ListDirectoryContentsRequest) -> Self {
        Self::ListDirectoryContents(req)
    }
}
impl From<GetFileContentRawRequest> for BitbucketRequest {
    fn from(req: GetFileContentRawRequest) -> Self {
        Self::GetFileContentRaw(req)
    }
}
impl From<ListPullRequestsRequest> for BitbucketRequest {
    fn from(req: ListPullRequestsRequest) -> Self {
        Self::ListPullRequests(req)
    }
}
impl From<GetPullRequestRequest> for BitbucketRequest {
    fn from(req: GetPullRequestRequest) -> Self {
        Self::GetPullRequest(req)
    }
}
impl From<ListPullRequestCommentsRequest> for BitbucketRequest {
    fn from(req: ListPullRequestCommentsRequest) -> Self {
        Self::ListPullRequestComments(req)
    }
}
impl From<ListIssuesRequest> for BitbucketRequest {
    fn from(req: ListIssuesRequest) -> Self {
        Self::ListIssues(req)
    }
}
impl From<GetIssueRequest> for BitbucketRequest {
    fn from(req: GetIssueRequest) -> Self {
        Self::GetIssue(req)
    }
}
impl From<ListIssueCommentsRequest> for BitbucketRequest {
    fn from(req: ListIssueCommentsRequest) -> Self {
        Self::ListIssueComments(req)
    }
}
impl From<ListIssueChangesRequest> for BitbucketRequest {
    fn from(req: ListIssueChangesRequest) -> Self {
        Self::ListIssueChanges(req)
    }
}
impl From<ListTagsRequest> for BitbucketRequest {
    fn from(req: ListTagsRequest) -> Self {
        Self::ListTags(req)
    }
}
impl From<GetTagRequest> for BitbucketRequest {
    fn from(req: GetTagRequest) -> Self {
        Self::GetTag(req)
    }
}
impl From<ListDownloadsRequest> for BitbucketRequest {
    fn from(req: ListDownloadsRequest) -> Self {
        Self::ListDownloads(req)
    }
}
impl From<GetDownloadRequest> for BitbucketRequest {
    fn from(req: GetDownloadRequest) -> Self {
        Self::GetDownload(req)
    }
}
impl From<ListWorkspaceReposRequest> for BitbucketRequest {
    fn from(req: ListWorkspaceReposRequest) -> Self {
        Self::ListWorkspaceRepos(req)
    }
}
