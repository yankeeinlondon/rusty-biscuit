// This code was automatically generated by schematic-gen. Do not edit manually.

use serde::{Deserialize, Serialize};
use crate::shared::{RequestParts, SchematicError};
use super::responses::*;
/// Request for `ListLlmModels` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::artificial_analysis::ListLlmModelsRequest;
///
/// let request = ListLlmModelsRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListLlmModelsRequest {}
impl ListLlmModelsRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/data/llms/models".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListLlmModelsRequest {
    type Response = LlmModelsResponse;
    const ENDPOINT_ID: &'static str = "ListLlmModels";
}
/// Request for `ListTextToImageModels` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::artificial_analysis::ListTextToImageModelsRequest;
///
/// let request = ListTextToImageModelsRequest::default()
///     .with_include_categories(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListTextToImageModelsRequest {
    /// Query parameter: Include a breakdown of Elo scores by category.
    pub include_categories: Option<bool>,
}
impl ListTextToImageModelsRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { include_categories: None }
    }
    /// Sets the `include_categories` query parameter.
    pub fn with_include_categories(mut self, value: bool) -> Self {
        self.include_categories = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/data/media/text-to-image".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.include_categories {
            query_pairs.push(("include_categories", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListTextToImageModelsRequest {
    type Response = MediaModelsResponse;
    const ENDPOINT_ID: &'static str = "ListTextToImageModels";
}
/// Request for `ListImageEditingModels` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::artificial_analysis::ListImageEditingModelsRequest;
///
/// let request = ListImageEditingModelsRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListImageEditingModelsRequest {}
impl ListImageEditingModelsRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/data/media/image-editing".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListImageEditingModelsRequest {
    type Response = MediaModelsResponse;
    const ENDPOINT_ID: &'static str = "ListImageEditingModels";
}
/// Request for `ListTextToSpeechModels` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::artificial_analysis::ListTextToSpeechModelsRequest;
///
/// let request = ListTextToSpeechModelsRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListTextToSpeechModelsRequest {}
impl ListTextToSpeechModelsRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/data/media/text-to-speech".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListTextToSpeechModelsRequest {
    type Response = MediaModelsResponse;
    const ENDPOINT_ID: &'static str = "ListTextToSpeechModels";
}
/// Request for `ListTextToVideoModels` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::artificial_analysis::ListTextToVideoModelsRequest;
///
/// let request = ListTextToVideoModelsRequest::default()
///     .with_include_categories(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListTextToVideoModelsRequest {
    /// Query parameter: Include a breakdown of Elo scores by category.
    pub include_categories: Option<bool>,
}
impl ListTextToVideoModelsRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { include_categories: None }
    }
    /// Sets the `include_categories` query parameter.
    pub fn with_include_categories(mut self, value: bool) -> Self {
        self.include_categories = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/data/media/text-to-video".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.include_categories {
            query_pairs.push(("include_categories", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListTextToVideoModelsRequest {
    type Response = MediaModelsResponse;
    const ENDPOINT_ID: &'static str = "ListTextToVideoModels";
}
/// Request for `ListImageToVideoModels` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::artificial_analysis::ListImageToVideoModelsRequest;
///
/// let request = ListImageToVideoModelsRequest::default()
///     .with_include_categories(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListImageToVideoModelsRequest {
    /// Query parameter: Include a breakdown of Elo scores by category.
    pub include_categories: Option<bool>,
}
impl ListImageToVideoModelsRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { include_categories: None }
    }
    /// Sets the `include_categories` query parameter.
    pub fn with_include_categories(mut self, value: bool) -> Self {
        self.include_categories = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/data/media/image-to-video".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.include_categories {
            query_pairs.push(("include_categories", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListImageToVideoModelsRequest {
    type Response = MediaModelsResponse;
    const ENDPOINT_ID: &'static str = "ListImageToVideoModels";
}
/// Request for `EvaluateCritPt` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::artificial_analysis::{EvaluateCritPtRequest, CritPtEvaluateBody};
///
/// let body = CritPtEvaluateBody {
///     // ... set required fields ...
///     ..Default::default()
/// };
/// let request = EvaluateCritPtRequest::new(body)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct EvaluateCritPtRequest {
    /// Request body
    pub body: CritPtEvaluateBody,
}
impl EvaluateCritPtRequest {
    /// Creates a new request with the required path parameters and body.
    pub fn new(body: CritPtEvaluateBody) -> Self {
        Self { body }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/critpt/evaluate".to_string();
        Ok((
            "POST",
            path,
            Some(
                serde_json::to_string(&self.body)
                    .map_err(|e| { SchematicError::SerializationError(e.to_string()) })?,
            ),
            vec![],
        ))
    }
}
impl From<CritPtEvaluateBody> for EvaluateCritPtRequest {
    fn from(body: CritPtEvaluateBody) -> Self {
        Self { body }
    }
}
impl crate::shared::EndpointSpec for EvaluateCritPtRequest {
    type Response = CritPtEvaluateResponse;
    const ENDPOINT_ID: &'static str = "EvaluateCritPt";
}
/// Request enum for ArtificialAnalysisData API.
///
/// Each variant wraps a strongly-typed request struct.
pub enum ArtificialAnalysisDataRequest {
    /// Returns primary metrics from independent LLM benchmarks: intelligence, speed, and pricing.
    ListLlmModels(ListLlmModelsRequest),
    /// Returns Elo ratings for text-to-image models.
    ListTextToImageModels(ListTextToImageModelsRequest),
    /// Returns Elo ratings for image-editing models.
    ListImageEditingModels(ListImageEditingModelsRequest),
    /// Returns Elo ratings for text-to-speech models.
    ListTextToSpeechModels(ListTextToSpeechModelsRequest),
    /// Returns Elo ratings for text-to-video models.
    ListTextToVideoModels(ListTextToVideoModelsRequest),
    /// Returns Elo ratings for image-to-video models.
    ListImageToVideoModels(ListImageToVideoModelsRequest),
}
impl ArtificialAnalysisDataRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// Delegates to the inner request struct's `into_parts()` method.
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        match self {
            Self::ListLlmModels(req) => req.into_parts(),
            Self::ListTextToImageModels(req) => req.into_parts(),
            Self::ListImageEditingModels(req) => req.into_parts(),
            Self::ListTextToSpeechModels(req) => req.into_parts(),
            Self::ListTextToVideoModels(req) => req.into_parts(),
            Self::ListImageToVideoModels(req) => req.into_parts(),
        }
    }
    /// Returns the endpoint identifier for this request.
    ///
    /// This is used internally for response hook lookup.
    #[must_use]
    pub fn endpoint_id(&self) -> &'static str {
        match self {
            Self::ListLlmModels(_) => {
                <ListLlmModelsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListTextToImageModels(_) => {
                <ListTextToImageModelsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListImageEditingModels(_) => {
                <ListImageEditingModelsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListTextToSpeechModels(_) => {
                <ListTextToSpeechModelsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListTextToVideoModels(_) => {
                <ListTextToVideoModelsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListImageToVideoModels(_) => {
                <ListImageToVideoModelsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
        }
    }
    /// Returns how this endpoint's response body is decoded.
    ///
    /// The generic `request::<T>()` decodes JSON; it consults this to
    /// reject text, binary, and empty endpoints up front.
    #[must_use]
    pub fn response_kind(&self) -> crate::shared::ResponseKind {
        match self {
            Self::ListLlmModels(_) => crate::shared::ResponseKind::Json,
            Self::ListTextToImageModels(_) => crate::shared::ResponseKind::Json,
            Self::ListImageEditingModels(_) => crate::shared::ResponseKind::Json,
            Self::ListTextToSpeechModels(_) => crate::shared::ResponseKind::Json,
            Self::ListTextToVideoModels(_) => crate::shared::ResponseKind::Json,
            Self::ListImageToVideoModels(_) => crate::shared::ResponseKind::Json,
        }
    }
}
impl From<ListLlmModelsRequest> for ArtificialAnalysisDataRequest {
    fn from(req: ListLlmModelsRequest) -> Self {
        Self::ListLlmModels(req)
    }
}
impl From<ListTextToImageModelsRequest> for ArtificialAnalysisDataRequest {
    fn from(req: ListTextToImageModelsRequest) -> Self {
        Self::ListTextToImageModels(req)
    }
}
impl From<ListImageEditingModelsRequest> for ArtificialAnalysisDataRequest {
    fn from(req: ListImageEditingModelsRequest) -> Self {
        Self::ListImageEditingModels(req)
    }
}
impl From<ListTextToSpeechModelsRequest> for ArtificialAnalysisDataRequest {
    fn from(req: ListTextToSpeechModelsRequest) -> Self {
        Self::ListTextToSpeechModels(req)
    }
}
impl From<ListTextToVideoModelsRequest> for ArtificialAnalysisDataRequest {
    fn from(req: ListTextToVideoModelsRequest) -> Self {
        Self::ListTextToVideoModels(req)
    }
}
impl From<ListImageToVideoModelsRequest> for ArtificialAnalysisDataRequest {
    fn from(req: ListImageToVideoModelsRequest) -> Self {
        Self::ListImageToVideoModels(req)
    }
}
/// Request enum for ArtificialAnalysisCritPt API.
///
/// Each variant wraps a strongly-typed request struct.
pub enum ArtificialAnalysisCritPtRequest {
    /// Submit a batch of code-generation submissions for evaluation against the CritPt benchmark.
    EvaluateCritPt(EvaluateCritPtRequest),
}
impl ArtificialAnalysisCritPtRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// Delegates to the inner request struct's `into_parts()` method.
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        match self {
            Self::EvaluateCritPt(req) => req.into_parts(),
        }
    }
    /// Returns the endpoint identifier for this request.
    ///
    /// This is used internally for response hook lookup.
    #[must_use]
    pub fn endpoint_id(&self) -> &'static str {
        match self {
            Self::EvaluateCritPt(_) => {
                <EvaluateCritPtRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
        }
    }
    /// Returns how this endpoint's response body is decoded.
    ///
    /// The generic `request::<T>()` decodes JSON; it consults this to
    /// reject text, binary, and empty endpoints up front.
    #[must_use]
    pub fn response_kind(&self) -> crate::shared::ResponseKind {
        match self {
            Self::EvaluateCritPt(_) => crate::shared::ResponseKind::Json,
        }
    }
}
impl From<EvaluateCritPtRequest> for ArtificialAnalysisCritPtRequest {
    fn from(req: EvaluateCritPtRequest) -> Self {
        Self::EvaluateCritPt(req)
    }
}
