// This code was automatically generated by schematic-gen. Do not edit manually.

use serde::{Deserialize, Serialize};
use crate::shared::{Paginated, RequestParts, SchematicError};
use super::responses::*;
/// Request for `ListRepositoryTree` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListRepositoryTreeRequest;
///
/// let request = ListRepositoryTreeRequest::new("id_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///     .with_recursive(/* value */)
///     .with_git_ref(/* value */)
///     .with_path(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListRepositoryTreeRequest {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
    /// Query parameter: Include files from subdirectories recursively
    pub recursive: Option<bool>,
    /// Query parameter: Branch, tag, or commit SHA to list tree for
    pub git_ref: Option<String>,
    /// Query parameter: Subdirectory path to list
    pub path: Option<String>,
}
impl ListRepositoryTreeRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            page: None,
            per_page: None,
            recursive: None,
            git_ref: None,
            path: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Sets the `recursive` query parameter.
    pub fn with_recursive(mut self, value: bool) -> Self {
        self.recursive = Some(value);
        self
    }
    /// Sets the `git_ref` query parameter.
    pub fn with_git_ref(mut self, value: String) -> Self {
        self.git_ref = Some(value);
        self
    }
    /// Sets the `path` query parameter.
    pub fn with_path(mut self, value: String) -> Self {
        self.path = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/repository/tree", urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if let Some(ref value) = self.recursive {
            query_pairs.push(("recursive", value.to_string()));
        }
        if let Some(ref value) = self.git_ref {
            query_pairs.push(("git_ref", value.to_string()));
        }
        if let Some(ref value) = self.path {
            query_pairs.push(("path", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListRepositoryTreeRequest {
    fn from(param: &str) -> Self {
        Self {
            id: param.to_string(),
            page: None,
            per_page: None,
            recursive: None,
            git_ref: None,
            path: None,
        }
    }
}
impl From<String> for ListRepositoryTreeRequest {
    fn from(param: String) -> Self {
        Self {
            id: param,
            page: None,
            per_page: None,
            recursive: None,
            git_ref: None,
            path: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListRepositoryTreeRequest {
    type Response = Vec<TreeItem>;
    const ENDPOINT_ID: &'static str = "ListRepositoryTree";
}
/// Request for `GetRepositoryFile` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::GetRepositoryFileRequest;
///
/// let request = GetRepositoryFileRequest::new("id_value", "file_path_value", "git_ref_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetRepositoryFileRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: file_path
    pub file_path: String,
    /// Path parameter: git_ref
    pub git_ref: String,
}
impl GetRepositoryFileRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(
        id: impl Into<String>,
        file_path: impl Into<String>,
        git_ref: impl Into<String>,
    ) -> Self {
        Self {
            id: id.into(),
            file_path: file_path.into(),
            git_ref: git_ref.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/repository/files/{}?ref={}", urlencoding::encode(& self.id
            .to_string()), urlencoding::encode(& self.file_path.to_string()),
            urlencoding::encode(& self.git_ref.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetRepositoryFileRequest {
    type Response = FileContent;
    const ENDPOINT_ID: &'static str = "GetRepositoryFile";
}
/// Request for `GetProject` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::GetProjectRequest;
///
/// let request = GetProjectRequest::new("id_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetProjectRequest {
    /// Path parameter: id
    pub id: String,
}
impl GetProjectRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self { id: id.into() }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!("/projects/{}", urlencoding::encode(& self.id.to_string()));
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for GetProjectRequest {
    fn from(param: &str) -> Self {
        Self { id: param.to_string() }
    }
}
impl From<String> for GetProjectRequest {
    fn from(param: String) -> Self {
        Self { id: param }
    }
}
impl crate::shared::EndpointSpec for GetProjectRequest {
    type Response = Project;
    const ENDPOINT_ID: &'static str = "GetProject";
}
/// Request for `ListGroupProjects` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListGroupProjectsRequest;
///
/// let request = ListGroupProjectsRequest::new("id_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///     .with_archived(/* value */)
///     .with_visibility(/* value */)
///     .with_order_by(/* value */)
///     .with_sort(/* value */)
///     .with_include_subgroups(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListGroupProjectsRequest {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
    /// Query parameter: Filter by archived status
    pub archived: Option<bool>,
    /// Query parameter: Filter by visibility level
    pub visibility: Option<String>,
    /// Query parameter: Sort field
    pub order_by: Option<String>,
    /// Query parameter: Sort direction
    pub sort: Option<String>,
    /// Query parameter: Include projects from subgroups
    pub include_subgroups: Option<bool>,
}
impl ListGroupProjectsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            page: None,
            per_page: None,
            archived: None,
            visibility: None,
            order_by: None,
            sort: None,
            include_subgroups: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Sets the `archived` query parameter.
    pub fn with_archived(mut self, value: bool) -> Self {
        self.archived = Some(value);
        self
    }
    /// Sets the `visibility` query parameter.
    pub fn with_visibility(mut self, value: String) -> Self {
        self.visibility = Some(value);
        self
    }
    /// Sets the `order_by` query parameter.
    pub fn with_order_by(mut self, value: String) -> Self {
        self.order_by = Some(value);
        self
    }
    /// Sets the `sort` query parameter.
    pub fn with_sort(mut self, value: String) -> Self {
        self.sort = Some(value);
        self
    }
    /// Sets the `include_subgroups` query parameter.
    pub fn with_include_subgroups(mut self, value: bool) -> Self {
        self.include_subgroups = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/groups/{}/projects", urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if let Some(ref value) = self.archived {
            query_pairs.push(("archived", value.to_string()));
        }
        if let Some(ref value) = self.visibility {
            query_pairs.push(("visibility", value.to_string()));
        }
        if let Some(ref value) = self.order_by {
            query_pairs.push(("order_by", value.to_string()));
        }
        if let Some(ref value) = self.sort {
            query_pairs.push(("sort", value.to_string()));
        }
        if let Some(ref value) = self.include_subgroups {
            query_pairs.push(("include_subgroups", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListGroupProjectsRequest {
    fn from(param: &str) -> Self {
        Self {
            id: param.to_string(),
            page: None,
            per_page: None,
            archived: None,
            visibility: None,
            order_by: None,
            sort: None,
            include_subgroups: None,
        }
    }
}
impl From<String> for ListGroupProjectsRequest {
    fn from(param: String) -> Self {
        Self {
            id: param,
            page: None,
            per_page: None,
            archived: None,
            visibility: None,
            order_by: None,
            sort: None,
            include_subgroups: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListGroupProjectsRequest {
    type Response = Vec<Project>;
    const ENDPOINT_ID: &'static str = "ListGroupProjects";
}
/// Request for `ListMergeRequests` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListMergeRequestsRequest;
///
/// let request = ListMergeRequestsRequest::new("id_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///     .with_state(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListMergeRequestsRequest {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
    /// Query parameter: Filter by merge request state
    pub state: Option<String>,
}
impl ListMergeRequestsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            page: None,
            per_page: None,
            state: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Sets the `state` query parameter.
    pub fn with_state(mut self, value: String) -> Self {
        self.state = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/merge_requests", urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if let Some(ref value) = self.state {
            query_pairs.push(("state", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListMergeRequestsRequest {
    fn from(param: &str) -> Self {
        Self {
            id: param.to_string(),
            page: None,
            per_page: None,
            state: None,
        }
    }
}
impl From<String> for ListMergeRequestsRequest {
    fn from(param: String) -> Self {
        Self {
            id: param,
            page: None,
            per_page: None,
            state: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListMergeRequestsRequest {
    type Response = Vec<MergeRequest>;
    const ENDPOINT_ID: &'static str = "ListMergeRequests";
}
/// Request for `GetMergeRequest` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::GetMergeRequestRequest;
///
/// let request = GetMergeRequestRequest::new("id_value", "merge_request_iid_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetMergeRequestRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: merge_request_iid
    pub merge_request_iid: String,
}
impl GetMergeRequestRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, merge_request_iid: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            merge_request_iid: merge_request_iid.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/merge_requests/{}", urlencoding::encode(& self.id.to_string()),
            urlencoding::encode(& self.merge_request_iid.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetMergeRequestRequest {
    type Response = MergeRequest;
    const ENDPOINT_ID: &'static str = "GetMergeRequest";
}
/// Request for `ListMergeRequestCommits` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListMergeRequestCommitsRequest;
///
/// let request = ListMergeRequestCommitsRequest::new("id_value", "merge_request_iid_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListMergeRequestCommitsRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: merge_request_iid
    pub merge_request_iid: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
}
impl ListMergeRequestCommitsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, merge_request_iid: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            merge_request_iid: merge_request_iid.into(),
            page: None,
            per_page: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/merge_requests/{}/commits", urlencoding::encode(& self.id
            .to_string()), urlencoding::encode(& self.merge_request_iid.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListMergeRequestCommitsRequest {
    type Response = Vec<Commit>;
    const ENDPOINT_ID: &'static str = "ListMergeRequestCommits";
}
/// Request for `ListMergeRequestChanges` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListMergeRequestChangesRequest;
///
/// let request = ListMergeRequestChangesRequest::new("id_value", "merge_request_iid_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListMergeRequestChangesRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: merge_request_iid
    pub merge_request_iid: String,
}
impl ListMergeRequestChangesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, merge_request_iid: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            merge_request_iid: merge_request_iid.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/merge_requests/{}/changes", urlencoding::encode(& self.id
            .to_string()), urlencoding::encode(& self.merge_request_iid.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListMergeRequestChangesRequest {
    type Response = MergeRequestChanges;
    const ENDPOINT_ID: &'static str = "ListMergeRequestChanges";
}
/// Request for `ListIssues` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListIssuesRequest;
///
/// let request = ListIssuesRequest::new("id_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///     .with_state(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssuesRequest {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
    /// Query parameter: Filter by issue state
    pub state: Option<String>,
}
impl ListIssuesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            page: None,
            per_page: None,
            state: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Sets the `state` query parameter.
    pub fn with_state(mut self, value: String) -> Self {
        self.state = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/issues", urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if let Some(ref value) = self.state {
            query_pairs.push(("state", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListIssuesRequest {
    fn from(param: &str) -> Self {
        Self {
            id: param.to_string(),
            page: None,
            per_page: None,
            state: None,
        }
    }
}
impl From<String> for ListIssuesRequest {
    fn from(param: String) -> Self {
        Self {
            id: param,
            page: None,
            per_page: None,
            state: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListIssuesRequest {
    type Response = Vec<Issue>;
    const ENDPOINT_ID: &'static str = "ListIssues";
}
/// Request for `GetIssue` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::GetIssueRequest;
///
/// let request = GetIssueRequest::new("id_value", "issue_iid_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetIssueRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: issue_iid
    pub issue_iid: String,
}
impl GetIssueRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, issue_iid: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            issue_iid: issue_iid.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/issues/{}", urlencoding::encode(& self.id.to_string()),
            urlencoding::encode(& self.issue_iid.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetIssueRequest {
    type Response = Issue;
    const ENDPOINT_ID: &'static str = "GetIssue";
}
/// Request for `ListIssueNotes` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListIssueNotesRequest;
///
/// let request = ListIssueNotesRequest::new("id_value", "issue_iid_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssueNotesRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: issue_iid
    pub issue_iid: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
}
impl ListIssueNotesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, issue_iid: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            issue_iid: issue_iid.into(),
            page: None,
            per_page: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/issues/{}/notes", urlencoding::encode(& self.id.to_string()),
            urlencoding::encode(& self.issue_iid.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssueNotesRequest {
    type Response = Vec<Note>;
    const ENDPOINT_ID: &'static str = "ListIssueNotes";
}
/// Request for `ListIssueParticipants` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListIssueParticipantsRequest;
///
/// let request = ListIssueParticipantsRequest::new("id_value", "issue_iid_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListIssueParticipantsRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: issue_iid
    pub issue_iid: String,
}
impl ListIssueParticipantsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, issue_iid: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            issue_iid: issue_iid.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/issues/{}/participants", urlencoding::encode(& self.id
            .to_string()), urlencoding::encode(& self.issue_iid.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for ListIssueParticipantsRequest {
    type Response = Vec<User>;
    const ENDPOINT_ID: &'static str = "ListIssueParticipants";
}
/// Request for `ListTags` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListTagsRequest;
///
/// let request = ListTagsRequest::new("id_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListTagsRequest {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
}
impl ListTagsRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            page: None,
            per_page: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/repository/tags", urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListTagsRequest {
    fn from(param: &str) -> Self {
        Self {
            id: param.to_string(),
            page: None,
            per_page: None,
        }
    }
}
impl From<String> for ListTagsRequest {
    fn from(param: String) -> Self {
        Self {
            id: param,
            page: None,
            per_page: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListTagsRequest {
    type Response = Vec<Tag>;
    const ENDPOINT_ID: &'static str = "ListTags";
}
/// Request for `GetTag` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::GetTagRequest;
///
/// let request = GetTagRequest::new("id_value", "tag_name_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetTagRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: tag_name
    pub tag_name: String,
}
impl GetTagRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, tag_name: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            tag_name: tag_name.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/repository/tags/{}", urlencoding::encode(& self.id
            .to_string()), urlencoding::encode(& self.tag_name.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetTagRequest {
    type Response = Tag;
    const ENDPOINT_ID: &'static str = "GetTag";
}
/// Request for `ListReleases` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListReleasesRequest;
///
/// let request = ListReleasesRequest::new("id_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListReleasesRequest {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
}
impl ListReleasesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            page: None,
            per_page: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/releases", urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListReleasesRequest {
    fn from(param: &str) -> Self {
        Self {
            id: param.to_string(),
            page: None,
            per_page: None,
        }
    }
}
impl From<String> for ListReleasesRequest {
    fn from(param: String) -> Self {
        Self {
            id: param,
            page: None,
            per_page: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListReleasesRequest {
    type Response = Vec<Release>;
    const ENDPOINT_ID: &'static str = "ListReleases";
}
/// Request for `GetRelease` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::GetReleaseRequest;
///
/// let request = GetReleaseRequest::new("id_value", "tag_name_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetReleaseRequest {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: tag_name
    pub tag_name: String,
}
impl GetReleaseRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>, tag_name: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            tag_name: tag_name.into(),
        }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/releases/{}", urlencoding::encode(& self.id.to_string()),
            urlencoding::encode(& self.tag_name.to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl crate::shared::EndpointSpec for GetReleaseRequest {
    type Response = Release;
    const ENDPOINT_ID: &'static str = "GetRelease";
}
/// Request for `GetLatestRelease` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::GetLatestReleaseRequest;
///
/// let request = GetLatestReleaseRequest::new("id_value")
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct GetLatestReleaseRequest {
    /// Path parameter: id
    pub id: String,
}
impl GetLatestReleaseRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self { id: id.into() }
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = format!(
            "/projects/{}/releases/permalink/latest", urlencoding::encode(& self.id
            .to_string())
        );
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for GetLatestReleaseRequest {
    fn from(param: &str) -> Self {
        Self { id: param.to_string() }
    }
}
impl From<String> for GetLatestReleaseRequest {
    fn from(param: String) -> Self {
        Self { id: param }
    }
}
impl crate::shared::EndpointSpec for GetLatestReleaseRequest {
    type Response = Release;
    const ENDPOINT_ID: &'static str = "GetLatestRelease";
}
/// Request for `ListProjectPipelines` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::gitlab::ListProjectPipelinesRequest;
///
/// let request = ListProjectPipelinesRequest::new("id_value")
///     .with_page(/* value */)
///     .with_per_page(/* value */)
///     .with_status(/* value */)
///     .with_source(/* value */)
///     .with_git_ref(/* value */)
///     .with_sha(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ListProjectPipelinesRequest {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: Page number (1-indexed, default: 1)
    pub page: Option<i64>,
    /// Query parameter: Items per page (default: 100, max: 100)
    pub per_page: Option<i64>,
    /// Query parameter: Filter by pipeline status
    pub status: Option<String>,
    /// Query parameter: Filter by pipeline source (e.g., push, merge_request_event, schedule)
    pub source: Option<String>,
    /// Query parameter: Filter by branch or tag name
    pub git_ref: Option<String>,
    /// Query parameter: Filter by commit SHA
    pub sha: Option<String>,
}
impl ListProjectPipelinesRequest {
    /// Creates a new request with the required path parameters.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            page: None,
            per_page: None,
            status: None,
            source: None,
            git_ref: None,
            sha: None,
        }
    }
    /// Sets the `page` query parameter.
    pub fn with_page(mut self, value: i64) -> Self {
        self.page = Some(value);
        self
    }
    /// Sets the `per_page` query parameter.
    pub fn with_per_page(mut self, value: i64) -> Self {
        self.per_page = Some(value);
        self
    }
    /// Sets the `status` query parameter.
    pub fn with_status(mut self, value: String) -> Self {
        self.status = Some(value);
        self
    }
    /// Sets the `source` query parameter.
    pub fn with_source(mut self, value: String) -> Self {
        self.source = Some(value);
        self
    }
    /// Sets the `git_ref` query parameter.
    pub fn with_git_ref(mut self, value: String) -> Self {
        self.git_ref = Some(value);
        self
    }
    /// Sets the `sha` query parameter.
    pub fn with_sha(mut self, value: String) -> Self {
        self.sha = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - The request body as a `RequestBody`
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if a JSON request body
    /// fails to serialize.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = format!(
            "/projects/{}/pipelines", urlencoding::encode(& self.id.to_string())
        );
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.page {
            query_pairs.push(("page", value.to_string()));
        }
        if let Some(ref value) = self.per_page {
            query_pairs.push(("per_page", value.to_string()));
        }
        if let Some(ref value) = self.status {
            query_pairs.push(("status", value.to_string()));
        }
        if let Some(ref value) = self.source {
            query_pairs.push(("source", value.to_string()));
        }
        if let Some(ref value) = self.git_ref {
            query_pairs.push(("git_ref", value.to_string()));
        }
        if let Some(ref value) = self.sha {
            query_pairs.push(("sha", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| {
                    format!("{}={}", urlencoding::encode(k), urlencoding::encode(v))
                })
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, crate::shared::RequestBody::Empty, vec![]))
    }
}
impl From<&str> for ListProjectPipelinesRequest {
    fn from(param: &str) -> Self {
        Self {
            id: param.to_string(),
            page: None,
            per_page: None,
            status: None,
            source: None,
            git_ref: None,
            sha: None,
        }
    }
}
impl From<String> for ListProjectPipelinesRequest {
    fn from(param: String) -> Self {
        Self {
            id: param,
            page: None,
            per_page: None,
            status: None,
            source: None,
            git_ref: None,
            sha: None,
        }
    }
}
impl crate::shared::EndpointSpec for ListProjectPipelinesRequest {
    type Response = Vec<Pipeline>;
    const ENDPOINT_ID: &'static str = "ListProjectPipelines";
}
impl Paginated for ListRepositoryTreeRequest {}
impl Paginated for ListGroupProjectsRequest {}
impl Paginated for ListMergeRequestsRequest {}
impl Paginated for ListMergeRequestCommitsRequest {}
impl Paginated for ListIssuesRequest {}
impl Paginated for ListIssueNotesRequest {}
impl Paginated for ListTagsRequest {}
impl Paginated for ListReleasesRequest {}
impl Paginated for ListProjectPipelinesRequest {}
/// Request enum for GitLab API.
///
/// Each variant wraps a strongly-typed request struct.
pub enum GitLabRequest {
    /// List repository tree recursively (files and directories)
    ListRepositoryTree(ListRepositoryTreeRequest),
    /// Get file content (Base64 encoded, provide git_ref for branch/tag)
    GetRepositoryFile(GetRepositoryFileRequest),
    /// Get project metadata (use URL-encoded path or numeric ID)
    GetProject(GetProjectRequest),
    /// List projects in a group
    ListGroupProjects(ListGroupProjectsRequest),
    /// List merge requests with metadata
    ListMergeRequests(ListMergeRequestsRequest),
    /// Get a single merge request by IID
    GetMergeRequest(GetMergeRequestRequest),
    /// List commits in a merge request
    ListMergeRequestCommits(ListMergeRequestCommitsRequest),
    /// Get merge request with file changes/diffs
    ListMergeRequestChanges(ListMergeRequestChangesRequest),
    /// List issues
    ListIssues(ListIssuesRequest),
    /// Get a single issue by IID
    GetIssue(GetIssueRequest),
    /// List comments/notes on an issue
    ListIssueNotes(ListIssueNotesRequest),
    /// List participants on an issue
    ListIssueParticipants(ListIssueParticipantsRequest),
    /// List repository tags (check `release` field for release tags)
    ListTags(ListTagsRequest),
    /// Get a single tag by name
    GetTag(GetTagRequest),
    /// List releases
    ListReleases(ListReleasesRequest),
    /// Get a single release by tag name
    GetRelease(GetReleaseRequest),
    /// Get the latest release
    GetLatestRelease(GetLatestReleaseRequest),
    /// List CI/CD pipelines for a project
    ListProjectPipelines(ListProjectPipelinesRequest),
}
impl GitLabRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// Delegates to the inner request struct's `into_parts()` method.
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        match self {
            Self::ListRepositoryTree(req) => req.into_parts(),
            Self::GetRepositoryFile(req) => req.into_parts(),
            Self::GetProject(req) => req.into_parts(),
            Self::ListGroupProjects(req) => req.into_parts(),
            Self::ListMergeRequests(req) => req.into_parts(),
            Self::GetMergeRequest(req) => req.into_parts(),
            Self::ListMergeRequestCommits(req) => req.into_parts(),
            Self::ListMergeRequestChanges(req) => req.into_parts(),
            Self::ListIssues(req) => req.into_parts(),
            Self::GetIssue(req) => req.into_parts(),
            Self::ListIssueNotes(req) => req.into_parts(),
            Self::ListIssueParticipants(req) => req.into_parts(),
            Self::ListTags(req) => req.into_parts(),
            Self::GetTag(req) => req.into_parts(),
            Self::ListReleases(req) => req.into_parts(),
            Self::GetRelease(req) => req.into_parts(),
            Self::GetLatestRelease(req) => req.into_parts(),
            Self::ListProjectPipelines(req) => req.into_parts(),
        }
    }
    /// Returns the endpoint identifier for this request.
    ///
    /// This is used internally for response hook lookup.
    #[must_use]
    pub fn endpoint_id(&self) -> &'static str {
        match self {
            Self::ListRepositoryTree(_) => {
                <ListRepositoryTreeRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetRepositoryFile(_) => {
                <GetRepositoryFileRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetProject(_) => {
                <GetProjectRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListGroupProjects(_) => {
                <ListGroupProjectsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListMergeRequests(_) => {
                <ListMergeRequestsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetMergeRequest(_) => {
                <GetMergeRequestRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListMergeRequestCommits(_) => {
                <ListMergeRequestCommitsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListMergeRequestChanges(_) => {
                <ListMergeRequestChangesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssues(_) => {
                <ListIssuesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetIssue(_) => {
                <GetIssueRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssueNotes(_) => {
                <ListIssueNotesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListIssueParticipants(_) => {
                <ListIssueParticipantsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListTags(_) => {
                <ListTagsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetTag(_) => {
                <GetTagRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListReleases(_) => {
                <ListReleasesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetRelease(_) => {
                <GetReleaseRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::GetLatestRelease(_) => {
                <GetLatestReleaseRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::ListProjectPipelines(_) => {
                <ListProjectPipelinesRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
        }
    }
    /// Returns how this endpoint's response body is decoded.
    ///
    /// The generic `request::<T>()` decodes JSON; it consults this to
    /// reject text, binary, and empty endpoints up front.
    #[must_use]
    pub fn response_kind(&self) -> crate::shared::ResponseKind {
        match self {
            Self::ListRepositoryTree(_) => crate::shared::ResponseKind::Json,
            Self::GetRepositoryFile(_) => crate::shared::ResponseKind::Json,
            Self::GetProject(_) => crate::shared::ResponseKind::Json,
            Self::ListGroupProjects(_) => crate::shared::ResponseKind::Json,
            Self::ListMergeRequests(_) => crate::shared::ResponseKind::Json,
            Self::GetMergeRequest(_) => crate::shared::ResponseKind::Json,
            Self::ListMergeRequestCommits(_) => crate::shared::ResponseKind::Json,
            Self::ListMergeRequestChanges(_) => crate::shared::ResponseKind::Json,
            Self::ListIssues(_) => crate::shared::ResponseKind::Json,
            Self::GetIssue(_) => crate::shared::ResponseKind::Json,
            Self::ListIssueNotes(_) => crate::shared::ResponseKind::Json,
            Self::ListIssueParticipants(_) => crate::shared::ResponseKind::Json,
            Self::ListTags(_) => crate::shared::ResponseKind::Json,
            Self::GetTag(_) => crate::shared::ResponseKind::Json,
            Self::ListReleases(_) => crate::shared::ResponseKind::Json,
            Self::GetRelease(_) => crate::shared::ResponseKind::Json,
            Self::GetLatestRelease(_) => crate::shared::ResponseKind::Json,
            Self::ListProjectPipelines(_) => crate::shared::ResponseKind::Json,
        }
    }
}
impl From<ListRepositoryTreeRequest> for GitLabRequest {
    fn from(req: ListRepositoryTreeRequest) -> Self {
        Self::ListRepositoryTree(req)
    }
}
impl From<GetRepositoryFileRequest> for GitLabRequest {
    fn from(req: GetRepositoryFileRequest) -> Self {
        Self::GetRepositoryFile(req)
    }
}
impl From<GetProjectRequest> for GitLabRequest {
    fn from(req: GetProjectRequest) -> Self {
        Self::GetProject(req)
    }
}
impl From<ListGroupProjectsRequest> for GitLabRequest {
    fn from(req: ListGroupProjectsRequest) -> Self {
        Self::ListGroupProjects(req)
    }
}
impl From<ListMergeRequestsRequest> for GitLabRequest {
    fn from(req: ListMergeRequestsRequest) -> Self {
        Self::ListMergeRequests(req)
    }
}
impl From<GetMergeRequestRequest> for GitLabRequest {
    fn from(req: GetMergeRequestRequest) -> Self {
        Self::GetMergeRequest(req)
    }
}
impl From<ListMergeRequestCommitsRequest> for GitLabRequest {
    fn from(req: ListMergeRequestCommitsRequest) -> Self {
        Self::ListMergeRequestCommits(req)
    }
}
impl From<ListMergeRequestChangesRequest> for GitLabRequest {
    fn from(req: ListMergeRequestChangesRequest) -> Self {
        Self::ListMergeRequestChanges(req)
    }
}
impl From<ListIssuesRequest> for GitLabRequest {
    fn from(req: ListIssuesRequest) -> Self {
        Self::ListIssues(req)
    }
}
impl From<GetIssueRequest> for GitLabRequest {
    fn from(req: GetIssueRequest) -> Self {
        Self::GetIssue(req)
    }
}
impl From<ListIssueNotesRequest> for GitLabRequest {
    fn from(req: ListIssueNotesRequest) -> Self {
        Self::ListIssueNotes(req)
    }
}
impl From<ListIssueParticipantsRequest> for GitLabRequest {
    fn from(req: ListIssueParticipantsRequest) -> Self {
        Self::ListIssueParticipants(req)
    }
}
impl From<ListTagsRequest> for GitLabRequest {
    fn from(req: ListTagsRequest) -> Self {
        Self::ListTags(req)
    }
}
impl From<GetTagRequest> for GitLabRequest {
    fn from(req: GetTagRequest) -> Self {
        Self::GetTag(req)
    }
}
impl From<ListReleasesRequest> for GitLabRequest {
    fn from(req: ListReleasesRequest) -> Self {
        Self::ListReleases(req)
    }
}
impl From<GetReleaseRequest> for GitLabRequest {
    fn from(req: GetReleaseRequest) -> Self {
        Self::GetRelease(req)
    }
}
impl From<GetLatestReleaseRequest> for GitLabRequest {
    fn from(req: GetLatestReleaseRequest) -> Self {
        Self::GetLatestRelease(req)
    }
}
impl From<ListProjectPipelinesRequest> for GitLabRequest {
    fn from(req: ListProjectPipelinesRequest) -> Self {
        Self::ListProjectPipelines(req)
    }
}
