// This code was automatically generated by schematic-gen. Do not edit manually.

//! Generated API client for [Eversolo](https://eversolo.com/Support/developer/).
//!
//! Eversolo DMP-A8 local-network HTTP control API (Zidoo lineage)
//!
//! ## Authentication
//!
//! No authentication required.
//!
//! ## Features
//!
//! **GET**:
//! - `DeviceGetModel` - Get device model, firmware, and network information
//! - `RemoteSendKey` - Send a remote control key command to the device
//! - `RemoteInputText` - Send text input to the device
//! - `MusicGetState` - Get current playback state, track info, and volume
//! - `MusicPlayOrPause` - Toggle play/pause on the current track
//! - `MusicPlayNext` - Skip to the next track
//! - `MusicPlayLast` - Go back to the previous track
//! - `MusicSeekTo` - Seek to a position in the current track
//! - `MusicGetInputOutputList` - List available audio inputs and outputs
//! - `MusicSetInput` - Set the active audio input
//! - `MusicSetOutput` - Set the active audio output
//! - `MusicSetVolume` - Set the absolute volume level
//! - `MusicSetMute` - Set or clear mute state
//! - `PowerGetOptions` - Get available power options (shutdown, reboot, etc.)
//! - `PowerSetOption` - Execute a power action (poweroff, reboot, screen, timeshutdown)
//! - `SystemGetScreenBrightness` - Get current screen brightness level (community-discovered)
//! - `SystemSetScreenBrightness` - Set screen brightness level (community-discovered)
//! - `SystemGetKnobBrightness` - Get current knob LED brightness level (community-discovered)
//! - `SystemSetKnobBrightness` - Set knob LED brightness level (community-discovered)
//! - `SystemGetVuModeList` - List available VU meter display modes (community-discovered)
//! - `SystemSetVuMode` - Set the VU meter display mode (community-discovered)
//! - `SystemGetSpectrumModeList` - List available spectrum display modes (community-discovered)
//! - `SystemSetSpectrumMode` - Set the spectrum display mode (community-discovered)
//! - `SystemChangeVuDisplay` - Toggle VU display open/close (community-discovered)
//!
//! ## Examples
//!
//! ```text
//! use schematic_schema::prelude::*;
//!
//! #[tokio::main]
//! async fn main() -> Result<(), SchematicError> {
//!     let client = Eversolo::new();
//!     let response = client.device_get_model().await?;
//!     println!("{:?}", response);
//!     Ok(())
//! }
//! ```
//!
//! ### Override Base URL
//!
//! ```text
//! use schematic_schema::prelude::*;
//!
//! let client = Eversolo::with_base_url("http://192.168.1.50:9529");
//! ```
use serde::{Deserialize, Serialize};
pub use schematic_definitions::eversolo::*;
use crate::shared::{RequestParts, SchematicError};
/// Request for `DeviceGetModel` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::DeviceGetModelRequest;
///
/// let request = DeviceGetModelRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct DeviceGetModelRequest {}
impl DeviceGetModelRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/ZidooControlCenter/getModel".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for DeviceGetModelRequest {
    type Response = GetModelResponse;
    const ENDPOINT_ID: &'static str = "DeviceGetModel";
}
/// Request for `RemoteSendKey` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::RemoteSendKeyRequest;
///
/// let request = RemoteSendKeyRequest::default()
///     .with_key(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct RemoteSendKeyRequest {
    /// Query parameter: Remote key constant (e.g., Key.VolumeUp, Key.MediaPlay, Key.PowerOff)
    pub key: Option<String>,
}
impl RemoteSendKeyRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { key: None }
    }
    /// Sets the `key` query parameter.
    pub fn with_key(mut self, value: String) -> Self {
        self.key = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooControlCenter/RemoteControl/sendkey".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.key {
            query_pairs.push(("key", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for RemoteSendKeyRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "RemoteSendKey";
}
/// Request for `RemoteInputText` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::RemoteInputTextRequest;
///
/// let request = RemoteInputTextRequest::default()
///     .with_text(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct RemoteInputTextRequest {
    /// Query parameter: Text string to input
    pub text: Option<String>,
}
impl RemoteInputTextRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { text: None }
    }
    /// Sets the `text` query parameter.
    pub fn with_text(mut self, value: String) -> Self {
        self.text = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooControlCenter/RemoteControl/inputtext".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.text {
            query_pairs.push(("text", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for RemoteInputTextRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "RemoteInputText";
}
/// Request for `MusicGetState` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicGetStateRequest;
///
/// let request = MusicGetStateRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicGetStateRequest {}
impl MusicGetStateRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/ZidooMusicControl/v2/getState".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicGetStateRequest {
    type Response = GetStateResponse;
    const ENDPOINT_ID: &'static str = "MusicGetState";
}
/// Request for `MusicPlayOrPause` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicPlayOrPauseRequest;
///
/// let request = MusicPlayOrPauseRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicPlayOrPauseRequest {}
impl MusicPlayOrPauseRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/ZidooMusicControl/v2/playOrPause".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicPlayOrPauseRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "MusicPlayOrPause";
}
/// Request for `MusicPlayNext` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicPlayNextRequest;
///
/// let request = MusicPlayNextRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicPlayNextRequest {}
impl MusicPlayNextRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/ZidooMusicControl/v2/playNext".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicPlayNextRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "MusicPlayNext";
}
/// Request for `MusicPlayLast` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicPlayLastRequest;
///
/// let request = MusicPlayLastRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicPlayLastRequest {}
impl MusicPlayLastRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/ZidooMusicControl/v2/playLast".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicPlayLastRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "MusicPlayLast";
}
/// Request for `MusicSeekTo` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicSeekToRequest;
///
/// let request = MusicSeekToRequest::default()
///     .with_time(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicSeekToRequest {
    /// Query parameter: Target position in milliseconds
    pub time: Option<i64>,
}
impl MusicSeekToRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { time: None }
    }
    /// Sets the `time` query parameter.
    pub fn with_time(mut self, value: i64) -> Self {
        self.time = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooMusicControl/v2/seekTo".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.time {
            query_pairs.push(("time", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicSeekToRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "MusicSeekTo";
}
/// Request for `MusicGetInputOutputList` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicGetInputOutputListRequest;
///
/// let request = MusicGetInputOutputListRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicGetInputOutputListRequest {}
impl MusicGetInputOutputListRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/ZidooMusicControl/v2/getInputAndOutputList".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicGetInputOutputListRequest {
    type Response = InputOutputListResponse;
    const ENDPOINT_ID: &'static str = "MusicGetInputOutputList";
}
/// Request for `MusicSetInput` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicSetInputRequest;
///
/// let request = MusicSetInputRequest::default()
///     .with_tag(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicSetInputRequest {
    /// Query parameter: Input tag from getInputAndOutputList
    pub tag: Option<String>,
}
impl MusicSetInputRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { tag: None }
    }
    /// Sets the `tag` query parameter.
    pub fn with_tag(mut self, value: String) -> Self {
        self.tag = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooMusicControl/v2/setInputList".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.tag {
            query_pairs.push(("tag", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicSetInputRequest {
    type Response = InputOutputListResponse;
    const ENDPOINT_ID: &'static str = "MusicSetInput";
}
/// Request for `MusicSetOutput` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicSetOutputRequest;
///
/// let request = MusicSetOutputRequest::default()
///     .with_tag(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicSetOutputRequest {
    /// Query parameter: Output tag from getInputAndOutputList
    pub tag: Option<String>,
}
impl MusicSetOutputRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { tag: None }
    }
    /// Sets the `tag` query parameter.
    pub fn with_tag(mut self, value: String) -> Self {
        self.tag = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooMusicControl/v2/setOutInputList".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.tag {
            query_pairs.push(("tag", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicSetOutputRequest {
    type Response = InputOutputListResponse;
    const ENDPOINT_ID: &'static str = "MusicSetOutput";
}
/// Request for `MusicSetVolume` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicSetVolumeRequest;
///
/// let request = MusicSetVolumeRequest::default()
///     .with_volume(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicSetVolumeRequest {
    /// Query parameter: Volume level (0 to maxVolume)
    pub volume: Option<i64>,
}
impl MusicSetVolumeRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { volume: None }
    }
    /// Sets the `volume` query parameter.
    pub fn with_volume(mut self, value: i64) -> Self {
        self.volume = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooMusicControl/v2/setDevicesVolume".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.volume {
            query_pairs.push(("volume", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicSetVolumeRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "MusicSetVolume";
}
/// Request for `MusicSetMute` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::MusicSetMuteRequest;
///
/// let request = MusicSetMuteRequest::default()
///     .with_is_mute(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MusicSetMuteRequest {
    /// Query parameter: Mute state: 0 = unmuted, 1 = muted
    pub is_mute: Option<i64>,
}
impl MusicSetMuteRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { is_mute: None }
    }
    /// Sets the `is_mute` query parameter.
    pub fn with_is_mute(mut self, value: i64) -> Self {
        self.is_mute = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooMusicControl/v2/setMuteVolume".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.is_mute {
            query_pairs.push(("isMute", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for MusicSetMuteRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "MusicSetMute";
}
/// Request for `PowerGetOptions` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::PowerGetOptionsRequest;
///
/// let request = PowerGetOptionsRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct PowerGetOptionsRequest {}
impl PowerGetOptionsRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/ZidooMusicControl/v2/getPowerOption".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for PowerGetOptionsRequest {
    type Response = PowerOptionsResponse;
    const ENDPOINT_ID: &'static str = "PowerGetOptions";
}
/// Request for `PowerSetOption` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::PowerSetOptionRequest;
///
/// let request = PowerSetOptionRequest::default()
///     .with_tag(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct PowerSetOptionRequest {
    /// Query parameter: Power option tag (poweroff, reboot, screen, timeshutdown)
    pub tag: Option<String>,
}
impl PowerSetOptionRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { tag: None }
    }
    /// Sets the `tag` query parameter.
    pub fn with_tag(mut self, value: String) -> Self {
        self.tag = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooMusicControl/v2/setPowerOption".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.tag {
            query_pairs.push(("tag", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for PowerSetOptionRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "PowerSetOption";
}
/// Request for `SystemGetScreenBrightness` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemGetScreenBrightnessRequest;
///
/// let request = SystemGetScreenBrightnessRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemGetScreenBrightnessRequest {}
impl SystemGetScreenBrightnessRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/SystemSettings/displaySettings/getScreenBrightness".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemGetScreenBrightnessRequest {
    type Response = BrightnessResponse;
    const ENDPOINT_ID: &'static str = "SystemGetScreenBrightness";
}
/// Request for `SystemSetScreenBrightness` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemSetScreenBrightnessRequest;
///
/// let request = SystemSetScreenBrightnessRequest::default()
///     .with_index(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemSetScreenBrightnessRequest {
    /// Query parameter: Brightness level index
    pub index: Option<i64>,
}
impl SystemSetScreenBrightnessRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { index: None }
    }
    /// Sets the `index` query parameter.
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/SystemSettings/displaySettings/setScreenBrightness".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.index {
            query_pairs.push(("index", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemSetScreenBrightnessRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "SystemSetScreenBrightness";
}
/// Request for `SystemGetKnobBrightness` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemGetKnobBrightnessRequest;
///
/// let request = SystemGetKnobBrightnessRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemGetKnobBrightnessRequest {}
impl SystemGetKnobBrightnessRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/SystemSettings/displaySettings/getKnobBrightness".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemGetKnobBrightnessRequest {
    type Response = BrightnessResponse;
    const ENDPOINT_ID: &'static str = "SystemGetKnobBrightness";
}
/// Request for `SystemSetKnobBrightness` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemSetKnobBrightnessRequest;
///
/// let request = SystemSetKnobBrightnessRequest::default()
///     .with_index(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemSetKnobBrightnessRequest {
    /// Query parameter: Brightness level index
    pub index: Option<i64>,
}
impl SystemSetKnobBrightnessRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { index: None }
    }
    /// Sets the `index` query parameter.
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/SystemSettings/displaySettings/setKnobBrightness".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.index {
            query_pairs.push(("index", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemSetKnobBrightnessRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "SystemSetKnobBrightness";
}
/// Request for `SystemGetVuModeList` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemGetVuModeListRequest;
///
/// let request = SystemGetVuModeListRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemGetVuModeListRequest {}
impl SystemGetVuModeListRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/SystemSettings/displaySettings/getVUModeList".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemGetVuModeListRequest {
    type Response = DisplayModeListResponse;
    const ENDPOINT_ID: &'static str = "SystemGetVuModeList";
}
/// Request for `SystemSetVuMode` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemSetVuModeRequest;
///
/// let request = SystemSetVuModeRequest::default()
///     .with_index(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemSetVuModeRequest {
    /// Query parameter: VU mode index from getVUModeList
    pub index: Option<i64>,
}
impl SystemSetVuModeRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { index: None }
    }
    /// Sets the `index` query parameter.
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/SystemSettings/displaySettings/setVUMode".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.index {
            query_pairs.push(("index", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemSetVuModeRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "SystemSetVuMode";
}
/// Request for `SystemGetSpectrumModeList` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemGetSpectrumModeListRequest;
///
/// let request = SystemGetSpectrumModeListRequest::default()
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemGetSpectrumModeListRequest {}
impl SystemGetSpectrumModeListRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let path = "/SystemSettings/displaySettings/getSpPlayModeList".to_string();
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemGetSpectrumModeListRequest {
    type Response = DisplayModeListResponse;
    const ENDPOINT_ID: &'static str = "SystemGetSpectrumModeList";
}
/// Request for `SystemSetSpectrumMode` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemSetSpectrumModeRequest;
///
/// let request = SystemSetSpectrumModeRequest::default()
///     .with_index(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemSetSpectrumModeRequest {
    /// Query parameter: Spectrum mode index from getSpPlayModeList
    pub index: Option<i64>,
}
impl SystemSetSpectrumModeRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { index: None }
    }
    /// Sets the `index` query parameter.
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/SystemSettings/displaySettings/setSpPlayModeList".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.index {
            query_pairs.push(("index", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemSetSpectrumModeRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "SystemSetSpectrumMode";
}
/// Request for `SystemChangeVuDisplay` endpoint.
///
/// ## Example
///
/// ```text
/// use schematic_schema::eversolo::SystemChangeVuDisplayRequest;
///
/// let request = SystemChangeVuDisplayRequest::default()
///     .with_open_type(/* value */)
///;
/// ```
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct SystemChangeVuDisplayRequest {
    /// Query parameter: Display open type
    pub open_type: Option<i64>,
}
impl SystemChangeVuDisplayRequest {
    /// Creates a new request with default values.
    pub fn new() -> Self {
        Self { open_type: None }
    }
    /// Sets the `open_type` query parameter.
    pub fn with_open_type(mut self, value: i64) -> Self {
        self.open_type = Some(value);
        self
    }
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// ## Returns
    ///
    /// A tuple of:
    /// - HTTP method as a static string (e.g., "GET", "POST")
    /// - Fully substituted path string with query parameters
    /// - Optional JSON body string
    /// - Endpoint-specific headers as key-value pairs
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        let mut path = "/ZidooMusicControl/v2/changVUDisplay".to_string();
        let mut query_pairs: Vec<(&str, String)> = Vec::new();
        if let Some(ref value) = self.open_type {
            query_pairs.push(("openType", value.to_string()));
        }
        if !query_pairs.is_empty() {
            let query_string: String = query_pairs
                .iter()
                .map(|(k, v)| format!("{}={}", k, urlencoding::encode(v)))
                .collect::<Vec<_>>()
                .join("&");
            if path.contains('?') {
                path.push_str(&format!("&{}", query_string));
            } else {
                path.push_str(&format!("?{}", query_string));
            }
        }
        Ok(("GET", path, None, vec![]))
    }
}
impl crate::shared::EndpointSpec for SystemChangeVuDisplayRequest {
    type Response = StatusResponse;
    const ENDPOINT_ID: &'static str = "SystemChangeVuDisplay";
}
/// Request enum for Eversolo API.
///
/// Each variant wraps a strongly-typed request struct.
pub enum EversoloRequest {
    /// Get device model, firmware, and network information
    DeviceGetModel(DeviceGetModelRequest),
    /// Send a remote control key command to the device
    RemoteSendKey(RemoteSendKeyRequest),
    /// Send text input to the device
    RemoteInputText(RemoteInputTextRequest),
    /// Get current playback state, track info, and volume
    MusicGetState(MusicGetStateRequest),
    /// Toggle play/pause on the current track
    MusicPlayOrPause(MusicPlayOrPauseRequest),
    /// Skip to the next track
    MusicPlayNext(MusicPlayNextRequest),
    /// Go back to the previous track
    MusicPlayLast(MusicPlayLastRequest),
    /// Seek to a position in the current track
    MusicSeekTo(MusicSeekToRequest),
    /// List available audio inputs and outputs
    MusicGetInputOutputList(MusicGetInputOutputListRequest),
    /// Set the active audio input
    MusicSetInput(MusicSetInputRequest),
    /// Set the active audio output
    MusicSetOutput(MusicSetOutputRequest),
    /// Set the absolute volume level
    MusicSetVolume(MusicSetVolumeRequest),
    /// Set or clear mute state
    MusicSetMute(MusicSetMuteRequest),
    /// Get available power options (shutdown, reboot, etc.)
    PowerGetOptions(PowerGetOptionsRequest),
    /// Execute a power action (poweroff, reboot, screen, timeshutdown)
    PowerSetOption(PowerSetOptionRequest),
    /// Get current screen brightness level (community-discovered)
    SystemGetScreenBrightness(SystemGetScreenBrightnessRequest),
    /// Set screen brightness level (community-discovered)
    SystemSetScreenBrightness(SystemSetScreenBrightnessRequest),
    /// Get current knob LED brightness level (community-discovered)
    SystemGetKnobBrightness(SystemGetKnobBrightnessRequest),
    /// Set knob LED brightness level (community-discovered)
    SystemSetKnobBrightness(SystemSetKnobBrightnessRequest),
    /// List available VU meter display modes (community-discovered)
    SystemGetVuModeList(SystemGetVuModeListRequest),
    /// Set the VU meter display mode (community-discovered)
    SystemSetVuMode(SystemSetVuModeRequest),
    /// List available spectrum display modes (community-discovered)
    SystemGetSpectrumModeList(SystemGetSpectrumModeListRequest),
    /// Set the spectrum display mode (community-discovered)
    SystemSetSpectrumMode(SystemSetSpectrumModeRequest),
    /// Toggle VU display open/close (community-discovered)
    SystemChangeVuDisplay(SystemChangeVuDisplayRequest),
}
impl EversoloRequest {
    /// Converts the request into (method, path, body, headers) parts.
    ///
    /// Delegates to the inner request struct's `into_parts()` method.
    ///
    /// ## Errors
    ///
    /// Returns `SchematicError::SerializationError` if the request body
    /// fails to serialize to JSON.
    pub fn into_parts(self) -> Result<RequestParts, SchematicError> {
        match self {
            Self::DeviceGetModel(req) => req.into_parts(),
            Self::RemoteSendKey(req) => req.into_parts(),
            Self::RemoteInputText(req) => req.into_parts(),
            Self::MusicGetState(req) => req.into_parts(),
            Self::MusicPlayOrPause(req) => req.into_parts(),
            Self::MusicPlayNext(req) => req.into_parts(),
            Self::MusicPlayLast(req) => req.into_parts(),
            Self::MusicSeekTo(req) => req.into_parts(),
            Self::MusicGetInputOutputList(req) => req.into_parts(),
            Self::MusicSetInput(req) => req.into_parts(),
            Self::MusicSetOutput(req) => req.into_parts(),
            Self::MusicSetVolume(req) => req.into_parts(),
            Self::MusicSetMute(req) => req.into_parts(),
            Self::PowerGetOptions(req) => req.into_parts(),
            Self::PowerSetOption(req) => req.into_parts(),
            Self::SystemGetScreenBrightness(req) => req.into_parts(),
            Self::SystemSetScreenBrightness(req) => req.into_parts(),
            Self::SystemGetKnobBrightness(req) => req.into_parts(),
            Self::SystemSetKnobBrightness(req) => req.into_parts(),
            Self::SystemGetVuModeList(req) => req.into_parts(),
            Self::SystemSetVuMode(req) => req.into_parts(),
            Self::SystemGetSpectrumModeList(req) => req.into_parts(),
            Self::SystemSetSpectrumMode(req) => req.into_parts(),
            Self::SystemChangeVuDisplay(req) => req.into_parts(),
        }
    }
    /// Returns the endpoint identifier for this request.
    ///
    /// This is used internally for response hook lookup.
    #[must_use]
    pub fn endpoint_id(&self) -> &'static str {
        match self {
            Self::DeviceGetModel(_) => {
                <DeviceGetModelRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::RemoteSendKey(_) => {
                <RemoteSendKeyRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::RemoteInputText(_) => {
                <RemoteInputTextRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicGetState(_) => {
                <MusicGetStateRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicPlayOrPause(_) => {
                <MusicPlayOrPauseRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicPlayNext(_) => {
                <MusicPlayNextRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicPlayLast(_) => {
                <MusicPlayLastRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicSeekTo(_) => {
                <MusicSeekToRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicGetInputOutputList(_) => {
                <MusicGetInputOutputListRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicSetInput(_) => {
                <MusicSetInputRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicSetOutput(_) => {
                <MusicSetOutputRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicSetVolume(_) => {
                <MusicSetVolumeRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::MusicSetMute(_) => {
                <MusicSetMuteRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::PowerGetOptions(_) => {
                <PowerGetOptionsRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::PowerSetOption(_) => {
                <PowerSetOptionRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemGetScreenBrightness(_) => {
                <SystemGetScreenBrightnessRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemSetScreenBrightness(_) => {
                <SystemSetScreenBrightnessRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemGetKnobBrightness(_) => {
                <SystemGetKnobBrightnessRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemSetKnobBrightness(_) => {
                <SystemSetKnobBrightnessRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemGetVuModeList(_) => {
                <SystemGetVuModeListRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemSetVuMode(_) => {
                <SystemSetVuModeRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemGetSpectrumModeList(_) => {
                <SystemGetSpectrumModeListRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemSetSpectrumMode(_) => {
                <SystemSetSpectrumModeRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
            Self::SystemChangeVuDisplay(_) => {
                <SystemChangeVuDisplayRequest as crate::shared::EndpointSpec>::ENDPOINT_ID
            }
        }
    }
}
impl From<DeviceGetModelRequest> for EversoloRequest {
    fn from(req: DeviceGetModelRequest) -> Self {
        Self::DeviceGetModel(req)
    }
}
impl From<RemoteSendKeyRequest> for EversoloRequest {
    fn from(req: RemoteSendKeyRequest) -> Self {
        Self::RemoteSendKey(req)
    }
}
impl From<RemoteInputTextRequest> for EversoloRequest {
    fn from(req: RemoteInputTextRequest) -> Self {
        Self::RemoteInputText(req)
    }
}
impl From<MusicGetStateRequest> for EversoloRequest {
    fn from(req: MusicGetStateRequest) -> Self {
        Self::MusicGetState(req)
    }
}
impl From<MusicPlayOrPauseRequest> for EversoloRequest {
    fn from(req: MusicPlayOrPauseRequest) -> Self {
        Self::MusicPlayOrPause(req)
    }
}
impl From<MusicPlayNextRequest> for EversoloRequest {
    fn from(req: MusicPlayNextRequest) -> Self {
        Self::MusicPlayNext(req)
    }
}
impl From<MusicPlayLastRequest> for EversoloRequest {
    fn from(req: MusicPlayLastRequest) -> Self {
        Self::MusicPlayLast(req)
    }
}
impl From<MusicSeekToRequest> for EversoloRequest {
    fn from(req: MusicSeekToRequest) -> Self {
        Self::MusicSeekTo(req)
    }
}
impl From<MusicGetInputOutputListRequest> for EversoloRequest {
    fn from(req: MusicGetInputOutputListRequest) -> Self {
        Self::MusicGetInputOutputList(req)
    }
}
impl From<MusicSetInputRequest> for EversoloRequest {
    fn from(req: MusicSetInputRequest) -> Self {
        Self::MusicSetInput(req)
    }
}
impl From<MusicSetOutputRequest> for EversoloRequest {
    fn from(req: MusicSetOutputRequest) -> Self {
        Self::MusicSetOutput(req)
    }
}
impl From<MusicSetVolumeRequest> for EversoloRequest {
    fn from(req: MusicSetVolumeRequest) -> Self {
        Self::MusicSetVolume(req)
    }
}
impl From<MusicSetMuteRequest> for EversoloRequest {
    fn from(req: MusicSetMuteRequest) -> Self {
        Self::MusicSetMute(req)
    }
}
impl From<PowerGetOptionsRequest> for EversoloRequest {
    fn from(req: PowerGetOptionsRequest) -> Self {
        Self::PowerGetOptions(req)
    }
}
impl From<PowerSetOptionRequest> for EversoloRequest {
    fn from(req: PowerSetOptionRequest) -> Self {
        Self::PowerSetOption(req)
    }
}
impl From<SystemGetScreenBrightnessRequest> for EversoloRequest {
    fn from(req: SystemGetScreenBrightnessRequest) -> Self {
        Self::SystemGetScreenBrightness(req)
    }
}
impl From<SystemSetScreenBrightnessRequest> for EversoloRequest {
    fn from(req: SystemSetScreenBrightnessRequest) -> Self {
        Self::SystemSetScreenBrightness(req)
    }
}
impl From<SystemGetKnobBrightnessRequest> for EversoloRequest {
    fn from(req: SystemGetKnobBrightnessRequest) -> Self {
        Self::SystemGetKnobBrightness(req)
    }
}
impl From<SystemSetKnobBrightnessRequest> for EversoloRequest {
    fn from(req: SystemSetKnobBrightnessRequest) -> Self {
        Self::SystemSetKnobBrightness(req)
    }
}
impl From<SystemGetVuModeListRequest> for EversoloRequest {
    fn from(req: SystemGetVuModeListRequest) -> Self {
        Self::SystemGetVuModeList(req)
    }
}
impl From<SystemSetVuModeRequest> for EversoloRequest {
    fn from(req: SystemSetVuModeRequest) -> Self {
        Self::SystemSetVuMode(req)
    }
}
impl From<SystemGetSpectrumModeListRequest> for EversoloRequest {
    fn from(req: SystemGetSpectrumModeListRequest) -> Self {
        Self::SystemGetSpectrumModeList(req)
    }
}
impl From<SystemSetSpectrumModeRequest> for EversoloRequest {
    fn from(req: SystemSetSpectrumModeRequest) -> Self {
        Self::SystemSetSpectrumMode(req)
    }
}
impl From<SystemChangeVuDisplayRequest> for EversoloRequest {
    fn from(req: SystemChangeVuDisplayRequest) -> Self {
        Self::SystemChangeVuDisplay(req)
    }
}
/// Eversolo DMP-A8 local-network HTTP control API (Zidoo lineage) client.
pub struct Eversolo {
    client: reqwest::Client,
    base_url: String,
    /// Environment variable names for authentication credentials.
    env_auth: Vec<String>,
    /// Authentication strategy for this API client.
    auth_strategy: schematic_define::AuthStrategy,
    /// Environment variable for Basic auth username.
    env_username: Option<String>,
    /// Headers builder with environment variable support for credentials.
    headers: schematic_define::Headers,
    /// Variant-specific hooks for response customization.
    variant_hooks: crate::shared::VariantHooks,
}
impl Eversolo {
    /// Base URL for the API.
    pub const BASE_URL: &'static str = "http://192.168.1.1:9529";
    /// Official API documentation URL, if available.
    pub const DOCS_URL: Option<&'static str> = Some(
        "https://eversolo.com/Support/developer/",
    );
    /// Creates a new API client with the default base URL.
    pub fn new() -> Self {
        Self {
            client: reqwest::Client::new(),
            base_url: Self::BASE_URL.to_string(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::None,
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Creates a new API client with a custom base URL.
    ///
    /// ## Examples
    ///
    /// ```text
    /// let client = Api::with_base_url("http://localhost:8080/v1");
    /// ```
    pub fn with_base_url(base_url: impl Into<String>) -> Self {
        Self {
            client: reqwest::Client::new(),
            base_url: base_url.into(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::None,
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Creates a new API client with a pre-configured reqwest client.
    ///
    /// Use this when you need custom timeouts, connection pools, or middleware.
    ///
    /// ## Examples
    ///
    /// ```text
    /// let custom_client = reqwest::Client::builder()
    ///     .timeout(std::time::Duration::from_secs(60))
    ///     .build()
    ///     .unwrap();
    /// let api = Api::with_client(custom_client);
    /// ```
    pub fn with_client(client: reqwest::Client) -> Self {
        Self {
            client,
            base_url: Self::BASE_URL.to_string(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::None,
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Creates a new API client with a pre-configured reqwest client and custom base URL.
    ///
    /// ## Examples
    ///
    /// ```text
    /// let custom_client = reqwest::Client::builder()
    ///     .timeout(std::time::Duration::from_secs(60))
    ///     .build()
    ///     .unwrap();
    /// let api = Api::with_client_and_base_url(custom_client, "http://localhost:8080");
    /// ```
    pub fn with_client_and_base_url(
        client: reqwest::Client,
        base_url: impl Into<String>,
    ) -> Self {
        Self {
            client,
            base_url: base_url.into(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::None,
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Creates a variant builder for customizing this API client.
    ///
    /// The builder pattern allows fluent configuration of:
    /// - Base URL (for proxies, mock servers, or different environments)
    /// - Authentication credentials (different env var names)
    /// - Authentication strategy
    /// - Response hooks for JSON transformation and mutation
    ///
    /// ## Examples
    ///
    /// ```text
    /// use schematic_define::UpdateStrategy;
    ///
    /// let api = Api::new();
    ///
    /// // Create a variant pointing to a staging server with a response hook
    /// let staging = api.variant()
    ///     .base_url("https://staging.api.com/v1")
    ///     .env_auth(vec!["STAGING_API_KEY".to_string()])
    ///     .mutate_response::<ListModelsRequest>(|ctx, response| {
    ///         response.data.retain(|m| !m.id.contains("deprecated"));
    ///         Ok(())
    ///     })
    ///     .build();
    /// ```
    #[must_use]
    pub fn variant(&self) -> EversoloVariantBuilder<'_> {
        EversoloVariantBuilder::new(self)
    }
    /// Creates a variant of this API client with different configuration.
    ///
    /// This is a convenience method equivalent to:
    /// ```text
    /// api.variant()
    ///     .base_url(base_url)
    ///     .env_auth(env_auth)
    ///     .auth_update(strategy)
    ///     .build()
    /// ```
    ///
    /// ## Arguments
    ///
    /// * `base_url` - New base URL for this variant
    /// * `env_auth` - New environment variable names for credentials
    /// * `strategy` - How to update the auth strategy:
    ///   - `UpdateStrategy::NoChange` - Keep current auth strategy
    ///   - `UpdateStrategy::ChangeTo(auth)` - Use specified auth strategy
    pub fn variant_with(
        &self,
        base_url: impl Into<String>,
        env_auth: Vec<String>,
        strategy: schematic_define::UpdateStrategy,
    ) -> Self {
        self.variant()
            .base_url(base_url)
            .env_auth(env_auth)
            .auth_update(strategy)
            .build()
    }
    /// Creates a variant of this API client with custom headers configuration.
    ///
    /// This is a convenience method for creating variants with fully customized
    /// headers, including environment variable mapping for credentials.
    ///
    /// ## Arguments
    ///
    /// * `headers` - Headers builder with environment mapping configured
    ///
    /// ## Examples
    ///
    /// ```text
    /// use schematic_define::{Headers, EnvMapping, EnvList};
    ///
    /// let custom_headers = Headers::default()
    ///     .with_env_mapping(EnvMapping {
    ///         bearer_token: Some(EnvList::single("STAGING_TOKEN")),
    ///         basic_user: None,
    ///         basic_pass: None,
    ///         api_key: None,
    ///     })
    ///     .header("X-Environment", "staging");
    ///
    /// let staging_client = api.variant_with_headers(custom_headers);
    /// ```
    pub fn variant_with_headers(&self, headers: schematic_define::Headers) -> Self {
        self.variant().headers_builder(headers).build()
    }
    /// Returns a reference to the underlying HTTP client.
    ///
    /// Use this for custom requests that aren't covered by the generated methods,
    /// such as paginated endpoints that require query parameters.
    pub fn http_client(&self) -> &reqwest::Client {
        &self.client
    }
    /// Returns the base URL for this API client.
    pub fn api_base_url(&self) -> &str {
        &self.base_url
    }
    /// Returns the API key header name and value for authentication.
    ///
    /// Returns `None` if the authentication strategy is not `ApiKey`
    /// or if the API key environment variable is not set.
    pub fn api_key_header(&self) -> Option<(String, String)> {
        match &self.auth_strategy {
            schematic_define::AuthStrategy::ApiKey { header } => {
                for env_name in &self.env_auth {
                    if let Ok(value) = std::env::var(env_name) {
                        return Some((header.clone(), value));
                    }
                }
                None
            }
            _ => None,
        }
    }
}
impl Default for Eversolo {
    fn default() -> Self {
        Self::new()
    }
}
/// Builder for creating customized variants of the API client.
///
/// Use [`#struct_name::variant()`] to create a builder, then chain
/// configuration methods and call [`build()`](Self::build) to create
/// the variant client.
pub struct EversoloVariantBuilder<'a> {
    base: &'a Eversolo,
    base_url: Option<String>,
    env_auth: Option<Vec<String>>,
    auth_update: schematic_define::UpdateStrategy,
    headers: Option<schematic_define::Headers>,
    pre_response_json: Option<std::sync::Arc<crate::shared::PreResponseJsonHook>>,
    response_mutators: std::collections::HashMap<
        &'static str,
        std::sync::Arc<dyn crate::shared::AnyResponseMutator>,
    >,
}
impl<'a> EversoloVariantBuilder<'a> {
    /// Creates a new variant builder from the base API client.
    fn new(base: &'a Eversolo) -> Self {
        Self {
            base,
            base_url: None,
            env_auth: None,
            auth_update: schematic_define::UpdateStrategy::NoChange,
            headers: None,
            pre_response_json: None,
            response_mutators: std::collections::HashMap::new(),
        }
    }
    /// Sets the base URL for the variant.
    ///
    /// If not set, the original client's base URL is used.
    #[must_use]
    pub fn base_url(mut self, url: impl Into<String>) -> Self {
        self.base_url = Some(url.into());
        self
    }
    /// Sets the environment variable names for authentication.
    ///
    /// If not set, the original client's env_auth is used.
    #[must_use]
    pub fn env_auth(mut self, env_auth: Vec<String>) -> Self {
        self.env_auth = Some(env_auth);
        self
    }
    /// Sets how the authentication strategy should be updated.
    ///
    /// - `UpdateStrategy::NoChange` - Keep current auth strategy (default)
    /// - `UpdateStrategy::ChangeTo(auth)` - Use specified auth strategy
    #[must_use]
    pub fn auth_update(mut self, strategy: schematic_define::UpdateStrategy) -> Self {
        self.auth_update = strategy;
        self
    }
    /// Sets custom headers for the variant using a Headers builder.
    ///
    /// This replaces the entire headers configuration, including environment
    /// variable mapping. If not set, the original client's headers are used.
    ///
    /// ## Examples
    ///
    /// ```text
    /// use schematic_define::{Headers, EnvMapping, EnvList};
    ///
    /// let custom = Headers::default()
    ///     .with_env_mapping(EnvMapping {
    ///         bearer_token: Some(EnvList::single("STAGING_KEY")),
    ///         basic_user: None,
    ///         basic_pass: None,
    ///         api_key: None,
    ///     })
    ///     .header("X-Custom", "value");
    ///
    /// let variant = api.variant().headers_builder(custom).build();
    /// ```
    #[must_use]
    pub fn headers_builder(mut self, headers: schematic_define::Headers) -> Self {
        self.headers = Some(headers);
        self
    }
    /// Sets a pre-response JSON transformation hook.
    ///
    /// This hook runs after receiving an HTTP response but before
    /// deserializing to the response type. Use it to reshape JSON
    /// payloads to match expected structures.
    ///
    /// ## Examples
    ///
    /// ```text
    /// // Unwrap a { data: ... } envelope
    /// variant.pre_response_json(|ctx, json| {
    ///     Ok(json.get("data").cloned().unwrap_or(json))
    /// })
    /// ```
    #[must_use]
    pub fn pre_response_json<F>(mut self, hook: F) -> Self
    where
        F: Fn(
                &crate::shared::ResponseContext,
                serde_json::Value,
            ) -> Result<serde_json::Value, crate::shared::SchematicError> + Send + Sync
            + 'static,
    {
        self.pre_response_json = Some(std::sync::Arc::new(hook));
        self
    }
    /// Registers a response mutation hook for a specific endpoint.
    ///
    /// This hook runs after deserializing the response and can mutate
    /// the response object in place. The endpoint is identified by its
    /// request type.
    ///
    /// ## Examples
    ///
    /// ```text
    /// variant.mutate_response::<ListModelsRequest>(|ctx, response| {
    ///     response.data.retain(|m| !m.id.contains("deprecated"));
    ///     Ok(())
    /// })
    /// ```
    #[must_use]
    pub fn mutate_response<R, F>(mut self, hook: F) -> Self
    where
        R: crate::shared::EndpointSpec,
        R::Response: Send + Sync + 'static,
        F: Fn(
                &crate::shared::ResponseContext,
                &mut R::Response,
            ) -> Result<(), crate::shared::SchematicError> + Send + Sync + 'static,
    {
        self.response_mutators
            .insert(
                R::ENDPOINT_ID,
                std::sync::Arc::new(crate::shared::TypedMutator::new(hook)),
            );
        self
    }
    /// Builds the variant API client with the configured options.
    ///
    /// Options not explicitly set will inherit from the base client.
    #[must_use]
    pub fn build(self) -> Eversolo {
        let auth_strategy = match self.auth_update {
            schematic_define::UpdateStrategy::NoChange => self.base.auth_strategy.clone(),
            schematic_define::UpdateStrategy::ChangeTo(auth) => auth,
            _ => self.base.auth_strategy.clone(),
        };
        Eversolo {
            client: self.base.client.clone(),
            base_url: self.base_url.unwrap_or_else(|| self.base.base_url.clone()),
            env_auth: self.env_auth.unwrap_or_else(|| self.base.env_auth.clone()),
            auth_strategy,
            env_username: self.base.env_username.clone(),
            headers: self.headers.unwrap_or_else(|| self.base.headers.clone()),
            variant_hooks: crate::shared::VariantHooks {
                pre_response_json: self.pre_response_json,
                response_mutators: self.response_mutators,
            },
        }
    }
}
impl Eversolo {
    /// Builds and sends an HTTP request, returning the raw response plus context.
    ///
    /// This is an internal helper method used by the public request methods.
    /// Returns both the response and the context needed for hook processing.
    async fn build_and_send_request(
        &self,
        request: impl Into<EversoloRequest>,
    ) -> Result<(reqwest::Response, crate::shared::ResponseContext), SchematicError> {
        let request = request.into();
        let endpoint_id = request.endpoint_id();
        let (method, path, body, endpoint_headers) = request.into_parts()?;
        let url = format!("{}{}", self.base_url, path);
        let mut req_builder = match method {
            "GET" => self.client.get(&url),
            "POST" => self.client.post(&url),
            "PUT" => self.client.put(&url),
            "PATCH" => self.client.patch(&url),
            "DELETE" => self.client.delete(&url),
            "HEAD" => self.client.head(&url),
            "OPTIONS" => self.client.request(reqwest::Method::OPTIONS, &url),
            _ => return Err(SchematicError::UnsupportedMethod(method.to_string())),
        };
        if !self.headers.has_authorization() {
            match &self.auth_strategy {
                schematic_define::AuthStrategy::None => {}
                schematic_define::AuthStrategy::BearerToken { header } => {
                    let header_name = header.as_deref().unwrap_or("Authorization");
                    let token = self
                        .env_auth
                        .iter()
                        .find_map(|var| std::env::var(var).ok())
                        .ok_or_else(|| SchematicError::MissingCredential {
                            env_vars: self.env_auth.clone(),
                        })?;
                    req_builder = req_builder
                        .header(header_name, format!("Bearer {}", token));
                }
                schematic_define::AuthStrategy::ApiKey { header } => {
                    let key = self
                        .env_auth
                        .iter()
                        .find_map(|var| std::env::var(var).ok())
                        .ok_or_else(|| SchematicError::MissingCredential {
                            env_vars: self.env_auth.clone(),
                        })?;
                    req_builder = req_builder.header(header.as_str(), key);
                }
                schematic_define::AuthStrategy::Basic => {
                    let username_env = self
                        .env_username
                        .as_deref()
                        .unwrap_or("USERNAME");
                    let password_env = self
                        .env_auth
                        .first()
                        .map(String::as_str)
                        .unwrap_or("PASSWORD");
                    let username = std::env::var(username_env)
                        .map_err(|_| SchematicError::MissingCredential {
                            env_vars: vec![username_env.to_string()],
                        })?;
                    let password = std::env::var(password_env)
                        .map_err(|_| SchematicError::MissingCredential {
                            env_vars: vec![password_env.to_string()],
                        })?;
                    req_builder = req_builder.basic_auth(username, Some(password));
                }
                schematic_define::AuthStrategy::OAuth2(_) => {
                    return Err(SchematicError::OAuthAuthenticationRequired {
                        message: "This API uses OAuth2 authentication. Obtain a token using schematic-oauth and pass it via .variant_with_headers(Headers::default().use_bearer_token(token))"
                            .to_string(),
                    });
                }
                _ => {}
            }
        }
        let api_headers = self.headers.clone().build()?;
        let merged_headers = Self::merge_headers(&api_headers, &endpoint_headers);
        for (key, value) in merged_headers {
            req_builder = req_builder.header(key.as_str(), value.as_str());
        }
        if let Some(body) = body {
            req_builder = req_builder
                .header("Content-Type", "application/json")
                .body(body);
        }
        let response = req_builder.send().await?;
        if !response.status().is_success() {
            let status = response.status().as_u16();
            let body_text = response.text().await.unwrap_or_default();
            return Err(SchematicError::ApiError {
                status,
                body: body_text,
            });
        }
        let status = response.status().as_u16();
        let headers: Vec<(String, String)> = response
            .headers()
            .iter()
            .map(|(k, v)| (k.to_string(), v.to_str().unwrap_or("").to_string()))
            .collect();
        let ctx = crate::shared::ResponseContext::new(
            endpoint_id,
            method,
            path.clone(),
            url,
            status,
            headers,
        );
        Ok((response, ctx))
    }
    /// Merges API-level and endpoint-level headers.
    ///
    /// Endpoint headers override API headers for matching keys (case-insensitive).
    /// Returns a new Vec with the merged headers.
    fn merge_headers(
        api_headers: &[(String, String)],
        endpoint_headers: &[(String, String)],
    ) -> Vec<(String, String)> {
        let mut result: Vec<(String, String)> = Vec::new();
        for (api_key, api_value) in api_headers {
            let has_override = endpoint_headers
                .iter()
                .any(|(k, _)| k.eq_ignore_ascii_case(api_key));
            if !has_override {
                result.push((api_key.clone(), api_value.clone()));
            }
        }
        for (key, value) in endpoint_headers {
            result.push((key.clone(), value.clone()));
        }
        result
    }
    /// Executes an API request expecting a JSON response.
    ///
    /// Takes any request type that can be converted into the request enum
    /// and returns the deserialized response. If response hooks are configured
    /// via the variant builder, they will be applied.
    ///
    /// ## Hooks
    ///
    /// If configured via `variant()`:
    /// - `pre_response_json` - Transforms JSON before deserialization
    /// - `mutate_response` - Mutates the response after deserialization
    ///
    /// ## Errors
    ///
    /// Returns an error if:
    /// - The HTTP request fails (network error, timeout, etc.)
    /// - The response indicates a non-success status code
    /// - The response body cannot be deserialized as JSON
    /// - A hook returns an error
    #[must_use = "this returns a Future that must be awaited"]
    pub async fn request<T: serde::de::DeserializeOwned + Send + Sync + 'static>(
        &self,
        request: impl Into<EversoloRequest>,
    ) -> Result<T, SchematicError> {
        let (response, ctx) = self.build_and_send_request(request).await?;
        let has_pre_hook = self.variant_hooks.pre_response_json.is_some();
        let has_mutator = self
            .variant_hooks
            .response_mutators
            .contains_key(ctx.endpoint_id);
        if has_pre_hook || has_mutator {
            let bytes = response.bytes().await?;
            let mut json_value: serde_json::Value = serde_json::from_slice(&bytes)?;
            if let Some(ref hook) = self.variant_hooks.pre_response_json {
                json_value = hook(&ctx, json_value)?;
            }
            let mut result: T = serde_json::from_value(json_value)?;
            if let Some(mutator) = self
                .variant_hooks
                .response_mutators
                .get(ctx.endpoint_id)
            {
                mutator.mutate(&ctx, &mut result)?;
            }
            Ok(result)
        } else {
            let result = response.json::<T>().await?;
            Ok(result)
        }
    }
}
