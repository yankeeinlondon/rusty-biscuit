// This code was automatically generated by schematic-gen. Do not edit manually.

//! Generated API client for OpenAI.
//!
//! The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
//!
//! ## Authentication
//!
//! Uses Bearer token authentication via the `Authorization` header.
//!
//! ## Features
//!
//! **DELETE**:
//! - `DeleteVoiceConsent` - Deletes a voice consent recording.
//! - `DeleteChatCompletion` - Delete a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` can be deleted.
//! - `DeleteContainer` - Delete Container
//! - `DeleteContainerFile` - Delete Container File
//! - `DeleteConversationItem` - Delete an item from a conversation with the given IDs.
//! - `DeleteEval` - Delete an evaluation.
//! - `DeleteEvalRun` - Delete an eval run.
//! - `DeleteFile` - Delete a file and remove it from all vector stores.
//! - `DeleteFineTuningCheckpointPermission` - **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
//! - `DeleteModel` - Delete a fine-tuned model. You must have the Owner role in your organization to delete a model.
//! - `AdminApiKeysDelete` - Delete an organization admin API key
//! - `DeleteCertificate` - Delete a certificate from the organization.
//! - `DeleteGroup` - Deletes a group from the organization.
//! - `UnassignGroupRole` - Unassigns an organization role from a group within the organization.
//! - `RemoveGroupUser` - Removes a user from a group.
//! - `DeleteInvite` - Delete an invite. If the invite has already been accepted, it cannot be deleted.
//! - `DeleteProjectApiKey` - Deletes an API key from the project.
//! - `RemoveProjectGroup` - Revokes a group's access to a project.
//! - `DeleteProjectModelPermissions` - Deletes model permissions for a project.
//! - `DeleteProjectServiceAccount` - Deletes a service account from the project.
//! - `DeleteProjectSpendAlert` - Deletes a project spend alert.
//! - `DeleteProjectUser` - Deletes a user from the project.
//! - `DeleteRole` - Deletes a custom role from the organization.
//! - `DeleteOrganizationSpendAlert` - Deletes an organization spend alert.
//! - `DeleteUser` - Deletes a user from the organization.
//! - `UnassignUserRole` - Unassigns an organization role from a user within the organization.
//! - `UnassignProjectGroupRole` - Unassigns a project role from a group within a project.
//! - `DeleteProjectRole` - Deletes a custom role from a project.
//! - `UnassignProjectUserRole` - Unassigns a project role from a user within a project.
//! - `DeleteResponse` - Deletes a model response with the given ID.
//! - `DeleteVectorStore` - Delete a vector store.
//! - `DeleteVectorStoreFile` - Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.
//! - `DeleteConversation` - Delete a conversation. Items in the conversation will not be deleted.
//! - `Deleteorganizationspendlimit` - Delete the organization's hard spend limit.
//! - `Deleteprojectspendlimit` - Delete a project's hard spend limit.
//! - `DeleteVideo` - Permanently delete a completed or failed video and its stored assets.
//! - `DeleteSkill` - Delete a skill by its ID.
//! - `DeleteSkillVersion` - Delete a skill version.
//! - `DeleteThreadMethod` - Delete a ChatKit thread along with its items and stored attachments.
//! - `BetaDeleteResponse` - Deletes a model response with the given ID.
//!
//! **GET**:
//! - `ListVoiceConsents` - Returns a list of voice consent recordings.
//! - `GetVoiceConsent` - Retrieves a voice consent recording.
//! - `ListBatches` - List your organization's batches.
//! - `RetrieveBatch` - Retrieves a batch.
//! - `ListChatCompletions` - List stored Chat Completions. Only Chat Completions that have been stored with the `store` parameter set to `true` will be returned.
//! - `GetChatCompletion` - Get a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` will be returned.
//! - `GetChatCompletionMessages` - Get the messages in a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` will be returned.
//! - `ListContainers` - List Containers
//! - `RetrieveContainer` - Retrieve Container
//! - `ListContainerFiles` - List Container files
//! - `RetrieveContainerFile` - Retrieve Container File
//! - `RetrieveContainerFileContent` - Retrieve Container File Content
//! - `ListConversationItems` - List all items for a conversation with the given ID.
//! - `GetConversationItem` - Get a single item from a conversation with the given IDs.
//! - `ListEvals` - List evaluations for a project.
//! - `GetEval` - Get an evaluation by ID.
//! - `GetEvalRuns` - Get a list of runs for an evaluation.
//! - `GetEvalRun` - Get an evaluation run by ID.
//! - `GetEvalRunOutputItems` - Get a list of output items for an evaluation run.
//! - `GetEvalRunOutputItem` - Get an evaluation run output item by ID.
//! - `ListFiles` - Returns a list of files.
//! - `RetrieveFile` - Returns information about a specific file.
//! - `DownloadFile` - Returns the contents of the specified file.
//! - `ListFineTuningCheckpointPermissions` - **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
//! - `ListPaginatedFineTuningJobs` - List your organization's fine-tuning jobs
//! - `RetrieveFineTuningJob` - Get info about a fine-tuning job.
//! - `ListFineTuningJobCheckpoints` - List checkpoints for a fine-tuning job.
//! - `ListFineTuningEvents` - Get status updates for a fine-tuning job.
//! - `ListModels` - Lists the currently available models, and provides basic information about each one such as the owner and availability.
//! - `RetrieveModel` - Retrieves a model instance, providing basic information about the model such as the owner and permissioning.
//! - `AdminApiKeysList` - List organization API keys
//! - `AdminApiKeysGet` - Retrieve a single organization API key
//! - `ListAuditLogs` - List user actions and configuration changes within this organization.
//! - `ListOrganizationCertificates` - List uploaded certificates for this organization.
//! - `GetCertificate` - Get a certificate that has been uploaded to the organization.
//! - `UsageCosts` - Get costs details for the organization.
//! - `RetrieveOrganizationDataRetention` - Retrieves organization data retention controls.
//! - `ListGroups` - Lists all groups in the organization.
//! - `RetrieveGroup` - Retrieves a group.
//! - `ListGroupRoleAssignments` - Lists the organization roles assigned to a group within the organization.
//! - `RetrieveGroupRole` - Retrieves an organization role assigned to a group.
//! - `ListGroupUsers` - Lists the users assigned to a group.
//! - `RetrieveGroupUser` - Retrieves a user in a group.
//! - `ListInvites` - Returns a list of invites in the organization.
//! - `RetrieveInvite` - Retrieves an invite.
//! - `ListProjects` - Returns a list of projects.
//! - `RetrieveProject` - Retrieves a project.
//! - `ListProjectApiKeys` - Returns a list of API keys in the project.
//! - `RetrieveProjectApiKey` - Retrieves an API key in the project.
//! - `ListProjectCertificates` - List certificates for this project.
//! - `RetrieveProjectDataRetention` - Retrieves project data retention controls.
//! - `ListProjectGroups` - Lists the groups that have access to a project.
//! - `RetrieveProjectGroup` - Retrieves a project's group.
//! - `RetrieveProjectHostedToolPermissions` - Returns hosted tool permissions for a project.
//! - `RetrieveProjectModelPermissions` - Returns model permissions for a project.
//! - `ListProjectRateLimits` - Returns the rate limits per model for a project.
//! - `ListProjectServiceAccounts` - Returns a list of service accounts in the project.
//! - `RetrieveProjectServiceAccount` - Retrieves a service account in the project.
//! - `ListProjectSpendAlerts` - Lists project spend alerts.
//! - `RetrieveProjectSpendAlert` - Retrieves a project spend alert.
//! - `ListProjectUsers` - Returns a list of users in the project.
//! - `RetrieveProjectUser` - Retrieves a user in the project.
//! - `ListRoles` - Lists the roles configured for the organization.
//! - `RetrieveRole` - Retrieves an organization role.
//! - `ListOrganizationSpendAlerts` - Lists organization spend alerts.
//! - `RetrieveOrganizationSpendAlert` - Retrieves an organization spend alert.
//! - `UsageAudioSpeeches` - Get audio speeches usage details for the organization.
//! - `UsageAudioTranscriptions` - Get audio transcriptions usage details for the organization.
//! - `UsageCodeInterpreterSessions` - Get code interpreter sessions usage details for the organization.
//! - `UsageCompletions` - Get completions usage details for the organization.
//! - `UsageEmbeddings` - Get embeddings usage details for the organization.
//! - `UsageFileSearchCalls` - Get file search calls usage details for the organization.
//! - `UsageImages` - Get images usage details for the organization.
//! - `UsageModerations` - Get moderations usage details for the organization.
//! - `UsageVectorStores` - Get vector stores usage details for the organization.
//! - `UsageWebSearchCalls` - Get web search calls usage details for the organization.
//! - `ListUsers` - Lists all of the users in the organization.
//! - `RetrieveUser` - Retrieves a user by their identifier.
//! - `ListUserRoleAssignments` - Lists the organization roles assigned to a user within the organization.
//! - `RetrieveUserRole` - Retrieves an organization role assigned to a user.
//! - `ListProjectGroupRoleAssignments` - Lists the project roles assigned to a group within a project.
//! - `RetrieveProjectGroupRole` - Retrieves a project role assigned to a group.
//! - `ListProjectRoles` - Lists the roles configured for a project.
//! - `RetrieveProjectRole` - Retrieves a project role.
//! - `ListProjectUserRoleAssignments` - Lists the project roles assigned to a user within a project.
//! - `RetrieveProjectUserRole` - Retrieves a project role assigned to a user.
//! - `GetResponse` - Retrieves a model response with the given ID.
//! - `ListInputItems` - Returns a list of input items for a given response.
//! - `ListVectorStores` - Returns a list of vector stores.
//! - `GetVectorStore` - Retrieves a vector store.
//! - `GetVectorStoreFileBatch` - Retrieves a vector store file batch.
//! - `ListFilesInVectorStoreBatch` - Returns a list of vector store files in a batch.
//! - `ListVectorStoreFiles` - Returns a list of vector store files.
//! - `GetVectorStoreFile` - Retrieves a vector store file.
//! - `RetrieveVectorStoreFileContent` - Retrieve the parsed contents of a vector store file.
//! - `GetConversation` - Get a conversation
//! - `Getorganizationspendlimit` - Get the organization's hard spend limit.
//! - `Getprojectspendlimit` - Get a project's hard spend limit.
//! - `ListVideos` - List recently generated videos for the current project.
//! - `GetVideoCharacter` - Fetch a character.
//! - `GetVideo` - Fetch the latest metadata for a generated video.
//! - `RetrieveVideoContent` - Download the generated video bytes or a derived preview asset.
//! - `ListSkills` - List all skills for the current project.
//! - `GetSkill` - Get a skill by its ID.
//! - `GetSkillContent` - Download a skill zip bundle by its ID.
//! - `ListSkillVersions` - List skill versions for a skill.
//! - `GetSkillVersion` - Get a specific skill version.
//! - `GetSkillVersionContent` - Download a skill version zip bundle.
//! - `ListThreadItemsMethod` - List items that belong to a ChatKit thread.
//! - `GetThreadMethod` - Retrieve a ChatKit thread by its identifier.
//! - `ListThreadsMethod` - List ChatKit threads with optional pagination and user filters.
//! - `BetaGetResponse` - Retrieves a model response with the given ID.
//! - `BetaListInputItems` - Returns a list of input items for a given response.
//!
//! **POST**:
//! - `CreateSpeech` - Generates audio from the input text.
//! - `CreateTranscription` - Transcribes audio into the input language.
//! - `CreateTranslation` - Translates audio into English.
//! - `CreateVoiceConsent` - Upload a voice consent recording.
//! - `UpdateVoiceConsent` - Updates a voice consent recording (metadata only).
//! - `CreateVoice` - Creates a custom voice.
//! - `CreateBatch` - Creates and executes a batch from an uploaded file of requests
//! - `CancelBatch` - Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file.
//! - `CreateChatCompletion` - **Starting a new project?** We recommend trying [Responses](/docs/api-reference/responses) to take advantage of the latest OpenAI platform features. Compare [Chat Completions with Responses](/docs/guides/responses-vs-chat-completions?api-mode=responses).
//! - `UpdateChatCompletion` - Modify a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` can be modified. Currently, the only supported modification is to update the `metadata` field.
//! - `CreateCompletion` - Creates a completion for the provided prompt and parameters.
//! - `CreateContainer` - Create Container
//! - `CreateContainerFile` - Create a Container File
//! - `CreateConversationItems` - Create items in a conversation with the given ID.
//! - `CreateEmbedding` - Creates an embedding vector representing the input text.
//! - `CreateEval` - Create the structure of an evaluation that can be used to test a model's performance. An evaluation is a set of testing criteria and the config for a data source, which dictates the schema of the data used in the evaluation. After creating an evaluation, you can run it on different models and model parameters. We support several types of graders and datasources. For more information, see the [Evals guide](/docs/guides/evals).
//! - `UpdateEval` - Update certain properties of an evaluation.
//! - `CreateEvalRun` - Kicks off a new run for a given evaluation, specifying the data source, and what model configuration to use to test. The datasource will be validated against the schema specified in the config of the evaluation.
//! - `CancelEvalRun` - Cancel an ongoing evaluation run.
//! - `CreateFile` - Upload a file that can be used across various endpoints. Individual files can be up to 512 MB, and each project can store up to 2.5 TB of files in total. There is no organization-wide storage limit. Uploads to this endpoint are rate-limited to 1,000 requests per minute per authenticated user.
//! - `RunGrader` - Run a grader.
//! - `ValidateGrader` - Validate a grader.
//! - `CreateFineTuningCheckpointPermission` - **NOTE:** Calling this endpoint requires an [admin API key](../admin-api-keys).
//! - `CreateFineTuningJob` - Creates a fine-tuning job which begins the process of creating a new model from a given dataset.
//! - `CancelFineTuningJob` - Immediately cancel a fine-tune job.
//! - `PauseFineTuningJob` - Pause a fine-tune job.
//! - `ResumeFineTuningJob` - Resume a fine-tune job.
//! - `CreateImageEdit` - Creates an edited or extended image given one or more source images and a prompt. This endpoint supports GPT Image models (`gpt-image-1.5`, `gpt-image-1`, `gpt-image-1-mini`, and `chatgpt-image-latest`) and `dall-e-2`.
//! - `CreateImage` - Creates an image given a prompt. [Learn more](/docs/guides/images).
//! - `CreateImageVariation` - Creates a variation of a given image. This endpoint only supports `dall-e-2`.
//! - `CreateModeration` - Classifies if text and/or image inputs are potentially harmful. Learn more in the [moderation guide](/docs/guides/moderation).
//! - `AdminApiKeysCreate` - Create an organization admin API key
//! - `UploadCertificate` - Upload a certificate to the organization. This does **not** automatically activate the certificate.
//! - `ActivateOrganizationCertificates` - Activate certificates at the organization level.
//! - `DeactivateOrganizationCertificates` - Deactivate certificates at the organization level.
//! - `ModifyCertificate` - Modify a certificate. Note that only the name can be modified.
//! - `UpdateOrganizationDataRetention` - Updates organization data retention controls.
//! - `CreateGroup` - Creates a new group in the organization.
//! - `UpdateGroup` - Updates a group's information.
//! - `AssignGroupRole` - Assigns an organization role to a group within the organization.
//! - `AddGroupUser` - Adds a user to a group.
//! - `InviteUser` - Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.
//! - `CreateProject` - Create a new project in the organization. Projects can be created and archived, but cannot be deleted.
//! - `ModifyProject` - Modifies a project in the organization.
//! - `ArchiveProject` - Archives a project in the organization. Archived projects cannot be used or updated.
//! - `ActivateProjectCertificates` - Activate certificates at the project level.
//! - `DeactivateProjectCertificates` - Deactivate certificates at the project level. You can atomically and idempotently deactivate up to 10 certificates at a time.
//! - `UpdateProjectDataRetention` - Updates project data retention controls.
//! - `AddProjectGroup` - Grants a group access to a project.
//! - `UpdateProjectHostedToolPermissions` - Updates hosted tool permissions for a project.
//! - `UpdateProjectModelPermissions` - Updates model permissions for a project.
//! - `UpdateProjectRateLimits` - Updates a project rate limit.
//! - `CreateProjectServiceAccount` - Creates a new service account in the project. By default, this also returns an unredacted API key for the service account.
//! - `UpdateProjectServiceAccount` - Updates a service account in the project.
//! - `CreateProjectSpendAlert` - Creates a project spend alert.
//! - `UpdateProjectSpendAlert` - Updates a project spend alert.
//! - `CreateProjectUser` - Adds a user to the project. Users must already be members of the organization to be added to a project.
//! - `ModifyProjectUser` - Modifies a user's role in the project.
//! - `CreateRole` - Creates a custom role for the organization.
//! - `UpdateRole` - Updates an existing organization role.
//! - `CreateOrganizationSpendAlert` - Creates an organization spend alert.
//! - `UpdateOrganizationSpendAlert` - Updates an organization spend alert.
//! - `ModifyUser` - Modifies a user's role in the organization.
//! - `AssignUserRole` - Assigns an organization role to a user within the organization.
//! - `AssignProjectGroupRole` - Assigns a project role to a group within a project.
//! - `CreateProjectRole` - Creates a custom role for a project.
//! - `UpdateProjectRole` - Updates an existing project role.
//! - `AssignProjectUserRole` - Assigns a project role to a user within a project.
//! - `CreateRealtimeCall` - Create a new Realtime API call over WebRTC and receive the SDP answer needed to complete the peer connection.
//! - `AcceptRealtimeCall` - Accept an incoming SIP call and configure the realtime session that will handle it.
//! - `HangupRealtimeCall` - End an active Realtime API call, whether it was initiated over SIP or WebRTC.
//! - `ReferRealtimeCall` - Transfer an active SIP call to a new destination using the SIP REFER verb.
//! - `RejectRealtimeCall` - Decline an incoming SIP call by returning a SIP status code to the caller.
//! - `CreateRealtimeClientSecret` - Create a Realtime client secret with an associated session configuration.
//! - `CreateRealtimeSession` - Create an ephemeral API token for use in client-side applications with the Realtime API. Can be configured with the same session parameters as the `session.update` client event.
//! - `CreateRealtimeTranscriptionSession` - Create an ephemeral API token for use in client-side applications with the Realtime API specifically for realtime transcriptions. Can be configured with the same session parameters as the `transcription_session.update` client event.
//! - `CreateRealtimeTranslationClientSecret` - Create a Realtime translation client secret with an associated translation session configuration.
//! - `CreateResponse` - Creates a model response. Provide [text](/docs/guides/text) or [image](/docs/guides/images) inputs to generate [text](/docs/guides/text) or [JSON](/docs/guides/structured-outputs) outputs. Have the model call your own [custom code](/docs/guides/function-calling) or use built-in [tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search) or [file search](/docs/guides/tools-file-search) to use your own data as input for the model's response.
//! - `CancelResponse` - Cancels a model response with the given ID. Only responses created with the `background` parameter set to `true` can be cancelled. [Learn more](/docs/guides/background).
//! - `CreateUpload` - Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it.
//! - `CancelUpload` - Cancels the Upload. No Parts may be added after an Upload is cancelled.
//! - `CompleteUpload` - Completes the [Upload](/docs/api-reference/uploads/object).
//! - `AddUploadPart` - Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.
//! - `CreateVectorStore` - Create a vector store.
//! - `ModifyVectorStore` - Modifies a vector store.
//! - `CreateVectorStoreFileBatch` - Create a vector store file batch.
//! - `CancelVectorStoreFileBatch` - Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible.
//! - `CreateVectorStoreFile` - Create a vector store file by attaching a [File](/docs/api-reference/files) to a [vector store](/docs/api-reference/vector-stores/object).
//! - `UpdateVectorStoreFileAttributes` - Update attributes on a vector store file.
//! - `SearchVectorStore` - Search a vector store for relevant chunks based on a query and file attributes filter.
//! - `CreateConversation` - Create a conversation.
//! - `UpdateConversation` - Update a conversation
//! - `Updateorganizationspendlimit` - Create or replace the organization's hard spend limit.
//! - `Updateprojectspendlimit` - Create or replace a project's hard spend limit.
//! - `CreateanAPIkeyforaserviceaccount` - Creates an API key for a service account in the project.
//! - `Createcontentprovenancecheck` - Check whether an image or audio file contains known OpenAI provenance signals. [Learn more about content provenance](/api/docs/guides/content-provenance).
//! - `CreateVideo` - Create a new video generation job from a prompt and optional reference assets.
//! - `CreateVideoCharacter` - Create a character from an uploaded video.
//! - `CreateVideoEdit` - Create a new video generation job by editing a source video or existing generated video.
//! - `CreateVideoExtend` - Create an extension of a completed video.
//! - `CreateVideoRemix` - Create a remix of a completed video using a refreshed prompt.
//! - `Getinputtokencounts` - Returns input token counts of the request.
//! - `Compactconversation` - Compact a conversation. Returns a compacted response object.
//! - `CreateSkill` - Create a new skill.
//! - `UpdateSkillDefaultVersion` - Update the default version pointer for a skill.
//! - `CreateSkillVersion` - Create a new immutable skill version.
//! - `CancelChatSessionMethod` - Cancel an active ChatKit session and return its most recent metadata.
//! - `CreateChatSessionMethod` - Create a ChatKit session.
//! - `BetaCreateResponse` - Creates a model response. Provide [text](/docs/guides/text) or [image](/docs/guides/images) inputs to generate [text](/docs/guides/text) or [JSON](/docs/guides/structured-outputs) outputs. Have the model call your own [custom code](/docs/guides/function-calling) or use built-in [tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search) or [file search](/docs/guides/tools-file-search) to use your own data as input for the model's response.
//! - `BetaCancelResponse` - Cancels a model response with the given ID. Only responses created with the `background` parameter set to `true` can be cancelled. [Learn more](/docs/guides/background).
//! - `BetaCompactconversation` - Compact a conversation. Returns a compacted response object.
//! - `BetaGetinputtokencounts` - Returns input token counts of the request.
//!
//! ## Examples
//!
//! ```text
//! use schematic_schema::prelude::*;
//!
//! #[tokio::main]
//! async fn main() -> Result<(), SchematicError> {
//!     let client = OpenAI::new();
//!     let response = client.list_voice_consents().await?;
//!     println!("{:?}", response);
//!     Ok(())
//! }
//! ```
//!
//! ### Override Base URL
//!
//! ```text
//! use schematic_schema::prelude::*;
//!
//! let client = OpenAI::with_base_url("https://staging.example.com/v1");
//! ```
pub mod requests;
pub mod responses;
pub mod client;
pub use requests::*;
pub use responses::*;
/// The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details. client.
pub struct OpenAI {
    client: reqwest::Client,
    base_url: String,
    /// Environment variable names for authentication credentials.
    env_auth: Vec<String>,
    /// Authentication strategy for this API client.
    auth_strategy: schematic_define::AuthStrategy,
    /// Effective authentication policy for this API client.
    auth_policy: schematic_define::AuthPolicy,
    /// Environment variable for Basic auth username.
    env_username: Option<String>,
    /// Headers builder with environment variable support for credentials.
    headers: schematic_define::Headers,
    /// Variant-specific hooks for response customization.
    variant_hooks: crate::shared::VariantHooks,
}
impl OpenAI {
    /// Base URL for the API.
    pub const BASE_URL: &'static str = "https://api.openai.com/v1";
    /// Official API documentation URL, if available.
    pub const DOCS_URL: Option<&'static str> = None;
    /// Creates a new API client with the default base URL.
    pub fn new() -> Self {
        Self {
            client: reqwest::Client::builder()
                .timeout(std::time::Duration::from_secs(30))
                .build()
                .unwrap_or_else(|_| reqwest::Client::new()),
            base_url: Self::BASE_URL.to_string(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::BearerToken {
                header: None,
            },
            auth_policy: schematic_define::AuthPolicy {
                explicit: vec![
                    schematic_define::AuthMethod::BearerToken { header : None }
                ],
                env_fallback: Some(schematic_define::EnvAuthStrategy::BearerToken {
                    header: None,
                }),
            },
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Creates a new API client with a custom base URL.
    ///
    /// ## Examples
    ///
    /// ```text
    /// let client = Api::with_base_url("http://localhost:8080/v1");
    /// ```
    pub fn with_base_url(base_url: impl Into<String>) -> Self {
        Self {
            client: reqwest::Client::builder()
                .timeout(std::time::Duration::from_secs(30))
                .build()
                .unwrap_or_else(|_| reqwest::Client::new()),
            base_url: base_url.into(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::BearerToken {
                header: None,
            },
            auth_policy: schematic_define::AuthPolicy {
                explicit: vec![
                    schematic_define::AuthMethod::BearerToken { header : None }
                ],
                env_fallback: Some(schematic_define::EnvAuthStrategy::BearerToken {
                    header: None,
                }),
            },
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Creates a new API client with a pre-configured reqwest client.
    ///
    /// Use this when you need custom timeouts, connection pools, or middleware.
    ///
    /// ## Examples
    ///
    /// ```text
    /// let custom_client = reqwest::Client::builder()
    ///     .timeout(std::time::Duration::from_secs(60))
    ///     .build()
    ///     .unwrap();
    /// let api = Api::with_client(custom_client);
    /// ```
    pub fn with_client(client: reqwest::Client) -> Self {
        Self {
            client,
            base_url: Self::BASE_URL.to_string(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::BearerToken {
                header: None,
            },
            auth_policy: schematic_define::AuthPolicy {
                explicit: vec![
                    schematic_define::AuthMethod::BearerToken { header : None }
                ],
                env_fallback: Some(schematic_define::EnvAuthStrategy::BearerToken {
                    header: None,
                }),
            },
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Creates a new API client with a pre-configured reqwest client and custom base URL.
    ///
    /// ## Examples
    ///
    /// ```text
    /// let custom_client = reqwest::Client::builder()
    ///     .timeout(std::time::Duration::from_secs(60))
    ///     .build()
    ///     .unwrap();
    /// let api = Api::with_client_and_base_url(custom_client, "http://localhost:8080");
    /// ```
    pub fn with_client_and_base_url(
        client: reqwest::Client,
        base_url: impl Into<String>,
    ) -> Self {
        Self {
            client,
            base_url: base_url.into(),
            env_auth: vec![],
            auth_strategy: schematic_define::AuthStrategy::BearerToken {
                header: None,
            },
            auth_policy: schematic_define::AuthPolicy {
                explicit: vec![
                    schematic_define::AuthMethod::BearerToken { header : None }
                ],
                env_fallback: Some(schematic_define::EnvAuthStrategy::BearerToken {
                    header: None,
                }),
            },
            env_username: None,
            headers: schematic_define::Headers::default()
                .with_env_mapping(schematic_define::EnvMapping {
                    bearer_token: None,
                    basic_user: None,
                    basic_pass: None,
                    api_key: None,
                    oauth_client_id: None,
                    oauth_client_secret: None,
                    oauth_redirect_uri: None,
                }),
            variant_hooks: crate::shared::VariantHooks::default(),
        }
    }
    /// Returns a reference to the underlying HTTP client.
    ///
    /// Use this for custom requests that aren't covered by the generated methods,
    /// such as paginated endpoints that require query parameters.
    pub fn http_client(&self) -> &reqwest::Client {
        &self.client
    }
    /// Returns the base URL for this API client.
    pub fn api_base_url(&self) -> &str {
        &self.base_url
    }
    /// Returns the effective authentication policy for this client.
    pub fn auth_policy(&self) -> &schematic_define::AuthPolicy {
        &self.auth_policy
    }
    /// Returns OAuth2 provider metadata, if this client accepts OAuth tokens.
    pub fn oauth_config(&self) -> Option<&schematic_define::OAuth2Config> {
        self.auth_policy.oauth2()
    }
    /// Returns the API key header name and value for authentication.
    ///
    /// Returns `None` if the authentication strategy is not `ApiKey`
    /// or if the API key environment variable is not set.
    pub fn api_key_header(&self) -> Option<(String, String)> {
        let (header, value_prefix) = self
            .auth_policy
            .explicit
            .iter()
            .find_map(|method| match method {
                schematic_define::AuthMethod::ApiKey { header, value_prefix } => {
                    Some((header.clone(), value_prefix.clone()))
                }
                _ => None,
            })
            .or_else(|| {
                self.headers
                    .env_mapping()
                    .api_key
                    .as_ref()
                    .map(|api_key| (api_key.header.clone(), None))
            })?;
        self.headers
            .env_mapping()
            .api_key
            .as_ref()
            .and_then(|api_key| {
                api_key
                    .names
                    .names()
                    .iter()
                    .find_map(|env_name| std::env::var(env_name).ok())
            })
            .map(|value| {
                let value = format!(
                    "{}{}", value_prefix.clone().unwrap_or_default(), value
                );
                (header, value)
            })
    }
    /// Creates a variant builder for customizing this API client.
    ///
    /// The builder pattern allows fluent configuration of:
    /// - Base URL (for proxies, mock servers, or different environments)
    /// - Authentication credentials (different env var names)
    /// - Authentication strategy
    /// - Response hooks for JSON transformation and mutation
    ///
    /// ## Examples
    ///
    /// ```text
    /// use schematic_define::UpdateStrategy;
    ///
    /// let api = Api::new();
    ///
    /// // Create a variant pointing to a staging server with a response hook
    /// let staging = api.variant()
    ///     .base_url("https://staging.api.com/v1")
    ///     .env_auth(vec!["STAGING_API_KEY".to_string()])
    ///     .mutate_response::<ListModelsRequest>(|ctx, response| {
    ///         response.data.retain(|m| !m.id.contains("deprecated"));
    ///         Ok(())
    ///     })
    ///     .build();
    /// ```
    #[must_use]
    pub fn variant(&self) -> OpenAIVariantBuilder<'_> {
        OpenAIVariantBuilder::new(self)
    }
    /// Creates a variant of this API client with different configuration.
    ///
    /// This is a convenience method equivalent to:
    /// ```text
    /// api.variant()
    ///     .base_url(base_url)
    ///     .env_auth(env_auth)
    ///     .auth_update(strategy)
    ///     .build()
    /// ```
    ///
    /// ## Arguments
    ///
    /// * `base_url` - New base URL for this variant
    /// * `env_auth` - New environment variable names for credentials
    /// * `strategy` - How to update the auth strategy:
    ///   - `UpdateStrategy::NoChange` - Keep current auth strategy
    ///   - `UpdateStrategy::ChangeTo(auth)` - Use specified auth strategy
    pub fn variant_with(
        &self,
        base_url: impl Into<String>,
        env_auth: Vec<String>,
        strategy: schematic_define::UpdateStrategy,
    ) -> Self {
        self.variant()
            .base_url(base_url)
            .env_auth(env_auth)
            .auth_update(strategy)
            .build()
    }
    /// Creates a variant of this API client with custom headers configuration.
    ///
    /// This is a convenience method for creating variants with fully customized
    /// headers, including environment variable mapping for credentials.
    ///
    /// ## Arguments
    ///
    /// * `headers` - Headers builder with environment mapping configured
    ///
    /// ## Examples
    ///
    /// ```text
    /// use schematic_define::{Headers, EnvMapping, EnvList};
    ///
    /// let custom_headers = Headers::default()
    ///     .with_env_mapping(EnvMapping {
    ///         bearer_token: Some(EnvList::single("STAGING_TOKEN")),
    ///         basic_user: None,
    ///         basic_pass: None,
    ///         api_key: None,
    ///     })
    ///     .header("X-Environment", "staging");
    ///
    /// let staging_client = api.variant_with_headers(custom_headers);
    /// ```
    pub fn variant_with_headers(&self, headers: schematic_define::Headers) -> Self {
        self.variant().headers_builder(headers).build()
    }
    /// Returns a clone of this client configured with an explicit bearer token.
    #[must_use]
    pub fn bearer_token(&self, token: impl Into<String>) -> Self {
        self.variant()
            .headers_builder(self.headers.clone().use_bearer_token(token))
            .build()
    }
}
impl Default for OpenAI {
    fn default() -> Self {
        Self::new()
    }
}
/// Builder for creating customized variants of the API client.
///
/// Use [`#struct_name::variant()`] to create a builder, then chain
/// configuration methods and call [`build()`](Self::build) to create
/// the variant client.
pub struct OpenAIVariantBuilder<'a> {
    base: &'a OpenAI,
    base_url: Option<String>,
    env_auth: Option<Vec<String>>,
    auth_update: schematic_define::UpdateStrategy,
    headers: Option<schematic_define::Headers>,
    pre_response_json: Option<std::sync::Arc<crate::shared::PreResponseJsonHook>>,
    response_mutators: std::collections::HashMap<
        &'static str,
        std::sync::Arc<dyn crate::shared::AnyResponseMutator>,
    >,
}
impl<'a> OpenAIVariantBuilder<'a> {
    /// Creates a new variant builder from the base API client.
    fn new(base: &'a OpenAI) -> Self {
        Self {
            base,
            base_url: None,
            env_auth: None,
            auth_update: schematic_define::UpdateStrategy::NoChange,
            headers: None,
            pre_response_json: None,
            response_mutators: std::collections::HashMap::new(),
        }
    }
    /// Sets the base URL for the variant.
    ///
    /// If not set, the original client's base URL is used.
    #[must_use]
    pub fn base_url(mut self, url: impl Into<String>) -> Self {
        self.base_url = Some(url.into());
        self
    }
    /// Sets the environment variable names for authentication.
    ///
    /// If not set, the original client's env_auth is used.
    #[must_use]
    pub fn env_auth(mut self, env_auth: Vec<String>) -> Self {
        self.env_auth = Some(env_auth);
        self
    }
    /// Sets how the authentication strategy should be updated.
    ///
    /// - `UpdateStrategy::NoChange` - Keep current auth strategy (default)
    /// - `UpdateStrategy::ChangeTo(auth)` - Use specified auth strategy
    #[must_use]
    pub fn auth_update(mut self, strategy: schematic_define::UpdateStrategy) -> Self {
        self.auth_update = strategy;
        self
    }
    /// Sets custom headers for the variant using a Headers builder.
    ///
    /// This replaces the entire headers configuration, including environment
    /// variable mapping. If not set, the original client's headers are used.
    ///
    /// ## Examples
    ///
    /// ```text
    /// use schematic_define::{Headers, EnvMapping, EnvList};
    ///
    /// let custom = Headers::default()
    ///     .with_env_mapping(EnvMapping {
    ///         bearer_token: Some(EnvList::single("STAGING_KEY")),
    ///         basic_user: None,
    ///         basic_pass: None,
    ///         api_key: None,
    ///     })
    ///     .header("X-Custom", "value");
    ///
    /// let variant = api.variant().headers_builder(custom).build();
    /// ```
    #[must_use]
    pub fn headers_builder(mut self, headers: schematic_define::Headers) -> Self {
        self.headers = Some(headers);
        self
    }
    /// Sets a pre-response JSON transformation hook.
    ///
    /// This hook runs after receiving an HTTP response but before
    /// deserializing to the response type. Use it to reshape JSON
    /// payloads to match expected structures.
    ///
    /// ## Examples
    ///
    /// ```text
    /// // Unwrap a { data: ... } envelope
    /// variant.pre_response_json(|ctx, json| {
    ///     Ok(json.get("data").cloned().unwrap_or(json))
    /// })
    /// ```
    #[must_use]
    pub fn pre_response_json<F>(mut self, hook: F) -> Self
    where
        F: Fn(
                &crate::shared::ResponseContext,
                serde_json::Value,
            ) -> Result<serde_json::Value, crate::shared::SchematicError> + Send + Sync
            + 'static,
    {
        self.pre_response_json = Some(std::sync::Arc::new(hook));
        self
    }
    /// Registers a response mutation hook for a specific endpoint.
    ///
    /// This hook runs after deserializing the response and can mutate
    /// the response object in place. The endpoint is identified by its
    /// request type.
    ///
    /// ## Examples
    ///
    /// ```text
    /// variant.mutate_response::<ListModelsRequest>(|ctx, response| {
    ///     response.data.retain(|m| !m.id.contains("deprecated"));
    ///     Ok(())
    /// })
    /// ```
    #[must_use]
    pub fn mutate_response<R, F>(mut self, hook: F) -> Self
    where
        R: crate::shared::EndpointSpec,
        R::Response: Send + Sync + 'static,
        F: Fn(
                &crate::shared::ResponseContext,
                &mut R::Response,
            ) -> Result<(), crate::shared::SchematicError> + Send + Sync + 'static,
    {
        self.response_mutators
            .insert(
                R::ENDPOINT_ID,
                std::sync::Arc::new(crate::shared::TypedMutator::new(hook)),
            );
        self
    }
    /// Builds the variant API client with the configured options.
    ///
    /// Options not explicitly set will inherit from the base client.
    #[must_use]
    pub fn build(self) -> OpenAI {
        let auth_update = self.auth_update.clone();
        let auth_policy = match auth_update.clone() {
            schematic_define::UpdateStrategy::NoChange => self.base.auth_policy.clone(),
            schematic_define::UpdateStrategy::ChangeTo(ref auth) => {
                schematic_define::AuthPolicy::from_auth_strategy(auth)
            }
            _ => self.base.auth_policy.clone(),
        };
        let auth_strategy = match auth_update.clone() {
            schematic_define::UpdateStrategy::NoChange => self.base.auth_strategy.clone(),
            schematic_define::UpdateStrategy::ChangeTo(auth) => auth,
            _ => self.base.auth_strategy.clone(),
        };
        let has_env_auth_override = self.env_auth.is_some();
        let env_auth = self.env_auth.unwrap_or_else(|| self.base.env_auth.clone());
        let headers = match self.headers {
            Some(headers) => headers,
            None if has_env_auth_override
                || !matches!(auth_update, schematic_define::UpdateStrategy::NoChange) => {
                self.base
                    .headers
                    .clone()
                    .with_env_mapping(
                        schematic_define::RestApi::legacy_env_mapping_for(
                            &auth_strategy,
                            &env_auth,
                            self.base.env_username.as_deref(),
                        ),
                    )
            }
            None => self.base.headers.clone(),
        };
        OpenAI {
            client: self.base.client.clone(),
            base_url: self.base_url.unwrap_or_else(|| self.base.base_url.clone()),
            env_auth,
            auth_strategy,
            auth_policy,
            env_username: self.base.env_username.clone(),
            headers,
            variant_hooks: crate::shared::VariantHooks {
                pre_response_json: self.pre_response_json,
                response_mutators: self.response_mutators,
            },
        }
    }
}
