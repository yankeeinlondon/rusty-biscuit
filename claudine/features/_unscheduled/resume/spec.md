# Resume Functionality

When we introduced the idea of [validations and handlers](@claudine/docs/topics/validates-and-handlers.md) we included the `resume` handler which relied on the fact that with non-interactive prompts we would always be able to get the Agent's **session id** and with that we now had the ticket for _resumability_.


