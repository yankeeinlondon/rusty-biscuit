We developed the TUI for `claudine config` before we started creating reusable ratatui components in the biscuit-tui package area. Now that biscuit-tui has been tested and is ready to be used we'd like to start using these components where it makes sense.

Use the 'biscuit-tui' and 'claudine' skills to have the right context on both package areas we will be evaluating.
