# Config TUI Refresh

This feature looks to improve the existing `claudine config` TUI. It will do that via two primary sources:

- try to leverage the newly created `biscuit-tui` library of widgets
- try to improve UX by including more instructional information for configuration items

