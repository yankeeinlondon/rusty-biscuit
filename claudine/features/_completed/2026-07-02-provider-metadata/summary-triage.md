# Summary-Surfaced Behavior-Gap Triage Backlog

> Seeded 2026-07-03 from the cross-provider summaries (`docs/research/summary/*.md`).
> Governing rule (spec, "Expectation, 2026-07-03"): every item gets an explicit
> disposition — **implement / schedule / won't-do** — at the Phase D checkpoint for its
> topic; surfacing alone is not completion. Add new items as later summaries/fleets
> surface them; never delete, mark disposition instead.
>
> Disposition legend: `[ ]` untriaged · `[I]` implement now · `[S]` scheduled · `[W]` won't-do

## MCP (`summary/mcp.md`)

- [S] Goose/Kimi/Qwen have provider-native MCP support with no Claudine import/export/runtime path **Ruled `[S]` (Ken, 2026-07-05):** real feature work — scheduled into the "MCP rollout round 2" bundle; orthogonal to the metadata pipeline
- [S] Claude Code native `--mcp-config` one-run mechanism has no Claudine runtime injector **Ruled `[S]` (Ken, 2026-07-05):** same "MCP rollout round 2" bundle; per-run injection is cleaner than shadow-home for claude
- [S] Plugin-declared MCP servers (Kimi plugins, Gemini/Qwen extensions, Kilo Marketplace `MCP.yaml`) are a fourth discovery source for the MCP catalog — import candidates with credential stripping; catalog stays authoritative, plugin entries are never authoritative **Ruled `[S]` (Ken, 2026-07-05):** scheduled behind the plugin-extraction work; import candidates with credential stripping, catalog stays authoritative
- [S] ACP `session/new` `mcpServers` injection is a candidate MCP delivery path (cleaner than shadow config where wired; NOT wired for Pi, partial for Codex) **Ruled `[S]` (Ken, 2026-07-05):** scheduled behind the ACP adoption track; capability-gate per provider

## Agent Logging (`summary/agent-logging.md`)

- [S] Provider-log **evidence adapters** feeding `claudine logs` as a federated observability layer: WAL-aware SQLite readers (OpenCode, Kilo CLI, Goose, Codex state DBs), Gemini `$set`-aware transcript parsing, Pi tree-preserving transcript reader **Ruled `[S]` (Ken, 2026-07-05):** its own track — the "evidence-adapter track" bundle; large and well-specced by the summary
- [I] Kimi Wire protocol-version tolerance — observed 1.9 parser pin vs 1.10 server is live breakage (**triage early**). **Ruled (Ken, 2026-07-04): option D — full 1.10 adoption**, not just a version-tolerance window: relax the strict `WIRE_PROTOCOL_VERSION` equality in `validate_initialize_response` to accept {1.9, 1.10} with a remediation-bearing failure on unknown versions, AND model the 1.10 event surface the parser lacks (`StepRetry` retry observability, `StatusUpdate.mcp_status`, the richer `Notification` payload) plus the unmodeled on-disk surfaces (`tasks/`, compaction snapshots `context_{N}.jsonl`). Version-scope the facts per the kimi-cli (1.x, `~/.kimi`) vs Kimi Code (0.x, `~/.kimi-code`) product split. Queued as its own work item after Phase D step 3
- [S] Shared path/volatility inventory: resume storage (Codex `state_5.sqlite`, session indexes, Kimi state files) overlaps the shadow-home volatile-SQLite skip list and the evidence-adapter plan — one inventory, multiple consumers **Ruled `[S]` (Ken, 2026-07-05):** folded INTO the "evidence-adapter track" bundle — one inventory, consumers: shadow-home skip list + adapters

## Agent Permissions (`summary/agent-permissions.md`)

- [S] Decide the Goose wrapper's default posture — Goose's own default `GOOSE_MODE=auto` is effectively YOLO (auto-approves all visible tools, ignores `permission.yaml`) **Ruled `[S]` (Ken, 2026-07-05):** posture flip interacts with approval transport; batched into the permissions/PolicyEngine six-axis round rather than a risky standalone flip
- [S] Model OpenCode `--auto` as auto-reply-**once**, not a standing approval **Ruled `[S]` (Ken, 2026-07-05):** same permissions/PolicyEngine six-axis round; low-cost model correction, batched
- [S] Kimi's only precise programmatic approval transport is ACP (feeds the ACP-adoption disposition below) **Ruled `[S]` (Ken, 2026-07-05):** recorded as input to the ACP adoption disposition

## System Prompt (`summary/system-prompt.md`)

- [S] Kimi `--rsp` disposition: current Kimi Code has NO per-session replace surface; only legacy `kimi-cli --agent-file` can replace — split by implementation, version-scoped (`since`/`until`) **Ruled `[S]` (Ken, 2026-07-05):** "system-prompt facts truth-up" bundle — version-scoped `SystemPromptSpec` corrections + remediation-bearing wrapper errors
- [S] Goose `--rsp` disposition: no clean per-launch replace (persistent template config mutates user config; `GOOSE_SYSTEM_PROMPT_FILE_PATH` undocumented; ACP `set_session_system_prompt` is `goose acp`-only) **Ruled `[S]` (Ken, 2026-07-05):** same "system-prompt facts truth-up" bundle
- [S] Goose `--asp`: `goose run --system` only (inline text, argv limits, conflicts with `--recipe`, no `goose session` equivalent) — interactive fallback is a weaker context file **Ruled `[S]` (Ken, 2026-07-05):** same "system-prompt facts truth-up" bundle
- [I] `OPENCODE_CONFIG_CONTENT` merge contract: system-prompt injection, MCP injection, and permission overlays all write ONE env var — overwriting breaks the others (also lands in plan Phase C shared prep) **Ruled `[I]` (Ken, 2026-07-05):** already implemented in Phase C — `wrapper_mcp.rs` merge + `wrapper_stages.rs` stage 10.5 YOLO-overlay merge; user-supplied value preserved. Marked done with pointer
- [S] Qwen/Gemini: inline-only prompt flags (argv limits), `QWEN_SYSTEM_MD`/`GEMINI_SYSTEM_MD` missing-file is FATAL; Gemini replace can drop dynamic substitution slots (tools/skills/subagents) **Ruled `[S]` (Ken, 2026-07-05):** same "system-prompt facts truth-up" bundle
- [W] Kimi legacy Jinja2 `StrictUndefined`: stray `${...}` in Claudine-composed prompt content can abort the session **Ruled `[W]` (Ken, 2026-07-05):** legacy Python kimi-cli (1.x) only; Claudine wraps Kimi Code (0.x) — version-scoped out by the product split
- [I] Docs: define `--rsp` as "replace the provider base prompt where possible" — never context isolation (memory files/skills/MCP instructions still load; OpenCode replaces slot 0 only) **Ruled `[I]` (Ken, 2026-07-05):** cheap docs fix; execute in the "system-prompt facts truth-up" bundle's doc pass

## Non-Interactive Sessions (`summary/non-interactive-sessions.md`)

- [I] Kimi Wire peer obligations: verify Claudine answers/rejects blocking `ApprovalRequest`/`QuestionRequest`/`HookRequest`/`ToolCallRequest` by policy (`--afk` = safe unattended posture); print mode is a lossy projection, not an equivalent fallback **Ruled `[I]` (Ken, 2026-07-05):** largely closed by the step-0 wire work (all four request types answered by policy in `wiring/dispatch.rs`); remaining: a posture verification note + fixtures in the kimi wire follow-up
- [S] OpenCode promoted stderr (`--print-logs --log-level INFO`) is CONTRACT, not noise — reconcile with `stderr_noise_prefixes` / `suppress_structured_stderr_on_success` semantics; the model-mismatch guard depends on this channel **Ruled `[S]` (Ken, 2026-07-05):** lands with Phase E record grammar (promoted-structured vs diagnostic stderr is a named Phase E deliverable)
- [S] Goose invocation hardening: `--quiet` mandatory for parse-safe stdout; generate stable `--name` run-id; no init event (capture launch metadata wrapper-side); `error`-then-`complete` must taint the run (`complete` carries no status) **Ruled `[S]` (Ken, 2026-07-05):** Goose-hardening work item; the error-then-`complete` taint rule itself lands in the Phase E migration map
- [S] Qwen: accept both `system/init` and `system/session_start`; classify exit codes 53 (max turns) / 55 (wall-clock) / 130 (interrupt) which bypass `result` (also Phase E declarative-record candidates) **Ruled `[S]` (Ken, 2026-07-05):** Phase E declarative-record candidates (already named in plan §E.4)
- [S] Codex exec JSONL omits launch metadata (cwd/model/provider/auth/version/MCP/sandbox/approval) — wrapper-side capture required **Ruled `[S]` (Ken, 2026-07-05):** folded into the "wrapper-metadata capture layer" bundle (next item)
- [S] Cross-provider wrapper-metadata capture layer (argv, cwd, prompt-delivery mode, sandbox/approval, provider version, session/resume ids) — one systematic capability, not eight patches **Ruled `[S]` (Ken, 2026-07-05):** its own work item — the "wrapper-metadata capture layer" bundle; one systematic capability, not eight patches

## Hooks (`summary/hooks.md`)

- [S] **Codex is no longer notify-only**: first-class 10-event hook system (blocking, `updatedInput` mutation, permission allow/deny) — Claudine's Codex registration under-covers the canonical events (**triage early**). **Ruled `[S]` (Ken, 2026-07-05):** scheduled as its own work item after Phase D step 3, alongside the Kimi Wire option-D work. Phasing: (1) registration writer (install/uninstall/sync/backups beside the kept notify wrapper) + `claudine handle` payload adapter (Claude-like stdin JSON) + codex `event_mapping` facts update/regen — observability parity first; (2) blocking-semantics wiring (PolicyEngine/protect for prompt/tool_call/permission) + registration-health kill-switch detection — after the `after_compact` canonical-enum ruling so compaction events land once. Research caveat carried: PreToolUse interception is explicitly incomplete (unified_exec/WebSearch bypass) — hooks-based protect is defense-in-depth, never a boundary
- [S] No `after_compact` canonical event — Codex/Kimi/Qwen `PostCompact` + OpenCode `session.compacted` all collapse into `notification` (canonical-enum change; Phase A1 catalog-types) **Ruled `[S]` (Ken, 2026-07-05):** gates Codex-hooks `[S]` phase 2; catalog-types enum + event_mapping regen; scheduled immediately before that work
- [W] Tool-selection phase (Gemini `BeforeToolSelection`, OpenCode `tool.definition`) is unmappable to `before_tool` — fires before a concrete tool call exists **Ruled `[W]` (Ken, 2026-07-05):** fires before a concrete tool call exists; no honest canonical mapping — document as unmapped in the hooks topic
- [S] OpenCode blocking authority requires dedicated plugin hooks, NOT the observe-only event bus — verify which mechanism Claudine bridges **Ruled `[S]` (Ken, 2026-07-05):** verify + bridge in the hooks round (OpenCode registration writer)
- [I] Goose `SubagentStart`/`SubagentStop` appear in the Open Plugins spec but are NOT emitted — don't register as active **Ruled `[I]` (Ken, 2026-07-05):** cheap facts/event_mapping correction — never register/claim them
- [S] Goose hook registration = authoring an Open Plugin (`~/.agents/plugins/<root>/hooks/hooks.json`, two config files to enable, `sh -c` execution — Windows needs Git Bash/MSYS2) **Ruled `[S]` (Ken, 2026-07-05):** Goose registration-writer work item in the hooks round (Windows Git Bash/MSYS2 caveat noted)
- [I] Kimi undocumented client-side wire-hook channel (JSON-RPC `HookRequest` at ACP/wire init) — no Claudine last mile **Ruled `[I]` (Ken, 2026-07-05):** largely implemented already — wire mode dispatches `HookRequest` through the canonical pipeline (`wiring/dispatch.rs::dispatch_hook_request`); remaining gap is registration/docs only
- [S] Registration-health detection: per-provider kill switches silently defeat Claudine hooks (Qwen `disableAllHooks`/`--bare`/safe-mode, Codex feature flags + `/hooks` trust disable, OpenCode `OPENCODE_PURE`) **Ruled `[S]` (Ken, 2026-07-05):** already scheduled inside Codex-hooks phase 2; extend cross-provider afterward
- [S] `human_in_the_loop` has no uniform provider hook — dispatchability review **Ruled `[S]` (Ken, 2026-07-05):** small analysis in the hooks round

## Session Resumption (`summary/session-resumption.md`)

- [x] **Resume parity (ruled by Ken, 2026-07-04):** if the provider natively supports resume, Claudine must support using it. The gemini/goose/opencode wrapper gap (stale legacy `resume_supported: false` despite first-class provider support) was closed same-day: all 7 profiles now implement `supports_resume` + `build_resume_args` from the research-ratified argv forms, guarded by `every_provider_profile_supports_resume`. Remaining half: graduate `ResumeSpec` into the typed catalog (Phase D field expansion) so support derives from catalog data and only argv mechanics stay behavior
- [S] Handle capture + persistence is THE prerequisite: capture a stable session handle early (structured output/hooks/list APIs), persist in run metadata, resume by explicit handle — "continue latest"/pickers are not recovery primitives **Ruled `[S]` (Ken, 2026-07-05):** anchor item of the "resume round 2" bundle — THE prerequisite
- [S] Resume does not restore launch environment (model/sandbox/approval/cwd/MCP may be recalculated) — record launch metadata and reapply **Ruled `[S]` (Ken, 2026-07-05):** "resume round 2" bundle (record + reapply launch metadata)
- [S] Serialize resume attempts per provider session id (Claude warns of interleaved transcript writes) **Ruled `[S]` (Ken, 2026-07-05):** "resume round 2" bundle
- [I] Kimi current surface: `KIMI_CODE_HOME` → `~/.kimi-code` (not `~/.kimi`), follow-up via `-p/--prompt` not legacy `--print` — version-scope the facts **Ruled `[I]` (Ken, 2026-07-05):** folded into this session's kimi version-scoping pass (step 0 already establishes the axis)
- [I] Goose: plain `--resume` is GLOBAL, not repo-scoped — automation must use explicit `--session-id` (also preserves provider/model metadata); wrapper restores cwd **Ruled `[I]` (Ken, 2026-07-05):** already implemented by the post-C resume ruling — `goose.rs::build_resume_args` pins explicit `--session-id`. Marked done with pointer
- [S] Record non-resumable launch conditions at launch time (Codex `--ephemeral`, Claude `--no-session-persistence`, Goose `--no-session`, archived Qwen sessions) **Ruled `[S]` (Ken, 2026-07-05):** "resume round 2" bundle
- [S] Two-step API resume paths (Codex app-server `thread/resume`+`turn/start`, Qwen daemon `load`/`resume`+`prompt`, OpenCode server endpoints) — the durable-HITL surfaces, no Claudine last mile **Ruled `[S]` (Ken, 2026-07-05):** durable-HITL track after "resume round 2" (couples with ACP/server surfaces)
- [S] Claude durable HITL: `PreToolUse` defer in print/SDK flows (capture `tool_deferred` + ids, resume same session after out-of-band input) — feeds `human_input_requested`/`session_resumable` signals **Ruled `[S]` (Ken, 2026-07-05):** "resume round 2" bundle — but reserve `human_input_requested`/`session_resumable` in the Phase E taxonomy NOW (cheap while designing `SignalEvent`)

## ACP (`summary/acp.md`)

- [S] **Adoption disposition (proposed by summary)**: hybrid — keep CLI wrappers as baseline; add ACP provider-by-provider as typed streaming + permission backend first; capability-gate every reverse-request handler; record ACP event streams into reporting before ACP replaces any execution path **Ruled `[S]` (Ken, 2026-07-05):** the summary's hybrid ratified as standing direction — anchors the "ACP adoption track" bundle: wrappers stay baseline; ACP provider-by-provider as typed streaming + permission backend; capability-gate reverse-request handlers; record streams into reporting before ACP replaces execution
- [I] Enforcement-plane framing is valid per delegation TIER, not per protocol — only Gemini and Goose route fs+terminal through reverse requests; Claude/Codex adapters keep execution internal **Ruled `[I]` (Ken, 2026-07-05):** absorbed into ruling B3's field-semantics notes + facts
- [S] Adapter dependency drift: Claude/Codex/Pi need external `npx` adapter packages; Pi has two divergent adapter lines **Ruled `[S]` (Ken, 2026-07-05):** with the "ACP adoption track" — track adapter identity/version in facts when integration lands
- [I] Gemini `--experimental-acp` is deprecated → `gemini --acp`; purge stale references **Ruled `[I]` (Ken, 2026-07-05):** cheap — purge stale references at next touch
- [I] ACP `session/update` stream is a missing `source` value in the signal detection-record enum (Phase E) **Ruled `[I]` (Ken, 2026-07-05):** lands in Phase E record grammar (plan §E.4 already mandates `source: acp`)

## Skills / Slash Commands / Plugins (`summary/agent-skills.md`, `summary/slash-commands.md`, `summary/agent-plugins.md`)

- [S] Generic `.agents/skills/` root (scanned by 8 of 9 providers) is an unexploited single projection target; linking into brand paths on providers that also scan `.claude/skills/` risks double-discovery **Ruled `[S]` (Ken, 2026-07-05):** one consolidated "linker v2" round — all these items touch the linking module + the `resource_support` graduation; piecemeal passes would churn the same code
- [S] Precedence/conflict semantics are provider-divergent (first-wins / last-wins / tiered) — linked skills can be silently shadowed; linker assumes none of them **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [S] Activation dependencies can silently disable linked skills (Gemini consent gate, Goose Summon dependency, OpenCode/Kilo `skill`-tool permission, Pi `read`-tool gating) — "Linked But Degraded" warnings needed **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [S] Inventory-only resource class: managed/bundled/plugin/built-in/URL/marketplace scopes must be inventoried, never synced **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [S] Format-convertible command artifacts (Gemini TOML, Goose config+recipe, OpenCode JSON entries) break link-as-file — linker needs parse-and-emit-native conversion **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [S] Codex (`$name` skills only) and Kimi (`/skill:`/`/flow:`) have no user `/name` surface — don't present linked commands as native slash commands there **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [S] Argument/interpolation grammar rewrite map (`$ARGUMENTS`/`$N` vs `{{args}}` vs Jinja vs append-only; shell/file injection syntaxes) — cross-linking without it silently corrupts behavior **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [I] Reconcile the two portability taxonomies (skills 5-class vs commands 4-case) into ONE classification enum — **ratified 2026-07-03 (Ken)**: 5-class skills enum, `PortableWithProviderMapping` widened to cover deterministic format conversion, conversion-ness carried as `artifact_kind` + `conversion: none/mechanical/semantic` facts (see spec table-B row)
- [S] Plugin-carried resources are invisible to the linker — extraction with provenance (source provider, plugin name/version, namespace) is the ruled approach **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [S] Plugin bundles as a Claudine distribution channel: summary answers "yes, selectively" (generated per-provider manifests; never one shared package) — needs explicit disposition **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (consolidated linking-module work)
- [I] Runtime-executable plugin surfaces (OpenCode/Kilo `plugin/` dirs, Pi TypeScript extensions) must be codified as linking exclusions **Ruled `[I]` (Ken, 2026-07-05):** cheap safety rule — codify the exclusion list now

## Subagents (`summary/subagents.md`)

- [S] **Subagent observation adapters** with one normalized child record (parent session id, child agent/session id, type, invocation prompt, start/stop-or-idle markers, final text, transcript/store handle, termination reason, permission mode): Claude/Codex/Qwen/legacy-Kimi map to real `SubagentStart`/`Stop`; Gemini must be synthesized from `tool_use`/`tool_result`; OpenCode from `subtask` part → `session.created parentID` → `session.idle` (this is the recipe for the known OpenCode subagent-invisibility blind spot); Goose from `subagent_tool_request` MCP notifications + `load()` result metadata **Ruled `[S]` (Ken, 2026-07-05):** couples with the logging "evidence-adapter track" bundle (same stores/readers)
- [S] **Observability-strength gating** for child recovery: `strong_child_identity / session_child_identity / tool_call_with_captured_output / tool_call_only` — only strong identity can support future child `resume`/`proxy`/transcript reporting; don't promise uniformly **Ruled `[S]` (Ken, 2026-07-05):** same "evidence-adapter track" item as observation adapters — the gating enum is its design core
- [I] Kimi subagent linking is version-sensitive: legacy `kimi-cli` YAML `--agent-file` per-session only (no user/repo discovery dirs — NOT a filesystem-linking target); newer `kimi-code` drops that surface **Ruled `[I]` (Ken, 2026-07-05):** version-scoping note in this session's kimi facts pass; linking exclusion for the legacy YAML surface
- [S] Compatibility-origin tagging: Goose scans `.claude/agents/` (same file discoverable via two providers — tag as compatibility-origin, report ignored Claude-only frontmatter); Qwen deliberately bridges Claude agent schema but the reverse direction is lossy — represent as ASYMMETRIC compatibility **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round
- [S] Codex agent files are full TOML config layers (model/sandbox/approval/MCP/skills) with no `--agent` main-session mode — never equivalent to a Claude/OpenCode primary agent; linking requires body→`developer_instructions` + frontmatter→TOML translation **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (translation, never equivalence)
- [S] Goose frontmatter `model` is metadata-only (runtime model comes from parent/delegate/recipe) — don't project model intent into it **Ruled `[S]` (Ken, 2026-07-05):** "linker v2" round (don't project model intent)

## Local Runners / Model Config (`summary/local-runners.md`, `summary/model-config.md`)

- [S] Typed runner-detection surface (sniff) with confidence ladder + identify-by-response-marker never by port (oMLX and vLLM share :8000; llama.cpp serves Ollama-style `/api/tags`); include bind-exposure reporting (vLLM defaults `0.0.0.0`) **Ruled `[S]` (Ken, 2026-07-05):** sniff-side work item opening the "local-runner bridge track" (separate package area); confidence ladder + identify-by-response-marker
- [S] Bridge-config generation: Claudine writing OpenAI-compatible provider blocks (`http://localhost:<port>/v1`) / `ANTHROPIC_BASE_URL` env for detected runners **Ruled `[S]` (Ken, 2026-07-05):** own feature anchoring the "bridge-config track", after runner detection
- [S] Partial-compatibility warnings: per-runner Anthropic feature gaps (Ollama: no prompt caching/token counting/`tool_choice`; LM Studio: no extended thinking; vLLM: `Bearer` not `x-api-key`) — a working `/v1/messages` ≠ full feature support **Ruled `[S]` (Ken, 2026-07-05):** with the "bridge-config track"
- [I] Gemini CLI cannot bridge to local runners without a Gemini-protocol translating proxy — exclude or document **Ruled `[I]` (Ken, 2026-07-05):** cheap doc note — exclude from bridge scope
- [S] OpenAI dialect split: Codex is Responses-only (`/v1/responses`) — bridge generation must distinguish `openai_chat` vs `openai_responses` and know when a proxy is required **Ruled `[S]` (Ken, 2026-07-05):** with the "bridge-config track" (proxy-required detection)
- [S] Codex `model_catalog_json` is replacement-shaped (touch it → own the whole catalog); provider ids `openai`/`ollama`/`lmstudio` are RESERVED — generated Codex config must respect both **Ruled `[S]` (Ken, 2026-07-05):** with the "bridge-config track" (generation constraints)
- [S] Stale-manual-block detection: shadow-semantics providers + Kilo's hourly Models.dev refresh make stale user model blocks a detectable drift condition (fits Phase F drift-channel SignalEvents) **Ruled `[S]` (Ken, 2026-07-05):** Phase F drift channel (plan already points there)
- [I] Qwen project-scope settings REPLACE user-level `modelProviders` (not merge) — config inspection/generation trap **Ruled `[I]` (Ken, 2026-07-05):** cheap — note in model-config facts/docs as a config-generation guard

## Cross-Topic

- [W] **Roo refresh sweep** — Roo research is missing/stale across six topics (agent-cli, agent-logging, hooks, resume, acp, subagents) and absent from skills/permissions rosters; one consolidated sweep, not per-topic patches. **Won't-do (2026-07-04): superseded by Checkpoint B's full Roo Code removal** (enum variant, roster entry, research documents, facts file all deleted); there is no Roo surface left to refresh
