# Help System Touch Up

We recently organized the help system's commands into categories and we're going to continue that by:

- create a new group called `<b>Hook Events and Actions:</b>`
    - move the "hooks" and "actions" commands into this group
    - this group should be directly after "Shared Resources:"

In addition the title line for Claudine should be changed away from `Claudine — cross-agent hook/event system for agentic CLIs` to:

- `<b><yellow>Claudine</yellow></b>\n<dim><i>{description}</i></dim>`
- where the description is taken from the CLI's "description" property in Cargo.toml


