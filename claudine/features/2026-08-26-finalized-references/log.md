---
implementation_1: "2026-09-07T19:29:15-07:00"
implementation_2: "2026-09-07T22:05:36-07:00"
---

## Implementation of Review Findings #1

> **started at:** 2026-08-27T11:23:38+01:00

- this implementation is attempting to implement _all_ of the review findings found in 'claudine/features/2026-08-26-finalized-references/review-1.md'
- this is iteration 1 of the review-to-implement cycle
- starting the work on 'final cross-platform validation contract' at 11:25:19
        - confirmed that AC9 and the remote portion of AC10 remain incomplete because the required native environments are unhealthy, not because a functional regression has been demonstrated
        - confirmed that the current-tree macOS gates already pass in biscuit-file, Darkmatter, and Claudine; WSL passed biscuit-file and Darkmatter before Claudine compilation exhausted the filesystem
        - confirmed that native Linux stopped accepting bounded batch-only connections before a gate could start and native Windows failed the non-bypassable 50 GiB capacity preflight, leaving the junction/reparse-point fixture unexecuted
        - deferred the missing Linux, WSL Claudine, and native-Windows validation evidence to CI/CD as directed by the review's explicit non-blocker decision
        - did not retry the known-unhealthy external builders or rerun already-recorded local gates; no implementation code, tests, or symbols required changes
- work completed for 'final cross-platform validation contract' at 11:25:45
- starting the work on 'active documentation finalized-reference grammar' at 11:27:19
        - inspected every location named by review finding 2 and searched active biscuit-file, Darkmatter, Claudine, and portable Claudine-skill Rust/Markdown authorities for removed `!` grammar, repository-first precedence, incomplete `@` ordering, and missing `&`/`^` completion forms
        - GitNexus rated `resolve_proxy_target` HIGH (8 direct dependents, 21 total across coordinator, harness orchestration, loop control, and tests) and the system-prompt resolution path CRITICAL (1 direct dependent, 18 total, including the `construct_argv_and_system_prompt` flow); the orchestrator acknowledged the warning and authorized strictly documentation-only rustdoc alignment
        - lower-risk impact results were MEDIUM for Darkmatter `resolve_path` (8 direct, 12 total, one transclusion flow) and Claudine `resolve_system_prompt_source` (14 direct), LOW for `FileReference::resolve`/`resolve_from`, Darkmatter `ResolutionContext`, `with_package_area`, `portable_destination`, error rendering methods, and the boundary test, with no executable behavior changed
        - rewrote the active biscuit-file `FileReference` technical specification around finalized D1/D3/D6/D8 grammar, captured contexts, containment, completion parity, and test obligations; updated public resolver rustdoc and precedence test documentation
        - updated active Darkmatter expression, schema, transclusion, context, and materialization documentation plus related rustdoc to composition-CWD-first implicit resolution, `@`/`&`/`^`, absolute native file materialization, and passive-validation boundaries
        - updated active Claudine composition, execution-flow, lifecycle, sequence, system-prompt, completion, CLI README, portable skill snapshots, and source/test comments to the finalized grammar and complete effective magic-root order
        - left historical completed feature/review records and the dated Claudine timeline unchanged; also left executable removed-sigil diagnostics and runtime error hints unchanged under the documentation-only scope
        - refreshed existing Markdown state hashes with Darkmatter via `md hash` for `.claude/skills/claudine/composition.md`, `claudine/docs/topics/composition.md`, and `claudine/docs/topics/execution-flow.md`; introduced no new hash fields
        - closure searches found no stale finalized-reference grammar in active documentation or rustdoc; remaining matches were the dated timeline, accurate removed-sigil diagnostics, unrelated expression/filter negation, a test assertion diagnostic, and an unrelated repository-discovery comment
        - `just test` passed in biscuit-file (813 tests plus 6 no-default-features tests), Darkmatter (7,561 tests, 50 skipped), and Claudine (6,680 tests, 11 skipped)
        - `just lint` passed in biscuit-file, Darkmatter, and Claudine; scoped `git diff --check` also passed
- work completed for 'active documentation finalized-reference grammar' at 12:00:32

### Successful Completion

The implementation of review cycle 1 has completed successfully in 36 minutes 54 seconds. During this implementation all 2 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 1 was fixed, 1 was deferred (see reasons below):

- `final cross-platform validation contract` was deferred because the required native builders were unhealthy: Linux stopped accepting bounded batch-only connections, WSL exhausted its filesystem while compiling Claudine, and native Windows failed the mandatory 50 GiB capacity preflight. CI/CD will provide the missing final-tree Linux, WSL, and native-Windows evidence, including execution of the Windows junction/reparse-point fixture.

The files changed for the fixed documentation finding are recorded in its progress entry above; no executable behavior changed.

## Implementation of Review Findings #1

> **started at:** 2026-09-07T19:29:15-07:00

- this implementation is attempting to implement _all_ of the review findings found in 'claudine/features/2026-08-26-finalized-references/review-1.md'
- this is iteration 1 of the review-to-implement cycle
- context for this run: an earlier iteration-1 pass by `codex/default` (2026-08-27) is recorded above; this pass re-enters the same cycle as `claude/default` on the `feat/unifi` branch, which has taken merges since that pass, so each finding is re-verified against the current tree rather than assumed closed
- starting the work on 'final cross-platform validation contract' (finding 1, High) at 19:29:40
        - re-read the finding's explicit `DECISION` clause, which rules it a **non-blocker** and directs the missing native evidence to CI/CD rather than to local builders; that ruling still governs, so no attempt was made to revive the unhealthy Linux/WSL/native-Windows hosts
        - verified against tree `78d114f819d49580ec7bae449c960570c9da2d30` that CI is now actively producing the exact matrix the finding delegated to it — GitHub Actions run `34174352874` on `feat/unifi` fans every package area out across `ubuntu-latest`, `macos-latest`, and `windows-latest`, plus a WSL2 leg
        - materially better evidence than the 2026-08-27 pass could record: `biscuit-file`, `biscuit-file-cli`, and `claudine` (lib) have already completed **green on all three native OSes**, and `darkmatter-cli` is green on Ubuntu
        - still outstanding at the close of this pass: `darkmatter` (lib), `claudine-cli`, and the remaining `darkmatter-cli` legs were `queued`/`in_progress`, and the WSL2 test legs had not finished, so the matrix is not yet complete
        - the Windows-only `repository_containment_rejects_an_external_junction` fixture runs inside the `claudine`/`biscuit-file` Windows legs that have now passed, which is the first native-Windows execution of that containment evidence
        - finding remains **deferred** per its own ruling: closure is CI/CD's to record, not this implementation cycle's, and the run had not finished when this pass completed
- work completed for 'final cross-platform validation contract' at 19:31:20
- starting the work on 'active documentation finalized-reference grammar' (finding 2, Medium) at 19:32:22
        - re-verified all eight locations the finding names; every one is already correct on this tree, so the finding's own examples are closed — the residual drift below was found by an independent sweep beyond the cited list
        - `biscuit-file/lib/src/file_reference/mod.rs:338-347` — `CompletionEntryForm::Magic` documented only "git root and the user's home directory", and `CompletionEntryForm::ImplicitRelative` documented the **superseded repository-first order** ("enclosing git root and the caller-provided base directory, in that order"); both corrected against `completion_roots`, which is base-then-repository for implicit and prepends → package → package area → repository → home → appends for magic
        - `biscuit-file/lib/src/file_reference/resolve.rs:170` — `compute_effective_anchoring` still listed the retired `package` kind among the non-anchoring kinds; replaced with repository-root/repository-scoped
        - `biscuit-file/docs/topics/file-references.md:20,318` — the opening doctest comment and the recursive-magic example both described `@` as "git repo root, then HOME"
        - `biscuit-file/lib/README.md:22,27` and `biscuit-file/cli/README.md:143` — `@` described as repo-root-then-HOME, and the resolution context still named "Cargo workspace" from the removed package resolver
        - `biscuit-file/lib/tests/precedence_flip.rs:219` — assertion message said the implicit interpolation "resolves repository-first"; the fixture actually proves the repository-root **fallback** (the file exists only at the root), and the module doc above it already states base-first
        - `darkmatter/cli/src/args/command.rs:327` and `darkmatter/lib/src/markdown/compose/expression/resolve_ctx.rs:295` — rustdoc still advertised `!` as a supported file-reference sigil; replaced with `&`/`^`
        - `darkmatter/docs/topics/magic-paths.md` — the "Default Search Order" section published the two-root intrinsic order (git root, HOME); rewritten to the D6 four-root intrinsic order with the exactly-once guarantee, and the worked prepend/append example extended accordingly
        - `darkmatter/docs/transclusion/block-transclusion.md:60-64` — `@` was documented as the "Repo Root based path"; split into a repository-contained group (`&` exact, `^` most-specific-first) and a separate magic-path group that names the home fallback
        - `darkmatter/docs/transclusion/transclusion-design.md:277,290,298` — supported-path list called `@` "repo-root relative" and the security boundary named `@` as the contained form; corrected to `&`/`^` and the full magic order
        - `claudine/lib/src/harness/error.rs:212` — `RepoRootRequired` was documented as "a `@`-prefixed path requires a repo root"; `@` now falls back to home and the variant has no production constructor, so the doc records what actually reports the condition
        - `claudine/lib/src/harness/resolve.rs` and `claudine/lib/src/composition/sequence/source.rs:32` — resolution-rule lists omitted `&`/`^` entirely
        - `claudine/lib/src/composition/sequence/tests.rs:512,525` — the `^` fixture's comment and assertion message used the retired "package reference" vocabulary
        - `.claude/skills/biscuit-file/references/cli.md:41` — portable skill snapshot repeated the repo-root-then-HOME `@` order
        - GitNexus upstream impact for every symbol whose rustdoc changed: `resolve_sequence_reference` **CRITICAL** (60 impacted, 13 direct), `resolve_harness_path` **CRITICAL** (26 impacted, 14 direct), `resolve_document_file_ref` **HIGH** (41 impacted, 8 direct), `compute_effective_anchoring` LOW (6), `CompletionEntryForm` LOW (0); all edits are comment-only, so no impacted symbol changes behavior
        - deliberately left unchanged: `claudine/docs/topics/file-referencing.md` (normative design-intent document, needs Ken's approval); the dated `2026-07-17` entry in `.claude/skills/claudine/timeline.md`, which still records the superseded repository-first flip but is excluded as a historical record; `biscuit-file/plans/2026-03.file-references.md` and `biscuit-file/docs/superpowers/plans/…`, which are dated plan artifacts; and every accurate removed-sigil parse diagnostic
        - no edited Markdown file carries a `hash:` frontmatter property, so no `md hash` refresh was required and none was introduced
        - `just test` passed in biscuit-file (813 tests, 0 skipped, plus 6 no-default-features tests), Darkmatter (7,709 tests, 51 skipped), and Claudine (6,840 tests, 11 skipped); `just lint` passed in all three areas
- work completed for 'active documentation finalized-reference grammar' at 19:56:10
- starting the work on 'runtime hints advertising the removed `!` sigil' (finding 2, extension) at 19:58:04
        - the sweep surfaced three **user-facing runtime hint strings** that do not merely describe the retired grammar but actively tell an operator to *use* it; the 2026-08-27 pass had explicitly scoped them out as "documentation-only", but they are the sharpest instance of finding 2's stated harm ("a caller following these docs can select the wrong reference spelling"), so they were closed here rather than left
        - `darkmatter/lib/src/markdown/compose/transclusion/types.rs:511` published `Supported: ./relative, @/repo, !package, vault:, %, {{ENV}}` — a shipped claim that `!package` is a supported reference form; rewritten to the D1 catalog `./relative, implicit, @magic, &repo-root, ^repo-scoped, ~/home, vault:, %, {{ENV}}`
        - `darkmatter/lib/src/markdown/compose/transclusion/types.rs:677` and `darkmatter/lib/src/markdown/reference/errors.rs:303` both hinted `Check repository-root (`@/`) or package (`!`) prefix usage.`, which is wrong twice over — `!` is removed and `@` is multi-homed, not repository-root; both now read `Check sigil usage: `@` magic, `&` repository root, `^` repository-scoped.`
        - this is a deliberate **behavior change** (rendered CLI output), kept in its own step so the preceding documentation work stays comment-only per the repo's scope-discipline rule
        - the two guarding tests were themselves asserting the retired grammar — `darkmatter/lib/tests/error_snapshots/reference.rs:103-104` and `.../transclusion.rs:314-315` required the rendered hint to contain `"@/"` and `"!"`, so they would have *failed* on correct output; assertions retargeted to `"@"`, `"&"`, `"^"`, which is exactly the "test documentation contradicts the behavior it guards" case the finding names
        - four `insta` snapshots regenerated (`transclusion__file_reference`, `reference__file_reference`, `transclusion__unsupported_reference_type`, `transclusion__unsupported_reference_type_markup`); the two `unsupported_reference_type` snapshots required a second pass because the longer hint word-wraps inside the Prose status block, so the accepted snapshots are the renderer's own output rather than hand-written text
- work completed for 'runtime hints advertising the removed `!` sigil' at 20:07:41
- orchestrator verification (independent of the implementing subagent), at 20:10:33
        - re-ran every gate first-hand rather than accepting the subagent's report: `just lint` exit 0 in biscuit-file, Darkmatter, and Claudine; `just test` 813/813 in biscuit-file, 7,709/7,709 (51 skipped) in Darkmatter, 6,840/6,840 (11 skipped) in Claudine — all counts matched the subagent's claims exactly
        - confirmed the documentation step is genuinely non-behavioral: the only non-comment lines in its Rust diff are two `assert!` **message** strings, with no assertion conditions, constants, or rendering touched
        - independently verified the headline drift claim against the implementation — `completion_roots` (`biscuit-file/lib/src/file_reference/resolve.rs:1402-1407`) pushes `anchors.base` before `anchors.repository_root`, so the old `ImplicitRelative` rustdoc really did publish the retired repository-first order that D3 reverses

### Successful Completion

The implementation of review cycle 1 has completed successfully in 41 minutes 18 seconds. During this implementation all 2 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 1 was fixed, 1 was deferred (see reasons below):

- `final cross-platform validation contract` (finding 1, High) was deferred because the finding carries its own explicit `DECISION` clause ruling it a **non-blocker** and directing the missing native evidence to CI/CD rather than to local builders. Closure is therefore CI/CD's to record, not this cycle's. The position is nevertheless materially stronger than at the 2026-08-27 pass: GitHub Actions run `34174352874` on `feat/unifi` completed `biscuit-file`, `biscuit-file-cli`, `darkmatter`, `darkmatter-cli`, `claudine` (lib), and `claudine-catalog-types` **green on `ubuntu-latest`, `macos-latest`, `windows-latest`, and WSL2**, which is the first native-Windows and native-Linux evidence this feature has had, and it executes the Windows-only junction/reparse-point containment fixture. Two `claudine-cli` legs failed, and both were confirmed unrelated to this feature: `handle_turn_complete_fast_path_completes_under_3s` (Windows) is a wall-clock assertion that missed its 3 s budget by 1.08 s on a shared runner, and `level2_completion_schema_failure_renders_the_full_plain_status_report` (macOS L2) timed out waiting 30 s for the `claudine_rc:` exit marker — a capture-harness artifact. Neither touches file-reference grammar, and the four additional red legs in the run belong to `sniff`, which is outside this feature's three package areas. The run had not fully finished (the Darkmatter WSL2 leg was still in flight) when this pass closed.

The files changed for the fixed documentation finding are recorded in its progress entries above. Two items are flagged for Ken rather than changed unilaterally:

- `.claude/skills/claudine/timeline.md:20` still states that a bare path resolves "repository-root first, then the source document's directory", and no entry supersedes it. The file's own preamble declares it "historical context, not a spec", and the review directs that historical records be left unchanged, so it was left alone — but the drift-maintenance gap is real and should be closed with a new dated entry when this feature is ratified, not by rewriting the 2026-07-17 record.
- `claudine/lib/src/harness/error.rs::RepoRootRequired` has no production constructor (only tests and diagnostic-conformance fixtures) yet remains in the published error catalog; worth a decision on retirement.

## Implementation of Review Findings #2

> **started at:** 2026-09-07T22:05:36-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/claudine/features/2026-08-26-finalized-references/review-2.md'
- this is iteration 2 of the review-to-implement cycle
- review-2 carries three findings:
        - **High** — the spawn inventory guard does not prove that every executable child is governed (`claudine/cli/tests/spawn_inventory.rs`)
        - **High** — the final cross-platform acceptance matrix (AC10) is incomplete and includes failed legs
        - **Medium** — `darkmatter/docs/cli/index.md` still publishes an incomplete file-reference grammar
- impacted package areas per the spec's `packages` frontmatter: `biscuit-file`, `darkmatter`, `claudine`; test/lint gates are scoped to those three areas only
- starting the work on 'binding- and execution-aware spawn inventory guard' (finding 1, High) at 22:06:44
- starting the work on 'active Darkmatter CLI page publishes an incomplete grammar' (finding 3, Medium) at 22:50:34
        - before the change, `darkmatter/docs/cli/index.md:46-53` listed only three bullets — "relative paths", "absolute paths", and "magic paths (e.g., starts with `@`) which match on repo relative, package relative, and user home relative paths" — with four distinct defects:
                - `&` and `^` were absent entirely
                - the `@` root list omitted the package-area root and both registered prepend/append positions, and stated the intrinsic roots in the **wrong order** (repository before package)
                - implicit-relative ordering was never stated, so a reader could not tell it is composition-CWD-first
                - `vault:`, `http(s)://`, `%`, `{{VAR}}`, and the removal of `!` went unmentioned
        - the "more details" link pointed at `biscuit-file/README.md` rather than the canonical topic document
        - the published order was verified **against the implementation, not against prose**: `completion_roots()` (`biscuit-file/lib/src/file_reference/resolve.rs:1375-1426`, which mirrors the execution candidate builder `collect_roots`) gives `Magic` = prepends → package → package area → repository → home → appends; `ImplicitRelative` = base directory → repository root (D3's flip, **not** the superseded repository-first order); `RepositoryRoot` (`&`) = repository root only; `RepositoryScoped` (`^`) = package → package area → repository, with no home entry — all four using `push_unique`, so lexical duplicates collapse in first-seen order
        - cross-checked that result against spec D1/D3/D4/D5/D6/D9 and `biscuit-file/docs/topics/file-references.md`; the three agreed, so no ambiguity had to be ruled
        - rewrote the section in the review's preferred "accurate summary plus prominent canonical link" shape: a nine-row GFM table (`written as` / `kind` / `candidate order`) covering explicit relative, implicit relative, absolute, `~`, `@`, `&`, `^`, `vault:`, and `http(s)://`; a definition of "base directory" distinguishing a shell-typed argument from a reference authored inside a document; a modifiers list for `%` and `{{VAR}}`; and four caveats — `&` needs shell quoting (`md render '&docs/plan.md'`), `&`/`^` are repository-contained and error rather than miss, `@` is the one multi-homed introducer that can leave the repository via `$HOME`, and `!` is now a parse error suggesting `^`
        - both relative links in the replacement (`../../../biscuit-file/docs/topics/file-references.md` and `../topics/magic-paths.md`) were resolved on disk from `darkmatter/docs/cli/`
        - the sweep beyond the cited page found **one further active drift**: `darkmatter/docs/cli/compose.md:145` claimed "All file path arguments support `@`-prefixed file references (resolved from git root)", wrong on both the root set and the order; replaced with a correct one-line summary plus a link to the new section
        - already correct and left alone: `darkmatter/docs/topics/magic-paths.md` (correct four-root intrinsic order, fixed in iteration 1) and `darkmatter/docs/transclusion/block-transclusion.md` (already documents `&`/`^`); the seven other `cli/*.md` argument descriptions say only "supports `@` file references" — incomplete but not incorrect, and expanding the grammar into each argument list would duplicate the canonical table, so they were left under Rule 3
        - neither edited file carries YAML frontmatter, so there is no `hash:` property to refresh and none was introduced
        - `just lint` exit 0 in Darkmatter; `just test` was not required — `grep` for `cli/index.md`/`cli/compose.md` across all Rust sources returns no hits, so no test reads either file
- work completed for 'active Darkmatter CLI page publishes an incomplete grammar' at 22:59:44
        - **the old heuristic, precisely.** `FunctionScanner`/`scan_function` (former lines 153-229) collected two flat per-function line lists — constructor lines and `contribute_child_environment` call lines — and declared every constructor governed when `contributions.len() >= constructors.len()`, or when a single contribution followed all constructors. Nothing associated a helper call with a binding, a receiver, an execution, or a control-flow path
        - the review's exact counterexample was reproduced and confirmed accepted by the old rule (two commands, two helper calls on one of them: 2 >= 2 → both "governed"), along with four more the sweep found: a helper placed **after** `spawn`/`status`/`output` (1 >= 1 → "governed"), a helper applied to the wrong receiver, a helper applied inside only one `if`/`match` arm, and a helper applied after the command was already handed to another function
        - **the new analysis** is a flow-ordered abstract walk of each function body. `Command::new` (std/tokio, aliased, renamed, fully qualified) mints a command id bound to its `let`/assignment target; ids propagate through builder chains, `?`, `.await`, parens, and moves between locals. `contribute_child_environment(&mut x)` marks **only the ids the first argument resolves to**. The proof obligation is discharged at any point the command can execute or leaves local control — `.spawn()`/`.status()`/`.output()`/`.exec()`, a return/tail-position hand-off, a by-value or `&mut` argument hand-off, or an untrackable destructure — and being unhelped at that point makes the site `UNCONTROLLED`
        - branches (`if`/`else`, `match` arms, let-else) are analyzed per path and merged **pessimistically**: a command stays helped only if every branch that constructed it also helped it, while a branch that never saw the command abstains rather than voting it unhelped — the abstention rule is what keeps `providers.rs::gen_command`'s two-arm construction correct. Diverging paths (`return`/`break`/`continue`) drop out of the merge. Loops run two passes, the second seeded from the entry-exit merge, so a helper at the bottom of a body cannot govern the execution at the top of the next iteration
        - the rule is **fail-closed**: a site is governed only when the helper provably reached it and no unhelped execution or hand-off was found; unknown expression shapes fall back to treating every command that reaches one as leaving local control
        - **a second, larger false negative was found and closed.** The scanner recorded only `Name`/`Rename` use-trees and dropped `UseTree::Glob`, so `cli/src/commands/wrap/exec/wiring/session.rs` — which says `use super::*` and whose parent `wiring/mod.rs:35` imports `std::process::Command` — produced **zero sites**. That file builds a genuine provider `Command` (all three stdio piped, `env_clear`, `spawn()` at line 79) and was invisible to a guard whose whole purpose is to fail on an ungoverned child. This was outside the review's cited examples but is the same defect class and a strictly worse instance, so it was closed here rather than deferred
        - fixed with bounded intra-crate glob resolution: a `GlobResolver` maps a glob path to the module's backing file using rustc layout (`foo.rs` / `foo/mod.rs`), handles `super::*`, `self::x::*`, `crate::a::b::*`, and bare `foo::*` (child module only — Rust 2018+ reads a bare glob as an external crate, never a sibling), follows the target's own globs transitively with a depth cap of 3 plus a visited set, and merges re-exports over-approximately. The visited set rather than the cap is what breaks the real cycle: `wiring/mod.rs` has `pub(crate) use session::*` while `session.rs` has `use super::*`
        - **glob resolution required tightening the alias rule first.** `kind_for_constructor` matched on the last two path segments, so any file that inherited a `Command` alias would also have matched `clap::Command::new` — `cli/src/main.rs:122,167`, `argv/partition.rs:129`, and `completion/mod.rs:69` would have become false census entries. The alias set is now consulted only for two-segment paths; three-or-more segments must spell `std::process::Command::new` or `tokio::process::Command::new` in full
        - API shape preserved for the fixtures: `scan_source(path, source)` stays pure and filesystem-free so every in-test source-string fixture works unchanged, with `scan_crate_file(...)` as the new filesystem-aware entry point used by `generate_inventory`
        - **full sweep for other newly visible seams.** All 38 `Command::new` occurrences across `claudine/lib/src` and `claudine/cli/src` were enumerated and classified: 17 in the census (16 prior + `session.rs`); 4 excluded `clap::Command::new`; 1 doc-comment mention (`bash.rs:46`); the remainder inside `#[cfg(test)] mod tests`, each confirmed by locating the enclosing attribute — including `termination/windows.rs:599`'s `#[cfg(all(test, windows))]` block spanning 600-852, whose nested `cfg` list the existing `meta_contains_test` recursion already handles. **No production seam is UNCONTROLLED**, so there is no `AGENT_CWD` defect to escalate
        - artifact change is exactly one added entry — `cli/src/commands/wrap/exec/wiring/session.rs:55`, `run_kimi_wire_session`, `std`, `contribute_child_environment`; no existing entry changed path, line, kind, or governance, and the orchestrator confirmed this by reading `git diff` of `claudine/docs/providers/spawn-seam-inventory.json` (+7 lines, one object)
        - fixtures added, all as in-test source strings or `tempfile::tempdir()` crates so no production source is mutated during a test (AC6): `scanner_requires_the_helper_on_the_executing_command` (the review's verbatim duplicate-helper counterexample, helper-after-`spawn`, helper-after-a-bound-`status`, wrong receiver, helper after a `&mut` hand-off); `scanner_requires_the_helper_on_every_branch` (single-arm `if`, single-arm `match`, one-of-two constructing arms, both arms helped → governed, every arm constructing with one shared helper → 2 governed, helper in an `if`-condition with an early return → governed); `scanner_reads_loops_as_repeating` (helper before the loop and helper first inside the body → governed; helper after the execution → `UNCONTROLLED`); `scanner_follows_a_command_into_a_deferred_body` (`thread::spawn` closure and `async` block); `a_glob_import_inherits_the_command_alias_from_the_crate` (temp crate mirroring the real `lib.rs` → `wiring/mod.rs` → `session.rs` layout, asserting the same source yields **nothing** through the pure `scan_source` — the exact invisibility this change removes); `a_non_crate_glob_supplies_no_command_alias`; `a_qualified_clap_command_is_not_a_process_command`; and `indirect_governor_applies_only_to_the_allowlisted_factories` (the same `system_shell_command` source is `caller:SystemTaskShell::run` at the allowlisted path and `UNCONTROLLED` anywhere else)
        - every fixture asserts an exact (governed, uncontrolled) tally, so a scanner that saw nothing fails the assertion; each counterexample's expected tally differs from what the old heuristic computed for the same source, which is what makes them non-vacuous
        - documented scope limits kept in the module header: closure and `async` bodies are analyzed from the state at their definition site and do not flow back; macro arguments count as observations, not hand-offs; globs written inside an inline `mod { ... }` are skipped because their relative paths anchor to that inline module rather than to the file
        - **orchestrator verification, first-hand rather than accepting the report:** `cargo nextest run -p claudine-cli --test spawn_inventory` → 10/10 pass; the review's counterexample confirmed present verbatim in `scanner_requires_the_helper_on_the_executing_command`; `just test` in Claudine → 6,848 passed, 11 skipped; `just lint` in Claudine → exit 0
- work completed for 'binding- and execution-aware spawn inventory guard' at 23:07:27
- starting the work on 'final cross-platform acceptance matrix (AC10)' (finding 2, High) at 23:08:12
        - unlike iteration 1, this pass did **not** treat the finding as delegated-and-closed; the review-2 wording explicitly withdraws that posture ("The earlier decision to treat remote execution as a non-blocker for implementation work does not satisfy the specification's production-readiness gate"), so the orchestrator ran every AC10 leg this host can actually produce and pinned down exactly what remains
        - **the two red legs review-2 cited were identified precisely.** CI run `34174352874` (tree `7aa5945a5`) has now *finished*, where the review saw it in flight. In-scope reds were exactly two: `claudine-cli / test (windows-latest)` — `handle_deadline handle_turn_complete_fast_path_completes_under_3s`, 2,126 of 2,127 passed — and `claudine-cli / test-l2 (macos-latest)` — `level2_typed_error_render_capture level2_completion_schema_failure_renders_the_full_plain_status_report`, 238 of 239 passed. The other four reds in that run belong to `sniff`, which is outside this feature's three package areas
        - **both are already fixed in the tree.** Commit `ebc28e107` ("test: isolate CI fixtures and stabilize terminal input", HEAD) touches exactly `claudine/cli/tests/handle_deadline.rs`, `claudine/cli/tests/level2_typed_error_render_capture.rs`, and `sniff/lib/src/filesystem/just.rs` — the three failing files and nothing else. Reading the diff confirms the fixes are causal, not incidental: the Windows test moved onto `CliProcessFixture` (hermetic PATH, no host `USERPROFILE` config, rendezvous reporting off) which removes the host work that blew the 3 s budget, and the macOS L2 test now stages its multiline environment into a shell script instead of pushing it through the terminal's bounded canonical input buffer, which is what starved the `claudine_rc:` exit marker. **That commit has not yet been through CI**, so the green matrix it should produce does not exist yet
        - **local macOS legs were run first-hand on the exact final tree** (working tree including this cycle's changes):
                - `just test` — biscuit-file 813/813; Darkmatter 7,709/7,709 (51 skipped); Claudine 6,848/6,848 (11 skipped). **Green.**
                - `just lint` — exit 0 in all three areas. **Green.**
                - `just test-l2` — biscuit-file reports "not applicable" (the area has no real-terminal tests, a documented recipe-level N/A); Darkmatter 90/90 green serially (18 lib + 69 CLI + 3 dmls); Claudine 234/239
        - **the five Claudine L2 failures were triaged individually rather than waved through as "flaky".** Four pass in isolation and are load/contention artifacts of the shared real-terminal pane: `level2_dry_run_not_installed_renders_yellow_dim_in_tmux` (captured the wrong table row entirely), `level2_perf_tree_renders_styled_in_tmux` (frame still held the previous test's shell prompt), `level2_perf_tree_renders_styled_in_wezterm` (under load "environment setup" took 51% and outranked the `::shell` directive the assertion expects to dominate), and `level2_opencode_completion_only_tool_use_renders_one_tool_line` (fixture timeout at 30.7 s, 1.6 s in isolation)
        - the fifth, `level2_initialize_proxy_block_auto_detects_osc8_in_wezterm`, reproduces even in isolation, and the captured frame names the cause outright — an interactive `Atuin AI is not yet configured. / Enable Atuin AI / > Disable ? Keybind / Cancel` prompt sitting in the spawned WezTerm login pane, swallowing the shell input so the `; echo claudine_rc:$?` trailer never runs. That is a **host shell-startup condition** on this machine, not a repo defect: the L2 WezTerm harness spawns an interactive login shell, so anything the host's startup does interactively lands inside the captured frame. It was left unchanged rather than papered over, since neutralizing a host first-run prompt from inside the test harness is a separate decision
        - none of the five touches file-reference grammar, `ctx.cwd`, or `AGENT_CWD`; all are real-terminal capture surfaces
        - **a structural AC10 gap was found that no amount of re-running can close.** AC10 requires `just test-l2` on macOS, native Linux, WSL, and native Windows, but `.github/workflows/_package-ci.yml:573-576` records that "Windows and wsl2-ubuntu are absent by construction" for the L2 tier — `l2-environments` is derived from package policy crossed with the environment capability table, because tmux/PTY is the only backend headless CI can host, and the absence is deliberately recorded as a **policy gap in the rollup** rather than a green zero-run cell. This is an authorized, documented CI design decision that predates this feature, so AC10's Windows/WSL L2 requirement cannot be satisfied as written and needs a formal specification amendment rather than an implementation change
        - **why this finding is still deferred:** producing the final green matrix requires a CI run on the current tree, which requires a commit and push. This cycle has no authorization to commit or push (repo rule: never commit unless explicitly instructed), and the working tree additionally carries this cycle's uncommitted changes. Re-running the existing CI run would only re-test `7aa5945a5`, which predates the fixes. Closure therefore needs one authorized push plus a Ken-level ruling on the Windows/WSL L2 policy gap
- work completed for 'final cross-platform acceptance matrix (AC10)' at 23:35:02

### Successful Completion

The implementation of review cycle 2 has completed successfully in 1 hour 29 minutes 26 seconds. During this implementation all 3 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 2 were fixed, 1 was deferred (see reasons below):

- `final cross-platform acceptance matrix (AC10)` (finding 2, High) was deferred for two independent reasons, neither of which is an unverified claim of flakiness:
        - **the remaining evidence needs a CI run this cycle cannot authorize.** The two red legs review-2 named are already fixed in the tree by commit `ebc28e107`, whose diff touches exactly the two failing test files and whose fixes are causally matched to the two failure modes. Producing the green matrix that proves it requires committing this cycle's changes and pushing, and the repository rule is that commits happen only when explicitly instructed. Re-running CI run `34174352874` would only re-test `7aa5945a5`, which predates those fixes.
        - **part of AC10 is unsatisfiable as written and needs Ken, not code.** AC10 demands `just test-l2` on native Windows and WSL, but `.github/workflows/_package-ci.yml:573-576` documents that those L2 legs are "absent by construction" because tmux/PTY is the only backend headless CI can host, and records the absence as a policy gap rather than a false green. Closing this requires an authorized specification amendment, which is the review's own prescribed route ("resolve or formally exclude failures through an authorized specification change").
        - the position is nevertheless materially stronger than the review found it. Every AC10 leg this host can produce was run first-hand on the exact final tree: `just test` and `just lint` are green in all three package areas on macOS, Darkmatter's L2 tier is green (90/90), and the only reproducible Claudine L2 failure was traced to a host Atuin first-run prompt captured verbatim in the failing frame — a machine condition, not a repo defect. CI has separately proved L1 green on ubuntu, macOS, native Windows, and WSL2 for all three areas bar the one Windows test that `ebc28e107` fixes, with lint green everywhere.

No performance measurement was required by any finding in this review, so no performance test was deferred and the `deferred_perf_measurement` frontmatter flag remains unset.

The files changed for the two fixed findings are recorded in their progress entries above:

- finding 1 (High) — `claudine/cli/tests/spawn_inventory.rs` (rewritten: binding- and execution-aware flow analysis, bounded intra-crate glob resolution, tightened alias matching, eight new fixtures) and `claudine/docs/providers/spawn-seam-inventory.json` (exactly one added entry: the previously invisible `wiring/session.rs:55` provider seam, which classifies GOVERNED). No production source changed.
- finding 3 (Medium) — `darkmatter/docs/cli/index.md` (the `## File References` section replaced with the complete finalized catalog, candidate orders, modifiers, caveats, and a link to the canonical `biscuit-file` reference) and `darkmatter/docs/cli/compose.md` (one sibling drift: `@` was documented as "resolved from git root"). Documentation only.

Two items are flagged for Ken rather than decided unilaterally:

- the AC10 Windows/WSL Level-2 policy gap described above — an authorized specification amendment is the only route to closing it.
- the host Atuin first-run AI prompt that wedges WezTerm-backed L2 capture on this machine. It is a host condition, but it will keep producing a red macOS L2 leg locally until either the prompt is answered on the host or the L2 harness is hardened against interactive shell-startup output. That hardening is a testing-infrastructure decision outside this feature's scope.

## Implementation of Review Findings #3

> **started at:** 2026-09-08T04:54:01-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/claudine/features/2026-08-26-finalized-references/review-3.md'
- this is iteration 3 of the review-to-implement cycle
- review-3 carries two findings, both **High**:
        - the spawn inventory still fails open for valid construction and governance forms (`claudine/cli/tests/spawn_inventory.rs`) — three named counterexamples
        - AC10 remains incomplete and conflicts with the repository's Level 2 platform policy
- impacted package areas per the spec's `packages` frontmatter: `biscuit-file`, `darkmatter`, `claudine`; the review's findings both land in `claudine`, so test/lint gates are scoped there unless a change reaches wider
- starting the work on 'fail-open construction and governance forms in the spawn inventory' (finding 1, High) at 04:55:34
