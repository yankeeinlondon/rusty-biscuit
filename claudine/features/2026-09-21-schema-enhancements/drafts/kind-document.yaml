kind: schema
description: formalizes the use of the `kind` Frontmatter for documents in this monorepo
$schema:
    kind: |-
        enum(
            schema,
            schema-target,
            sequence,
            task,
            index,
            canonical-documentation;
            required
        ) -> a formal identifier of the _kind_ of document this document is
