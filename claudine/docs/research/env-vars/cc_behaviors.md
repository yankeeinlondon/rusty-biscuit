---
prompt: |-
    ## Task

	In Claude Code we have environment variables like:

    - ENABLE_TOOL_SEARCH (boolean)
    - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS (boolish)
    - CLAUDE_CODE_FILE_READ_MAX_OUTPUT_TOKENS (number)
    - CLAUDE_CODE_DISABLE_AUTO_MEMORY (boolean)
    - CLAUDE_CODE_DISABLE_BACKGROUND_TASKS (boolean)
    - CLAUDE_CODE_MAX_OUTPUT_TOKENS (number)

    All of these effect how the Claude Code Agent will solve problems and have an impact directly or indirectly on token use.

    - What other ENV variables should be grouped in with the set included above?
    - What "categories" of behavior can these ENV variables be grouped into?
    - For each of these ENV variables describe what explicitly the variable controls
    - If there is a CLI switch which has a similar effect mention that and be sure to point out which of the two has precedence when both are used
    - 

    **IMPORTANT:** do not guess, always use research to answer questions and if you can't find an answer then express that you couldn't find an answer, do not make things up!

    ## 

last_updated: 2026-04-07
---
