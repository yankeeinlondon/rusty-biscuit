# Abstracted Model Names

There are many cases where we might want to specify which model we want to use but when we do we are unintentionally making our prompt or document less portable across agents or across different users.

For this reason Claudine has setup a set of portable model names which can be used that will be replaced by a real model name at runtime.
