---
reviewed: false
implemented: false
---
# Claudine CLI ↔ Library Boundary Review

**Date:** 2026-08-12

**Scope:** business logic currently living in `claudine-cli` (`claudine/cli/src/`) that belongs in the `claudine` library (`claudine/lib/src/`). Reviewed by five parallel area reviews (wrap execution core; wrap orchestration & composition; compose/handle/context commands; admin & resource commands; CLI infrastructure), with the highest-impact claims spot-verified against source.

**Baseline:** `claudine-cli` carries ~70,200 production lines (~34,300 of them under `commands/wrap/`) plus ~33,700 in-src test lines; the library carries ~129,500 production lines. Only `claudine-cli` and `claudine-contract` consume the library today; rendezvous, Reaper, and Darkmatter are named future consumers.

## Executive assessment

The recurring structural pattern across all five areas is: **the library decides, the CLI applies.** The lib owns the decision functions, state types, and metadata catalogs (`decide_lifecycle_transition`, `LifecycleCatchProtocol`, `ActiveDocumentState`, `RunLedger`, `SessionCompatibilityKey`, `ProviderInfo`, `ContentDetector`, the MCP stores, the hook configurators) — but the *drivers* that sequence those decisions into behavior live in the CLI: the provider-attempt loop, the proxy-handoff chain, the launch rebuild, the schema-interactive flow, the hook sync plan, the completion candidate engine. A decision function without its driver is not reusable, which is why the boundary has already failed in practice:

1. **`claudine-contract` has forked the CLI.** The contract crate (which depends on `claudine` but not `claudine-cli`) hand-rolls its own provider profile mapping (`contract/src/profile.rs`), shadow-HOME isolation (`home.rs`), child-env assembly and argv construction (`session.rs`), with fresh `match Provider` arms at `session.rs:188/220/242/247/264` and `support.rs:59-79` — exactly the decentralized dispatch `cli/tests/dispatch_inventory.rs` exists to prevent, in a crate the guard does not scan.
2. **The forks have already diverged into live defects.** The most concrete: the CLI's Codex `ConfigKeyInline` system-prompt delivery escapes only `"` (`cli/src/commands/wrap/system_prompt.rs:282`), while the contract's `toml_basic_string` does full TOML basic-string escaping — a system prompt containing a backslash or newline produces malformed `-c key="…"` argv on the CLI path. The full defect inventory is below.
3. **Correctness invariants are maintained by copy-paste.** The `ComposeContext` construction idiom appears at nine CLI sites; the proxy-handoff chain drive at three (one divergent); the provider/model resolution ladder at five; the hook-registration drift rule at five (one wrong); git-root discovery at five across both crates with three different `.git` predicates.

The good news is that the migration tax is lower than it looks. The library already depends on `biscuit-terminal` and `darkmatter` and already hosts render components (`MetricsReport`, `EventRenderer`) — so "it renders" is not by itself a reason to stay CLI-side. The real boundary is **clap / shell protocol / TTY interaction / process edge vs. everything else**. The two recurring mechanical taxes are: `color_eyre::Report` returns must become typed `thiserror` variants (color-eyre is CLI-only), and functions that emit warnings mid-flight must return them instead.

## Live defects caused by the split

These were found incidentally while tracing the boundary; each is independently fixable and most are prerequisites for the moves (so migrations don't cement stale data). Items marked ✓ were re-verified against source during synthesis.

| # | Defect | Where |
|---|--------|-------|
| D1 ✓ | Codex inline system-prompt escaping handles only `"`; backslash/newline content produces malformed TOML argv. Contract's fork does it correctly — two implementations, one wrong. | `cli/src/commands/wrap/system_prompt.rs:282` vs `contract/src/session.rs` (`toml_basic_string`) |
| D2 ✓ | Config TUI save path: the trailing `if app.repo_dirty` block runs *after* the main save block, unconditionally re-saving the repo config with no `is_empty()` check — deleting-then-recreating a just-cleared repo override and printing two "saved" messages. | `cli/src/commands/config_tui/mod.rs:294-306` |
| D3 ✓ | `SKILL_PEER_DIRS` hardcodes 7 provider dotfile dirs; omits `.kilo`, `.pi`, and `.agents` (Antigravity), so three providers silently get no skill scopes. A second stale copy sits in the lib itself. | `cli/src/completion/scopes.rs:284-292`; `lib/src/composition/resolve.rs:484-486` |
| D4 ✓ | Telemetry `effective_cwd` match omits `Commands::Sequence` **and** the Kilo/Pi/Antigravity wrappers — the root span reports the wrong effective CWD for four commands. | `cli/src/telemetry.rs:71-88` |
| D5 ✓ | Operator-precedence bug in native-exit classification: `"no such file" \|\| "not found" && exit==127` — any stderr containing "no such file" classifies as `FileNotFound` regardless of exit code. | `cli/src/output/error_report.rs:404` |
| D6 | Proxy-chain divergence: the sequence copy passes the *original* file-resolution context into every hop, so hop 2 of a chain inside a sequence step resolves its target relative to the step document instead of hop 1's target. The compose copy re-points the context correctly. | `cli/src/commands/wrap/sequence/iterate.rs:674-678` vs `cli/src/commands/compose/prep.rs:357-358` |
| D7 | `claudine sync --dry-run` disagrees with its own wet run for Codex: the dry-run filters on `supports_event_via_hook` alone, ignoring `registerable_events()` (Codex registers only `TurnComplete`), so it reports `WouldAdd` for events that can never register. | `cli/src/commands/sync.rs:183-203` vs `lib/src/config/codex.rs:176-179` |
| D8 | Lib tests for TTS voice selection are hand-copied replicas of the TUI's match arms and have gone stale: the lib test asserts `Single → Single` while the shipping reducer promotes `Single → Gendered`. The test passes because it asserts against its own inline copy — vacuous, and documenting behavior the product no longer has. | `lib/src/config/tts.rs:168-230` vs `cli/src/commands/config_tui/reducers.rs:36-68` |
| D9 | The whole `commands/init/` directory (1,267 lines) compiles only under `#[cfg(test)]`; the shipping wizard (`init_wizard.rs`) implements *different* defaults. Three divergent "fresh install" policies exist, including a hardcoded sound set contradicting the lib's `INIT_RECOMMENDED_EVENTS`, and a `CanonicalProviderSettings` computation whose result is discarded. | `cli/src/commands/mod.rs:14-15`, `cli/src/commands/init/mod.rs:139-196,459-475`, `cli/src/commands/init_wizard.rs:276-280` |
| D10 | `to_agentic` in the live sink is a hand-maintained duplicate of the lib's semantic→hook event table and **overwrites** the lib's answer; the two already diverge for `OutputText`/`Reasoning`, with no coverage test enforcing agreement. | `cli/src/commands/wrap/live_semantic_sink/mod.rs:684-733` vs `lib/src/stream/reporting.rs:314-415` |
| D11 | Git-root discovery is implemented five times across the two crates with three different `.git` existence predicates; only one handles a worktree's `.git` *file*. Repo-root resolution differs by code path inside a linked worktree. | `cli/src/telemetry.rs:178-187`, `cli/src/completion/scopes.rs:269-277`, `cli/src/completion/engine/mod.rs:563-572`, `lib/src/invocation_context.rs:1077`, `lib/src/composition/prepare.rs:277` |
| D12 | The completion engine probes both `config.json` and `config.json5`; the lib's `user_config_path()` probes only `.json`, and `main.rs` uses the lib probe to decide whether to run the init wizard — a user with a `config.json5` gets the wizard on every invocation while completion correctly reports them configured. | `cli/src/completion/engine/mod.rs:541-556` vs `lib/src/dispatch/loader.rs` |
| D13 | Uninstall iterates a different provider set than install (`detect_agents()` filters on `config_exists`; install uses `is_available()` = config **or** on-PATH), so a provider registered from PATH whose config later vanished is never deregistered. Also deletes the user config with bare `remove_file`, bypassing the lib's backup/atomic machinery. | `cli/src/commands/uninstall.rs:18-45` |
| D14 | The schema-validation error downgrade reconstructs the lib's error message format by hand (`format!("{pointer}: {reason}")`) — a cross-crate duplicated format contract with a byte-identical-output requirement, acknowledged in its own doc comment. | `cli/src/commands/schema_interactive/mod.rs:222-239` |
| D15 | `LoggingProfile` is fully prompted for during init but never written into the config — three prompts whose answers are discarded. | `cli/src/commands/init/prompts.rs:135-158` vs `cli/src/commands/init/mod.rs:559-573` |

## Dispatch-guard blind spots

`cli/tests/dispatch_inventory.rs` scans `lib/src` and `cli/src` for `match`/`matches!`/`==`/tuple-array dispatch over `Provider::<Variant>`. Three structural evasions surfaced:

1. **String-keyed dispatch.** `repo_home.rs:149-167` matches on `agent_offset` strings (`".claude"`, `".codex"`, …); the live sink reads provider wire vocabulary out of untyped maps (`extra["task_id"]`, `extra["subagent_type"]`); `SKILL_PEER_DIRS` and telemetry's `provider_subcommand_name` (keyed on the clap enum) encode per-provider facts as bare string tables. All invisible to the scanner.
2. **`claudine-contract` is not scanned at all.** Extending the scan to `contract/src/` would make the `WrapperProfile` gap (P1) fail the build today — worth doing regardless of migration scheduling.
3. Consider extending the scanner to flag `&[&str]` constants whose elements superset-match known `agent_offset`/`slug`/`binary` values.

## Findings

Findings are grouped into six workstreams. Effort S/M/L · Risk low/med/high · Value low/med/high. "Home" is the proposed library module.

### P — Provider behavior layer (the keystone)

**P1. `WrapperProfile` — the entire provider argv/env behavior table is CLI-private.**
`cli/src/commands/wrap/profile/` (~5,900 lines with tests): trait with ~30 methods (`binary`, `supports_resume`, `apply_yolo_for_mode`, `apply_entrypoint`, `apply_model`, `apply_output_format`, `apply_system_prompt`, `apply_sandbox`, `prompt_delivery`, `stdout_noise_prefixes`, `parse_captured_output`, `allowed_env_keys`, …) plus ten per-provider impls and the `PROVIDER_COUNT`-indexed registry. Nearly every default body already reads `claudine::provider::provider_info(...)` — the *metadata* is in the lib; only the *application* is stranded. This inversion is what forced `claudine-contract` to fork (see Executive assessment). The CLI's `profile/*.rs` files are already blanket-exempt in the dispatch inventory, so moving them changes no guard verdict. `OutputFormat`, `PromptDelivery`, `PromptSource`, `YoloOutcome`, and `SystemPromptApplication` (temp-file RAII, not terminal-coupled) move with it; the `<blue>` Prose markup inside `reject_direct_yolo`'s error message becomes a typed error the CLI renders.
Home: `claudine::provider::profile` (beside the existing `behavior.rs`), plus `claudine::composition::launch::assemble_argv(...) -> AssembledArgv { args, env_overlay, artifacts, warnings }`.
**L / high / high — prerequisite for L1, L4, and the contract crate's de-forking.**

**P2. System-prompt delivery interpreter.** `cli/src/commands/wrap/system_prompt.rs:206-318`: the only code that turns `SystemPromptSpec` + mode + interactivity into argv/env/tempfiles — mode×interactivity lookup, Gemini `GEMINI.md` append-merge, Codex 64 KiB argv cap, TOML escaping (defect D1). Lib owns the spec types (`lib/src/provider/system_prompt.rs`) and content preparation (`lib/src/system_prompt/prepare.rs`); the interpreter is stranded between them. Independently shippable ahead of P1, and fixes D1. `ResolvedSystemPrompt::describe` (`system_prompt.rs:60-137`, pure enum→vocabulary projection) ships with it.
Home: `claudine::system_prompt::delivery::apply_spec(...) -> DeliveryPlan { args, env, artifacts, warnings }`.
**M / low / high.**

**P3. Prompt extraction from provider argv + OpenCode model precedence.** `cli/src/commands/wrap/profile/resolve.rs:80-328`: (a) `extract_prompt_source_from_passthrough` — self-described as "the single place in the codebase that knows how to locate a prompt inside provider passthrough arguments" — pure argv parsing over the lib-owned `PromptArgConventions`/`COMMON_VALUE_TAKING_FLAGS` tables (contract already improvises its own version from `prompt_flags.first()`); (b) `resolve_opencode_model` — the CLI > `OPENCODE_MODEL` > `~/.config/opencode/config.json` precedence chain. `OpenCodeModelSource::status_markup` stays CLI; the enum and precedence move.
Home: (a) `claudine::provider::prompt_args`; (b) `claudine::opencode_config` or `claudine::config`.
**S / low / high.**

**P4. Resume-forwarded-flag table and resume support gate.** `cli/src/commands/wrap/resume.rs:5-64`: a hardcoded table of which flags survive into a resumed launch (really OpenCode structured-stream contract preservation) and a resume-support precondition returning an untyped eyre error.
Home: `ProviderInfo.resume_forwarded_flags` field; typed `HarnessError` variant.
**S / low / med.**

**P5. `has_explicit_native_output_request`.** `cli/src/commands/wrap/flags.rs:204-223`: walks the provider's `output_formats` catalog to decide whether user argv already requested a native format (suppressing structured streaming). Pure catalog interrogation living next to a clap struct.
Home: `claudine::provider` (output-format module).
**S / low / med.**

**P6. Provider display names.** `cli/src/output/mod.rs:715-722` `capitalize_provider` hand-title-cases `Display` output at six call sites (all user-facing error text) when `ProviderInfo.display_name` is the catalog authority. Delete and call the catalog.
**S / low / med.**

### L — Lifecycle & execution engine

**L1. The provider-attempt lifecycle loop.** `cli/src/commands/wrap/harness_orch/loop_control.rs` (2,163 lines + 6 submodules; ~8,400 test lines in `harness_orch`): the three-phase attempt cycle — prepare (fresh read → staged boot → plan parse → `start`) / execute / classify — driving retry, resume, proxy surfacing, budget accounting, and terminal-event ordering. Every *decision* is already a lib call; the CLI holds the sequencing contract (which events fire in which order, which failures route `blocked` vs `failure`, that `proxy` skips the source's `finalize`). Roughly a dozen render/log calls in an otherwise pure orchestration.
Home: `claudine::harness::loop_engine::run_attempt_loop(ctx, runner: &mut dyn AttemptRunner)` — the runner trait (process spawn + rendering) is the one CLI seam, mirroring how the lib's sequence task module already declares "the library never owns provider launch."
**L / high / high — the umbrella; land L2–L6 first.**

**L2. Lifecycle transition dispatch — written three times against one lib decision function.** `harness_orch/loop_control/control_dispatch.rs:49-254`, `composition/pipeline.rs:1339-1530`, `composition/preflight.rs:126-288`: three exhaustive match tables turning `LifecycleTransitionDecision` + `StackControl` into a loop action — each re-encoding budget consumption, hop/cycle accounting, the same four typed refusals, and provenance stamping. `pipeline.rs:1521` asserts a *lib* invariant with a CLI-side `unreachable!`.
Home: `claudine::composition::lifecycle::runtime::apply_transition(...) -> TransitionAction`, next to `decide_lifecycle_transition`.
**M / med / high — highest ROI in the lifecycle workstream.**

**L3. Active-document read model.** `harness_orch/types.rs:20-175`, `harness_orch/prompt.rs:99-326`: `HarnessPromptState`/`MaterializedHarnessPrompt`/`materialize_harness_prompt` — the document-refresh contract (disk re-read per re-entry, pre-schema `proxy.with` overlay merged into authored frontmatter, `mcp_body_tags` captured from the composed body before resume substitution). Zero terminal references; one CLI type.
Home: `claudine::composition::active_document`. **M / low / high — L1, L4, L5, C1 all need this type lib-visible.**

**L4. Per-attempt launch rebuild (retry/resume identity engine).** `harness_orch/loop_control/target_launch.rs:304-655` + env-baseline half in `composition/pipeline.rs:520-552,1070-1152`, plus the sibling pure state machine `wrap/launch_plan.rs:109-953` (launch-plan re-derivation: `EnvChange` patches, credential re-admission across provider moves, baseline restores so a dropped `model:` unsets `MODEL`, the `OPENCODE_CONFIG_CONTENT` fold order, non-interactive MCP ambiguity resolution from recorded state). Together these are the deterministic function `(refreshed document, immutable invocation intent) → launch identity` that makes retry/resume coherent. ~2,270 test lines migrate.
Home: `claudine::composition::launch` (`rebuild`, `launch_plan`). Depends on P1.
**L / med / high.**

**L5. Session-compatibility key projection.** `harness_orch/session_key.rs:90-214`: the rule for *what makes two provider sessions compatible* — which argv flags carry system-prompt content vs paths, that file-backed flags hash file *bytes* not temp paths, that MCP identity is a sorted signal set, `resume_protocol`. The lib defines the struct and comparator (`lib/src/composition/coordinator/active.rs`); the CLI defines what goes in it. A resume implemented elsewhere silently compares a differently-derived key — the exact failure the type exists to prevent. 325 test lines migrate verbatim.
Home: `SessionCompatibilityKey::from_launch(...)` in the lib. **S / low / high — smallest high-value move in `harness_orch`.**

**L6. Proxy-handoff chain drive — three copies, one divergent (D6).** `sequence/iterate.rs:643-720`, `compose/prep.rs:319-372`, partial third at `loop_control.rs:996-1053`: commit `SurfacedHandoff` against the ledger → resolve target → merge `with:` overlay at documented precedence → adopt-without-recommitting → repeat. Lib owns every primitive (`commit_proxy_in_context`, `ProxyHandoff`, `RunLedger`); nobody owns the loop. `merge_frontmatter_overlay` (`wrap/overlay.rs:17-29`, shallow-with-null-removal semantics with a documented why-not-deep-merge rationale) moves with it.
Home: `claudine::composition::coordinator::chain::drive_handoff_chain(...)`. **M / med / high — fixes D6 and deletes two copies.**

**L7. Termination semantics.** `wrap/exec/termination/{reasons,summary,message,coordinator}.rs` (~720 lines): watchdog/detector inputs → `EarlyTermination` → `ProcessTermination` mapping — including the load-bearing rule that content-guard trips become `Aborted`, never `TimedOut` (which would route through the retryable-timeout lifecycle and reproduce the runaway) — plus the `StreamExecutionSummary` projection and the host-independent interrupt-ladder coordinator. Every type touched is already a lib type; verified no `biscuit_terminal`/`crate::log` imports. Platform loops (`unix.rs`, `windows.rs`, `handle.rs` — signal handlers, Job Objects, process-global registries) stay CLI and consume the moved policy.
Home: `claudine::harness::termination`. **M / low / high.**

**L8. Timeout resolution, evaluation, and the conflict rule.** Three stranded layers over lib-owned `parse_timeout`: (a) `composition/timeouts.rs:31-229` — the CLI > frontmatter > env > built-in precedence ladder, the "`0s` disables" convention (zero-literal recognizer duplicated at `harness_orch/launch.rs:132-142`), built-in defaults, and the swallow-parse-errors-so-syntax-diagnostics-win extractor; (b) `exec/timeouts.rs` + `exec/watchdog/evaluate.rs` — `TimeoutConfig::resolve` env handling and the explicitly pure `evaluate_timeout_tick` (wall-clock precedence, stuck-aware suppression, byte-heartbeat backstop, both OpenCode grace windows); (c) the resolved-interactive × explicitly-sourced-timeout conflict rule (`pipeline.rs:994-1031`). ~1,400 test lines migrate.
Home: `claudine::harness::timeouts` (resolution + conflict) and `claudine::harness::timeout` (tick evaluation). **S–M / low / high — (a)+(c) is the cheapest high-confidence win in the lifecycle workstream.**

**L9. Subagent liveness state model.** `wrap/exec/subagent_watchdog.rs` (555 lines): `WatchdogState` — active-subagent tracking, bounded completion ring, `outstanding_at_breach`. Its own doc says it is "intentionally decoupled … so it can be unit-tested in isolation"; the decoupling is done, it's just in the wrong crate. Already converts into lib `StuckSubagentInfo`. Leads the L7/L8 wave.
Home: `claudine::stream::progress::subagents`. **S / low / med.**

**L10. Kimi `--wire` JSON-RPC protocol (speaking half).** `wrap/exec/wiring/` (~1,050 production + 916 test lines): request builders, `WireClientCapabilities`, protocol version, request classification, the Kimi-hook-name → `AgenticEvent` table, the dispatch bridge, allow/deny/ask negotiation, terminal-line predicates. The lib already owns the *parsing* half (`lib/src/stream/protocol/kimi.rs`). Split at the pure/impure line: builders/dispatch/predicates move; `WireWriter` + session spawn stay for now.
Home: `claudine::stream::protocol::kimi::wire`. **M / low / high — the largest single block of provider-specific behavior unreachable by contract/rendezvous.**

**L11. Stream plumbing with three-way provider dispatch.** `wrap/policy.rs:95-284`: `build_structured_plumbing` is a 150-line OpenCode/Codex/else branch wiring the semantic sink, stderr log bridges, and detector trip channel — three of the CLI's dispatch-guard allowlist entries point here. Genericize over a sink trait and turn the branch into a `ProviderInfo` descriptor (`stderr_bridge: StderrBridgeKind`), retiring guard entries rather than grandfathering them.
Home: `claudine::stream::plumbing`. **M / med / med-high.**

**L12. Runaway guard: config resolution and driving policy.** Two halves: (a) `wrap/runaway_guard.rs:42-368` — three-layer (user/repo/frontmatter) fail-closed config merge, two-phase scope filtering, detector compilation; written *entirely* in lib vocabulary, no CLI type anywhere in the file — a `git mv` plus visibility changes; (b) the driving rules in `live_semantic_sink` (`event_sink.rs:135-206`, `mod.rs:494-641`) — feed only `OutputText`/`Reasoning`, reset on `TurnComplete`, trip-is-terminal, suppress-after-trip, and `SessionStart` model re-scoping. A headless consumer gets these guards **fail-open** today — the worst failure direction. `trip_to_early_termination` (a CLI converter between two lib types) moves too.
Home: (a) `claudine::runaway::resolve`; (b) `claudine::runaway::stream::GuardedEventStream::observe(...) -> GuardOutcome`. **(a) S / low / high · (b) M–L / med / high.**

**L13. Semantic→hook event mapping duplicate (D10).** Replace the sink's `to_agentic` with lib `hook_event_for_semantic(&SemanticEvent) -> Option<AgenticEvent>` (the hook-dispatchability of `OutputText`/`Reasoning` is pipeline policy, not rendering), called by `destructure_semantic`, with a strum `VariantNames` coverage test. **S / low / high — cheapest clear-cut move in the lifecycle workstream.**

**L14. Section tracking, inline closure, and small stream moves.**
- `Section`/`SectionTracker`/`section_for` (`wrap/section.rs:20-74`, sink `sections.rs`): the lib's `RenderUnit.class` exists *solely* to feed this CLI type, and `event_renderer/mod.rs:289` enforces a lib invariant about it in prose. → `claudine::render::section`. **M / low / high.**
- Inline-closure finalization (`wrap/inline.rs:99-191`): extract → re-read/compare → stamp `today` → apply → classify new/reverted/cleaned; wall-clock injected as a domain input, rendering woven through. → `claudine::composition::closure::finalize_inline_closure(..., today) -> InlineClosureReport`; CLI keeps the renderer. **M / low / high.**
- Final-response accumulator (`event_sink.rs:45-66`, `policy.rs:67-84`): the rule that a `ToolCall` discards accumulated text (narration ≠ closing answer) — determines what `inline-compose` writes into documents. → `claudine::stream::summary`. **S / low / med.**
- Subagent-id extraction from untyped `extra` maps (`event_sink.rs:235-274`) — provider wire knowledge invisible to the dispatch guard; normalize upstream in `lib/src/stream/providers/opencode.rs`. **S / low / med-high.**
- `strip_prompt_tags_for_provider` (`inline.rs:8-32`, hardcodes which providers need `#tag` stripping) and `was_interrupted` (POSIX 130/143) → provider capability + `ProcessTermination` method. **S / low / med.**

### E — Environment & workspace

**E1. Credential admission policy and secret redaction.** `wrap/env/sanitize.rs:12-213`: `is_sensitive_key` (13-clause heuristic), `admits_sensitive_key`, `sanitize_process_env`, `redact_sensitive_args` (`sk-`/`ghp_`/`xox[bp]-`/`AKIA` shapes). This is a *security* policy with a **drifting narrower duplicate in the lib** (`lib/src/protect/scrub.rs:76-82` misses `PASSWORD`, `PRIVATE_KEY`, `CREDENTIAL`, `ACCESS_KEY`, `PASSPHRASE`, `_AUTH`, `_PAT`, `_PWD`, `_PEM`) and a third unrelated allow-list in `contract/src/support.rs`. One canonical home, consulted by `protect::scrub`.
Home: `claudine::protect::env` (or `claudine::env::sanitize`). **S / low / high.**

**E2. Shadow-HOME materialization.** `wrap/repo_home.rs` (+490 test lines): `--repo` isolation — symlink/hardlink fan-out with per-provider exclusions, volatile-SQLite purge (the split-WAL corruption hazard), Codex repo-scoped prompt materialization, `CODEX_SQLITE_HOME` resolution. The `excluded_dirs` match on `agent_offset` strings is guard-invisible provider dispatch — make it a `ProviderInfo` field (`repo_home_excluded_dirs`), removing the site entirely. Contract's `home.rs` is a weaker from-scratch reimplementation.
Home: `claudine::provider::repo_home`. **M / low / high.**

**E3. Launch-workspace and package-context resolution.** `wrap/env/package_context.rs:22-273`: repo root, child cwd, and monorepo `PackageContext` resolution — including the past-bug rule that `repo_root` may follow a document hint while `child_cwd` always follows the launch directory. The *output* types already live in the lib (`LaunchWorkspaceContext`, `PackageContext` re-exported from `claudine::composition`); only the producers are stranded — the textbook incomplete migration.
Home: `claudine::composition` beside its types. **S / low / med-high.**

**E4. Child-environment assembly.** `wrap/env/mod.rs:45-408` (+801 test lines): `build_child_env_with_launch` — the contract defining what a Claudine-wrapped session *is* (`AGENT`, `YOLO`, `AGENT_PARAMS`, `CLAUDINE_SESSION_ID`, `CLAUDINE_PID`, `PACKAGE`/`PACKAGE_AREA`, `PWD`, the `OPENCODE_CONFIG_CONTENT` merge); `interactivity_env` ("the only place that function lives"); `detect_wrap_startup`. Contract re-derives a thinner version. One real CLI leak: `EnvPlan.perf_substages` → replace with a neutral timing tree. Last in the E-chain (depends on P1, E1, E2, E3).
Home: `claudine::env`. **L / med / high.**

**E5. Scoped tmp dir + `.gitignore` maintenance.** `wrap/system_prompt.rs:139-204` encodes the trust-boundary rule (repo root → `.claudine/tmp/`, bare CWD → `.claudine-tmp/`) *and* the matching `.gitignore` append — the doc records a past bug from computing the two independently. Meanwhile `init/mod.rs:334-371` has a second, disagreeing `.gitignore` mutator (creates vs bails when absent; matches both spellings vs one; propagates vs swallows errors). One `ensure_gitignore_entry` beside `config/atomic.rs`; tmp-dir rule to `claudine::composition::launch_workspace`. **S / low / med-high.**

**E6. Interrupt state duplication.** `cli/src/output/mod.rs:603-679`: a CLI `USER_INTERRUPTED` atomic exactly duplicating `lib/src/interrupt.rs`; `mark` writes both, `observe` reads only the CLI's — a lib-side interrupt source would be invisible to the CLI's gate. `WAIT_LOOP_DEPTH`/`WaitLoopActiveGuard` encodes the real domain rule ("while a child's SIGINT ladder owns the signal, the outer guard must not force-exit"). Delete the duplicate; move the guard into `lib/src/interrupt.rs`. **S / low / med.**

### C — Composition & sequence commands

**C1. The active-document coordinator driver.** `compose/prep.rs:122-374,393-648,1078-1314`: the lib's coordinator module owns the nouns (`ActiveDocumentState`, `RunLedger`, `DocumentTransition`, …) and explicitly delegates effects to "the CLI driver" — but the verb (commit → re-resolve → overlay-merge → re-derive context → replace overlay → rerun loop-vs-single) exists only as two thin CLI loops (compose + sequence-step) with no lib referee. `CompositionPrepContext`'s ten fields are all lib types — a lib-shaped struct that happens to live in the CLI. Merge with L6; add a `DocumentRunner` trait for the effectful part. `SingleCompositionOutcome` (the "one provider attempt finished" shape — precisely what `claudine-contract` produces) moves to the lib as part of this, unblocking the `LoopIterationOutput` translation (`compose/loop_run.rs:73-123`).
Home: `claudine::composition::coordinator::drive`. **L / med / high.**

**C2. `ComposeContext` construction — one invariant, nine hand-rolled sites.** `prep.rs:545-581,1132-1148`, `wrap/overlay.rs:59`, `harness_orch/prompt.rs:173`, `wrap/composition/pipeline.rs:1310`, `wrap/sequence/{mod.rs:93,253, jit.rs:274, task_run.rs:210}`: the four-step requirements→evidence→capture→env-overrides idiom, whose comment declares it must match the preflight snapshot "so the commands the audit discovered are the commands this compose expands." The lib already has a *private* version (`lib/src/system_prompt/prepare.rs:193`). Publish one `compose_context::for_document(...)`; collapse the private helper onto it. Every input/output is a lib or Darkmatter type — the cleanest extraction in the review. **S / low / high.**

**C3. Schema-verdict ownership rule.** `prep.rs:67-73,215,449-453,1173-1174`: `defers_schema_verdict_to_initialize` — who owns a document's schema verdict (the immediate read vs the stabilized reread after `initialize`), keyed deliberately on authored frontmatter, consulted at four points; hard-coding it wrong once already caused a real bug (documented at `prep.rs:1169-1172`). `SchemaStage` is a lib type; the predicate that selects it is not.
Home: `SchemaStage::for_document(source, entry_reason)`, folding in the proxied-target-always-defers clause. **S / low / high.**

**C4. Schema-interactive resolution flow.** `schema_interactive/mod.rs`: the lib owns the primitives (`pre_validate_schema`, `MissingProperty`, `InteractiveShape`); the CLI owns the policy sequencing them — which error classes are interactively recoverable, validate-once-retry-once, unsupported-shape short-circuit ordering, override merge precedence, empty-collection-means-original-error. Outright misplaced: `parse_number` (int/float coercion with min/max bounds — belongs beside `InteractiveShape::Number`, which declares those bounds) and `provided_partial_candidates` (glob + case-insensitive filtering). Also fixes D14 (`into_schema_validation` round-trip becomes one type's responsibility). None of this flow is unit-testable without a TTY today; 356 test lines migrate.
Home: `claudine::composition::schema::interactive::resolve_schema_interactively(..., &mut dyn MissingValueSource)` — CLI implements the source with biscuit-tui, contract as never-supply, tests as a scripted map. **M / med / high.**

**C5. Provider/model resolution ladder — five copies.** `composition/target.rs:85-577`, `sequence/mod.rs:300-521` + `sequence/resolve.rs:17-110`, consumed a fifth time from `target_launch.rs:495-524`: explicit flag → frontmatter hint → snapshot classification → typed error, with dry-run branch, TTY gate, catalog-refresh gating. The lib already exposes `resolve_target_non_tty_with_hints`; every CLI copy's doc comment says "mirrors the direct compose path" — prose standing in for a shared function.
Home: fold into `claudine::composition::select::decide_target(...) -> TargetVerdict { Resolved | NeedsPicker | Abort }`; `is_terminal()` becomes an `OperatorPresence` parameter. The inquire picker and OSC8 markup stay CLI. **M / med / high.**

**C6. Sequence step driver + `compose_step`.**
- `sequence/iterate.rs:52-268`: poll interrupt → run → classify → accumulate → decide continuation — duplicating the lib's own serial group scheduler one level down, with `"interrupted by SIGINT"` as a **string-equality sentinel** whose lib doc calls it "a contract, not prose" while producer and comparator both live in the CLI. → `claudine::composition::sequence::driver` with `SequenceStepResult::interrupted()`. **M / med / high.**
- `sequence/jit.rs:87-313`: `compose_step` — the six-stage "validated == executed" pipeline (pre-validate → preflight options → template shell approvals → prepare inline-vs-direct → lifecycle approvals → union forward), plus the sequence env contract and `NAME_COERCION_KEYS` application. The compose-options recipe is duplicated at `sequence/mod.rs:98-106` and `compose/prep.rs:566`. → `claudine::composition::sequence::step`. **M / low / high.**
- `sequence/task_run.rs:199-299`: the `PreflightTask` expression scan — a lib type's exhaustiveness invariant (no-`..` destructure so a new field is a compile error) enforced from a downstream crate; under-capturing is a documented correctness bug. Zero coupling; cleanest move in the workstream. → `impl PreflightTask { fn expression_scan() }`. **S / low / high.**
- Phase-1c aggregation (`sequence/phase1c.rs`) and the sequence-review unification policy (`selection_ui.rs:91-322`, incl. the drafts[0]-vs-all-drafts lock inconsistency and `"__default__"` sentinel strings) → lib with typed `ModelChoice::Default`. **M / low-med / med-high.**

**C7. Shell-approval policy roots and the freeze/thaw rule.** `harness_orch/shell_options.rs`: which directory's shell policy governs a command (ancestor `.git` walk; observed non-repo sources never search ancestors), `--yolo` auto-approve, `--dry-run` CI-gate message swap, and the freeze/thaw rule (only a *document change* thaws approvals — retry/resume re-read the same document where the freeze is the point). Any non-CLI executor must reproduce this byte-for-byte or reach a different approval verdict. `YoloApprovalHandler` has zero CLI deps.
Home: `claudine::harness::policy` with `ApprovalMode = Interactive(handler) | AutoApprove | DenyOnly`. **S / low / high.**

**C8. MCP session policy.** `composition/pipeline.rs:564-698` + `wrap/wrapper_mcp.rs:34-248`: when ambiguity may prompt, resolutions recorded so a retry never re-prompts, missing-tag fatality under `--strict`, the retry-proofing rule that the shadow HOME materializes even when *this* provider's injector doesn't need one, no-injector hard error with remediation; plus `merge_injected_env_into_plan` — the `OPENCODE_CONFIG_CONTENT` merge-not-overwrite rule whose doc says it exists "so the merge semantics stay in one place" (that place being the CLI, with variations re-implemented at `launch_plan.rs:890-950` and `env/mod.rs:268-283`).
Home: `claudine::mcp::session::plan_mcp_session(inputs, &mut dyn AmbiguityResolver)` — lib ships a `NonInteractive` resolver; CLI supplies the `inquire::Select` one. **M / med / high.**

**C9. Sequence admissibility and source loading.** `commands/sequence.rs:61-141`: `reject_sequence_interactive` (returns a lib error variant, delegates parsing to a lib fn — lib code typed in the CLI, called back-to-back with lib `reject_non_sequence_kind`) and `resolve_sequence_source` (four lib calls of glue encoding the real contract that YAML and Markdown refs resolve through the same prompt magic roots). Also `expand_shell_source` (`:167-196`): the sequence shell-source runner has no lib default although its lifecycle twin (`SystemShellRunner`) ships one.
Home: `claudine::composition` beside their siblings. **S / low / med-high.**

**C10. Compose small pieces.** Bundle: inline-compose contract validation with its load-bearing ordering comment (`compose/mod.rs:441-521` → `validate_inline_contract`, reporting hoisted); positional-setter pieces (`compose/setters.rs`: `merge_set_overrides` precedence, JSON5-then-string coercion, and `json_type_name` — which exists solely to populate a *lib* error's payload); `build_loop_options` env/flag precedence (`prep.rs:734-746` → `LoopExecutionOptions::from_env_with_overrides`); dry-run facts assembly (`composition/dry_run.rs:75-150` — reclassification + redaction, the "dry run predicts the live path" invariant untestable outside the CLI today). **S each / low / med.**

**C11. `handle.rs` hook-event domain rules.** `commands/handle.rs`: the event → session-status contribution table with documented precedence (so a weak `idle` never clobbers `waiting_on_user`); the interactivity gate ("a non-interactive turn-complete is the agent auto-proceeding, not waiting on a human"); the provider resolution ladder (hint → `AGENT` env → `detect_from_payload`, only the last rung in the lib); and the 15s deadline / exit-124 policy (hosts kill hook handlers at ~30s — a fact about the agent platform, not this CLI). Return a lib-owned status enum; keep the ~6-line rendezvous proto mapping CLI-side to avoid inverting the dependency layering.
Home: `claudine::hook_adapters` / `claudine::dispatch`. **S–M / low / med.**

**C12. Diagnostic-code catalog projection.** `commands/errors.rs`: `Severity::as_str()` (the asymmetry: `Category`/`Disposition`/`Origin` already have one), `code_catalog_grouped()`. The CLI keeps `json_view` if the minimal-derives stance holds. **S / low / low-med.**

### A — Admin & resource commands

**A1. Hook registration drift/plan — five copies, one wrong (D7).** The core domain question ("which events *should* provider X have, and how does that differ from reality") is answered at `hooks/list.rs:23-64`, `sync.rs:183-203`, `providers.rs:123-128`, `init_wizard.rs:482-487`, `init/mod.rs:533-538` — only the first honors `registerable_events()`. Worse: the lib already computes the diff (`is_in_sync` exists on five concrete configurators) but as inherent methods unreachable through `Box<dyn AgentConfigurator>`, and `register()` computes-then-discards the diff, forcing `sync.rs` to reverse-engineer outcomes with three `registered_events()` round-trips.
Home: `ProviderHookPlan::for_provider(...)` as the single expected-events rule; `AgentConfigurator::diff(...) -> HookSyncDiff` promoted onto the trait; `register()` returns the diff. **M / low / high.**

**A2. Install plan (fixes D9, D15).** Three divergent fresh-install policies (dead `init/`, shipping wizard, lib defaults). Home: `claudine::config::init_plan::InstallPlan::compute(agents, answers)` + `::quick()` + `::apply()`; both wizards reduce to prompts → `InitAnswers` → plan; the dead `init/` directory is then deleted outright — the largest net line reduction available in this review. Behavior must be *reconciled*, not merely moved (pick one sound policy deliberately). **M / med / high.**

**A3. Mesh dashboard domain model + session-status wire contract.** `dashboard/model.rs` (384 lines, imports only `serde_json` + std; 604 test lines migrate): `MeshSnapshot::fold`, staleness bucketing, the stale-replica trust rule. The sharper problem: the session-register schema is an **untyped string contract between two CLI files with a daemon in between** — `wrap/session_report.rs` writes `"waiting_on_user"`/`permission_signal`, `dashboard/model.rs:376-378` string-matches them, `report.rs:378-399` decodes `status_basis`/`status_producer` vocabularies as bare literals. Nothing types it; nothing tests producer/consumer agreement; rendezvous stores it in between. The `session_report.rs` reducer (four-producer precedence, unix-ns LWW revisions) is the same workstream — if adding `rendezvous-core` to the lib is unacceptable, move the enums and reducer as provider-neutral types and keep proto conversion CLI-side.
Home: new `claudine::session_register` (types + fold) — RPC plumbing stays CLI. **M / low-med / high.**

**A4. MCP referential integrity.** The remove cascade (defaults cleanup) exists only in `cli/mcp/remove.rs:44-90`; lib `remove_server` doesn't do what its own comment claims, while lib `validate_defaults` exists specifically to *report* the corruption a non-CLI caller would create. Also: `collect_provider_presence` hand-walks store internals including the repo-key stringification convention (`mcp/list.rs:142-183`), and `auth_summary` probes provider-specific JSON keys in a CLI helper (`mcp/mod.rs:260-275`).
Home: `remove_server_cascading(...) -> RemovalReport`; `McpProviderStateStore::presence_for(...)`; `McpServer::auth_mode()`. **S / low / high.**

**A5. `signals check` verification engine.** `commands/signals.rs:166-778`: doc↔table joins, positive replay, negative-overlap with exclusions ledger, version-scoped engine selection, corpus loading. The CLI's own tests call `check_provider` directly — tests wanting a library function and reaching through a command module to get one. Constraint: `claudine-gen` deliberately does not depend on the lib (bootstrap boundary), so its mirrored helpers stay duplicated by design.
Home: `claudine::signals::verify`. **M / low / med-high.**

**A6. Config TUI stranded logic (fixes D2, D8).** Bundle: `HookAction` field reflection/parsing incl. clamps that exist nowhere else and an untested mapper mini-grammar (`tabs/actions/fields.rs` → `HookAction::editable_fields()/set_field()` with real `FromStr`/`Display` on `Mapper`); voice-selection reducer (`reducers.rs:36-68` → `impl VoiceSelection`, deleting the vacuous lib test copies); save-path fix + `validate()` on save (delete `mod.rs:294-306`; `save_claudine_config` validates); host voice enumeration (shelling `say -v ?`/`espeak --voices` + an 11-entry Kokoro list) belongs in `biscuit-speaks`, not claudine at all; the repo-override merge rule re-derived in `effective_action_entries` (make lib `merge.rs` logic `pub`); `ProtectRuleToggles::set/toggle` (the get exists, the set is a 13-arm CLI match, duplicated twice). **S–M each / low / high in aggregate.**

**A7. Messenger route model — three-to-four copies.** Lib's `bridge_provider_config` (the total conversion) is **private**, so `test_connection.rs` hand-writes a partial copy silently returning `None` for bot routes; default env-var names exist in five places; the empty-string→`None` normalization producer and its validator live in different crates; the positional `fields.get(1)` contract is enforced only by index agreement across three functions.
Home: publish `MessengerProviderConfig::to_route()` + a `descriptor()` (labels, default envs, secret flags) in `config/messaging_block.rs`. **S–M / low / high.**

**A8. Resource listing consistency.** `skills.rs`/`agents.rs`/`slash_commands.rs` each re-parse the filter to patch `report.exceptions` and clear diagnostics — a domain decision made three times in the CLI because the lib returns a half-filtered report; also two filter types (`SkillFilter` vs `ResourceFilter`) for one concept. Lib returns filter-consistent reports; unify the filter types. **S / low / med-high.**

**A9. Small admin moves.** Uninstall symmetry + backup (fixes D13; → `config::uninstall_all`); config path resolution (publish `repo_config_path`; widen `user_config_path` to `.json5` — fixes D12); sound-effect validation gap (lib validates `default_sounds` but not per-action effects; the five-stage fuzzy suggester belongs in `playa::SoundEffect::suggest`); canonical-provider pairing rule (`link_display.rs:33-80`); `LogTarget` constructors out of prompt functions; TTS install planner (already pure and unit-tested; its 12-entry display-name table diverges from lib `INIT_TTS_PROVIDERS`). **S each / low / med.**

### I — CLI infrastructure

**I1. `error_report.rs` — a second, parallel provider-error classification system.** `cli/src/output/error_report.rs` (~540 lines): (a) `AgentErrorCategory` duplicates lib `SemanticErrorKind` minus `Unknown`; (b) its presentation table duplicates lib `error_kind_presentation` — same labels/colors, one copy per enum, guaranteed drift; (c) `classify_native_cli_cause` is a hand-written stderr keyword classifier (with defect D5) beside the lib's *generated* per-provider `ErrorKeywords` vocabulary; (d) `no_model_provided` hardcodes OpenCode paths/env/commands for *any* provider — `AgentErrorReport::no_model_provided(Provider::Codex)` would print OpenCode instructions, while `ProviderInfo` already carries `config_paths`, `model_env_vars`, `model_cli_flag`. The pattern to rewrite toward is the CLI's own `output/error_walker.rs`, which resolves through `select_effective_diagnostic` and explicitly refuses to keep a local downcast list. Fold in `api_errors.rs` (the Anthropic-style API error envelope parser + retryability taxonomy — a domain rule a retry loop or contract adapter needs and cannot reach) as one "provider failure taxonomy" workstream.
Home: `claudine::diagnostics::native_exit` (+ `api_error`) flowing through the standard discovery seam; `claudine::render::agent_error` keyed on `SemanticErrorKind`; remediation text becomes catalog data. **L / med / high.**

**I2. Completion candidate computation.** `cli/src/completion/` — ~2,450 lines of domain logic behind a shell-protocol façade: budgeted filesystem walking with skip policy, scope-priority resolution, fuzzy/prefix matching, Darkmatter schema introspection, `file(match(...))` globset semantics, frontmatter mode contracts. Two tells: `file_candidate_paths` is consumed by the *interactive ENTER path*, not shell completion; and `operation_file.rs` constructs `CompositionError::Autocomplete*` variants — the CLI building a lib error from outside. Private re-implementations of lib code throughout: `ScopeContext::discover_from` ≈ `capture_file_resolution_context`; three copies of `has_markdown_extension` vs the lib's `is_markdown_path` (which is `#[allow(dead_code)]` *because* the CLI re-implemented it); `keys::is_required` ≈ `schema/classify.rs`; `load_effective_schema` ≈ the private lib version. ~3,860 of 4,739 in-module test lines migrate and become terminal-free; `cli/tests/completion_perf.rs`'s 100ms budget becomes a lib property needing a lib-side counterpart. Lib gains `ignore` + `globset` deps. Shell bootstrap, `CompleteEnv`, ratatui chooser, TTY gating stay.
Home: `claudine::composition::{candidates, discovery, scopes, fuzzy}` + `schema/completion`. **L / med / high — largest single block of stranded domain logic; do after D3 is fixed.**

**I3. Argv grammar.** `cli/src/argv/`: `partition_composition_tail` is Claudine's composition-invocation *shape contract* (file first; owned flags win before `--`; first unowned switch starts the agent tail) — any non-clap frontend interpreting the same user intent needs it; `Ownership`/`classify` is a reusable POSIX/GNU arity algorithm; `looks_like_setter` is duplicated **three** times (argv, engine/tokens, setter_value). Rules 1–2 (provider boolean → `--provider`; fuzzy canonicalization) are domain vocabulary already catalog-derived — only the rewrite is stranded. Rule 4 (help hoist) is pure clap workaround glue and stays (or dies with `disable_help_flag`). The clap-introspection adapter (`OwnedFlags::absorb_*`) stays as the CLI's ~50-line bridge. One correctness note: Rule 2's `--provider` handling passes `--` through by accident of a bound rather than an explicit guard.
Home: `claudine::composition::argv` (`OwnedFlagSurface`, `partition_composition_tail`, `canonicalize_provider_tokens`, one `looks_like_setter`). **M / low / high.**

**I4. Perf instrumentation model.** `cli/src/perf/{mod,tree,report}.rs`: `PerfNode`/`NodeRole` plus the reconciliation invariant engine (`finalize_reconciling` synthesizes an unattributed remainder so parent totals always equal Σ children; `tree_reconciles` asserts it) — a general property of hierarchical timing data preventing the "78.6ms headline vs 1.57s body" bug class. `tree.rs` imports nothing beyond std + darkmatter; `report.rs`'s only deps are already lib deps. 1,584 test lines migrate unchanged. `render.rs` (PerfNode → MetricNode) may follow into `lib/src/render` per the `MetricsReport` precedent or stay. The one seam, `scan_perf_bootstrap`, is argv scanning and stays/moves with I3.
Home: `claudine::perf`. **M / low / high — close to a pure file move.**

**I5. Telemetry domain leaks (fixes D4).** The CWD-switch rule restated as a stale clap-variant match — expose one predicate from `launch_workspace` and have telemetry read the resolved value; `provider_subcommand_name` reproduces `Provider::as_slug()` keyed on the clap enum (guard-invisible) — route through `Commands → Provider` and call `as_slug()`. **S / low / med.**

**I6. Git-root discovery (fixes D11).** One `discover_repo_root` in the lib (worktree-correct predicate, counted by the existing `InvocationWorkSnapshot.git_root_discoveries` — which currently meters only two of the five sites), all five call sites converted. **S / low / med.**

## Correctly placed — boundary exemplars

Full per-area "stays put" lists live in the underlying review notes; the load-bearing summary:

- **Clap surface & shell protocol:** `args.rs`, `main.rs`, `provider_values.rs`, completion bootstrap/`CompleteEnv`/embedded shell scripts, the clap-introspection `OwnedFlags` adapter, Rule 4.
- **Terminal channel plumbing & rendering:** `log.rs`, `output/mod.rs` Prose assembly, `output/switches.rs`, `stream_io.rs`, sink spacing/wiring, dry-run/report renderers, `table_utils.rs`, hooks/logs/context report renderers, `help.rs`.
- **Process edge:** `switch_process_cwd`, signal-handler installation and Windows Job Objects (`exec/termination/{unix,windows,handle}.rs`), watchdog ticker threads, SIGINT registration, tokio runtime gating.
- **Interactive prompts:** inquire pickers (kept as injected closures/traits per C4/C8), biscuit-tui prompt drivers, the ratatui config TUI screens and autocomplete chooser, `--edit` `$EDITOR` flow.
- **Named exemplars of the right pattern:** `output/error_walker.rs` (resolves through the lib's diagnostic seam and refuses a local downcast list — the model for I1); `sequence/task_frames.rs` (lib defines the sink and renders; CLI only picks the channel); `compose/interrupt.rs` (decision separated from effect, unit-tested on every host).
- **Deliberate non-moves:** `claudine-gen`'s mirrored helpers (bootstrap boundary — gen must not depend on the lib); `context/` reports (presentation of *Darkmatter's* catalogs — if shared, they belong upstream in Darkmatter, not claudine); `middle_elide`/`render_inline_code` (generic — candidates for biscuit-terminal, not claudine).

## Migration mechanics

- **Error types:** `color-eyre` is CLI-only. Every moved `Result<_, Report>` becomes a typed `thiserror` variant; Prose markup inside error messages moves to the CLI render edge. This is the single most repeated tax.
- **Warnings:** functions that `log::warn` mid-flight return `warnings: Vec<_>` instead; the CLI owns verbosity gating and rendering.
- **`&Terminal` is not a blocker:** the lib already depends on biscuit-terminal and takes `&Terminal` in `StackExecutionContext`.
- **New lib dependencies:** `ignore` + `globset` (I2). The rendezvous-core question (A3) has a no-new-dep fallback.
- **Env/process reads:** parameterize `std::env` access (`EnvLookup`, injected iterators) during moves — the lib's `select.rs` already models this — so migrated tests drop `EnvGuard`/`#[serial]`.
- **Test migration:** well over 12,000 in-src CLI test lines ride along with their subjects and become terminal-free lib unit tests (session key 325; target_launch 1,347; env 801; timeouts ~1,400; completion ~3,860; perf 1,584; dashboard 604; schema-interactive 356; launch_plan 921; repo_home 490; more in L/C bundles).

## Recommended sequencing

**Phase 0 — defects & guard gaps (no moves, independently landable):** D1 (adopt full TOML escaping), D2, D3 (must precede I2), D4, D5, D6 (must precede L6 or land with it), D7 (or with A1), D10 (or with L13), D11, D12, D13, D15; extend the dispatch-inventory scan to `contract/src/`.

**Phase 1 — cheap, independent, high-value moves:** L8(a)+(c) timeouts · L13 semantic→hook table · C2 compose-context · C3 schema-stage rule · C6 preflight scan · C7 shell policy · L5 session key · L12(a) runaway resolve · E1 sanitize · P2 system-prompt delivery · P3 prompt extraction · A4 MCP cascade · I5/I6.

**Phase 2 — the enabling models:** L3 active-document model → L2 transition dispatch → L6+C1 handoff chain & coordinator driver · E2/E3 · A1 hook plan · A3 session register · I3 argv grammar · I4 perf.

**Phase 3 — the keystone:** P1 `WrapperProfile` → L4 launch rebuild/plan → E4 child env → L1 the attempt loop itself · then de-fork `claudine-contract` onto the lib surface and let the extended dispatch guard enforce it.

**Independent tracks (any time after Phase 1):** C4 schema-interactive · C5 target resolution · C6 sequence driver/step · C8 MCP session plan · L7 termination · L9/L10/L11 · L12(b)/L14 sink policies · A2 install plan · A5 signals verify · A6/A7 config TUI & messenger · I1 error taxonomy · I2 completion engine (last; largest).
