---
$schema: fix-log.yaml
fix: 2026-08-31-silent-success-and-startup-stall
deferred_perf_measurement: false
implementation_1: "2026-09-07T15:40:40-07:00"
---

# Implementation Log — Silent Success and Startup Stall

## Implementation of Review Findings #1

> **started at:** 2026-09-07T14:10:56-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/fix-cli-slow-tests/claudine/fixes/2026-08-31-silent-success-and-startup-stall/review-1.md'
- this is iteration 1 of the review-to-implement cycle
- review scope and mapping to the ratified execution plan:
        - **Finding 1 (Critical)** — `step_timeout` cannot terminate startup silence; maps to plan phases 2 and 3
        - **Finding 2 (Critical)** — stopped/unresolved Claude tasks become silent success; maps to plan phases 4 and 5
        - **Finding 3 (High)** — regression evidence and documentation did not land; maps to plan phases 3, 6, and 7
- orchestration strategy: one subagent per finding, dispatched **serially** because finding 3 depends on the behavior landed by findings 1 and 2
- package area under change: `claudine/` (library `claudine`, CLI `claudine-cli`); verification gates are `just test`, `just test-l2`, and `just lint` run from `claudine/`
- starting the work on 'Finding 1: step_timeout startup silence' at 14:12:38-07:00
        - baseline on the untouched tree: `just test-cli watchdog` — 67 passed, 0 failed. The contradictory `opencode_cold_start_suppresses_even_when_both_clocks_stale` was among the passing tests, confirming the defect was compiled and reachable
        - design decision: one silence reference, owned by the library as `LiveMetricsState::silence_reference(spawned_at)` = newest of spawn / `last_event_at` / `last_byte_at`. Both the kill rule (`evaluate_timeout_tick`) and the warning (`should_warn_stall`) read it, so the warning can never promise a deadline the kill rule would not enforce
        - design decision: the OpenCode **cold-start** arm is deleted rather than wrapped in `silence < budget`. Once the shared reference exists, "suppress while the age is under budget" is exactly what the general rule already does, so a bounded cold-start arm would be dead code. The **mid-step** arm (`step_in_flight && !both_clocks_stale`) is left byte-for-byte unchanged, per spec §1 "the existing mid-step rule remains"
        - `LiveMetricsState::record_byte_activity` already had the right polarity (non-whitespace refreshes, empty/whitespace-only does not); no change was needed there, only new coverage that crosses the startup boundary
        - verified by reading `exec/spawn/setup.rs::wall_clock_timeout_ticker`: the capture and direct/passthrough paths spawn only the wall-clock ticker and never `spawn_timeout_watchdog_ticker`, so `step_timeout` — old or new — cannot reach them
        - Phase 3 lives in a **new** platform-neutral binary `claudine/cli/tests/wrap_watchdog_startup_stall.rs`; the existing `wrap_watchdog_timeout.rs` stays `#![cfg(unix)]` and untouched. Only the fake-provider script is `cfg`-selected (`sh` / `.cmd`); all assertions are on the rendered diagnostic and `session_end.extra.exit_reason`, never a signal number
        - dropped `#[serial_test::serial]` from the new binary after observing its tests run concurrently under nextest — the in-process lock coordinates nothing when every test is its own process, and the tests share no resource
        - non-vacuity evidence (each guard neutralized alone, focused test observed failing, implementation restored, suite re-run green):
                - **spawn fallback** — `silence_reference` made unreachable when no activity exists: `evaluate_timeout_tick_fires_from_spawn_when_no_activity_ever_arrives` FAILED, `should_warn_stall_warns_from_spawn_when_no_activity_seen_yet` FAILED, and `watchdog_startup_stall_terminates_without_a_wall_clock_timeout` **timed out at 30 s** — the pre-fix hang, reproduced
                - **byte-heartbeat polarity** — `record_byte_activity` made unconditional: `silence_reference_ignores_whitespace_only_output` FAILED, `evaluate_timeout_tick_whitespace_startup_bytes_do_not_refresh_the_spawn_clock` FAILED, `watchdog_startup_whitespace_does_not_move_the_deadline` timed out at 30 s
                - **bounded OpenCode cold start** — unconditional `is_cold_start` arm restored: `evaluate_timeout_tick_opencode_cold_start_is_bounded_by_the_budget`, `opencode_cold_start_breaches_when_both_clocks_are_stale`, and `opencode_startup_with_no_output_at_all_breaches_from_spawn` all FAILED; `watchdog_opencode_stall_before_first_step_is_bounded` timed out at 30 s. The counter-cases `opencode_cold_start_suppresses_within_the_budget` and `opencode_cold_start_breach_yields_to_the_wall_clock` correctly stayed green, proving they do not depend on the bound
                - **startup warning** — `should_warn_stall` reverted to consulting only `last_event_at`: `should_warn_stall_warns_from_spawn_when_no_activity_seen_yet`, `should_warn_stall_startup_warning_resets_after_activity`, and `should_warn_stall_honors_the_byte_clock` FAILED; separately, `render_step_timeout_warn_prose` made to ignore `SilenceOrigin` failed `step_timeout_warn_uses_startup_wording_before_any_output` and `step_timeout_warn_startup_wording_keeps_the_hard_deadline_segment`
        - `claudine/docs/providers/dispatch-inventory.json` was regenerated (`CLAUDINE_UPDATE_INVENTORY=1`): its `cli_dispatch_guard` counts `Provider::*` references in `cli/src`, and the four new `Provider::OpenCode` mentions in the new watchdog tests moved `sites` 1473 → 1477. The diff touches only that count and line numbers under `watchdog/tests/opencode.rs`
        - gates from `claudine/`: `just lint` exit 0; `just test` exit 0 — **6777 tests run, 6777 passed, 11 skipped**. `just test-l2` was not run: the timeout semantics are L1 and this change adds no terminal-rendering claim
- work completed for 'Finding 1: step_timeout startup silence' at 14:54:29-07:00
- starting the work on 'Finding 2: silent success from stopped tasks' at 14:56:04-07:00
        - **Phase 4** — new library module `claudine/lib/src/stream/task_ledger.rs` owns the whole contract: `TaskOutcome`, the serializable `SubagentOutcome` fact, the status vocabulary, and the unbounded `TaskLedger`. `WatchdogState::recent_subagents` was not touched
        - design decision: identity is the **nonempty provider task ID and nothing else**. `entry_index` keys a `HashMap<String, usize>` on that ID; a missing or whitespace-only ID enrolls a `TaskIdentity::Anonymous(n)` entry that no lookup can ever reach, so one anonymous observation can never clear another. `SubagentOutcome.task_id` stays `None` for those — the synthetic counter is never written into machine output
        - design decision: two status vocabularies, not one. `SUCCESS_STATUSES` (`completed`/`success`/`succeeded`) and `UNSUCCESSFUL_STATUSES` (`stopped` + 11 unambiguous failure words). `terminal_outcome_for_status` returns `None` for anything else, and that `None` is what keeps a `task_notification` with an unrecognized status on the `Info` path. An *inherently* terminal event (`task_completed`) instead runs through `forced_terminal_outcome`, so an unknown status there becomes `UnknownStatus` — preserved verbatim, finalized as unresolved, never read as success
        - the alternative (treat every unrecognized notification status as terminal) was rejected: it converts fail-closed into fail-always the moment Claude ships a new progress word, and the spec's own wording is "a `task_notification` **with a terminal status**"
        - `apply_to_summary` sets `is_error` and the facts unconditionally when work is incomplete, but only stamps `error_kind: "incomplete_subagents"` when the parser had not already recorded a more specific provider failure (a rate limit is the more actionable headline). `exit_code` is never touched
        - `handle_task_completed` was renamed `handle_task_terminal` and is now the single terminal path shared with `handle_task_notification`; `ClaudeEvent::TaskProgress` and `ClaudeEvent::TaskNotification` no longer share a match arm
        - **Phase 5** — `AttemptOutcome.is_error`, `classify_failure` reading it, `build_attempt_outcome` populating it, all three CLI attempt paths threading it (`false` on capture and interactive, which have no parser), the top-level `extra["subagent_outcomes"]` projection, and a new `claudine::render::IncompleteSubagents` (`Prose` + `UnorderedList`) rendered from `emit_stream_summary_inner`
        - **one change beyond the plan's letter, deliberately:** the failure boundary in `loop_control.rs` returned `outcome.exit_code` verbatim, so a semantically-failed run still exited **0** to the shell. That is the same silent success the failure stack had just fired to prevent — `just commit` would have carried on. The boundary now returns `1` for the single shape `Completed && exit_code == 0`, which was **unreachable before this fix** (a completed exit-0 attempt classified as success), so no previously-failing run changes code. The provider's own exit code in the summary and the JSONL row is still 0
        - **deviation from the spec's literal wording, flagged for Ken:** spec §4 asks for `err.kind = "incomplete_subagents"`. In this repo `err.kind` is the deprecated alias for the diagnostic *category* and only falls back to a raw label when the error is facet-less. `incomplete_subagents` maps to no locked catalog code, so it projects as `err.variant == "incomplete_subagents"` with `err.kind == "LifecycleAction"` — the same shape every unmapped `error_kind` already has. Adding a catalog row would touch the locked registry and `error_guards`, which is out of this fix's scope
        - audited the machine-data consumers: JSONL (`write_summary_event`) serializes the whole `EventMeta`; SQLite ingest stores `serde_json::to_string(&meta.extra)` whole into `extra_json`; `claudine logs` query rows re-parse it whole into `extra: Option<JsonValue>`. All three are schema-free for extras — **no consumer needed changing**, only explicit retention tests. Rendezvous/dashboard never carried the summary at all: `session_report.rs` reports a status string, and the DuckDB projection row is a generic `metadata_json` blob
        - `lib/src/stream/summary.rs` crossed the 300-line inline-test budget enforced by `cli/tests/test_placement.rs`; its `mod tests` moved to a sibling `summary/tests.rs`, which is the convention the rest of the module already follows
        - `claudine/docs/providers/dispatch-inventory.json` regenerated twice (`CLAUDINE_UPDATE_INVENTORY=1`), 1477 → 1481: four `reference`-class `Provider::*` fixture mentions in new/moved library tests. No new conditional dispatch
        - non-vacuity evidence (each guard neutralized alone, focused tests observed failing, implementation restored, full suite re-run green):
                - **terminal notification routing** — `handle_task_notification` forced down the progress branch: `terminal_task_notification_preserves_id_name_and_raw_status` and `stopped_task_notifications_poison_a_native_exit_zero_session` FAILED, and 3 of the 4 CLI replay tests FAILED — including `completed_subagents_still_fire_the_success_stack`, which proves the routing carries successes as well as stops
                - **ledger poisoning** — `incomplete_outcomes` made to emit nothing: 12 of 18 ledger tests, all three Claude-parser finalization tests, and the two incident CLI tests FAILED; the reconciliation and clean-run companions correctly stayed green
                - **`is_error` classification** — the `|| outcome.is_error` disjunct removed from `classify_failure`: `a_non_task_semantic_error_with_native_exit_zero_is_an_agent_failure`, `incomplete_subagents_with_native_exit_zero_is_an_agent_failure_not_a_timeout`, and `build_attempt_outcome_carries_the_summary_semantic_verdict` FAILED, plus both incident CLI tests. `a_clean_completed_exit_zero_attempt_is_still_a_success` stayed green, proving it does not depend on the new disjunct
                - **`session_end` projection** — the `extra["subagent_outcomes"]` insert disabled: `summary_event_projects_subagent_outcomes_at_the_top_level_of_extra`, `subagent_outcomes_survive_the_jsonl_serialization_round_trip`, and both incident CLI tests FAILED; `summary_event_omits_subagent_outcomes_on_a_clean_run` stayed green
        - gates from `claudine/`: `just lint` exit 0; `just test` exit 0 — **6829 tests run, 6829 passed, 11 skipped**. `just test-l2` was not run: the new diagnostic makes no claim about terminal-specific layout, styling, glyph widths, or scrolling — the assertions are on plain rendered substrings, lifecycle side-effect files, and JSONL
- work completed for 'Finding 2: silent success from stopped tasks' at 15:36:15-07:00
- starting the work on 'Finding 3: regression evidence and documentation' at 15:37:58-07:00
        - **orchestrator resumed at 2026-09-07T15:40:40-07:00** after the previous run was interrupted partway into this finding. Continuing the same section rather than opening a second `## Implementation of Review Findings #1` heading, because this is the same iteration against the same review
        - state assessed on resume from `git status --short` and the source itself:
                - findings 1 and 2 are landed — 29 modified files, 5 new files, and both `just test` and `just lint` reported green in the entries above
                - the behavior-bearing code comments the review called out are **already reconciled**: `watchdog/evaluate.rs` module doc and `evaluate_timeout_tick` rustdoc now describe the `started_at`/`last_event_at`/`last_byte_at` reference, `exec/timeouts.rs::detect_step_timeout` is re-documented as a non-authoritative legacy helper, and `HarnessPlan::step_timeout` says the silence clock starts at child spawn
                - the acceptance fixtures the review listed as absent are **present**: `cli/tests/wrap_watchdog_startup_stall.rs` (5 tests: startup stall, byte heartbeat, whitespace polarity, healthy OpenCode step, OpenCode pre-first-step stall) and `cli/tests/wrap_incomplete_subagents.rs` (4 tests: incident replay, clean-run counter-case, same-ID reconciliation, display-truncation-vs-machine-record), plus the library-side ledger/parser/classifier/serde coverage
                - **no documentation had landed** — `claudine/docs/topics/timeouts.md` and `.claude/skills/claudine/timeouts.md` both still carried the "First-event grace … the rule cannot fire … Slow provider startup … will not be killed" bullet, which is now false
        - therefore the remaining Finding 3 scope is exactly plan Phase 6 (documentation and operational guidance) plus the Phase 7 audit
        - **timeout contract docs** — `claudine/docs/topics/timeouts.md` and its snapshot `.claude/skills/claudine/timeouts.md` both carried the false **"First-event grace"** bullet ("Until at least one activity signal has been observed on either clock, the rule cannot fire. Slow provider startup or a long first model response will not be killed"). Replaced with a **"Bounded from spawn"** bullet, and five further corrections applied identically to both copies:
                - the **Formula** now names the real reference — the newest of the child's monotonic `started_at`, `last_event_at`, and `last_byte_at`, per `LiveMetricsState::silence_reference`
                - the Overview table's `step_timeout` row: "since the last parent-stream event" → "since the last activity — or since child spawn, while there has been none"
                - the Activity-vocabulary preamble: two clocks → three instants
                - the `step_timeout_warn` bullet: was `now - last_event_at`; now the shared reference plus the startup-wording and episode-reset contract
                - the OpenCode per-step grace bullet: added the "*and* at least one activity clock is inside the budget" condition, the both-clocks-stale breach, and an explicit "**no companion cold-start grace**" — the arm was deleted, not merely bounded
                - the OpenCode hang-class worked example: `last_activity_at` → `silence_reference`
        - **new `### Startup stalls` section** in both timeout copies: the spawn-to-first-output window is inside `step_timeout`; no separate duration, configuration key, or termination reason exists; a startup breach is `TimedOut` + `error_kind: "step_timeout"` on the normal termination ladder; the in-flight gate has nothing to suppress against before the first tool/task start. Its `#### OpenCode plugin resolution and @latest` sub-section records the 2026-08-30/31 incident, recommends **exact plugin-version pins**, notes that `step_timeout` now bounds this class so a wall-clock `timeout` is no longer the only backstop, and states explicitly that **Claudine must not mutate a user's OpenCode configuration**
        - **signal-handling docs** (topic + skill) — the `ProcessTermination` table row `| Completed | … | AgentFailure only if exit ≠ 0 |` is now false; it reads `AgentFailure` when exit ≠ 0 **or** the stream summary set `is_error`. One paragraph added to *Watchdog-driven SIGTERM*: the SIGTERM → grace → SIGKILL ladder is armed from spawn, a silent child reaches the same escalation on budget, and nothing about the ladder, the grace period, or the Windows Ctrl+Break → `TerminateJobObject` equivalent is special-cased for it
        - **non-interactive-sessions doc** — corrected the harness Timeout table's step-silence row, and added a new `### Exit Code 0 Is Not Success` subsection covering the `is_error` → `AgentFailure` route, the attempt-scoped ledger reconciled by provider task ID only, `error_kind: "incomplete_subagents"` deferring to a more specific provider failure, preservation of `exit_code` and `ProcessTermination::Completed`, and `session_end.extra.subagent_outcomes` as the stable machine contract
                - its skill counterpart `.claude/skills/claudine/summaries/non-interactive-sessions.md` was **deliberately not hand-edited: it is generated**. `claudine/justfile:401 publish-summary-research` composes `./docs/research/summary/*.md` through `md compose` into that directory, and the source `claudine/docs/research/summary/non-interactive-sessions.md` exists. Its content is a cross-provider research comparison, not a Claudine behavior contract, so it asserted neither obsolete claim
        - **`opencode-event-sources.md` — required-document-list corrected, not restored.** The spec §5 list named `claudine/docs/topics/opencode-event-sources.md`; that file has **never existed**. The fix that created the document (`claudine/fixes/_completed/2026-05-12-opencode-stderr-returns/spec.md:159`) explicitly asked for "A new file under `.claude/skills/claudine/`" — it is skill-only by design. Spec §5 now lists only the skill path with a parenthetical citing that fix. The skill file's *Watchdog interaction* section was then updated with the bounded-from-spawn contract: an OpenCode process that boots, hangs resolving plugins, and never reaches `SessionCreated` is killed on budget; no cold-start exception; the mid-step grace is the only remaining OpenCode suppression
        - `.claude/skills/claudine/timeline.md` — new `2026-08-31 — silent-success-and-startup-stall` entry at the head of the `## 2026-08` section, matching the existing format and newest-first ordering. It records the shared `silence_reference`, the deletion of the cold-start arm and why, the unchanged mid-step arm, ID-only task identity with unreachable anonymous entries, and the `loop_control.rs` exit-code trap
        - `md hash --save` refreshed the `hash:` frontmatter on the two hashed files (`opencode-event-sources.md`, `timeline.md`) and re-stamped their `last_updated:` to today
        - **drift sweep** over `claudine/docs/`, `.claude/skills/claudine/`, `claudine/lib/src`, and `claudine/cli/src` for both obsolete contracts (unbounded startup / first-event grace / "cannot fire until"; and "exit code 0 means success" / completed-plus-zero):
                - six documentation hits, all listed above, all fixed
                - **no source drift found** — `watchdog/evaluate.rs`, `exec/timeouts.rs::detect_step_timeout`, `TimeoutConfig`, `HarnessPlan::step_timeout`, `LiveMetricsState`/`SilenceOrigin`/`should_warn_stall`, `classify_failure`, `build_attempt_outcome`, and `AttemptOutcome::{exit_code,is_error}` already described the new contract, as expected from findings 1 and 2
                - research-corpus hits and `completions/shell-completions.md`'s "useful from a cold start" are unrelated senses of the words; unchanged
        - **one pre-existing drift found and fixed by the orchestrator:** `non-interactive-sessions.md:412` claimed a **5-second** SIGTERM→SIGKILL grace. The real default is **10s** (`TimeoutConfig::default().kill_grace`, overridable via `CLAUDINE_KILL_GRACE`), which is what `timeouts.md` and `signal-handling.md` already said. Corrected to 10 seconds with the env var named. Per the repo drift rule the code is authoritative; the subagent had left it as out-of-scope, but it sits two lines from a row this change edited and would have been re-read as current
        - **one item deliberately not done:** the "per-step `provider_status` grace" *label* was left alone even though `step_in_flight`, not `provider_status`, is the gate today (`provider_status` is now read only to word the breach). Two other docs refer to it by that name; renaming ripples beyond this fix. The bullet's **body** was corrected instead, so no reader is misled about behavior
- work completed for 'Finding 3: regression evidence and documentation' at 15:59:16-07:00

### Verification

- final gates, all run from `claudine/` through the canonical `just` recipes (never `cargo test`), unpiped so no exit code was masked:
        - `just lint` — **exit 0**
        - `just test` — **exit 0**; **6829 tests run, 6829 passed (25 slow), 11 skipped**
        - `just test-l2` — **exit 0**; `claudine-cli` **236 run, 236 passed, 2497 skipped** and `claudine-gen` **3 run, 3 passed, 155 skipped**
- `git diff --check` clean — no whitespace errors or conflict markers
- two flakes were observed and each was proven to be a host-load artifact rather than a regression, by re-running the test in isolation and then the full suite again:
        - `composition::sequence::preflight::tests::shell::bracket_target_identity_is_rejected_on_every_graph_shell_surface` hit the 30 s nextest timeout on one `just test` run; it passes in **3.66 s** in isolation and the full suite is green. It is a shell-preflight test with no relationship to anything this change touched
        - `level2_lifecycle_control::level2_shipped_implement_plan_supplied_commit_message_runs_exact_commit_branch` failed once on a loaded `just test-l2` run (fast assertion failure at 1.5 s, with the captured pane showing a sibling test's expected content — the signature of cross-test pane interference under the parallel self-spawn path). It passes in **1.30 s** in isolation, and the immediately following full L2 run was **236/236 green**
- `just test-l2` **was** run for this cycle even though the two earlier subagents had each declined it. Their reasoning was sound on its own terms — the timeout and lifecycle semantics are L1, and the new diagnostic makes no claim about terminal-specific layout, styling, glyph widths, or scrolling — but the review names the package's canonical `just test-l2` as a required regression gate under acceptance criterion 13, so it was run as a regression gate rather than as feature-specific evidence
- GitNexus `detect_changes` was not run: nothing is being committed in this task, and the repository rule scopes that check to pre-commit

## Successful Completion

The implementation of review cycle 1 has completed successfully in 1 hour 48 minutes. During this implementation all 3 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 3 were fixed, 0 were deferred.

No finding was deferred, and no performance measurement was required by this review, so `deferred_perf_measurement` remains `false`.

One item inside Finding 3 was resolved by **correcting the specification rather than implementing it**, using the latitude the review itself granted: spec §5 required updating `claudine/docs/topics/opencode-event-sources.md`, a file that has never existed because that document was authored skill-only by design in the 2026-05-12 `opencode-stderr-returns` fix. The required-document list was corrected to name only `.claude/skills/claudine/opencode-event-sources.md`, and that file was updated.

One deviation from the specification's literal wording is carried forward from Finding 2 and still **needs Ken's ruling**: spec §4 asks for `err.kind = "incomplete_subagents"`, but in this repository `err.kind` is the deprecated alias for the diagnostic *category*. `incomplete_subagents` maps to no locked catalog code, so it projects as `err.variant == "incomplete_subagents"` with `err.kind == "LifecycleAction"` — the same shape every unmapped `error_kind` already has. Adding a catalog row would touch the locked registry and `error_guards`, which is outside this fix's scope.

## Implementation of Review Findings #2

> **started at:** 2026-09-07T16:27:15-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/fix-cli-slow-tests/claudine/fixes/2026-08-31-silent-success-and-startup-stall/review-2.md'
- this is iteration 2 of the review-to-implement cycle
- review scope: six findings — one Critical, four High, one Medium
        - **Finding 1 (Critical)** — `both_clocks_stale` suppresses an OpenCode mid-step breach forever when one activity clock is populated-but-stale and the other is absent
        - **Finding 2 (High)** — unknown `task_notification` terminal statuses are downgraded to `Info` and their raw status is lost
        - **Finding 3 (High)** — `error_kind`/`err.kind` identity for incomplete sub-agents does not match the ratified specification
        - **Finding 4 (High)** — the startup-termination deadline is never asserted by the process-level test (60 s outer timeout only)
        - **Finding 5 (High)** — the full terminal diagnostic has no feature-specific Level 2 verification
        - **Finding 6 (Medium)** — the required Claudine skill summary was not updated
- orchestration strategy: one subagent per finding, dispatched **serially**, because findings 2 and 3 both land in the ledger/parser surface and finding 5 depends on the diagnostic shape finding 3 settles
- package area under change: `claudine/` (library `claudine`, CLI `claudine-cli`); verification gates are `just test`, `just test-l2`, and `just lint` run from `claudine/`
