---
$schema: fix-log.yaml
fix: 2026-08-31-silent-success-and-startup-stall
deferred_perf_measurement: false
implementation_1: "2026-09-07T15:40:40-07:00"
implementation_2: "2026-09-07T16:27:15-07:00"
---

# Implementation Log — Silent Success and Startup Stall

## Implementation of Review Findings #1

> **started at:** 2026-09-07T14:10:56-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/fix-cli-slow-tests/claudine/fixes/2026-08-31-silent-success-and-startup-stall/review-1.md'
- this is iteration 1 of the review-to-implement cycle
- review scope and mapping to the ratified execution plan:
        - **Finding 1 (Critical)** — `step_timeout` cannot terminate startup silence; maps to plan phases 2 and 3
        - **Finding 2 (Critical)** — stopped/unresolved Claude tasks become silent success; maps to plan phases 4 and 5
        - **Finding 3 (High)** — regression evidence and documentation did not land; maps to plan phases 3, 6, and 7
- orchestration strategy: one subagent per finding, dispatched **serially** because finding 3 depends on the behavior landed by findings 1 and 2
- package area under change: `claudine/` (library `claudine`, CLI `claudine-cli`); verification gates are `just test`, `just test-l2`, and `just lint` run from `claudine/`
- starting the work on 'Finding 1: step_timeout startup silence' at 14:12:38-07:00
        - baseline on the untouched tree: `just test-cli watchdog` — 67 passed, 0 failed. The contradictory `opencode_cold_start_suppresses_even_when_both_clocks_stale` was among the passing tests, confirming the defect was compiled and reachable
        - design decision: one silence reference, owned by the library as `LiveMetricsState::silence_reference(spawned_at)` = newest of spawn / `last_event_at` / `last_byte_at`. Both the kill rule (`evaluate_timeout_tick`) and the warning (`should_warn_stall`) read it, so the warning can never promise a deadline the kill rule would not enforce
        - design decision: the OpenCode **cold-start** arm is deleted rather than wrapped in `silence < budget`. Once the shared reference exists, "suppress while the age is under budget" is exactly what the general rule already does, so a bounded cold-start arm would be dead code. The **mid-step** arm (`step_in_flight && !both_clocks_stale`) is left byte-for-byte unchanged, per spec §1 "the existing mid-step rule remains"
        - `LiveMetricsState::record_byte_activity` already had the right polarity (non-whitespace refreshes, empty/whitespace-only does not); no change was needed there, only new coverage that crosses the startup boundary
        - verified by reading `exec/spawn/setup.rs::wall_clock_timeout_ticker`: the capture and direct/passthrough paths spawn only the wall-clock ticker and never `spawn_timeout_watchdog_ticker`, so `step_timeout` — old or new — cannot reach them
        - Phase 3 lives in a **new** platform-neutral binary `claudine/cli/tests/wrap_watchdog_startup_stall.rs`; the existing `wrap_watchdog_timeout.rs` stays `#![cfg(unix)]` and untouched. Only the fake-provider script is `cfg`-selected (`sh` / `.cmd`); all assertions are on the rendered diagnostic and `session_end.extra.exit_reason`, never a signal number
        - dropped `#[serial_test::serial]` from the new binary after observing its tests run concurrently under nextest — the in-process lock coordinates nothing when every test is its own process, and the tests share no resource
        - non-vacuity evidence (each guard neutralized alone, focused test observed failing, implementation restored, suite re-run green):
                - **spawn fallback** — `silence_reference` made unreachable when no activity exists: `evaluate_timeout_tick_fires_from_spawn_when_no_activity_ever_arrives` FAILED, `should_warn_stall_warns_from_spawn_when_no_activity_seen_yet` FAILED, and `watchdog_startup_stall_terminates_without_a_wall_clock_timeout` **timed out at 30 s** — the pre-fix hang, reproduced
                - **byte-heartbeat polarity** — `record_byte_activity` made unconditional: `silence_reference_ignores_whitespace_only_output` FAILED, `evaluate_timeout_tick_whitespace_startup_bytes_do_not_refresh_the_spawn_clock` FAILED, `watchdog_startup_whitespace_does_not_move_the_deadline` timed out at 30 s
                - **bounded OpenCode cold start** — unconditional `is_cold_start` arm restored: `evaluate_timeout_tick_opencode_cold_start_is_bounded_by_the_budget`, `opencode_cold_start_breaches_when_both_clocks_are_stale`, and `opencode_startup_with_no_output_at_all_breaches_from_spawn` all FAILED; `watchdog_opencode_stall_before_first_step_is_bounded` timed out at 30 s. The counter-cases `opencode_cold_start_suppresses_within_the_budget` and `opencode_cold_start_breach_yields_to_the_wall_clock` correctly stayed green, proving they do not depend on the bound
                - **startup warning** — `should_warn_stall` reverted to consulting only `last_event_at`: `should_warn_stall_warns_from_spawn_when_no_activity_seen_yet`, `should_warn_stall_startup_warning_resets_after_activity`, and `should_warn_stall_honors_the_byte_clock` FAILED; separately, `render_step_timeout_warn_prose` made to ignore `SilenceOrigin` failed `step_timeout_warn_uses_startup_wording_before_any_output` and `step_timeout_warn_startup_wording_keeps_the_hard_deadline_segment`
        - `claudine/docs/providers/dispatch-inventory.json` was regenerated (`CLAUDINE_UPDATE_INVENTORY=1`): its `cli_dispatch_guard` counts `Provider::*` references in `cli/src`, and the four new `Provider::OpenCode` mentions in the new watchdog tests moved `sites` 1473 → 1477. The diff touches only that count and line numbers under `watchdog/tests/opencode.rs`
        - gates from `claudine/`: `just lint` exit 0; `just test` exit 0 — **6777 tests run, 6777 passed, 11 skipped**. `just test-l2` was not run: the timeout semantics are L1 and this change adds no terminal-rendering claim
- work completed for 'Finding 1: step_timeout startup silence' at 14:54:29-07:00
- starting the work on 'Finding 2: silent success from stopped tasks' at 14:56:04-07:00
        - **Phase 4** — new library module `claudine/lib/src/stream/task_ledger.rs` owns the whole contract: `TaskOutcome`, the serializable `SubagentOutcome` fact, the status vocabulary, and the unbounded `TaskLedger`. `WatchdogState::recent_subagents` was not touched
        - design decision: identity is the **nonempty provider task ID and nothing else**. `entry_index` keys a `HashMap<String, usize>` on that ID; a missing or whitespace-only ID enrolls a `TaskIdentity::Anonymous(n)` entry that no lookup can ever reach, so one anonymous observation can never clear another. `SubagentOutcome.task_id` stays `None` for those — the synthetic counter is never written into machine output
        - design decision: two status vocabularies, not one. `SUCCESS_STATUSES` (`completed`/`success`/`succeeded`) and `UNSUCCESSFUL_STATUSES` (`stopped` + 11 unambiguous failure words). `terminal_outcome_for_status` returns `None` for anything else, and that `None` is what keeps a `task_notification` with an unrecognized status on the `Info` path. An *inherently* terminal event (`task_completed`) instead runs through `forced_terminal_outcome`, so an unknown status there becomes `UnknownStatus` — preserved verbatim, finalized as unresolved, never read as success
        - the alternative (treat every unrecognized notification status as terminal) was rejected: it converts fail-closed into fail-always the moment Claude ships a new progress word, and the spec's own wording is "a `task_notification` **with a terminal status**"
        - `apply_to_summary` sets `is_error` and the facts unconditionally when work is incomplete, but only stamps `error_kind: "incomplete_subagents"` when the parser had not already recorded a more specific provider failure (a rate limit is the more actionable headline). `exit_code` is never touched
        - `handle_task_completed` was renamed `handle_task_terminal` and is now the single terminal path shared with `handle_task_notification`; `ClaudeEvent::TaskProgress` and `ClaudeEvent::TaskNotification` no longer share a match arm
        - **Phase 5** — `AttemptOutcome.is_error`, `classify_failure` reading it, `build_attempt_outcome` populating it, all three CLI attempt paths threading it (`false` on capture and interactive, which have no parser), the top-level `extra["subagent_outcomes"]` projection, and a new `claudine::render::IncompleteSubagents` (`Prose` + `UnorderedList`) rendered from `emit_stream_summary_inner`
        - **one change beyond the plan's letter, deliberately:** the failure boundary in `loop_control.rs` returned `outcome.exit_code` verbatim, so a semantically-failed run still exited **0** to the shell. That is the same silent success the failure stack had just fired to prevent — `just commit` would have carried on. The boundary now returns `1` for the single shape `Completed && exit_code == 0`, which was **unreachable before this fix** (a completed exit-0 attempt classified as success), so no previously-failing run changes code. The provider's own exit code in the summary and the JSONL row is still 0
        - **deviation from the spec's literal wording, flagged for Ken:** spec §4 asks for `err.kind = "incomplete_subagents"`. In this repo `err.kind` is the deprecated alias for the diagnostic *category* and only falls back to a raw label when the error is facet-less. `incomplete_subagents` maps to no locked catalog code, so it projects as `err.variant == "incomplete_subagents"` with `err.kind == "LifecycleAction"` — the same shape every unmapped `error_kind` already has. Adding a catalog row would touch the locked registry and `error_guards`, which is out of this fix's scope
        - audited the machine-data consumers: JSONL (`write_summary_event`) serializes the whole `EventMeta`; SQLite ingest stores `serde_json::to_string(&meta.extra)` whole into `extra_json`; `claudine logs` query rows re-parse it whole into `extra: Option<JsonValue>`. All three are schema-free for extras — **no consumer needed changing**, only explicit retention tests. Rendezvous/dashboard never carried the summary at all: `session_report.rs` reports a status string, and the DuckDB projection row is a generic `metadata_json` blob
        - `lib/src/stream/summary.rs` crossed the 300-line inline-test budget enforced by `cli/tests/test_placement.rs`; its `mod tests` moved to a sibling `summary/tests.rs`, which is the convention the rest of the module already follows
        - `claudine/docs/providers/dispatch-inventory.json` regenerated twice (`CLAUDINE_UPDATE_INVENTORY=1`), 1477 → 1481: four `reference`-class `Provider::*` fixture mentions in new/moved library tests. No new conditional dispatch
        - non-vacuity evidence (each guard neutralized alone, focused tests observed failing, implementation restored, full suite re-run green):
                - **terminal notification routing** — `handle_task_notification` forced down the progress branch: `terminal_task_notification_preserves_id_name_and_raw_status` and `stopped_task_notifications_poison_a_native_exit_zero_session` FAILED, and 3 of the 4 CLI replay tests FAILED — including `completed_subagents_still_fire_the_success_stack`, which proves the routing carries successes as well as stops
                - **ledger poisoning** — `incomplete_outcomes` made to emit nothing: 12 of 18 ledger tests, all three Claude-parser finalization tests, and the two incident CLI tests FAILED; the reconciliation and clean-run companions correctly stayed green
                - **`is_error` classification** — the `|| outcome.is_error` disjunct removed from `classify_failure`: `a_non_task_semantic_error_with_native_exit_zero_is_an_agent_failure`, `incomplete_subagents_with_native_exit_zero_is_an_agent_failure_not_a_timeout`, and `build_attempt_outcome_carries_the_summary_semantic_verdict` FAILED, plus both incident CLI tests. `a_clean_completed_exit_zero_attempt_is_still_a_success` stayed green, proving it does not depend on the new disjunct
                - **`session_end` projection** — the `extra["subagent_outcomes"]` insert disabled: `summary_event_projects_subagent_outcomes_at_the_top_level_of_extra`, `subagent_outcomes_survive_the_jsonl_serialization_round_trip`, and both incident CLI tests FAILED; `summary_event_omits_subagent_outcomes_on_a_clean_run` stayed green
        - gates from `claudine/`: `just lint` exit 0; `just test` exit 0 — **6829 tests run, 6829 passed, 11 skipped**. `just test-l2` was not run: the new diagnostic makes no claim about terminal-specific layout, styling, glyph widths, or scrolling — the assertions are on plain rendered substrings, lifecycle side-effect files, and JSONL
- work completed for 'Finding 2: silent success from stopped tasks' at 15:36:15-07:00
- starting the work on 'Finding 3: regression evidence and documentation' at 15:37:58-07:00
        - **orchestrator resumed at 2026-09-07T15:40:40-07:00** after the previous run was interrupted partway into this finding. Continuing the same section rather than opening a second `## Implementation of Review Findings #1` heading, because this is the same iteration against the same review
        - state assessed on resume from `git status --short` and the source itself:
                - findings 1 and 2 are landed — 29 modified files, 5 new files, and both `just test` and `just lint` reported green in the entries above
                - the behavior-bearing code comments the review called out are **already reconciled**: `watchdog/evaluate.rs` module doc and `evaluate_timeout_tick` rustdoc now describe the `started_at`/`last_event_at`/`last_byte_at` reference, `exec/timeouts.rs::detect_step_timeout` is re-documented as a non-authoritative legacy helper, and `HarnessPlan::step_timeout` says the silence clock starts at child spawn
                - the acceptance fixtures the review listed as absent are **present**: `cli/tests/wrap_watchdog_startup_stall.rs` (5 tests: startup stall, byte heartbeat, whitespace polarity, healthy OpenCode step, OpenCode pre-first-step stall) and `cli/tests/wrap_incomplete_subagents.rs` (4 tests: incident replay, clean-run counter-case, same-ID reconciliation, display-truncation-vs-machine-record), plus the library-side ledger/parser/classifier/serde coverage
                - **no documentation had landed** — `claudine/docs/topics/timeouts.md` and `.claude/skills/claudine/timeouts.md` both still carried the "First-event grace … the rule cannot fire … Slow provider startup … will not be killed" bullet, which is now false
        - therefore the remaining Finding 3 scope is exactly plan Phase 6 (documentation and operational guidance) plus the Phase 7 audit
        - **timeout contract docs** — `claudine/docs/topics/timeouts.md` and its snapshot `.claude/skills/claudine/timeouts.md` both carried the false **"First-event grace"** bullet ("Until at least one activity signal has been observed on either clock, the rule cannot fire. Slow provider startup or a long first model response will not be killed"). Replaced with a **"Bounded from spawn"** bullet, and five further corrections applied identically to both copies:
                - the **Formula** now names the real reference — the newest of the child's monotonic `started_at`, `last_event_at`, and `last_byte_at`, per `LiveMetricsState::silence_reference`
                - the Overview table's `step_timeout` row: "since the last parent-stream event" → "since the last activity — or since child spawn, while there has been none"
                - the Activity-vocabulary preamble: two clocks → three instants
                - the `step_timeout_warn` bullet: was `now - last_event_at`; now the shared reference plus the startup-wording and episode-reset contract
                - the OpenCode per-step grace bullet: added the "*and* at least one activity clock is inside the budget" condition, the both-clocks-stale breach, and an explicit "**no companion cold-start grace**" — the arm was deleted, not merely bounded
                - the OpenCode hang-class worked example: `last_activity_at` → `silence_reference`
        - **new `### Startup stalls` section** in both timeout copies: the spawn-to-first-output window is inside `step_timeout`; no separate duration, configuration key, or termination reason exists; a startup breach is `TimedOut` + `error_kind: "step_timeout"` on the normal termination ladder; the in-flight gate has nothing to suppress against before the first tool/task start. Its `#### OpenCode plugin resolution and @latest` sub-section records the 2026-08-30/31 incident, recommends **exact plugin-version pins**, notes that `step_timeout` now bounds this class so a wall-clock `timeout` is no longer the only backstop, and states explicitly that **Claudine must not mutate a user's OpenCode configuration**
        - **signal-handling docs** (topic + skill) — the `ProcessTermination` table row `| Completed | … | AgentFailure only if exit ≠ 0 |` is now false; it reads `AgentFailure` when exit ≠ 0 **or** the stream summary set `is_error`. One paragraph added to *Watchdog-driven SIGTERM*: the SIGTERM → grace → SIGKILL ladder is armed from spawn, a silent child reaches the same escalation on budget, and nothing about the ladder, the grace period, or the Windows Ctrl+Break → `TerminateJobObject` equivalent is special-cased for it
        - **non-interactive-sessions doc** — corrected the harness Timeout table's step-silence row, and added a new `### Exit Code 0 Is Not Success` subsection covering the `is_error` → `AgentFailure` route, the attempt-scoped ledger reconciled by provider task ID only, `error_kind: "incomplete_subagents"` deferring to a more specific provider failure, preservation of `exit_code` and `ProcessTermination::Completed`, and `session_end.extra.subagent_outcomes` as the stable machine contract
                - its skill counterpart `.claude/skills/claudine/summaries/non-interactive-sessions.md` was **deliberately not hand-edited: it is generated**. `claudine/justfile:401 publish-summary-research` composes `./docs/research/summary/*.md` through `md compose` into that directory, and the source `claudine/docs/research/summary/non-interactive-sessions.md` exists. Its content is a cross-provider research comparison, not a Claudine behavior contract, so it asserted neither obsolete claim
        - **`opencode-event-sources.md` — required-document-list corrected, not restored.** The spec §5 list named `claudine/docs/topics/opencode-event-sources.md`; that file has **never existed**. The fix that created the document (`claudine/fixes/_completed/2026-05-12-opencode-stderr-returns/spec.md:159`) explicitly asked for "A new file under `.claude/skills/claudine/`" — it is skill-only by design. Spec §5 now lists only the skill path with a parenthetical citing that fix. The skill file's *Watchdog interaction* section was then updated with the bounded-from-spawn contract: an OpenCode process that boots, hangs resolving plugins, and never reaches `SessionCreated` is killed on budget; no cold-start exception; the mid-step grace is the only remaining OpenCode suppression
        - `.claude/skills/claudine/timeline.md` — new `2026-08-31 — silent-success-and-startup-stall` entry at the head of the `## 2026-08` section, matching the existing format and newest-first ordering. It records the shared `silence_reference`, the deletion of the cold-start arm and why, the unchanged mid-step arm, ID-only task identity with unreachable anonymous entries, and the `loop_control.rs` exit-code trap
        - `md hash --save` refreshed the `hash:` frontmatter on the two hashed files (`opencode-event-sources.md`, `timeline.md`) and re-stamped their `last_updated:` to today
        - **drift sweep** over `claudine/docs/`, `.claude/skills/claudine/`, `claudine/lib/src`, and `claudine/cli/src` for both obsolete contracts (unbounded startup / first-event grace / "cannot fire until"; and "exit code 0 means success" / completed-plus-zero):
                - six documentation hits, all listed above, all fixed
                - **no source drift found** — `watchdog/evaluate.rs`, `exec/timeouts.rs::detect_step_timeout`, `TimeoutConfig`, `HarnessPlan::step_timeout`, `LiveMetricsState`/`SilenceOrigin`/`should_warn_stall`, `classify_failure`, `build_attempt_outcome`, and `AttemptOutcome::{exit_code,is_error}` already described the new contract, as expected from findings 1 and 2
                - research-corpus hits and `completions/shell-completions.md`'s "useful from a cold start" are unrelated senses of the words; unchanged
        - **one pre-existing drift found and fixed by the orchestrator:** `non-interactive-sessions.md:412` claimed a **5-second** SIGTERM→SIGKILL grace. The real default is **10s** (`TimeoutConfig::default().kill_grace`, overridable via `CLAUDINE_KILL_GRACE`), which is what `timeouts.md` and `signal-handling.md` already said. Corrected to 10 seconds with the env var named. Per the repo drift rule the code is authoritative; the subagent had left it as out-of-scope, but it sits two lines from a row this change edited and would have been re-read as current
        - **one item deliberately not done:** the "per-step `provider_status` grace" *label* was left alone even though `step_in_flight`, not `provider_status`, is the gate today (`provider_status` is now read only to word the breach). Two other docs refer to it by that name; renaming ripples beyond this fix. The bullet's **body** was corrected instead, so no reader is misled about behavior
- work completed for 'Finding 3: regression evidence and documentation' at 15:59:16-07:00

### Verification

- final gates, all run from `claudine/` through the canonical `just` recipes (never `cargo test`), unpiped so no exit code was masked:
        - `just lint` — **exit 0**
        - `just test` — **exit 0**; **6829 tests run, 6829 passed (25 slow), 11 skipped**
        - `just test-l2` — **exit 0**; `claudine-cli` **236 run, 236 passed, 2497 skipped** and `claudine-gen` **3 run, 3 passed, 155 skipped**
- `git diff --check` clean — no whitespace errors or conflict markers
- two flakes were observed and each was proven to be a host-load artifact rather than a regression, by re-running the test in isolation and then the full suite again:
        - `composition::sequence::preflight::tests::shell::bracket_target_identity_is_rejected_on_every_graph_shell_surface` hit the 30 s nextest timeout on one `just test` run; it passes in **3.66 s** in isolation and the full suite is green. It is a shell-preflight test with no relationship to anything this change touched
        - `level2_lifecycle_control::level2_shipped_implement_plan_supplied_commit_message_runs_exact_commit_branch` failed once on a loaded `just test-l2` run (fast assertion failure at 1.5 s, with the captured pane showing a sibling test's expected content — the signature of cross-test pane interference under the parallel self-spawn path). It passes in **1.30 s** in isolation, and the immediately following full L2 run was **236/236 green**
- `just test-l2` **was** run for this cycle even though the two earlier subagents had each declined it. Their reasoning was sound on its own terms — the timeout and lifecycle semantics are L1, and the new diagnostic makes no claim about terminal-specific layout, styling, glyph widths, or scrolling — but the review names the package's canonical `just test-l2` as a required regression gate under acceptance criterion 13, so it was run as a regression gate rather than as feature-specific evidence
- GitNexus `detect_changes` was not run: nothing is being committed in this task, and the repository rule scopes that check to pre-commit

## Successful Completion

The implementation of review cycle 1 has completed successfully in 1 hour 48 minutes. During this implementation all 3 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 3 were fixed, 0 were deferred.

No finding was deferred, and no performance measurement was required by this review, so `deferred_perf_measurement` remains `false`.

One item inside Finding 3 was resolved by **correcting the specification rather than implementing it**, using the latitude the review itself granted: spec §5 required updating `claudine/docs/topics/opencode-event-sources.md`, a file that has never existed because that document was authored skill-only by design in the 2026-05-12 `opencode-stderr-returns` fix. The required-document list was corrected to name only `.claude/skills/claudine/opencode-event-sources.md`, and that file was updated.

One deviation from the specification's literal wording is carried forward from Finding 2 and still **needs Ken's ruling**: spec §4 asks for `err.kind = "incomplete_subagents"`, but in this repository `err.kind` is the deprecated alias for the diagnostic *category*. `incomplete_subagents` maps to no locked catalog code, so it projects as `err.variant == "incomplete_subagents"` with `err.kind == "LifecycleAction"` — the same shape every unmapped `error_kind` already has. Adding a catalog row would touch the locked registry and `error_guards`, which is outside this fix's scope.

## Implementation of Review Findings #2

> **started at:** 2026-09-07T16:27:15-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/fix-cli-slow-tests/claudine/fixes/2026-08-31-silent-success-and-startup-stall/review-2.md'
- this is iteration 2 of the review-to-implement cycle
- review scope: six findings — one Critical, four High, one Medium
        - **Finding 1 (Critical)** — `both_clocks_stale` suppresses an OpenCode mid-step breach forever when one activity clock is populated-but-stale and the other is absent
        - **Finding 2 (High)** — unknown `task_notification` terminal statuses are downgraded to `Info` and their raw status is lost
        - **Finding 3 (High)** — `error_kind`/`err.kind` identity for incomplete sub-agents does not match the ratified specification
        - **Finding 4 (High)** — the startup-termination deadline is never asserted by the process-level test (60 s outer timeout only)
        - **Finding 5 (High)** — the full terminal diagnostic has no feature-specific Level 2 verification
        - **Finding 6 (Medium)** — the required Claudine skill summary was not updated
- orchestration strategy: one subagent per finding, dispatched **serially**, because findings 2 and 3 both land in the ledger/parser surface and finding 5 depends on the diagnostic shape finding 3 settles
- package area under change: `claudine/` (library `claudine`, CLI `claudine-cli`); verification gates are `just test`, `just test-l2`, and `just lint` run from `claudine/`
- starting the work on 'Finding 1: one-clock-stale OpenCode suppression' at 16:28:20-07:00
        - design decision: replaced the double-staleness *inference* with an explicit positive freshness predicate. Freshness is a property of a clock that **exists** — an absent clock is never fresh — so the old `!both_clocks_stale` reading (which returned `false` for every single-clock state and was then read as "recent activity") is gone rather than patched
        - exact predicate now in place (`claudine/cli/src/commands/wrap/exec/watchdog/evaluate.rs:155-163`): `let is_fresh = |clock: Option<Instant>| clock.is_some_and(|t| now.saturating_duration_since(t) < budget); let any_clock_fresh = is_fresh(last_event_at) || is_fresh(last_byte_at);` and the arm is now `provider == OpenCode && step_in_flight && any_clock_fresh`. Both clocks absent ⇒ not fresh ⇒ **not** suppressed, so a cold start inside an open step is bounded by the spawn-anchored silence budget
        - untouched, as required: the wall-clock rule and its precedence, the stuck-aware `any_active && !any_stuck` suppression, `silence_reference`, and every breach-message format
        - **observation carried forward for Ken's ruling:** under the ratified predicate this OpenCode arm is now logically *inert*. `silence_reference = max(started_at, last_event_at, last_byte_at)`, so `any_clock_fresh ⇒ silence < budget ⇒ no breach` on the general path anyway. The arm was kept because spec §1 ratifies it and it documents intent, but no test can isolate `step_in_flight` as the deciding factor any more. Deleting it outright is a separate, spec-touching decision
        - tests added (all in `cli/src/commands/wrap/exec/watchdog/tests/opencode.rs`):
                - `opencode_mid_step_breaches_when_event_clock_is_stale_and_byte_clock_absent` — the missing permutation the review named
                - `opencode_mid_step_breaches_when_byte_clock_is_stale_and_event_clock_absent` — its mirror
                - `opencode_mid_step_suppresses_when_the_only_event_clock_is_fresh` and `opencode_mid_step_suppresses_when_the_only_byte_clock_is_fresh` — counter-cases proving the predicate did not collapse into "always time out"
                - `opencode_mid_step_breaches_when_neither_clock_exists` — cold start *inside* an open step; also asserts the spawn-anchored wording `no output since the wrapped process launched`
        - two existing tests encoded the defect and had to move, not merely be joined: `opencode_step_in_flight_suppresses_silence` was **retired** (its setup — stale event clock, absent byte clock, `step_in_flight` — is exactly the defective state, so it became the regression case above), and `opencode_step_in_flight_resets_per_step` now feeds each `step_start` through `observe_event` at the current instant, because suppression legitimately needs an open step **and** a fresh clock; its `step_finish → breach` half is unchanged
        - non-vacuity (AC12): mutation = restore the old `both_clocks_stale` match and bind `any_clock_fresh = !both_clocks_stale`. The focused watchdog run went from **72 passed** to **64 passed / 3 FAILED**, the three being exactly the new breach-direction tests. The two fresh-clock counter-cases passed under both, as expected — they are direction proofs, not regression proofs. Mutation reverted; no `both_clocks_stale` residue remains in source
        - `claudine/docs/providers/dispatch-inventory.json` regenerated (`CLAUDINE_UPDATE_INVENTORY=1`), `sites` 1481 → 1485 (5 added − 1 retired `Provider::OpenCode` mention)
        - gates from `claudine/`, unpiped: `just lint` **exit 0**; `just test` **exit 0** — **6833 run, 6833 passed (15 slow), 11 skipped** in 60.0 s (+4 against review-2's 6,829 baseline). `just test-l2` not run for this finding: no terminal-rendering claim
- work completed for 'Finding 1: one-clock-stale OpenCode suppression' at 16:45:00-07:00
- starting the work on 'Finding 2: unknown task_notification statuses' at 16:45:18-07:00
        - root cause confirmed: `handle_task_notification` routed to terminal handling only when `terminal_outcome_for_status` recognized the status, so *every* unknown status fell through to `handle_task_progress` and became a status-free `SemanticEvent::Info` — the raw provider word was discarded and a previously started task degraded to merely `Unfinished`
        - design decision: the review offered two remedies (preserve-and-fail-closed, or specify an explicit nonterminal vocabulary). **Both were implemented together**, which is the only reading that satisfies spec §2's "preserve unknown terminal status strings … surface it as unresolved" without also failing every future progress word
        - a **third explicit vocabulary** `PROGRESS_STATUSES` now sits in `stream/task_ledger.rs` beside `SUCCESS_STATUSES` and `UNSUCCESSFUL_STATUSES`, behind **one seam** — `route_notification_status(Option<&str>) -> NotificationRouting { Progress | Terminal(TaskOutcome) }` — rather than three `contains` lookups reimplemented at the `claude.rs` call site
        - ratified four-branch routing rule (case-insensitive, trim-normalized):
                1. status **absent or whitespace-only** ⇒ `Progress` (`Info`). Nothing is lost — the provider said nothing to preserve
                2. status in the successful or unsuccessful vocabulary ⇒ `Terminal`, preserving ID, name, and raw status (unchanged)
                3. status in the progress vocabulary ⇒ `Progress` (`Info`)
                4. status **present but in none of the three** ⇒ `Terminal(TaskOutcome::UnknownStatus)`, raw string preserved verbatim in the ledger and in `SubagentOutcome.raw_status`; poisons success, reported unresolved. **This branch is the behavior change**
        - progress allowlist chosen: `thinking`, `running`, `in_progress`, `progress`, `started`, `starting`, `pending`, `queued`, `working`, `active`, `resumed` — each a word whose plain meaning is *still going*, so reading it as abandoned would be wrong. Deliberately small and extended with fixtures, never widened to silence a word whose meaning is genuinely unknown
        - `terminal_outcome_for_status` was retired as a public function; its two jobs are now private `normalized_status` + `recognized_terminal_outcome` helpers shared by the new router and by the unchanged `forced_terminal_outcome`. The inherently-terminal `task_completed` path is untouched
        - comment drift fixed in the same change: `UNSUCCESSFUL_STATUSES` claimed membership "is what makes the observation terminal at all" for `task_notification`, which the new rule falsifies — deleted
        - **spec amended**, as the review required before any status may be downgraded: §2 now states the three vocabularies and all four branches explicitly, and acceptance criterion 5 requires fixtures for each branch
        - documentation: `claudine/docs/topics/non-interactive-sessions.md` gained the three-vocabulary paragraph; `.claude/skills/claudine/timeline.md` carried the now-drifted claim that only records "**with a terminal status**" stopped collapsing into `Info` and was reconciled and re-hashed with `md hash --save`. `.claude/skills/claudine/summaries/non-interactive-sessions.md` carries no notification-status claim
        - tests added — `stream/task_ledger/tests.rs`: `an_absent_or_blank_notification_status_routes_to_progress`, `every_progress_word_routes_to_progress` (iterates the whole constant), `a_present_but_unrecognized_notification_status_is_terminal_and_unresolved`; two existing tests retargeted onto the new seam. `unknown_and_absent_statuses_are_not_terminal_on_their_own` was **replaced** — its premise is exactly the defect
        - tests added — `stream/providers/claude/tests.rs`: `a_task_notification_with_no_status_remains_info_and_stays_clean`, `an_unrecognized_notification_status_fails_closed_after_a_start`, `an_unrecognized_notification_status_enrolls_a_fact_without_a_prior_start`. The `thinking` test was renamed `a_task_notification_with_an_explicit_progress_status_remains_info` so it says *why* it stays `Info`
        - `cli/tests/wrap_incomplete_subagents.rs` untouched and still green — the routing change did not alter its expectations
        - non-vacuity (AC12), two mutations, both reverted:
                - branch 4 forced back to `Progress`: `a_present_but_unrecognized_notification_status_is_terminal_and_unresolved`, `an_unrecognized_notification_status_fails_closed_after_a_start` (`["subagent_start","info"]` vs `["subagent_start","subagent_stop"]`), and `an_unrecognized_notification_status_enrolls_a_fact_without_a_prior_start` FAILED
                - branch 3 allowlist check deleted: `every_progress_word_routes_to_progress` and `a_task_notification_with_an_explicit_progress_status_remains_info` FAILED, while the branch-1 test correctly stayed green
        - gates from `claudine/`, unpiped: `just lint` **exit 0**; `just test` **exit 0** — **6838 run, 6838 passed (10 slow), 11 skipped** (baseline 6833; +5). No inventory regeneration needed
- work completed for 'Finding 2: unknown task_notification statuses' at 16:57:00-07:00
- starting the work on 'Finding 3: incomplete-subagent machine identity' at 16:58:19-07:00
        - the finding splits cleanly in two, and **both halves landed** — including the `err.kind` item that iteration 1 had escalated as needing Ken's ruling
        - **Part A — `error_kind` precedence is now unconditional.** `TaskLedger::apply_to_summary` sets `error_kind = "incomplete_subagents"` and `error_message` whenever `incomplete_outcomes()` is nonempty; the `if summary.error_kind.is_none()` guard is deleted. `exit_code`, `ProcessTermination`, and the `subagent_outcomes` projection stay untouched
                - the displaced provider failure is **not discarded** — `error_message` is the only place its text survives once the slot is overwritten, so the headline now names both: `"<N> sub-agent tasks did not complete; after provider failure <kind>: <text>; incomplete: <list>"`. The no-prior-failure wording is byte-for-byte unchanged, so existing headline assertions still hold
                - ordering is load-bearing: count (the identity) → displaced clause → task list. `concise_message` clamps from the tail, so the part that gives way is the task list, whose complete form is in `subagent_outcomes` by contract. The displaced clause is separately budget-reserved at 96 chars — mirroring how `sanitize_message` reserves the `(attempt N)` suffix — so an arbitrarily long provider message can never crowd out every task name. The provider text is hygiened *before* the clamp, so a cut cannot land inside an ANSI sequence
        - **Part B — the specified `err.kind` was unachievable as written, and the contract has been corrected rather than waived.** In this repository `err.kind` is the deprecated alias for the diagnostic `Category`, a closed 12-value taxonomy; making `incomplete_subagents` a category would corrupt it for every other error. Rather than leave the failure facet-less (`err.kind == "LifecycleAction"`), the failure was made **properly classifiable**:
                - new locked-catalog row `provider.incomplete_subagents` in `diagnostics/registry.rs`, in the `provider` group after `provider.context_pressure`
                - `disposition: Unrecoverable` — this is what encodes spec §4's ratified fail-fast rule. `Transient` would invite a generic retry handler to loop a run that will abandon the same work again; `Correctable` would promise a fix the operator does not have. Same reasoning the `runaway.*` rows already use
                - `origin: Provider` (the provider abandoned its own work), `detail: ["provider", "incomplete_count"]`, severity defaulting to `error`
                - `"incomplete_subagents" => "provider.incomplete_subagents"` mapped in `code_for_error_kind`; verified `badge_category_for_kind` matches exact strings, so the label cannot be intercepted by the cap/auth half
                - end-to-end consequence: the failure stack now sees `err.code == "provider.incomplete_subagents"`, `err.category == "provider"`, `err.disposition == "unrecoverable"`, `err.origin == "provider"`, and the mirrored aliases `err.kind == "provider"` / `err.variant == "provider.incomplete_subagents"`. The `LifecycleAction` fallback is gone
        - catalog guard obligations satisfied without weakening any of them: `catalog_covers_the_ratified_count` 44 → 45 (provenance comment extended in the file's style); `every_mapped_code_is_in_the_locked_catalog` gained the new kind; the prefix/uniqueness/detail-shape/grouping/`errors.json` coverage guards all pass unmodified. The `error_guards` corpus guards impose no obligation — they key on codes a production `impl Diagnostic` *claims*, and this code is reached only through the synthesized-label seam. All 18 pass unmodified
        - **spec §4 amended** to the two-identity contract: the stable *machine* identity remains `summary.error_kind == "incomplete_subagents"` carried into `session_end` beside `extra.subagent_outcomes`; the *lifecycle-stack* identity is `err.code == "provider.incomplete_subagents"` under `err.category == "provider"`, and a `when:` clause is told to pin `err.code`. A closing paragraph records why the original wording was not implementable
        - `claudine/docs/topics/non-interactive-sessions.md` — the paragraph that presented the unratified precedence deviation as the settled contract now describes the unconditional rule, the retained displaced failure, and the budget reservation; a second paragraph states the lifecycle-stack identity. No skill snapshot carried the precedence claim
        - tests added: `incomplete_work_displaces_an_earlier_provider_error_kind_but_keeps_its_text` (replaces `a_more_specific_provider_error_kind_survives_ledger_finalization`, same fixture, inverted expectation), `a_displaced_provider_failure_without_text_is_still_named`, `the_composed_message_still_honors_the_concise_message_contract` (hostile 500-char multi-line ANSI-bearing displaced text plus 30 long-named stopped tasks; asserts single-line, escape-free, ≤240 chars, count-first, displaced failure named, and carries its own assertion that the list genuinely truncated), `incomplete_subagents_is_unrecoverable_provider_work`, `incomplete_subagents_classifies_as_unrecoverable_provider_work`, `action_failure_error_kind_projects_incomplete_subagent_facets`
        - `cli/tests/wrap_incomplete_subagents.rs` — the replay document's `failure` stack now emits `failure-code=` and `failure-category=` in place of `failure-variant=`, and the end-to-end test pins `provider.incomplete_subagents` + `provider`. Its `session_end` assertions (`exit_reason == "incomplete_subagents"`, complete `extra.subagent_outcomes`) are unchanged and still pass
        - non-vacuity (AC12), two mutations applied one at a time and each restored:
                - precedence guard restored ⇒ all three new ledger tests FAILED (19 passed / 3 failed in the `task_ledger` filterset)
                - `"incomplete_subagents"` arm removed from `code_for_error_kind` ⇒ `incomplete_subagents_classifies_as_unrecoverable_provider_work`, `every_mapped_code_is_in_the_locked_catalog`, and `action_failure_error_kind_projects_incomplete_subagent_facets` FAILED, plus the end-to-end `stopped_subagents_fire_the_failure_stack_and_never_the_success_stack`
        - gates from `claudine/`, unpiped: `just lint` **exit 0**; `just test` **exit 0** — **6843 run, 6843 passed, 11 skipped** (baseline 6838; +5). No inventory drift
- work completed for 'Finding 3: incomplete-subagent machine identity' at 17:17:00-07:00
- starting the work on 'Finding 4: assert the startup termination deadline' at 17:18:40-07:00
        - measured the cost structure before writing any assertion rather than assuming it: both terminating tests were instrumented with an `Instant` around the spawn plus a dump of the synthesized `session_end` row, then run 8× in isolation and once inside the full 6843-test parallel suite
        - key discovery: `session_end.extra.duration_ms` is, for these fixtures, the wrapper's own `started_at.elapsed()` fallback, and `started_at` is taken immediately before `command.spawn()` — **the same instant the silence rule anchors on**. That makes it a direct measure of the in-wrapper deadline, isolated from the test harness's own startup cost. The fake providers never reach a provider summary, so the provider-reported branch never wins
        - cost breakdown found: whole-command wall clock 2265–3259 ms; in-wrapper spawn→reap 2116–2416 ms; wrapper startup + preflight + composition + model discovery + post-run rendering 149–843 ms. Detection alone lands at 2.0–2.2 s, and the 500 ms kill grace is a ceiling these `/bin/sh` fixtures never consume — they die on SIGTERM
        - chose to assert **the reaped child**, on two quantities: `extra.duration_ms` as the tight primary gate (excludes harness cost, ~300 ms spread) and wall-clock elapsed as a secondary gate so a regression that somehow bypasses the summary still cannot hide. Detection alone is not assertable at L1 — the wrapper only exposes it at whole-second granularity in prose — and a raw 2.2 s detection bound would not be safe against the reap anyway
        - **spec clarified**, which is what the review asked for first: Required behavior §1 gains a bullet stating that the `step_timeout` + one-watchdog-interval bound governs **detection and the termination request**, and that `kill_grace` sits *outside* it, so final reap is bounded by `step_timeout + one watchdog interval + kill_grace`. Acceptance criterion 1 now names which event each bound applies to and records that integration coverage asserts the reap, with the outer process timeout retained only as a hang backstop
        - bounds committed, derived from named constants so changing a budget literal fails loudly: `REAP_DEADLINE_MS = 2700` (2000 + 200 + 500); in-wrapper gate 3000 ms against an observed 2116–2416 ms (~600 ms headroom); wall-clock gate 4700 ms against an observed 2265–3259 ms. The 60 s `.timeout(...)` survives **only** as a hang backstop, and the test's doc comment now says so
        - tests changed: `watchdog_startup_stall_terminates_without_a_wall_clock_timeout` and `watchdog_opencode_stall_before_first_step_is_bounded`, both through a shared `assert_reaped_within_deadline`. The three non-terminating siblings were left untouched; every assertion stays platform-neutral (no signal numbers) and nothing gains focus
        - non-vacuity (AC12): raising `CLAUDINE_STEP_TIMEOUT` from `2s` to `6s` made both tests fail as **assertion failures on the bound**, not outer timeouts — `the child lived 6128ms; the ratified reap deadline is 2700ms … plus 300ms of scheduling slack` — finishing in 6.39 s, far inside the 60 s backstop. The wall-clock arm is shadowed by the in-wrapper arm, so it was proven separately by short-circuiting the in-wrapper assert under the same mutation. Both mutations reverted
        - **caveat carried forward:** every timing sample is from one 16-core macOS host on a debug profile. The 2000 ms process slack is an estimate for a cold or contended CI runner, not a measurement. If the Linux or Windows legs ever approach 4.7 s, the honest fix is to lean harder on the in-wrapper assertion — which is load-insensitive by construction — rather than to inflate the wall-clock allowance
        - gates from `claudine/`, unpiped: `just lint` **exit 0**; `just test` **exit 0** — **6843 run, 6843 passed, 11 skipped** (unchanged; this finding adds assertions, not tests). Focused reruns 6/6 green
- work completed for 'Finding 4: assert the startup termination deadline' at 17:32:00-07:00
- starting the work on 'Finding 5: Level 2 verification of the full terminal diagnostic' at 17:31:48-07:00
        - new binary `claudine/cli/tests/level2_incomplete_subagents_capture.rs` drives the real end-to-end wrapped run — fake provider replaying the incident NDJSON, native exit 0 — inside a **tmux pane**, and asserts against the captured pane, not a pipe
        - backend and width: tmux at **70×200** with `COLUMNS=70` for the child. tmux is the portable headless backend and is immune to the "which terminal am I running inside" gate; the diagnostic uses no OSC 8 or graphics, so a GUI backend would add spawn cost and focus risk without adding coverage. 70 is the documented minimum-supported-width floor. The pane and `COLUMNS` are deliberately the *same* 70, so the emulator's wrap boundary is the width the renderer sized to
        - wrapping was **forced and then verified**, not assumed: seven stopped tasks whose `describe()` renders at 91 cells against a ~68-cell content budget. Every marker row must be followed by a non-empty row starting with the 2-column hanging indent, and every list row must fit within 69 cells
        - assertions: exit code 1 through the pane; the heading count sentence; the explanatory sentence; that the two arrive contiguous; **all seven** task entries (above the five-entry `recent_subagents` ring, so criterion 7's ledger-completeness is witnessed in a real terminal); exactly one marker row per task; the hanging-indent continuation; the list width bound; and the heading's `<b>` surviving as `ESC[1m`
        - the capture makes the ledger-completeness contract *visible*: the concise `⤫` lifecycle headline truncates at `commit-agent-2 st…` under its 240-character budget, while the full diagnostic directly above it names all seven
        - **the L2 test found a real rendering defect, which is exactly why the review demanded it.** `Prose::new(...)` carries `Layout::default()`, which hand-writes `word_wrap: WordWrap::None` (not `WordWrap::default()`), so the 113-cell heading reached the pane unfolded and the *emulator* soft-wrapped it mid-word: `…so t` / `his run is a failure…`. The list beneath it wrapped correctly the whole time
                - fixed with `.with_word_wrap(WordWrap::default())` on the heading. Sibling components already do exactly this — `render/task_stream.rs:154` carries an inline comment naming the identical `Layout::default()` trap, and `event_renderer/error_block.rs` and `final_message.rs` use the equivalent `WrapProse(None, None)`. `IncompleteSubagents` was the outlier, not a new convention
                - the L2 test had initially *pinned the defect* as an observed fact (asserting the heading row was exactly the pane width, plus a helper that undid the emulator's soft wraps). Both were replaced by a word-boundary assertion: the heading occupies more than one row, **no** heading row reaches the full pane width, and rejoining the heading rows on a single space reproduces the sentence pair exactly — which only holds if every break landed between words
                - captured before: `…exited normally, so t` / `his run is a failure…` (70 of 70 cells). Captured after: `…exited normally, so` (68) / `this run is a failure despite its exit code.` (44)
                - noted but **not** changed under scope discipline: `render/metrics_report.rs` uses bare `Prose::new(...)` for short single-line headings. They are well under any pane width so the defect does not manifest, but they carry the same latent shape
        - **question raised by the review and answered honestly rather than papered over:** the review asked the L2 test to assert "the heading, remediation, and every task entry". The component carries **no distinct remediation copy** — its second sentence explains the verdict ("The provider exited normally, so this run is a failure despite its exit code."), it does not tell the operator what to do. Spec §4 never required remediation text. The test asserts that sentence *as the explanation*. Adding remediation copy is a product decision and was deliberately not invented here — flagged for Ken below
        - L2 hazards handled from the documented list rather than rediscovered: no scrollback → 200-row pane plus a sentinel anchored with `rfind` so the shell's own wrapped echo cannot be mistaken for it; `FORCE_COLOR=1` on both pane and child, with an ambient `NO_COLOR` explicitly cleared (claudine treats it as *present*, not truthy, and it would out-vote `FORCE_COLOR` and make the styling assertion vacuous); tmux re-emits `ESC[1m` faithfully so WezTerm's SGR collapsing is not in play; sessions are detached and headless so **nothing gains focus** (AC11); the fake provider is a `/bin/sh` script in the fixture `bin`, never a production binary; the binary under test comes from `common::claudine_bin()`, never a bare `claudine` off the host `PATH`
        - a timeout hazard was found and fixed during development: an initial 90 s internal deadline collided with the 90 s nextest termination ceiling for `package(claudine-cli) & test(/level2_/)`, so nextest killed the test mid-poll, the panic never fired, and the tmux session leaked. The deadline is now 40 s, matching the convention that config documents; no session leaks afterwards
        - the incident NDJSON fixture, fake-provider writer, and compose documents were factored into `cli/tests/common/incomplete_subagents.rs` and are now shared by the L1 and L2 binaries. `wrap_incomplete_subagents.rs` keeps its own task identities (which target the 240-character headline budget) while L2 keeps its own (which target the 70-column wrap); L1 stream order is unchanged
        - non-vacuity (AC12), two rounds:
                - `IncompleteSubagents::render`'s item list truncated with `.take(1)`: the L2 test FAILED with `still missing ["• commit-agent-1 …", … "• commit-agent-6 …"]` and the full pane in the message. Proven not to be a silent skip — the neutered run executed the body, and a `--success-output=immediate` run showed `test result: ok. 1 passed` with no `skipping: requires` line
                - the wrap setting reverted to `WordWrap::None`: the new L1 `the_heading_wraps_on_word_boundaries` FAILED with the heading as a single 113-cell line, and the L2 test FAILED. Because the L2 width assertion is shadowed by an earlier one, the three earlier `flat.contains` asserts were additionally short-circuited to prove it load-bearing on its own — it then failed with `a heading row filling the pane is the emulator's mid-word soft wrap … row used 70 of 70 cells`
        - the binary is `#![cfg(unix)]` like every tmux-backed neighbor (the replay provider is a `/bin/sh` script); the shared fixture keeps its Windows `.cmd` branch for the L1 binary. No assertion names a signal number, path separator, or platform glyph
        - gates from `claudine/`, unpiped: `just lint` **exit 0**; `just test` **exit 0** — **6844 run, 6844 passed, 11 skipped**; `just test-l2` **exit 0** — claudine-cli **237 run, 237 passed, 2501 skipped** and claudine-gen **3 run, 3 passed, 155 skipped**. The new binary ran rather than skipped: `PASS [1.266s] (61/237) claudine-cli::level2_incomplete_subagents_capture level2_incomplete_subagent_diagnostic_renders_in_tmux`. The known `level2_lifecycle_control` load flake did not trip
        - note: `just lint` does **not** enable `terminal-tests`, so the new binary is not linted by the canonical recipe. It was linted explicitly with `cargo clippy -p claudine-cli --all-targets --features terminal-tests -- -D warnings`, which was clean
- work completed for 'Finding 5: Level 2 verification of the full terminal diagnostic' at 18:16:00-07:00
- starting the work on 'Finding 6: the required Claudine skill summary' at 18:17:54-07:00
        - **source structure investigated empirically, and the iteration-1 premise turned out to be wrong.** `claudine/docs/research/summary/non-interactive-sessions.md` is *not* prompt-only: its frontmatter holds the `sequence` and the `prompt`, but its **body is durable, hand-editable content** that `md compose` passes through verbatim. Proven by running `md compose` on the source and diffing against the published skill file — **byte-identical** except one trailing blank line the publish recipe strips
        - so what is generated is the *source body* (by a `claudine sequence` run), not the published file; the published file is a deterministic, provider-free `md compose` projection of a checked-in source. A hand edit to the source **survives** every `just publish-summary-research`. Iteration 1 stopped one level too early
        - **resolution (A) chosen — correct the source and regenerate** — over revising spec §5, because it satisfies the specification literally *and* survives republication
        - one genuinely drifted Claudine-facing recommendation was found and corrected: the Claude paragraph said "Claudine should treat `result` as semantic completion, use process exit as a consistency check". It now says `result` is the turn's **terminal** event but not proof of success on its own — it carries its own `is_error` verdict, and Claude can force-stop a background task, report `status: "stopped"`, finish the parent turn, and still exit 0; semantic completion requires `result` **plus** a clean `is_error` **plus** a terminal observation for every sub-agent task the stream started
        - two supporting corrections in the same voice: comparison axis 4 (exit semantics) gained the case the incident exposed — whether a terminal event *that reports success* can be trusted on its own — and the shared parser invariant now adds that a terminal event marks the end of *a* turn, not a *successful* one
        - the rest of the document was swept and no other Claudine-facing claim was falsified. Qwen's `subtype=success`/`is_error=false` line is a provider fact, not a recommendation
        - **regeneration obligation recorded where the existing convention says it belongs.** The source body *is* LLM-regenerated by its own sequence, so a body edit alone would be lost at the next research refresh. There is no refresh-notes convention under `claudine/docs/research/`; the only precedent (`features/_completed/2026-07-02-provider-metadata/model-config-refresh.md`) encodes corrections as **prompt iteration**. That precedent was followed rather than inventing a file: a dated **"Standing correction"** rule was added to the source's frontmatter `prompt`, naming the falsified claim, the `status: "stopped"` + exit-0 shape, and `docs/topics/non-interactive-sessions.md` → "Exit Code 0 Is Not Success" as authoritative. The next regeneration carries the correction forward instead of reverting it
        - `just publish-summary-research` republished the skill summary; `md set … last_updated 2026-09-07` and `md hash --save` restamped the source frontmatter. The published file carries no frontmatter, so it has no hash to refresh
        - **pre-existing breakage found in the publish recipe, deliberately not fixed here (out of scope, flagged below):** `just publish-summary-research` publishes 12 files then **aborts under `set -euo pipefail`** on `docs/research/summary/plugins.md`, whose frontmatter carries a self-referential `file: docs/research/summary/plugins.md` under an `$schema: file(required;eager)` that cannot resolve relative to its own directory. Untouched since commit `32969a1a2` (2026-07-09). `non-interactive-sessions.md` sorts *before* `plugins.md`, so this fix's republication completed; the four sources sorting after it are unmodified, so nothing was lost
        - audit of the authoritative `claudine/docs/topics/non-interactive-sessions.md` against ratified spec §1/§2/§4: **complete, no edits needed.** All six required contracts verified present and correct — spawn-bounded startup silence with no first-event grace, exit 0 not proof of success with `is_error` routing to `AgentFailure`, the three status vocabularies plus the fail-closed residue, the unconditional `error_kind` precedence with the displaced provider failure retained, the lifecycle identity `err.code == "provider.incomplete_subagents"`, and `session_end.extra.subagent_outcomes` as the stable machine contract
        - the review's tail concern — "avoid presenting the unratified `error_kind` precedence deviation as the settled contract" — is resolved **by ratification, not by softening the prose**: Finding 3 amended spec §4 to make the precedence unconditional, so the topic doc's presentation is now backed by the spec
        - drift sweep for `first-event grace` / `cannot fire` / `unbounded` / `semantic completion` across the topic doc and the published summary: **no hits**
- work completed for 'Finding 6: the required Claudine skill summary' at 18:22:30-07:00

### Verification

- final gates, all run by the orchestrator from `claudine/` through the canonical `just` recipes (never `cargo test`), unpiped so no exit code was masked:
        - `just lint` — **exit 0**
        - `just test` — **exit 0**; **6844 tests run, 6844 passed (1 slow), 11 skipped** (review-2 baseline was 6,829; +15 across the six findings)
        - `just test-l2` — **exit 0**; `claudine-cli` **237 run, 237 passed, 2501 skipped** and `claudine-gen` **3 run, 3 passed, 155 skipped**
- `git diff --check` — clean, no whitespace errors or conflict markers
- no flakes were observed in the final gate runs. The `level2_lifecycle_control` load flake recorded in iteration 1 did not trip in any run this cycle
- `just test-l2` now carries **feature-specific** evidence for the first time: `level2_incomplete_subagents_capture` ran (`PASS (61/237)`), which is precisely what Finding 5 said was missing
- GitNexus `detect_changes` was not run: nothing is being committed in this task, and the repository rule scopes that check to pre-commit

## Successful Completion

The implementation of review cycle 2 has completed successfully in 1 hour 58 minutes. During this implementation all 6 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 6 were fixed, 0 were deferred.

No finding was deferred, and no performance measurement was required by this review, so `deferred_perf_measurement` remains `false`. Finding 4 did require *timing* measurement, and that measurement was taken successfully — nine isolated runs plus two under the full parallel suite — so it is not a deferred performance measurement. Its one honest limitation is recorded in that finding's entry: the 2000 ms process-slack allowance is an estimate for a cold or contended CI runner rather than something measurable on this 16-core macOS host.

Three findings were resolved by **amending the ratified specification as well as the code**, in each case using latitude the review itself granted, and in each case because the specification asserted something that could not be implemented as literally written:

- **Finding 2** — spec §2 said a `task_notification` "with a terminal status" becomes a terminal observation, without defining what makes a status nonterminal. §2 now ratifies three explicit vocabularies and a fail-closed residue, which is what the review demanded before any status may be downgraded.
- **Finding 3** — spec §4 asked for `err.kind = "incomplete_subagents"`. In this repository `err.kind` is the deprecated alias for the diagnostic `Category`, a closed 12-value taxonomy, so the literal wording was unachievable without corrupting that taxonomy for every other error. §4 now ratifies a two-identity contract — machine identity `summary.error_kind == "incomplete_subagents"`, lifecycle identity `err.code == "provider.incomplete_subagents"` under `err.category == "provider"` — and a new locked-catalog row makes the failure genuinely classifiable with `disposition: unrecoverable`, which is what encodes the ratified fail-fast rule. **This closes the one item iteration 1 escalated as needing Ken's ruling; no open ruling is carried forward.**
- **Finding 4** — acceptance criterion 1 said "terminated within `step_timeout` plus one watchdog interval" without saying which event that bounds. §1 and criterion 1 now state that the bound governs detection and the termination request, and that `kill_grace` sits outside it.

Two items surfaced during the work that are **outside this fix's scope** and are flagged rather than actioned:

- The full terminal diagnostic carries **no distinct remediation copy** — its second sentence explains the verdict rather than telling the operator what to do. Spec §4 never required remediation text, and the Level 2 test asserts that sentence as the explanation. Adding remediation copy is a product decision and was deliberately not invented.
- `just publish-summary-research` **cannot currently run to completion**: it aborts on `docs/research/summary/plugins.md`, whose self-referential eager `file(...)` frontmatter reference cannot resolve. Untouched since 2026-07-09, unrelated to this fix, and it did not block this fix's republication — but the next person to touch a summary source sorting after `plugins.md` will be blocked. It deserves its own fix.

One further observation is recorded for a future ruling rather than acted on: after Finding 1's correction, the OpenCode mid-step suppression arm is provably **inert** — `any_clock_fresh` implies the silence age is inside the budget, so the general rule would decline to breach anyway. The arm was kept because spec §1 ratifies it and it documents intent, but it can no longer be isolated by a test.
