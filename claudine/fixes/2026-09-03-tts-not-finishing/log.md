---
implementation_2: 2026-09-04T02:06:53+01:00
implementation_3: 2026-09-04T03:32:22+01:00
deferred_perf_measurement: false
---

# Log: TTS Not Finishing

## Implementation of Review Findings #2

> **started at:** 2026-09-04T02:06:53+01:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/claudine/fixes/2026-09-03-tts-not-finishing/review-2.md'
- this is iteration 2 of the review-to-implement cycle
- host load at start: load averages 2.93 / 4.42 / 5.53 on a 5-day uptime; no performance measurement is required by the review findings
- the review contains 3 findings:
        - finding 1 (High): native device failures cannot reach host fallback (`playa`)
        - finding 2 (High): v1 spool records accept a job without the required executable identity (`playa`)
        - finding 3 (Medium): the background TTS process regressions are Unix-only (`biscuit-speaks`, `claudine`)
- findings will be implemented serially, each by a dedicated subagent that owns its log entries below
- starting the work on 'finding 1: native device failures cannot reach host fallback' at 02:08:25
        - skills activated: `rust`, `rust-testing`, `playa`, `claudine`
        - GitNexus upstream impact (repo `feat-unifi` worktree index): `should_fallback_to_host` — two candidates (`native_player.rs` and `sfx_player.rs`), 0 upstream callers indexed, LOW; `play_native` — 6 direct callers (4 unit tests in `native_player.rs`, `Playa::play_with_report`, `Playa::play_async_with_report`), 10 impacted through depth 3 including the `Detached` module, **HIGH** risk (blast radius is exactly the two production pipelines this finding names plus the detached delegate that calls them; no other consumers); `play_with_report` — 2 direct (detached `execute_job` and free functions), LOW; `play_async_with_report` — 1 direct, LOW; `run_delegate` — 1 direct (`run_if_worker`), LOW
        - discovery: `NativePlaybackError::Play` is `#[from] rodio::PlayError` only; nothing in `native_player.rs` ever produces it (`Player::connect_new` and `append` are infallible), so it can only be pre-submit if it ever appears — classified as fallback
        - discovery: `NativePlaybackError::Timeout` is produced solely by `wait_with_progress`, which runs after `player.append(source)`; it is the only post-submit variant
        - discovery: the SFX path (`sfx_player.rs` `SfxPlaybackError::should_fallback_to_host`, consumed by `effects.rs`) has the same narrow classification (`Decode` only); it is outside this finding and left untouched
        - discovery: no host-player fake exists in `playa` (the spec's `truncated_verdict_warns_once_and_returns_ok` was never implemented); `match_available_players` scans `PATH` fresh on every call via sniff's `CategoryDetector::new()` and `build_player_command` spawns by bare binary name, so a fake `mpv` shell script on a test-private `PATH` drives the real host route end to end — Unix only (Windows `Command::new` will not resolve a `.cmd` stub), so the `Host(..)`-proving tests are `#[cfg(unix)]`
        - discovery: `just test` runs `playa` + `playa-cli` in one nextest invocation; `playa-cli` defaults enable `native-playback` but nothing in that scope enables the lib's `async` feature, so async-pipeline tests do not execute under `just test` (pre-existing gap for the async tests in `playback.rs` too); async tests will be run explicitly with `--features native-playback,async`
        - design: `Playa::attempt_native` becomes the single classification point (sync and async both consume `NativeAttempt`); `resolve_native_attempt` is removed
        - design: test-only native backend seam (`#[cfg(test)]`, process-global `Mutex<Option<Box<dyn Fn>>>`) consulted by `play_native` after the format and breaker checks and before any file/device work; the seam trips the breaker for `DeviceOpenTimeout`/`Timeout` exactly where production does
        - design: spool memo — `native-disabled` marker in the spool root, 10-minute TTL, mtime-based; `run_delegate` forces host when the marker is fresh, removes it when stale, and writes it when the process-local breaker is tripped after the job
        - red-first: with only the test seam added and the classification untouched, `cargo nextest run -p playa --features native-playback,async -E 'test(/native_route/)'` → 8 run, 4 passed, 4 failed — the four failures are the device-open-timeout and stream-failure cases (sync and async), each returning `AudioSubsystem` instead of `Host(Mpv)`, which is the defect the finding describes; the native-OK and post-submit-stall cases were already green
        - implemented: `should_fallback_to_host` classifies by the submission boundary (`Timeout` alone is post-submit and fatal); `Playa::attempt_native` is the single classification point for sync and async; `resolve_native_attempt` removed; `play`/`play_async`/`play_native` docs rewritten
        - implemented: spool memo (`NATIVE_DISABLED_MARKER`, `NATIVE_DISABLED_MEMO_TTL`, `native_disabled_memo_is_fresh`, `record_native_disabled_memo`, `native_breaker_tripped`) and `execute_job(job, force_host)`; `native_disabled_memo_forces_host_only_while_fresh` added to `detached/tests.rs`
        - docs touched: `playa/README.md`, `.claude/skills/playa/SKILL.md`, `.claude/skills/playa/integration.md`
        - green run 1: 9 run, 8 passed, 1 failed — the failure was a wrong assertion in `post_submit_stall_is_fatal_and_never_replays_through_host` (it expected the *second* call after a stall to fail too; the tripped breaker correctly routes that call to the fake host player). Test corrected to assert the follow-up call reaches `Host(Mpv)` without touching the seam
        - green run 2 (targeted, `--features native-playback,async`): 9 run, 9 passed (8 `native_route` tests + `native_disabled_memo_forces_host_only_while_fresh`)
        - `cargo test -p playa --features native-playback,async --lib native_route` (single process, not nextest): 8 passed — the `ENV_LOCK` + native-audio lock serialization holds without process-per-test isolation
        - gate `just lint` (playa + playa-cli, clippy `--all-targets -D warnings`, default features): pass, 0 warnings
        - gate `just test` (playa + playa-cli, one nextest invocation): 164 run, 164 passed, 8 skipped
        - `cargo clippy -p playa --all-targets` (no features): pass, 0 warnings; `--all-features`: 2 pre-existing `needless_return` warnings in `playa/lib/src/ducking/factory.rs:56` and `:139` (untouched file, ducking feature only; CI lints without features) — left alone
        - `cargo test -p playa --doc`: 1 passed
        - full lib suite `cargo nextest run -p playa --features native-playback,async` (no tier filter): 133 run, 129 passed, 4 timed out — the 4 are the `real_` tier tests below
        - gate `just test-real` (verbatim): `Summary [  30.025s] 4 tests run: 0 passed, 4 timed out, 116 skipped` — `real_native_mono_reports_complete`, `real_each_installed_host_player_reports_mono_completion`, `real_zero_volume_stereo_control_reports_complete`, `real_explicit_mpv_mono_reports_complete`, each `TIMEOUT [ 30.025s]`; recipe exit code 100
        - test-real diagnosis: the host audio sink is wedged. `timeout 8 afplay /System/Library/Sounds/Tink.aiff` never returned (exit 124), and `pgrep` showed eight hung `mpv` processes — five from earlier `biscuit-speaks` TTS runs predating this session (left alone) and three spawned by the `real_` tests I ran (reaped). Load 2.97 / 5.05 / 5.84. This is the environment condition memory records as the DragonFly DAC / mpv mono bug, not this change; `PLAYA_REAL_AUDIO_REQUIRED` was not set. Note the behavioral shift on such a host: `real_native_mono_reports_complete` previously gave up at the 5 s device-open timeout, and now correctly falls back to the host route, where the wedged `mpv` hangs until the tier's 30 s slow-timeout
        - `cargo clippy -p playa --all-targets --features sfx-native -- -D warnings` (the memo's `native_breaker_tripped` cfg combination without `native-playback`): pass
        - open item for the orchestrator: `playa/lib/Cargo.toml` has no `[package.metadata.ci.tests]`, so neither `just test` nor CI compiles the lib's `async` feature; the four async `native_route` tests (and the pre-existing async tests in `playback.rs`) run only when `--features async` is passed explicitly. Adding `features = ["native-playback", "async"]` there would close the gap but widens CI scope, so it was not done unilaterally
        - orchestrator follow-up on the open item: enabled the lib's `async` feature for the L1 gate the same way `biscuit-icon`/`messenger` do — `playa/justfile` `test` now runs `_test_all "playa --features native-playback,async; playa-cli"`, and `playa/lib/Cargo.toml` gains `[package.metadata.ci.tests] features = ["native-playback", "async"]` so CI's per-package feature contract matches
        - gate `just test` (playa) after that change: 177 run, 177 passed, 8 skipped — the four async `native_route` tests and the pre-existing async `playback.rs` tests now execute under the standard recipe
- work completed for 'finding 1: native device failures cannot reach host fallback' at 02:34:21
- starting the work on 'finding 2: v1 accepts a job without the required executable identity' at 02:34:21
        - skills activated: `rust`, `rust-testing`, `playa`, `claudine`
        - GitNexus upstream impact: `JobEnvelope` — 2 direct, 14 impacted through depth 3, all in the playa `Detached` module (+1 test), one affected execution flow (`play_detached` in biscuit-speaks `speak.rs`, which only calls playa's public `enqueue`), LOW; `DelegatedReport` — 1 direct, 2 impacted, `Detached` only, LOW; `delegate_job` — 2 direct (`run_scheduler`, `replaced_delegate_executable_is_rejected_before_launch`), 1 at depth 2 (`run_if_worker`), LOW
        - discovery: only `playa/lib/src/detached/{protocol,mod,tests}.rs` name `enqueuer_fingerprint`/`delegate_fingerprint`; biscuit-speaks, claudine, `playa/lib/tests`, and `playa/cli/tests` never hand-write an envelope or report (the CLI test reads a pending record as untyped JSON and inspects only `payload.playback`)
        - discovery: every envelope construction site already computes the fingerprint — `enqueue_state_at` is the single writer for both `Ready` and `Preparing` reservations, so no path creates an envelope before its executable identity is known
        - discovery: `process_pending` already routes a failed typed parse (after the `schema_version` check) to `quarantine(.., None, "malformed_job")` — the record is moved, but with `None` no journal entry is written, which violates the spec's "journaled from its redacted header"; `header_identity` cannot help because it deserializes the *full* `JobEnvelope`, which is exactly what fails for a missing required field
        - discovery: `snapshot()` silently skips a pending record that fails the typed parse (`Err(_) => continue`); that remains correct once the field is required — an unparseable record is invisible to status until the scheduler quarantines it
        - discovery: neither `playa/README.md`, `playa/docs/`, nor `.claude/skills/playa/*.md` enumerate envelope fields; they only state that replaced/incompatible executables fail the job, which stays true — no doc edit required
        - design: `enqueuer_fingerprint` and `delegate_fingerprint` become plain `u64` (no Serde default); `DelegatedReport` gains `PartialEq` for the round-trip test
        - design: a `RedactedHeader` (job id, sequence, `enqueued_at`, source kind) replaces the full-envelope `header_identity`; `quarantine` journals from it, so a structurally incomplete v1 record and an unsupported version take the same quarantine-and-journal path; `journal_failure(&JobEnvelope, ..)` stays as a thin wrapper
        - design: the executable-identity check leaves `delegate_job` for `validate_enqueuer_identity(envelope) -> (PathBuf, u64)` so the delegation-seam test can run the production validation on the shipped artifact without spawning a delegate
        - design: shipped corpus gains `enqueuer_fingerprint`/`delegate_fingerprint` integers (the ready job and its report share one value because they describe the same executable), plus `v1-missing-identity-job.json` (sequence 44) whose only purpose is to be quarantined
        - red-first (types still `Option<u64>`, `validate_enqueuer_identity` extracted as a behavior-neutral seam so the delegation test compiles): `cargo nextest run -p playa --features native-playback,async -E 'test(/detached::tests::(job_envelope_round_trips|delegated_report_round_trips|shipped_v1_protocol_corpus|stale_shipped_ready|shipped_ready_artifact_reaches|missing_executable_identity)/)'` → 6 run, 2 passed, 4 failed — `missing_executable_identity_is_quarantined` failed with the executor called once (`left: 1, right: 0`), i.e. the incomplete record reached delegation; `job_envelope_round_trips_and_requires_the_enqueuer_fingerprint`, `delegated_report_round_trips_and_requires_the_delegate_fingerprint`, and the corpus test's new negative case each failed because the fingerprint-less JSON deserialized to `None`. `stale_shipped_ready_artifact_is_discarded_without_delegation` and `shipped_ready_artifact_reaches_delegation_with_valid_identity` were green before the fix, as expected — the latter proves the complete-record path, which the old model also accepted
        - implemented: `JobEnvelope.enqueuer_fingerprint` and `DelegatedReport.delegate_fingerprint` are required `u64` (no Serde default, always serialized); the dead "fingerprint is missing" runtime protocol error and the `Some(..)` comparisons are gone; `DelegatedReport` derives `PartialEq`
        - implemented: `validate_enqueuer_identity(envelope) -> (PathBuf, u64)` extracted from `delegate_job` (identical checks; `delegate_job` now calls it)
        - implemented: `RedactedHeader` (job id, sequence, `enqueued_at`, source kind) + `header_identity` parsing only that subset from the untyped header, with the source kind projected from `payload` when it parses and `Unknown` otherwise; `quarantine` journals from it, so the typed-parse-failure branch of `process_pending` now journals a `quarantined` transition exactly like `unsupported_schema`; `quarantine_abandoned_in_flight` uses the same path; `journal_failure(&JobEnvelope, ..)` became a wrapper over `journal_header_failure`; `process_pending` deserializes via `JobEnvelope::deserialize(&header)` so the header is borrowed rather than moved (it is still needed for the quarantine journal)
        - fixtures: `v1-ready-job.json` and `v1-delegated-report.json` carry `enqueuer_fingerprint`/`delegate_fingerprint` 6862371333114811590 (same executable, same job 42); `v1-preparing-job.json` carries 3138556841971103717; new `v1-missing-identity-job.json` (job-0000000000000044, sequence 44) is a ready v1 envelope with the fingerprint omitted; no fixtures README exists, none created
        - tests added: `job_envelope_round_trips_and_requires_the_enqueuer_fingerprint`, `delegated_report_round_trips_and_requires_the_delegate_fingerprint`, `shipped_ready_artifact_reaches_delegation_with_valid_identity` (fresh `enqueued_at`, enqueuer rewritten to the test binary with a `fingerprint_file` fingerprint; the executor runs the production `validate_enqueuer_identity` and is called exactly once with job 42 / sequence 42 / the fixture's `PlayFile` payload; journal ends with `completed`), `missing_executable_identity_is_quarantined` (executor never called, record moved to `.quarantine.json`, journal has a `quarantined` entry for job 44 with `source_kind: file` and no path text); `shipped_v1_protocol_corpus_deserializes_and_v2_remains_unsupported` now asserts the missing-identity fixture fails with an error naming `enqueuer_fingerprint` and that the ready job and report share a fingerprint; `shipped_ready_artifact_runs_through_normal_stale_policy` renamed to `stale_shipped_ready_artifact_is_discarded_without_delegation` (body unchanged apart from the shared fixture helper)
        - non-vacuity check: with the typed-parse-failure branch temporarily passing `None` instead of `header_identity(&header)`, `missing_executable_identity_is_quarantined` failed at the journal read ("quarantine should journal from the redacted header"); restored and re-verified
        - green: `cargo nextest run -p playa --features native-playback,async -E 'test(/detached::tests::/)'` → 21 run, 21 passed
        - gate `just lint` (playa): exit 0, 0 warnings
        - gate `just test` (playa): 181 run, 181 passed, 8 skipped (177 → 181: four new tests)
        - gate `cargo clippy -p playa --all-targets` (no features): exit 0, 0 warnings; with `--features native-playback,async`: exit 0, 0 warnings
        - downstream: `cargo check -p biscuit-speaks --all-targets` exit 0 — the changed types are `pub(crate)`, so biscuit-speaks and claudine (which only call playa's public `enqueue`/`reserve`/`publish_*`) are unaffected and their suites were not run
        - GitNexus `detect_changes` (scope all, worktree): 14 changed files, all under `playa/` and the fix directory (findings 1 and 2 combined); one affected execution flow (`play_detached → dry_run_enabled`, a line-shift touch), aggregate risk medium
- work completed for 'finding 2: v1 accepts a job without the required executable identity' at 02:45:38
- starting the work on 'finding 3: the background TTS process regression is Unix-only' at 02:47:13
        - skills activated: `rust`, `rust-testing`, `biscuit-speaks`, `claudine`
        - GitNexus upstream impact (repo `feat-unifi`): `DefaultLifecycleEmitter` — 0 direct, 0 processes, LOW (lower-bound: `LifecycleEmitter` has 7 implementations bound through the trait); `run_if_worker` (biscuit-speaks `detached.rs`) — 0 direct, 0 processes, LOW (exact). Neither symbol's body changes in this finding; only tests are touched
        - discovery: eSpeak readiness is presence-only — `ESpeakProvider::is_ready` is `which::which(&self.binary).is_ok()` (`biscuit-speaks/lib/src/providers/host/espeak.rs:170-172`), and its detached job is a `command_job` (no synthesis helper), so a Windows-runnable stub only needs to exist as `espeak.exe` on `PATH`; it is never executed by either Claudine test (test 1 holds `worker.lock`, test 2's spool root is a file so enqueue fails first)
        - discovery: eSpeak is in the Windows default stack (`detection.rs:37`, `"espeak" | "espeak-ng"` parse at `:141`) with no OS gate, so the `[File, Command, Command, File]` / `[1, 2, 3, 4]` expectations can stay provider-neutral across OSes — no SAPI branch needed
        - discovery: `ESpeakProvider::new()` prefers `espeak-ng` when it is on `PATH`; the tests prepend the fixture dir but keep the host `PATH`, so on a host with `espeak-ng` the readiness lookup finds the real binary instead of the stub — pre-existing behavior, unchanged here
        - discovery: playa's spool root is created with `create_dir_all` on Windows and the `0o700` mode/owner checks are `#[cfg(unix)]` (`playa/lib/src/detached/mod.rs:334-383`), so the claudine test's `0o700` block can stay Unix-only without breaking Windows
        - discovery: sniff's executable index defaults `PATHEXT` to `.EXE;.CMD;.BAT;.COM` when unset (`sniff/lib/src/executable_index.rs:375`), and Rust's std appends `.exe` to an extension-less program name on Windows, so `kokoro-tts.exe` / `mpv.exe` in the fixture dir satisfy both `which::which` readiness and `Command::new("kokoro-tts")` with `PATH` = fixture dir only; `PATHEXT` is still passed explicitly as `.EXE` so the `which` crate does not depend on host defaults
        - discovery: the workspace's existing `harness = false` targets are all `[[bench]]` (criterion); there is no `[[test]]` precedent, so nextest's handling of a non-libtest test binary is verified empirically below
        - design (biscuit-speaks CLI): `detached_background.rs` is now a `harness = false` `[[test]]` target whose `main` dispatches on `current_exe()`'s file stem — `kokoro-tts` and `mpv` run the stub roles (identical to the retired shell scripts: touch the started marker, poll the release marker every 20 ms for ~10 s, exit 124 on timeout; kokoro then writes `not-a-real-wave` to `argv[2]`), anything else runs the two tests in sequence under `catch_unwind`, printing libtest-style `test <name> ... ok|FAILED` lines and exiting 101 on failure. The tests copy the test binary itself into `<temp>/bin/{kokoro-tts,mpv}{EXE_SUFFIX}` (copy, not symlink), so no `PermissionsExt`, no shell, and no fixture binary in a production crate
        - design: the custom `main` honors the libtest CLI subset nextest drives — `--list --format terse` (prints `<name>: test`; the `--ignored` listing pass prints nothing), positional filters with `--exact`, and value-taking flags (`--format`, `--skip`, `--test-threads`, `--color`, `--logfile`, `-Z`) are skipped with their value
        - verified: `cargo nextest list -p biscuit-speaks-cli` shows both tests under `biscuit-speaks-cli::detached_background`; `cargo nextest run -p biscuit-speaks-cli -E 'binary(detached_background)'` → 2 run, 2 passed (0.6 s)
        - verified on macOS: `current_exe()` inside the copied binary resolves to the copy's path — the cache-miss test can only pass if the `kokoro-tts` copy takes the stub role (a recursive test run would never touch the started marker)
        - red-first (biscuit-speaks): with `main` matching the stem `"kokoro"` instead of `"kokoro-tts"`, `background_cache_miss_returns_after_reservation_then_completes_same_slot` failed after 10.18 s with `timed out waiting for blocked synthesis helper` (the copy fell through to the test runner, matched no test against its positional args, and exited 0 without touching the marker); `background_dry_run_creates_no_spool_or_worker_side_effects` stayed green. Restored the stem → 2 run, 2 passed
        - `so-you-say` is given `PATHEXT=.EXE` alongside the fixture-only `PATH` so the `which` crate's Windows lookup does not depend on host defaults; nothing else on the Kokoro/mpv path needs PowerShell or another program
        - note: `rustfmt --check` (read-only) reports one pre-existing wrap on the unchanged `.find(|entry| …ends_with(".pending.json"))` chain in the original file too; `just lint` runs clippy only, and `cargo fmt` is never run per repo rule, so that line is left as-is
        - design (claudine): `audio_emission.rs` gains `install_fixture_program(temp, name)` which copies `current_exe()` to `<temp>/bin/<name>{EXE_SUFFIX}`; both previously Unix-only tests use it for `espeak`, the `#[cfg(unix)]` on the tests and on the `fs4` import is gone, and only the spool `0o700` block stays under `#[cfg(unix)]` (with its `PermissionsExt` import scoped inside). This is the same pattern `biscuit-speaks/lib/tests/detached_phase4.rs:311-316` already uses for a readiness-only `kokoro-tts`
        - verified: `cargo nextest run -p claudine -E 'test(/audio_emission::/)'` → 19 run, 19 passed (incl. both converted tests)
        - sweep of other `cfg(unix)` guards around detached/background/spool/emitter tests: only `biscuit-speaks/lib/tests/detached_phase4.rs:323` and `claudine/cli/tests/detached_audio.rs:15` match, and both guard just the Unix `0o700` spool-mode block with a `cfg(windows)` sibling — already portable, left alone
        - docs: `biscuit-speaks/README.md`, `claudine/README.md`, `.claude/skills/{biscuit-speaks,so-you-say,claudine}/*.md` never describe these tests as Unix-only — no doc edits
        - red-first (claudine): with `PATH` temporarily set to the fixture dir only and the copy named `espeek`, `default_emitter_publishes_audio_in_phase_order_without_waiting_for_playback` failed with `left: [File, File]` vs `right: [File, Command, Command, File]` (eSpeak not ready, speech handoff warned, only the two effect jobs were published); renaming the copy to `espeak` with the same fixture-only `PATH` → 1 passed, proving the copied executable alone satisfies readiness. The committed test keeps its original `PATH` composition (fixture dir prepended to the host `PATH`), so on this host — which has `/opt/homebrew/bin/espeak-ng` — `ESpeakProvider::new()` still resolves the real `espeak-ng`; the stub carries the test on hosts without eSpeak (Windows CI)
        - Windows compile evidence, attempt 1: `cargo check -p biscuit-speaks-cli --tests --target x86_64-pc-windows-msvc` and `cargo check -p claudine --tests --target x86_64-pc-windows-msvc` both fail before reaching any workspace crate: `aws-lc-sys v0.40.0`'s build script cross-compiles C with `cc --target=x86_64-pc-windows-msvc` and dies on `jitterentropy-base-windows.h:49:10: fatal error: 'windows.h' file not found` (no Windows SDK headers on this macOS host). `cargo tree -i aws-lc-sys --target x86_64-pc-windows-msvc` shows it is non-optional for both packages (reqwest → rustls → aws-lc-rs, reached through sniff, playa, darkmatter, and schematic-schema), so the narrower `--test detached_background` / `--lib` forms hit the same build script and were not run
        - Windows compile evidence, fallback: an isolated probe crate at `/tmp/winprobe` (std + `tempfile` only) holding (a) `detached_background.rs` verbatim except the two workspace imports shimmed (`bin_exe!` → `PathBuf::from`, `CacheKey::…cache_path()` → a temp path) as a `harness = false` `[[test]]`, and (b) the claudine `install_fixture_program` helper plus the `#[cfg(unix)]` `0o700` block verbatim; `cargo check --offline --tests --target x86_64-pc-windows-msvc` → exit 0 (one `dead_code` warning that is an artifact of the probe's layout). Every OS-sensitive construct in the change (`ExitCode`, `EXE_SUFFIX`, `env::args_os`, `fs::copy` of `current_exe`, the `Command` env handling, the Unix-scoped `PermissionsExt`) type-checks for `x86_64-pc-windows-msvc`. Actual Windows execution of these tests is not possible from this session and is not claimed
        - gate `just lint` (biscuit-speaks: `biscuit-speaks` + `biscuit-speaks-cli`, clippy `--all-targets -D warnings`): exit 0, 0 warnings
        - gate `just lint` (claudine, all area packages): exit 0, 0 warnings
        - gate `just test` (biscuit-speaks, run concurrently with the claudine build; load 11.95): 478 run, 471 passed, 7 timed out, 19 skipped — the 7 are `providers::host::say::tests::{test_default_voice_integration_{any,female,male},test_enhanced_voices_have_good_quality,test_list_voices_no_eloquence,test_list_voices_returns_voices}` and `cache::tests::test_populate_cache_for_all_providers_runs_without_panic`, all macOS host-`say` discovery. Rerun alone at load 3.96 (`just test -- say::tests cache::tests::test_populate_cache_for_all_providers_runs_without_panic`): 47 run, 40 passed, the same 7 timed out. Diagnosis: the host `say` binary itself does not return — `timeout 10 say -v '?'` produced no output in 10 s — and `pgrep` shows stale `say` (PIDs 11317, 30786, 40888, 49657, 52342), `mpv` (12331, 38021, 68746), and `so-you-say` (23932, 37541) processes from earlier sessions wedged on the audio sink (the DragonFly DAC / mpv condition memory records for this host). These tests do not touch anything in this change and fail identically for every branch on this host right now; the processes were left alone. All 471 other tests pass, including both `detached_background` process tests
        - gate `just test` (claudine, first run; load peaked at 24.45 with both areas building): stopped at 4056/6726 — 4055 passed, 1 failed, 11 skipped. The failure is `claudine::tts_phase1_contract protocol_fixture_corpus_is_versioned_and_redacts_diagnostic_records`, which asserts the exact file list of `claudine/fixes/2026-09-03-tts-not-finishing/fixtures/`; finding 2 added `v1-missing-identity-job.json` to that corpus, so the expectation was stale (unrelated to this finding's edits, which are confined to `audio_emission.rs`). Fixed by adding the new fixture name to the expected list in `claudine/lib/tests/tts_phase1_contract.rs` (the fixture already carries `schema_version`/`job_id`/`sequence`, so the per-file assertions hold); full claudine `just test` rerun below
        - gate `just test` (claudine, rerun after the corpus fix; load 3.57 at start): 6726 run, 6726 passed (1 slow), 11 skipped, exit 0
        - GitNexus `detect_changes` (scope all, worktree): this finding's symbols are all test-side (`detached_background.rs` functions/structs, the two converted `audio_emission` tests, and the corpus expectation in `tts_phase1_contract.rs`); the only affected execution flow (`play_detached → dry_run_enabled`) is finding 1/2's playa touch, aggregate risk medium from the combined worktree diff
        - not done, and why: no Windows execution of either test is claimed — this session has no Windows host and `just cross-check` is off-limits (SSH); the evidence owed is the Windows-target type-check above. The workspace-level `cargo check --target x86_64-pc-windows-msvc` cannot succeed on this macOS host for any crate that transitively pulls `aws-lc-sys` (it needs the Windows SDK's `windows.h`), which is a host limitation rather than a change defect
        - orchestrator note: the claudine `tts_phase1_contract` fixture-corpus expectation had to absorb finding 2's new `v1-missing-identity-job.json`; that is finding-2 fallout surfaced by the claudine gate and was fixed in the same pass
- work completed for 'finding 3: the background TTS process regression is Unix-only' at 03:13:49

### Successful Completion

The implementation of review cycle 2 has completed successfully in 66 minutes 56 seconds. During this implementation all 3 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 3 were fixed, 0 were deferred (see reasons below):

- no finding was deferred; two evidence items inside fixed findings remain owed to the host environment rather than to code:
        - finding 1's required-sink real-audio run: `just test-real` in `playa` timed out all four `real_` tests because the host audio sink is wedged (`afplay` never returns; stale `mpv`/`say` processes from earlier sessions are hung on the DragonFly DAC). The Level 1 injection-seam tests through the production pipeline are green and are the code-level proof the review asked for; the real-tier verdict must be re-taken once the host sink is reset (quit the wedged players or re-plug the DAC), with `PLAYA_REAL_AUDIO_REQUIRED=1`
        - finding 3's Windows process execution: no Windows host is reachable from this non-interactive session and `just cross-check` (SSH) is off limits here. Workspace-level `cargo check --target x86_64-pc-windows-msvc` fails before any workspace crate on `aws-lc-sys` (no Windows SDK on macOS); an isolated probe crate type-checked the new `harness = false` test and the claudine fixture helper verbatim for that target. The tests are now platform-neutral by construction (copied test executable acting as `kokoro-tts`/`mpv`/`espeak`), and the next CI Windows leg or a `just cross-check biscuit-speaks-cli --host windows detached_background` run from an interactive session should provide the execution evidence
- `deferred_perf_measurement` stays `false`: no finding required a performance measurement
- the files changed by this cycle (from `git status`, excluding the fix directory) are listed below:
        - `.claude/skills/playa/SKILL.md`
        - `.claude/skills/playa/integration.md`
        - `biscuit-speaks/cli/Cargo.toml`
        - `biscuit-speaks/cli/tests/detached_background.rs`
        - `claudine/lib/src/composition/lifecycle/tests/audio_emission.rs`
        - `claudine/lib/tests/tts_phase1_contract.rs`
        - `playa/README.md`
        - `playa/justfile`
        - `playa/lib/Cargo.toml`
        - `playa/lib/src/detached/mod.rs`
        - `playa/lib/src/detached/protocol.rs`
        - `playa/lib/src/detached/tests.rs`
        - `playa/lib/src/native_player.rs`
        - `playa/lib/src/playa.rs`
- the fix-directory artifacts changed by this cycle: `fixtures/v1-ready-job.json`, `fixtures/v1-preparing-job.json`, `fixtures/v1-delegated-report.json` (complete v1 identity projection), new `fixtures/v1-missing-identity-job.json` (quarantine corpus case), and this log
- final gate summary:
        - `playa`: `just lint` clean; `just test` 181 passed, 8 skipped (the lib now compiles `native-playback,async` under the L1 recipe and CI metadata)
        - `biscuit-speaks`: `just lint` clean; `just test` 478 run, 471 passed, 7 timed out (macOS host `say` discovery, environmental, reproduced on rerun at load 3.96), 19 skipped
        - `claudine`: `just lint` clean; `just test` 6,726 passed, 11 skipped

## Implementation of Review Findings #3

> **started at:** 2026-09-04T03:32:22+01:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/claudine/fixes/2026-09-03-tts-not-finishing/review-3.md'
- this is iteration 3 of the review-to-implement cycle
- host load at start: load averages 3.36 / 4.05 / 5.68 on a 5-day uptime; no performance measurement is required by the review findings
- the review contains 3 findings:
        - finding 1 (High): the current implementation has no successful real-audio completion acceptance (`playa`, `biscuit-speaks`; host evidence)
        - finding 2 (High): the required public-path truncation regression `truncated_verdict_warns_once_and_returns_ok` is still missing (`playa`)
        - finding 3 (Medium): the repaired Windows process coverage has not run on Windows (`biscuit-speaks`, `claudine`; host evidence)
- findings will be worked serially; finding 2 is the only code change and goes to a dedicated subagent that owns its log entries; findings 1 and 3 are evidence runs the orchestrator drives directly

- starting the work on 'finding 2: the required public-path truncation regression is still missing' at 03:35:25
        - delegated to a dedicated `rust-developer` subagent (skills `rust`, `rust-testing`, `claudine`, `playa`); its log items are grouped below when it reports back
- starting the work on 'finding 1: no successful real-audio completion acceptance on the current implementation' at 03:35:25
        - host state at start: `pgrep` shows 15 wedged audio processes left by earlier sessions, all sleeping with under one CPU-second each: four `mpv` (1–4 h old, playing biscuit-speaks WAVs), five `say` (1–3 h old), five `so-you-say` notification requesters (1–3 h old), and one `kokoro-tts` Python synthesis (18 min old)
        - sink probe before any intervention: `afplay` of a freshly generated 0.5 s silent 24 kHz mono WAV did not return within 10 s
        - intervention: sent `SIGTERM`, then `SIGKILL`, to all 15 stale audio clients (they belong to this user; they were leftover notification playbacks from earlier sessions). Only the `kokoro-tts` Python synthesis died; the four `mpv`, five `say`, and five `so-you-say` processes survived `SIGKILL`, which means they are blocked inside the kernel audio HAL rather than merely idle. `ps` shows no `wchan` and state `S`
        - sink probe after the intervention: `afplay` timed out twice more at 15 s; `osascript -e 'get volume settings'` timed out at 10 s; `system_profiler SPAudioDataType` timed out at 40 s. `coreaudiod` (pid 58699) has been running 4 h 37 min and `launchctl` reports it `running`, but it answers no client. The AudioQuest DragonFly is still attached (`ioreg` shows it registered, matched, active)
        - conclusion: the sink is wedged at the `coreaudiod` / USB-DAC driver level. Restoring it requires either `sudo launchctl kickstart -kp system/com.apple.audio.coreaudiod` (or `sudo killall coreaudiod`), unplugging and re-plugging the DragonFly, or a reboot. All three are outside what this non-interactive session may do (no `sudo`, no physical access), and an unprivileged `killall coreaudiod` finds no process it owns
        - not re-run: `just test-real` in `playa` and `biscuit-speaks`. With `coreaudiod` unresponsive the only possible outcome is the same 30 s nextest timeouts review 2 already recorded, and every attempt leaves another kernel-blocked player process on Ken's host. Review 3 explicitly states a skip or timeout is not acceptance evidence, so a rerun would add wedged processes without adding evidence
        - deferred: the required-sink acceptance runs (`Native`/`Complete` for the 24 kHz mono fixture, `Complete` for explicit mpv and every installed host player, biscuit-speaks end-to-end `Complete`, two back-to-back background jobs, one lifecycle `say`). The exact recovery-then-verify procedure is recorded in `evidence.md` under "Review 3 sink state and recovery procedure" so it can be executed from an interactive session as soon as the sink is reset
- work completed for 'finding 1: no successful real-audio completion acceptance on the current implementation' at 03:39:16 (deferred to the host owner; no code change required)
- starting the work on 'finding 3: the repaired Windows process coverage has not run on Windows' at 03:39:16
        - the only local route to a Windows execution is `just cross-check <package> --host windows <filters>` (`scripts/cross-check.sh`), which ships the local tree as a patch over `ssh -o BatchMode=yes` to the standing `build-win-native` clone and runs `cargo nextest run -p <package>` there. This session's rules forbid invoking `ssh` (no host-specific authorization for those aliases is present), so the recipe cannot be run from here
        - the CI route (`_package-ci.yml` runs the L1 suite on `windows-latest` for every package leg) requires the changes to be committed and pushed; the working tree is staged but uncommitted, and committing is out of scope for this task. The last completed `ci` run on `feat/unifi` (2026-09-02 22:44Z, success) predates every change in this fix
        - review 2's Windows-target type-check of the two tests (isolated probe crate, `cargo check --target x86_64-pc-windows-msvc`) remains the strongest evidence available from this host; nothing in this cycle touched `biscuit-speaks/cli/tests/detached_background.rs` or `claudine/lib/src/composition/lifecycle/tests/audio_emission.rs`, so that evidence is still current
        - deferred: actual Windows execution of `biscuit-speaks-cli::detached_background` and claudine `audio_emission`. From an interactive session run, at the repo root: `just cross-check biscuit-speaks-cli --host windows detached_background` and `just cross-check claudine --host windows audio_emission` (confirm the remote standing clone is clean first; a dirty clone silently tests the wrong tree), or commit and push so the `windows-latest` package legs run them. Record that the requester exits while playback is blocked and that the reserved job subsequently completes
- work completed for 'finding 3: the repaired Windows process coverage has not run on Windows' at 03:39:16 (deferred to a Windows-capable session; no code change required)
- log items reported by the finding 2 subagent (grouped; times are local):
        - 03:35:23 read review-3 finding 2, spec D2 (lines 257-310) and the test list at 660-664; confirmed `report.rs` tests only exercise the private `warn_if_truncated` on pre-built reports
        - 03:35:40 traced the production route: `playa_with_player_and_options_with_report` / `..._async_with_report` probe the source, spawn `Command::new("mpv")` by bare name (no availability pre-check, no `PLAYA_DRY_RUN` gate on the free functions), and wrap the inner result with `.map(PlaybackReport::warn_if_truncated)`; a fake `mpv{EXE_SUFFIX}` on a private `PATH` therefore drives the real host route end to end
        - 03:36:00 host load at start 2.96 / 4.65 / 5.71; no doc drift found (`playa/README.md`, `playa/docs/*.md`, `.claude/skills/playa/*.md` carry no regression-test inventory), so no prose was touched
        - 03:37:30 added `playa/lib/tests/truncated_verdict.rs` (`harness = false`, dispatch on own file stem, same libtest CLI subset as `biscuit-speaks/cli/tests/detached_background.rs`); the fixture `mpv` sleeps 100 ms and exits 0 ignoring arguments. The test writes a 2 s 24 kHz mono 16-bit PCM WAV (44-byte header + 96,000 zero bytes), asserts `probe_audio_metadata` reports exactly 2 s / 1 channel, restricts `PATH` to the private `bin` dir through a restore-on-drop guard, and counts WARN events with a hand-rolled `tracing::Subscriber` installed via `set_default`. No new crate dependencies
        - 03:37:44 added `[[test]] name = "truncated_verdict" harness = false` to `playa/lib/Cargo.toml`
        - 03:38:51 first `just test -- truncated_verdict`: 2 passed (sync 0.222 s, async 0.117 s), 189 skipped
        - 03:39:09 red-first variant A (fixture sleeps 2.2 s): 2 run, 0 passed, 2 failed, both panicking at `fake mpv exits after ~100 ms; elapsed 2.21 s / 2.28 s`; restored the 100 ms sleep
        - 03:39:17 GitNexus `impact` upstream on both public wrappers before the temporary production edit: LOW risk, 2 direct callers each (tests), 0 execution flows
        - 03:39:33 red-first variant B (temporarily replaced `.map(PlaybackReport::warn_if_truncated)` with an identity map at `playback.rs:94` and `:281`): 2 run, 0 passed, 2 failed, both at the warning assertion (`left: []`, `right: ["audio playback ended before the expected duration"]`); `playback.rs` restored byte-identical to HEAD (`git diff --stat` empty)
        - 03:40:22 `just lint` red on one clippy `unused_mut` in the no-feature build; replaced the `Vec` builder with a `const TESTS` slice using a `#[cfg(feature = "async")]` element, matching the `detached_background.rs` pattern
        - 03:41:34 `just lint` green for `playa` and `playa-cli` (the no-feature build confirms the async test is gated out)
        - 03:41:50 `just test` (full area, `playa --features native-playback,async; playa-cli`): 183 run, 183 passed, 8 skipped (`real_`/`slow_` tier skips); no audio-sink timeouts; zero leftover `playa-truncated-verdict-*` temp dirs
        - residual: assertions are `elapsed < 1 s` and `missing >= 1 s` (the spec threshold for a 2 s source is 1.55 s; the 1 s floor tolerates a slow spawn on a loaded CI host without weakening the proof since any `Complete` result or missing warning still fails); Windows relies on `Command` appending `.exe` to a bare program name and on `fs::copy` producing a real `.exe`, same exposure as `detached_background.rs`; the WARN subscriber is thread-scoped, so moving the warning onto a spawned thread would make the test fail loudly rather than pass vacuously; the libtest shim is duplicated from `detached_background.rs` and could later be lifted into `biscuit-test-harness`
- orchestrator verification of finding 2 at 03:43:33: `just test -- truncated_verdict` rerun in `playa` → 2 run, 2 passed, 189 skipped; `git status` shows only `playa/lib/Cargo.toml` and the new `playa/lib/tests/truncated_verdict.rs` changed by this cycle
- work completed for 'finding 2: the required public-path truncation regression is still missing' at 03:43:33

### Successful Completion

The implementation of review cycle 3 has completed successfully in 11 minutes 11 seconds. During this implementation all 3 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 1 was fixed, 2 were deferred (see reasons below):

- finding 1 (real-audio completion acceptance) was deferred because the host audio sink is wedged below user space: `coreaudiod` answers no client, fifteen stale player processes survive `SIGKILL`, and the reset needs `sudo` or a physical DAC re-plug, neither of which this non-interactive session may perform. The recovery-then-verify procedure is in `evidence.md` under "Review 3 sink state and recovery procedure"
- finding 3 (Windows execution of the process tests) was deferred because the only local route is the SSH-backed `just cross-check`, which this session may not invoke, and the CI route needs a commit and push that are outside this task. The exact commands are recorded under finding 3 above and in `evidence.md`
- `deferred_perf_measurement` stays `false`: no finding required a performance measurement
- the files changed by this cycle:
        - `playa/lib/Cargo.toml` (new `[[test]]` target)
        - `playa/lib/tests/truncated_verdict.rs` (new)
        - `claudine/fixes/2026-09-03-tts-not-finishing/evidence.md` (review 3 sink state and recovery procedure)
        - `claudine/fixes/2026-09-03-tts-not-finishing/review-3.md` (metadata)
        - this log
- final gate summary: `playa` `just lint` clean; `just test` 183 passed, 8 skipped. `biscuit-speaks` and `claudine` were not re-gated because no file in either area changed in this cycle

