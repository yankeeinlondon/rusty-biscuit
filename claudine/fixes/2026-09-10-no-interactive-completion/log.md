---
fix: 2026-09-10-no-interactive-completion
area: claudine
deferred_perf_measurement: false
implementation_1: 2026-09-10T14:16:59-07:00
---

# Log: Restore Interactive Completion Before Initialize Consumes Caller File Inputs

## Implementation of Review Findings #1

> **started at:** 2026-09-10T14:16:59-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/claudine/fixes/2026-09-10-no-interactive-completion/review-1.md'
- this is iteration 1 of the review-to-implement cycle
- the review contains 8 findings; findings 1 and 2 are the two blockers listed under `## Required before ready: true`
- the impacted package area is `claudine` (packages `claudine` and `claudine-cli`), so `just test`, `just test-l2`, and `just lint` are run from `claudine/` only
- planned disposition:
        - findings 1, 2, 3, 5, 6, 7 are in scope for this cycle
        - finding 4 is a documented judgment call by the reviewer (PTY vs. shared harness) and is not a code change
        - finding 8 is explicitly recorded by the reviewer as outside this fix's scope (shipped prompt content, not code)
- starting the work on 'finding-1-launch-area-anchoring' at 14:20:26-07:00
        - scope confirmed as a **test-only** change; the production scope derivation in `claudine/cli/src/commands/schema_interactive/supplied.rs` is already correct and was left untouched
        - read path traced end to end: the closure builds `ScopeContext { cwd: origin.base_dir(), .. }`, `file_candidate_paths` walks from `scopes::property_value_root(ctx)` (which is `ctx.cwd`), and `path_matches_query` filters the resulting absolute paths — so a decoy that only differs by living outside the launch area is the right discriminator
        - changes made:
                - `claudine/cli/tests/level2_provided_partial_file_pty.rs` — `review_router_fixture` now also seeds `<repo-root>/fixes/2026-09-10-local-decoy/spec.md`, which matches the same `fixes/2026-09-10-local` substring but sits outside the `packages/example` launch area
                - same file — `review_router_session` was split so `review_router_session_in(fixture, router, launch_dir)` can launch the identical fixture from a different directory
                - same file — the single-match test now asserts the chooser did not render (no keyboard-enhancement push) and that the provider prompt does not contain `local-decoy`
                - same file — new test `level2_review_router_partial_repo_root_launch_widens_candidates_to_the_chooser` launches the same fixture from the repository root, waits for the chooser, and selects the decoy (`SELECTED=decoy`)
                - `claudine/cli/tests/common/pty.rs` — `KBD_ENHANCEMENT_PUSH` raised from private to `pub(crate)` so the confirmation-vs-chooser assertion can reuse it
        - non-vacuity evidence — perturbation 1 (production): scope `cwd` changed from `origin.base_dir()` to `origin.repository_root()`
                - `level2_review_router_partial_yolo_confirms_before_initialize_and_survives_proxy` **FAILED** — the transcript shows the chooser listing both `fixes/2026-09-10-local-decoy/spec.m` and `packages/example/fixes/2026-09-10-l…` instead of the confirmation
                - `level2_review_router_partial_chooser_keeps_selected_identity_in_proxy` and `level2_review_router_partial_decline_and_cancel_stop_before_initialize` also **FAILED**
                - `level2_review_router_partial_repo_root_launch_widens_candidates_to_the_chooser` **PASSED**, which is correct: its launch directory already is the repository root
        - non-vacuity evidence — perturbation 2 (the reviewer's exact claim): production still root-anchored **and** the decoy seed removed
                - the three pre-existing router tests went **green again** (3 passed), reproducing the vacuity finding 1 reported; only the new repo-root test **FAILED** (no chooser to wait for)
                - this is the proof in both directions: the decoy is what makes root anchoring observable, and the new repo-root test genuinely observes the out-of-area candidate rather than passing by construction
        - non-vacuity evidence — perturbation 3 (fixture, isolates the `local-decoy` assertion): decoy re-seeded *inside* the launch area as the sole candidate with `marker: alpha`, `local-a` withheld, and the helper's directory argument relaxed to `spec.md`
                - `SELECTED=alpha`, the directory check, `TOKEN=retained`, the single-dialog count, and the no-lifecycle-error check all still passed; `assert!(!prompt.contains("local-decoy"))` was the **sole failure**, with the panic showing `SPEC=…/packages/example/fixes/2026-09-10-local-decoy/spec.md`
        - honest caveat on one assertion: `!transcript.contains(KBD_ENHANCEMENT_PUSH)` cannot fail independently, because `wait_for_marker("Use this file")` panics first on every path that renders the chooser; it is kept as an explicit restatement of "confirmation, not chooser" and is commented as such
        - all three perturbations were reverted; the production file is byte-identical to its pre-perturbation state and the fixture is back to seeding `local-a`, the optional `local-b`, and the out-of-area decoy
        - verification (run from `claudine/`):
                - `just test-l2 review_router` — 4 tests run, **4 passed**
                - `just test-l2 --no-fail-fast` (full L2 suite) — 245 tests run, **243 passed, 2 failed**
                - both failures are `level2_dry_run_document_cell_renders_osc8_link_in_wezterm` and `level2_initialize_proxy_block_auto_detects_osc8_in_wezterm`; both panic on a captured frame containing `Atuin AI is not yet configured. / Enable Atuin AI`, i.e. the recorded Atuin-wedge host trap in the WezTerm capture panes — unrelated binaries, untouched sources, no PTY or schema involvement
                - `just lint` — clean; no clippy warnings, no errors, and the `claudine-cli::error_guards` lint suite reported 18 tests run, 18 passed
                - no terminal or browser window was brought into focus; the new test is an `expectrl` PTY session and opens no window
- work completed for 'finding-1-launch-area-anchoring' at 14:34:53-07:00
- starting the work on 'finding-2-vacuous-assertion' at 14:39:23-07:00
        - scope confirmed as a **test-only** change, contained in `assert_review_router_completed` and its four call sites in `claudine/cli/tests/level2_provided_partial_file_pty.rs`
        - the reviewer's claimed string was verified against current source and is **accurate**: `collect_file` at `claudine/cli/src/commands/schema_interactive/mod.rs:634-638` formats `"The {name} requires a valid file reference; choose from the files below:"` and writes it to stderr through `Prose`; the perturbation transcript below shows it rendered verbatim on one line (`The plan requires a valid file reference; choose from the files below:`), so no wrapping guard is needed
        - one trap found before writing the assertion: `collect_file` checks its candidate list **before** printing that intro, so a missing `file` property with zero glob candidates fails with ``no files available for `plan` `` and never emits the sentence — the perturbation therefore had to seed a candidate for the collected property, otherwise the "proof" would have been another vacuous pass
        - changes made:
                - `assert_review_router_completed` — `!plain.contains("MissingProperties")` (a Rust enum variant name the renderer never prints) replaced with `!plain.contains("requires a valid file reference")`, the text the missing-value `file` collector actually emits
                - same helper — added the stronger form the reviewer asked for as a new `widget_dialogs` parameter: `transcript.matches(KBD_ENHANCEMENT_PUSH).count()` must equal the number of `biscuit-tui` widgets the resolution legitimately needs, counted over the whole transcript through `provider reached`
                - the count is per-test rather than a fixed `1` because the two dialog kinds differ: the multi-candidate chooser runs through `run_standalone` and pushes the keyboard-enhancement flags, while the single-candidate confirmation (`completion/autocomplete_ui.rs:129`) calls bare `enable_raw_mode` and pushes nothing — so the confirmation tests pass `0` and the chooser tests pass `1`, and any extra prompt of either kind shows up as an extra push
        - non-vacuity evidence — the perturbation had to reproduce the regression the row guards, which took two attempts:
                - attempt 1 (production only): forcing the caller-document branch at `compose/prep.rs:220` past `defers_schema_verdict_to_initialize`, and then also passing `defer_schema_verdict = false`, still produced a **green** run — the shipped router's `$schema` is a **root-level union** (a YAML sequence of arms), so supplying `spec` satisfies its arm and `plan`/`review` are never missing to begin with
                - attempt 2 (production + fixture): same production perturbation, plus the fixture router rewritten from the union to a single map (`"\n    - "` → `"\n    "`) with the `review` arm dropped and a `plan.md` candidate seeded — this is the regression pair the acceptance row describes, and it made `plan` a genuine missing eager `file` property on the caller's document
                - result: `level2_review_router_partial_yolo_confirms_before_initialize_and_survives_proxy` **FAILED** at the new assertion (`level2_provided_partial_file_pty.rs:421`, `an unrelated property was collected before initialize`), with the transcript showing the schema status report (`⍉ plan: file - was not defined but is required`), the intro sentence, and the rendered chooser row
                - every other assertion in the helper still passed under that perturbation — `SELECTED=alpha`, the directory check, `TOKEN=retained`, the single `did not match a file directly` count, and the no-lifecycle-error check — so the failure is isolated to the replaced assertion
                - the widget count was proven **independently**: with the string assertion temporarily neutered, the same run failed one line later at `assert_eq!` (`unexpected number of interactive widgets`, `left == right` failed at `:425`), i.e. the extra chooser was caught by the count alone
                - for contrast, the assertion being replaced was re-checked against the same transcript: `MissingProperties` appears nowhere in it — the CLI renders that error as `⤫ CompositionError: missing properties`, confirming the old assertion could not have failed even in the middle of the regression
        - all perturbations reverted; `git diff HEAD -- claudine/cli/src/commands/compose/prep.rs` is empty, and the test file contains no perturbation leftovers
        - housekeeping: an external actor staged the working tree **while the perturbation was applied**, so the Git index briefly held the perturbed `prep.rs` and test file; the index was re-pointed at the reverted content (`git add` of the two files, no commit, no `git stash`, no worktree change) and `git status` now shows `prep.rs` clean against `HEAD`
        - verification (run from `claudine/`):
                - `just test-l2 review_router proxy_target_schema provided_partial` — 9 tests run, **9 passed** (4 router tests, the proxy-target test, and the 4 pre-existing partial-file PTY tests)
                - `just lint` — clean; no clippy warnings, no errors, and the `claudine-cli::error_guards` lint suite reported 18 tests run, 18 passed
                - no terminal or browser window was brought into focus; every test here is an `expectrl` PTY session
- work completed for 'finding-2-vacuous-assertion' at 14:50:03-07:00
- starting the work on 'finding-3-redundant-early-pass' at 14:51:38-07:00
        - the reviewer's redundancy claim was verified against source before any edit, and it **holds**
                - `run_composition_inner` calls `resolve_supplied_file_inputs` unconditionally at `compose/prep.rs:212`; the only `first == true` entry into `prepare_and_run_active_document` is the loop at `:321` in that same function, so the early pass always precedes it
                - the other caller of `prepare_and_run_active_document` is the sequence step's proxy loop (`wrap/sequence/iterate.rs:692`), which hard-codes `first: false` — it is unaffected by the gate
                - on the first iteration the second call receives byte-identical inputs: `source_context` is untouched between the two sites, so `file_resolution_context` is the same; `interactive` is the same `interactive_opts`
                - the deferred branch (`defers_schema_verdict_to_initialize` true, the reported router shape) leaves `source`, `set_overrides`, and `caller_input_records` untouched between the two sites
                - the non-deferred branch replaces them with `pre_validate_with_interactive_collection`'s output, and none of its outcomes can introduce a *new* unresolved supplied partial:
                        - `drop_invalid_optionals` only removes keys, so it can shrink the record set, never grow it
                        - a deferred `SchemaValidation` (which is what a non-empty caller layer gets) returns the original source and overrides unchanged
                        - `collect_file` writes `resolve_file_value`'s **absolute resolved path** (`schema_interactive/mod.rs:710-723`), and the residual `resolve_unresolved_file_reference` path likewise installs a resolved value
                        - a successful `pre_validate_schema_for_mode` means every eager `file` property already resolved, which is exactly the condition `unresolved_supplied_files` tests
                - and the first call leaves nothing pending: every selection is written back as `resolve_in_context(origin)`'s absolute path, which re-resolves to `Ok(Some(_))` on the next inspection and is skipped
        - changes made:
                - `claudine/cli/src/commands/compose/prep.rs` — the second `resolve_supplied_file_inputs` call is now gated on `!first`; the existing comment gained one sentence saying why the caller's own document is excluded (contract, not narration)
                - same file — one line added to the `"schema validation"` perf-substage comment recording that the span covers supplied-file resolution too, so it holds two effective-schema loads when the caller set a value
                - `claudine/cli/tests/compose_caller_file_provenance.rs` — new L1 process test `proxy_target_partial_is_resolved_before_the_target_initialize`
        - test coverage confirmed for both sides of the new gate:
                - `first == true`, document with `initialize` — the four `level2_review_router_partial_*` PTY tests
                - `first == true`, document without `initialize` — the four pre-existing `level2_pty_provided_partial_*` PTY tests
                - `first == false` — `level2_proxy_target_schema_resolves_partial_once_before_its_initialize` (PTY, `#![cfg(unix)]`), now joined by the new L1 test so the branch is also pinned on Windows and WSL2 where the PTY file is compiled out
                - the new test uses the same entry-proxies-to-shipped-router shape as the PTY test but runs non-TTY, so the expected result is the typed `no existing file matched reference` diagnostic rather than a dialog
        - non-vacuity evidence — perturbation: the gate inverted to `if first` (which is precisely "a proxy target silently loses its resolution pass", while leaving the caller's own document covered)
                - `proxy_target_partial_is_resolved_before_the_target_initialize` **FAILED**, and it reproduced the original defect verbatim — the rendered block was `⤫ CompositionError: lifecycle evaluation error`, raised in the target router's `when:` guard, with `Reason: invalid file path: fixes/2026-09-10-local`
                - the perturbation was reverted; `git diff` of `prep.rs` now shows only the gate plus the two comment additions
        - verification (run from `claudine/`):
                - `just test` — 6862 tests run, **6862 passed** (36 slow), 11 skipped
                - `just test-l2 provided_partial review_router proxy_target_schema` — 9 tests run, **9 passed**
                - `just lint` — clean across `claudine`, `claudine-contract`, `claudine-cli`, and `claudine-gen`; no clippy warnings or errors
                - no terminal or browser window was brought into focus; the L2 tests here are `expectrl` PTY sessions and the harness panes the recipe spawns are headless
        - no documentation drift: the gate changes cost, not behavior, and no public contract in the README, `docs/topics/composition.md`, or the claudine skill describes how many times the pass runs
- work completed for 'finding-3-redundant-early-pass' at 14:58:34-07:00
- starting the work on 'finding-5-comment-drift' at 14:59:55-07:00
        - scope: comment-only. The reviewer's suggested wording was treated as a starting point; each of the four sites was judged separately against the repo's comment-quality policy (reword / add one sentence / delete the drifted clause), and no comment was expanded mechanically
        - site 1 — `schema_interactive/mod.rs`, the `defer_schema_verdict` guard (**reword, net zero lines**)
                - the first two sentences are the load-bearing WHY (caller records need canonical projection; this pre-validator holds only raw setters) and were kept verbatim
                - the drifted tail claimed "its own outcomes (a resolved partial, a zero-match failure) are never deferred" — a resolved partial is now normally `resolve_supplied_file_inputs`'s outcome, not this function's, so naming it here as this branch's own outcome misattributes ownership
                - replaced with the invariant plus the new ownership: only the plain schema verdict defers; missing-value collection still runs here, as does the partial-file fallback for the shapes the early pass left unresolved
                - deliberately did not restate the deferral condition itself — the `matches!(err, SchemaValidation)` guard is on the next line and narrating it would be HOW-narration
        - site 2 — doc on `resolve_unresolved_file_reference` (**add one sentence-group, +4 lines**)
                - this is the one place where deletion would lose information: nothing in the signature or body says this function is no longer the primary supplied-partial resolver, and a reader tracing the PTY tests would otherwise expect to land here and be confused when they do not
                - added the residual-path statement naming the earlier resolver and the exact shapes that still arrive here, read off `lib/src/composition/schema/supplied.rs::supplied_file_shape`: root-level unions where zero or several arms validate (`selected` stays `None` / a second match returns `None`) and unions containing a non-inline arm (`SchemaArm::Inline` else-return)
                - used a plain code span, not an intra-doc link, for `resolve_supplied_file_inputs` — it is `pub(crate)` inside the private `supplied` submodule and the host item is itself private, so a `[...]` link would only resolve under `--document-private-items` and is a broken-link risk for no reader benefit
        - site 3 — `compose/prep.rs`, the caller-record rebuild after pre-validation (**delete the drifted clause, net -1 line**)
                - the load-bearing part is the invariant: the records must carry the same values as `pre.set_overrides`, still anchored at the launch origin. The enumeration of *which* mutations is illustrative
                - "replaced a `file(match)` partial" is the drifted clause. Rather than qualify it with a parenthetical about the residual path — which would make the primary description longer to describe the rarer case — it was deleted; site 2 now documents the residual path once, in the function that owns it
                - also retitled the subject from "Interactive collection" to "Pre-validation", since dropping invalid optionals is not interactive
        - site 4 — `level2_provided_partial_file_pty.rs` module doc (**add one paragraph, +8 lines**)
                - the existing doc describes only the Phase 3 group from `fixes/2026-06-30-completion-failures`; the file now holds a second group with a different contract (resolution must precede `initialize`, and survive a proxy handoff)
                - the non-obvious fact worth the lines is the fixture's shape — in particular that `review_router_fixture` seeds a same-substring decoy at the repository root whose entire purpose is to fail if candidate discovery is not anchored to the `packages/example` launch area. A reader deleting that decoy as redundant would silently un-verify finding 1's acceptance row
                - no code fence was added, so the new prose cannot become a doctest
        - comment-only verification
                - `git diff` over the three files shows exactly one non-comment hunk: the `if !first { ... }` gate in `prepare_and_run_active_document`. That hunk is finding 3's pre-existing work, confirmed against this log's own finding-3 entry ("the second `resolve_supplied_file_inputs` call is now gated on `!first`") and against the perf-substage comment addition recorded there
                - every `+`/`-` line in `schema_interactive/mod.rs` and `level2_provided_partial_file_pty.rs` begins with `//`, `///`, or `//!`; in `prep.rs` the only lines I authored are the three `//` lines of site 3
                - `git diff --stat` after the runs: `prep.rs` 30 changed (finding 3 + site 3), `schema_interactive/mod.rs` 11, the PTY test 8
        - verification (run from `claudine/`)
                - `just lint` — clean across `claudine-catalog-types`, `claudine`, `claudine-contract`, `claudine-cli`, and `claudine-gen`; no clippy warnings or errors, and the lifecycle-doc-facets guard still passes. This is real evidence for the doc edits because `just _lint <pkg>` runs `cargo clippy -p <pkg> --all-targets -- -D warnings`, so the PTY test file's module doc was compiled
                - `just test` — 6859 tests run, **6859 passed**, 11 skipped
                - no behavior changed, so no test outcome could shift; the runs exist to prove the doc edits did not break a rustdoc link or a target's compilation
        - transient blocker worth recording: the first `just test` attempt failed to compile, not from this work — a concurrent session sharing this worktree had staged a deletion of `claudine/test-support/locked_audio_spool.rs` (the in-flight silent-audio fix) while `claudine/cli/tests/detached_audio.rs` still `include!`d it, so `claudine-cli` could not build that target. The file was absent for roughly eleven minutes; once the sibling session restored it the suite ran clean on the first try. Nothing was restored, reverted, or stashed from here
        - no drift beyond the four sites: this change alters no public behavior, so the README, `docs/topics/composition.md`, and the claudine skill need no update
- work completed for 'finding-5-comment-drift' at 15:16:35-07:00
- starting the work on 'finding-6-chooser-io-error-triage' at 15:17:52-07:00
        - scope: observability only, confined to the production closure in `claudine/cli/src/commands/schema_interactive/supplied.rs`; no user-facing surface, no control flow, and no error type changed — every path that previously returned `None` still returns `None` and still downgrades to the same `no existing file matched reference` diagnostic
        - the user-cancel vs. real-failure split was made explicit, which is the whole point of the finding
                - `choose_provided_file_reference` returns `Ok(None)` for zero candidates, a declined confirmation, or a canceled chooser — all normal user outcomes, all left **silent**
                - only `Err(_)` (a terminal I/O failure inside `confirm_one_file` / `choose_one_file`) emits a debug event
                - the same rule was applied to the second swallow the reviewer named: `resolve_in_context`'s `Ok(None)` (the chosen path resolves to nothing) stays silent, `Err(_)` (a typed `FileReferenceError`) logs
        - changes made:
                - `.ok().flatten()?` replaced with an explicit `match`, so the `Ok(None)` arm keeps the `?` early-return and the `Err` arm logs before `return None`
                - `FileReference::new(...).ok()?.resolve_in_context(origin).ok()??` collapsed into `FileReference::new(...).and_then(|reference| reference.resolve_in_context(origin))` under one `match` — both halves share the `FileReferenceError` type, so the two former `.ok()?` swallows became a single logged `Err` arm without adding a second branch
                - three comment lines added stating the contract (decline/cancel is normal; only a broken chooser is a diagnostic) rather than narrating the match
        - tracing conventions were read off existing `claudine/cli` call sites rather than invented: 29 `debug!` sites, 20 of which carry an explicit `target:` in the `claudine::<area>` form (`claudine::completion`, `claudine::handle`, `claudine::session_report`, `claudine::wrap::yolo`, `claudine::lifecycle::requeue`). Both new events use `target: "claudine::schema_interactive"`, structured fields (`%err`, `property`, `provided` / `chosen`) and a lowercase message last, matching `session_report.rs:172-182` exactly. `tracing` is fully qualified because the file imports no `tracing` items
        - testability decision: **untested, deliberately, and stated plainly rather than papered over with a vacuous test**
                - `resolve_supplied_file_inputs_with` is parameterized on the *whole* selection closure, so the tests in this file substitute the closure that now holds the logging — the injection seam is above the code under discussion, not below it. Both existing unit tests already exercise the `None` outcome through their own stub closures and would keep passing if these debug lines were deleted
                - reaching the real `Err` arms requires a genuine terminal I/O failure inside `biscuit-tui`'s raw-mode dialogs (or a `FileReferenceError` on a path the chooser itself just produced from a live filesystem walk). There is no injection point for either without introducing a chooser trait — which is exactly the error-path refactor this low-severity finding says not to do
                - asserting on the event would additionally need a capturing subscriber layered onto a PTY test; the cost is far above the value of a debug line whose only consumer is support triage
                - so no test was added. The compile-time guarantee (both arms typecheck, both fields render) plus `just lint`'s `-D warnings` is the entirety of the mechanical evidence, and that is said here rather than implied by a test that asserts nothing
        - comment/doc drift check: the function's own doc ("Resolve supplied eager file partials without collecting gaps or judging the schema") describes behavior that did not change; finding 5's site-2 note on `resolve_unresolved_file_reference` describes the *residual* path and is untouched by this edit; no README, `docs/topics/composition.md`, or skill text documents this closure's internals
        - verification (run from `claudine/`):
                - `just lint` — clean across `claudine-catalog-types`, `claudine`, `claudine-contract`, `claudine-cli`, and `claudine-gen`; no clippy warnings or errors, and the `claudine-cli::error_guards` lint suite reported 18 tests run, 18 passed
                - `just test` — 6859 tests run, **6859 passed**, 11 skipped (34.3s)
                - `git diff` of the touched file is a single hunk, 31 insertions / 7 deletions, all inside the closure; findings 1, 2, 3, and 5 were not disturbed
                - the sibling session's in-flight `claudine/test-support/` and audio-test edits did not collide with this run; the suite compiled on the first attempt
- work completed for 'finding-6-chooser-io-error-triage' at 15:20:21-07:00
- starting the work on 'finding-7-sequence-proxy-denied-policy' at 15:21:00-07:00
        - scope: **test-only**. `git diff --stat` for this finding is one file, `claudine/cli/tests/sequence_schema.rs`, +115 lines. No production source was modified; the three perturbations described below were applied temporarily and reverted, and `git diff` over `claudine/cli/src/commands/wrap/` is empty at completion
        - test added: `sequence_proxy_target_partial_fails_typed_before_the_target_initialize` in `claudine/cli/tests/sequence_schema.rs`, an L1 process test (no tier prefix, no `#[ignore]`, selected by `just test`)
                - placement: `sequence_schema.rs` is the sequence area's schema-diagnostic suite and the only sequence binary that already builds through `CliProcessFixture` rather than a raw `assert_cmd::Command::cargo_bin` (it is absent from `spawn_site_guard.rs`'s allowlist), so the fixture's temp `cwd`/`home`/`bin` isolation applies by construction rather than by a per-test `.env` chain
                - fixture shape: a one-step sequence document whose `initialize` proxies to `./review.md`; the target is the **shipped** `prompts/review.md` via `include_str!`, matching the sibling `proxy_target_partial_is_resolved_before_the_target_initialize` in `compose_caller_file_provenance.rs` and the spec's "include a shipped-review-router regression fixture, not only helper-level tests"
                - a real candidate, `fixes/2026-09-10-local-affected-scope/spec.md` with `reviewed: true`, is seeded inside the fixture so the supplied partial `spec=fixes/2026-09-10-local` is genuinely completable and only the denied policy stops it; the provider is a fixture-owned Goose stub that appends to `call-count.txt`, so "no provider launched" is a file-existence assertion rather than an absence-of-output guess
                - a minimal hand-written target was tried first and **rejected**: with a plain (non-union) `$schema` mapping the run dies in Darkmatter's compose preflight with a generic "frontmatter did not satisfy the schema" and never reaches `initialize` at all, so it could not distinguish the two outcomes this finding is about. The shipped router's root-level union is what actually reproduces the reported shape
        - what it asserts, on the flattened stderr transcript (box glyphs dropped, whitespace collapsed, so the assertions name the message rather than the host's wrap points)
                - positive: `schema validation failed for`; `review.md` (the diagnostic belongs to the **adopted proxy target**, not the sequence document); `/spec` (the target's own property pointer); `no existing file matched reference`; `fixes/2026-09-10-local` (the caller's partial, verbatim)
                - negative, and this is the discriminator: **not** `lifecycle evaluation error` and **not** `invalid file path`. Those two are exactly what the original defect emits, so the test fails if the partial reaches `frontmatter(spec, ...)` instead of being stopped before it
                - negative: `call-count.txt` must not exist — no provider session for an unresolved supplied partial
                - observed passing transcript: `step 1/1 failed: schema validation failed for <fixture>/cwd/review.md: /spec: no existing file matched reference fixes/2026-09-10-local while resolving from <fixture>/cwd`. Note the failure precedes the `flow control redirected to review.md` announcement, which is the ordering the finding asked for: the target's supplied-file pass runs before anything the target's lifecycle can do
        - non-vacuity evidence — three perturbations, each applied, observed, and reverted
                - **P1 (red, and red for the right reason).** `claudine/cli/src/commands/compose/prep.rs`: gated the adopted-target `resolve_supplied_file_inputs` call off (`if !first` became `if !first && !cfg!(unix)`), so the partial reaches the target's `initialize`. The new test failed with `CompositionError: lifecycle evaluation error`, `A late-binding expression raised while the initialize lifecycle event was firing, in the when: guard (<fixture>/cwd/review.md)`, `Reason: invalid file path: fixes/2026-09-10-local`, and the step summary `lifecycle initialize evaluation error in when:`. That is the original bug, reproduced through the sequence entry. The sibling compose test `proxy_target_partial_is_resolved_before_the_target_initialize` was run under the same perturbation and produced the same lifecycle error, confirming the perturbation was genuine and not a fixture artifact
                - **P2 (green — recorded because it is a negative result, not a pass).** `claudine/cli/src/commands/wrap/sequence/iterate.rs`: replaced `InteractiveSchemaOptions::default()` in `run_step_proxy_loop` with all four gates set to `true`. The test still passed. This is expected and is stated plainly rather than hidden: in an L1 process there is no TTY, so `choose_provided_file_reference` cannot run a dialog and the outcome downgrades to the identical typed diagnostic. **The denied policy flag itself is not observable from L1.** What the test pins is the outcome contract the finding names — typed schema diagnostic, not lifecycle error — which is the part that can regress. A test that could discriminate the flag would need a PTY driving the sequence entry, i.e. a new sequence interaction mode, which the spec puts out of scope ("This fix does not introduce new sequence interaction modes"). The test's doc comment says this explicitly so a future reader does not mistake it for policy coverage
                - **P3 (red, and specific to the sequence entry).** `iterate.rs`: `run.compose.caller_input_records.clone()` became `Default::default()` at the `run_step_proxy_loop` call site. The test failed with the same `lifecycle evaluation error` / `invalid file path: fixes/2026-09-10-local`. This is the one that proves the test earns its place in the *sequence* suite rather than duplicating the compose sibling: it fails when the sequence entry's own caller-record plumbing into `run_step_proxy_loop` breaks, which P1's shared-code perturbation cannot show
                - after each perturbation the file was restored and re-verified: `git diff` over `claudine/cli/src/commands/wrap/` is empty, and the one `if !first {` line still present in `prep.rs`'s diff is finding 3's change, not residue
        - portability
                - the test body is portable by construction: no hard-coded path separators (`Path::join` throughout), no Unix-only API, no shell quoting of fixture paths, and no assertion on an absolute path spelling — the transcript assertions name the message and the repo-relative partial, both of which survive a Windows 8.3 temp path and a WSL2 mount
                - it is nevertheless **not reached on native Windows**, for one reason that predates it: `cli/tests/sequence_schema.rs` carries a file-level `#![cfg(unix)]`, because every existing test in it installs a `#!/bin/sh` provider stub. The new test follows that established pattern (`write_executable` with a `#!/bin/sh` counter stub) rather than introducing a second provider-stub convention into the file
                - the file-level gate was left alone deliberately. Every test in the file already carries its own `#[cfg(unix)]`, so the `#![cfg(unix)]` is redundant, but removing it would make the file compile on Windows where its `common` imports are unused — `-D warnings` in the lint gate would then fail the Windows leg. That is a separate cleanup, not this finding's
                - coverage of the same contract on native Windows already exists and is not lost: `proxy_target_partial_is_resolved_before_the_target_initialize` and `shipped_review_router_non_tty_partial_fails_before_initialize` in `compose_caller_file_provenance.rs` are cross-platform (that file compiles a native provider fixture on Windows) and pin the identical typed-diagnostic-before-initialize outcome for the compose proxy path. The sequence-specific delta is `run_step_proxy_loop`'s argument threading, which is platform-neutral Rust
                - so: macOS **verified locally**; Linux and WSL2 reached by the same `#![cfg(unix)]` leg and pending on CI; native Windows **not reached, by design of the host file**
        - verification (run from `claudine/`)
                - `just test sequence_` — 160 tests run, **160 passed**, 6711 skipped (3.9s); no sibling sequence test regressed
                - `just test` — 6860 tests run, **6860 passed**, 11 skipped (34.0s). One test more than finding 6's 6859, which is this test
                - `just lint` — clean across `claudine-catalog-types`, `claudine`, `claudine-contract`, `claudine-cli`, and `claudine-gen`; zero clippy warnings or errors; the lifecycle-doc-facets guard still passes. `cargo fmt` was **not** run
                - the sibling session's in-flight `claudine/test-support/` and audio-test edits did not collide; every suite compiled on the first attempt
        - comment/doc drift check: no production behavior changed, so no README, `docs/topics/composition.md`, `docs/topics/testing.md`, or claudine skill text needs an update. The test's own doc comment carries the two facts a future reader needs and cannot derive — why the shipped router is the fixture, and why the denied policy is not what the assertions discriminate
- work completed for 'finding-7-sequence-proxy-denied-policy' at 15:33:27-07:00
- final combined verification of all findings together, run from `claudine/` at 15:42:40-07:00
        - `just test` — 6860 tests run, **6860 passed**, 11 skipped (35.6s)
        - `just lint` — clean across `claudine-catalog-types`, `claudine`, `claudine-contract`, `claudine-cli`, and `claudine-gen`; zero clippy warnings or errors
        - `just test-l2 provided_partial review_router proxy_target_schema` — 9 tests run, **9 passed**, 2764 skipped
        - `cargo fmt` was **not** run at any point in this cycle
- two host-environment notes that are not attributable to this cycle's changes
        - the full `just test-l2` suite has two failures on this host: `level2_dry_run_document_cell_renders_osc8_link_in_wezterm` and `level2_initialize_proxy_block_auto_detects_osc8_in_wezterm`. Both panic on a captured WezTerm frame containing `Atuin AI is not yet configured. / Enable Atuin AI`, i.e. an Atuin onboarding overlay wedging the capture panes. Different binaries, untouched sources, no PTY or schema involvement — this is the recorded Atuin-wedge trap resurfacing in a new form, and it will redden any L2 capture run on this host until the overlay is suppressed
        - a session outside this cycle committed the finding-1 work to `feat/unifi` mid-run (`2f45d7328` and `8033cdaef`) and briefly staged, then restored, `claudine/test-support/locked_audio_spool.rs` from the in-flight silent-audio fix. Nothing in this cycle committed, stashed, or reverted anything; those commits were left in place

### Successful Completion

The implementation of review cycle 1 has completed successfully in 1 hour 26 minutes. During this implementation all 8 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 6 were fixed, 2 were deferred (see reasons below):

- **Finding 4 — L2 coverage uses a raw PTY rather than the shared terminal harness (medium, judgment call).** Deferred because it is not a defect, and the reviewer already recorded it as a deliberate, justified decision rather than an oversight. The delivered `expectrl` PTY tests match the sibling `level2_schema_prompt_pty.rs` suite and the pre-existing tests in the same file, the repo's own `rust-testing` taxonomy counts PTY as L2, and the properties under test are ordering and data flow rather than glyph rendering or key encoding. Converting the suite to a tmux capture harness would be a test-infrastructure change of materially larger scope than the rest of this cycle, and the review explicitly routes that decision to Ken ("If Ken wants a tmux capture for the acceptance path, the harness patterns in `level2_dry_run_metadata_capture.rs` apply directly"). **This one needs Ken's ruling before it can be closed either way.**
- **Finding 8 — shipped prompts that would still crash on a partial (informational, outside the spec).** Deferred because the review classifies it as outside this fix's scope, and it is prompt content rather than code. `prompts/_implement/implement-suggestions.md` and `prompts/clarify.md` declare `file(required; match(...))` inputs without `eager`, so the new early pass does not offer them a chooser. Changing a shipped prompt's schema alters behavior for every caller of those prompts and would need its own spec and its own regression fixtures. The finding's value is that it is now a written result of the shipped-prompt audit the spec asked for.

The files changed by this implementation cycle are:

- `claudine/cli/src/commands/compose/prep.rs` — gated the second `resolve_supplied_file_inputs` call on `!first` (finding 3), plus comment corrections (findings 3 and 5)
- `claudine/cli/src/commands/schema_interactive/supplied.rs` — `tracing::debug!` on genuinely failed chooser and file-resolution outcomes, leaving user decline/cancel silent (finding 6)
- `claudine/cli/src/commands/schema_interactive/mod.rs` — comment corrections on the deferral guard and on `resolve_unresolved_file_reference` (finding 5)
- `claudine/cli/tests/level2_provided_partial_file_pty.rs` — out-of-launch-area decoy fixture, `review_router_session_in` helper, new repo-root-launch test, replaced vacuous assertion, widget-count assertion, module-doc update (findings 1, 2, 5)
- `claudine/cli/tests/common/pty.rs` — `KBD_ENHANCEMENT_PUSH` raised to `pub(crate)` (finding 1)
- `claudine/cli/tests/compose_caller_file_provenance.rs` — new cross-platform L1 test `proxy_target_partial_is_resolved_before_the_target_initialize` (finding 3)
- `claudine/cli/tests/sequence_schema.rs` — new L1 test `sequence_proxy_target_partial_fails_typed_before_the_target_initialize` (finding 7)
