---
implementation_1: "2026-09-11T18:31:51-07:00"
implementation_2: "2026-09-11T20:20:54-07:00"
implementation_3: "2026-09-11T22:46:25-07:00"
implementation_4: "2026-09-11T23:40:13-07:00"
implementation_5: "2026-09-12T01:27:50-07:00"
implementation_6: "2026-09-12T09:22:04-07:00"
---

## Implementation of Review Findings #1

> **started at:** 2026-09-11T18:31:51-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/fixes/2026-09-11-cicd-cleanup/review-1.md'
- this is iteration 1 of the review-to-implement cycle
- orchestration plan decided at 18:34:00 after reading the review, spec, plan, rollout notes, and the open-questions document:
        - implementable in this cycle (no ruling needed): findings 1 (receipt environment/revision), 2 (dispatch scope setup), 3 (hook constraint ordering), 4 (compile target selectors), 5 (report retention), 8 (workflow contracts in CI), 10 (authoritative scope evidence)
        - blocked on unruled open questions and therefore expected to be deferred: finding 6 (`ci-verdict` removal needs OQ3/B3 plus the scratch-repository fixture B5), finding 7 (gap publication needs OQ4/B4 plus a live GitHub run), finding 9 (persistent constraint discovery needs OQ2/B2)
        - findings are worked serially by one subagent each; gates are run only in the impacted areas: `scripts/ci/*.py` suites, `.githooks/tests/test-pre-push.sh`, the `scripts/` Nextest bins, and `tools/test-toolkit`
        - host load at start: 16 logical cores, load average 3.70 (1 min), so timing is not a concern for these logic suites
- starting the work on 'Receipt verification can credit the wrong environment' at 18:33:39
        - discovered: reproduced all three defects against the committed `scripts/ci/local_evidence.py` before changing it — a v2 macOS receipt filed under `refs/notes/ci-local/wsl2-ubuntu` was accepted as `alpha/wsl2-ubuntu/L1`, a v1 macOS note under that ref was accepted as both `wsl2-ubuntu` cells, and a v2 receipt on the head declaring the base's head/tree was accepted as `prior-local` because equivalence compared the note's commit (the head) with the head; none produced a rejection
        - discovered: `_accept_legacy` took a `plan` argument it never read; the Rust rollup reads only `origins` and `accepted_gap_state` from `contract.json`, and `.github/ci/schemas/README.md` names `schema.REJECTIONS` without enumerating the codes, so the vocabulary change lands in `schema.py` plus the regenerated `contract.json` only
        - discovered: `tools/test-toolkit/justfile` has no `lint` recipe (only `verify-nextest-config`); the root `just lint` orchestrates every area, so the per-package `just _lint test-toolkit` was used instead
        - changed `scripts/ci/schema.py`: added `environment-mismatch` and `revision-mismatch` to `REJECTIONS` (the pre-existing unused `tree-mismatch` is untouched); the docstring edits in that file's diff predate this work
        - changed `.github/ci/schemas/contract.json`: regenerated with `python3 scripts/ci/schema.py` (two added vocabulary entries)
        - changed `scripts/ci/local_evidence.py`: `verify_cells` now rejects a v2 receipt whose declared `environment` differs from its notes ref (`environment-mismatch`) or whose declared `head`/`tree` differ from the commit the note is attached to and that commit's tree (`revision-mismatch`), both before the exact/equivalence decision; `_accept_legacy` applies the same environment check (its unused `plan` parameter became the `note_commit` the check names); `verify_cells` docstring left as-is since nothing in it became false
        - changed `scripts/ci/test_evidence_reuse.py`: new `NoteAttachmentTests` (wrong-ref v2 receipt credited to neither environment, wrong declared head, wrong declared tree, vocabulary pin) plus `LegacyReceiptTests.test_a_v1_note_under_the_wrong_environment_ref_is_credited_to_neither`
        - non-vacuity: the five new tests were run against a scratch copy of `scripts/ci` with `local_evidence.py` taken from `HEAD`; the four behavioral tests failed at `assertEqual([], self.keys(accepted))` with `['alpha/wsl2-ubuntu/L1']`, `['alpha/macos-latest/L1']` (x2), and `['alpha/wsl2-ubuntu/L1', 'beta/wsl2-ubuntu/L1']` respectively, and the vocabulary pin passed only because the scratch copy carried the new `schema.py`
        - gates: `python3 scripts/ci/test_evidence_reuse.py` 52 tests OK (47 before); `test_schema.py` 37 OK; `test_local_evidence.py` 5 OK; `test_resolved_plan.py` 36 OK; `python3 -m py_compile scripts/ci/*.py` OK; `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` 183 passed; `just test test-toolkit` 153 passed, 2 skipped; `just _lint test-toolkit` clean
- work completed for 'Receipt verification can credit the wrong environment' at 18:39:23
- starting the work on 'Manual full runs fail during scope setup' at 18:39:23
        - discovered: confirmed the finding by executing the extracted `run: |` body of the "Calculate package and area scope" step with `EVENT_NAME=workflow_dispatch`: exit 1, `bash: line 22: base: unbound variable`, before the planner is invoked; identical on the working-tree and `HEAD` copies of `ci.yml`
        - discovered: a second defect in the same step that the required test exposed: for `pull_request` and real-base `push` events the step built `scope_args=(-- <files>)` and then appended `--plan-out resolved-plan.json --base --head` AFTER the `--`, so argparse read all five tokens as file paths; the step exited 0, no `resolved-plan.json` was written (the `if-no-files-found: error` upload would have failed), and the plan's `base`/`head` stayed the null OID. Verified directly: `affected_scope.py -- README.md --plan-out /tmp/y.json` writes nothing
        - discovered: `scripts/ci/affected_scope.py --all` with a full plan takes ~0.65 s on this host, so the new test runs the REAL planner and evidence verifier four times (~3.7 s total) rather than stubbing them; the step's cwd-relative `scripts/ci/*.py` spellings are satisfied by two-line `runpy` trampolines in the temp cwd (no symlinks, so the fixture is portable to the Windows host), and `rust-toolchain.toml` is copied because the summary's `grep` runs under `pipefail`
        - changed `.github/workflows/ci.yml`: the `workflow_dispatch` branch now sets `base` to the all-zero OID and `head="$PUSH_HEAD"` (`github.sha`) with one comment stating the identity contract; the planner options are now placed ahead of the `--`/file list so they are parsed as options on every event
        - identity contract chosen for dispatch: base = `NULL_OID`, head = `github.sha`. `NULL_OID` is the planner's own documented "no revision pair resolved" value and exactly what a branch-creating push already carries in `github.event.before`, so the two full-run shapes record the same identity; `base == head` was rejected because it would claim a zero-width diff range that a reader could act on. Nothing verifies receipts against the pair (the verify step is skipped for dispatch) and `schema._sha` accepts the all-zero form
        - changed `scripts/ci/test_ci_local.py`: new `workflow_step_script()` extractor (fixed-layout `run: |` body reader; the Rust `job_block` and Python `jobs_in` helpers split whole jobs, not steps, so neither was reusable) and `WorkflowScopeStepTests` (4 tests: dispatch, pull_request, push with a real base, push with an all-zero `PUSH_BASE`), each asserting exit 0, a schema-valid written plan whose `head` is the fixture head, the expected `full_scope=` output, and the expected recorded `base`. The suite is already listed in the `ci-tooling` job and the `just ci-local` self-test loop, so no wiring changed
        - non-vacuity: the four tests were run with the extractor pointed at the pre-fix working-tree copy and at `git show HEAD:.github/workflows/ci.yml`; both gave `FAILED (failures=1, errors=2)`: the dispatch test failed with `AssertionError: 0 != 1 : bash: line 22: base: unbound variable`, and the pull_request and real-base push tests errored with `FileNotFoundError: .../resolved-plan.json` (the swallowed-options defect); only the all-zero-base push passed
        - gates: `python3 scripts/ci/test_ci_local.py` 21 tests OK (17 before); `test_resolved_plan.py` 36 OK; `test_runner_loss.py` 31 OK; `python3 -m py_compile scripts/ci/*.py` OK; `just test test-toolkit` 153 passed, 2 skipped; `just _lint test-toolkit` clean; `actionlint .github/workflows/ci.yml` exit 0 with no findings, and the repository-wide run still reports exactly the six pre-existing `SC2086:info` items in `_package-ci.yml`/`_wsl-ci.yml`
- work completed for 'Manual full runs fail during scope setup' at 18:45:16
- starting the work on 'The hook rejects constraints already satisfied by evidence' at 18:45:16
        - discovered: reproduced the finding against both the committed and the pre-edit working-tree hook — with a wsl2-ubuntu prohibition in place the hook exited 1 before resolving any plan, even for a plan whose only WSL cell is `reuse`d and for a plan with no WSL cell; it also passed no `--repository`, so a record scoped to another repository could not be filtered
        - discovered: two traps that shaped the design. (1) The planner, when handed the store, resolves an unsatisfied prohibited cell to `execution=omit, state=prohibited`, so `constraints.py check --plan` on such a plan saw no `execute` and would have reported the constraint SATISFIED — a false green. (2) `ci-local --plan` passes no repository or branch to `read_prohibitions()`, so letting it enforce would refuse on records scoped elsewhere. The hook therefore runs the plan resolution with `BISCUIT_CI_CONSTRAINTS_DIR=''` and makes `constraints.py check --plan` the single decision point; `unsatisfied()` additionally counts a `prohibited` cell as executing so a pre-resolved plan can never read as satisfied
        - discovered: `git rev-parse --show-toplevel` basename is `feat-unifi` in this worktree, so it is unusable as repository identity; chose the `origin` URL normalized to `host/path` (`github.com/yankeeinlondon/rusty-biscuit`), matching SSH, HTTPS, and `ssh://` spellings and every worktree of a clone; a checkout without `origin` matches every repository-scoped record (fail closed)
        - discovered: the hook's `while read` ref loop blocks forever when the test suite inherits an open stdin (a heredoc in the invoking command was enough); the suite now feeds every hook invocation `</dev/null`
        - discovered: `ci-local --plan` exits 0 without writing a plan only on "no changes relative to the base"; the hook treats an empty plan file after a successful resolution as scheduling nothing and skips the check, with the reason recorded in a comment
        - discovered: `RUSTY_BISCUIT_PRE_PUSH_AREAS` narrows only the local gates; the hook's plan resolution now deliberately omits the selection (previously `scope-only` passed it, which produced an `--all` plan unrelated to what CI schedules)
        - discovered: no pending-contract wrapper existed for this behavior in the hook suite, so none was removed; `ci_workflow_contracts.rs::ci_never_reads_the_execution_constraint_store` still requires the hook to reference `ci/constraints.py`, which the chosen shape preserves
        - changed `.githooks/pre-push`: every mode first runs `BISCUIT_CI_CONSTRAINTS_DIR='' just ci-local --plan --plan-out $PLAN_REVIEW_FILE` (non-zero exit blocks), then `python3 scripts/ci/constraints.py check --plan $PLAN_REVIEW_FILE --repository "$(git remote get-url origin)" --branch "$(git rev-parse --abbrev-ref HEAD)"` with the store directory resolved by Python (`directory_from_environment()`, so filling `default_directory()` needs no hook change); `scope-only` exits after that; header comment rewritten for the new order; the review file is in the cleanup trap; the `BISCUIT_CI_*_OUT` exports now follow the plan step so the review run cannot clobber them
        - changed `scripts/ci/constraints.py`: `repository_identity()` normalizer used by `applies_to`; `unsatisfied()` counts `state == "prohibited"` cells; `read_plan()` raises on an unreadable file or a plan without a readable `cells` list and `check` exits 1 naming the plan (fail closed even with an empty store); module docstring documents the repository identity; `--plan`/`--repository` help text
        - changed `scripts/ci/test_constraints.py`: 7 new tests (repository match across four URL spellings and mismatch, full-URL record spelling, prohibited-state cell unsatisfied, unreadable plan blocks with an empty store, plan without cells blocks, missing plan file blocks, CLI repository+branch filtering)
        - changed `.githooks/tests/test-pre-push.sh`: the fake `just` copies a fixture plan to `--plan-out`, records the store directory it was handed, and honors an exit-code file; `assert_log_lacks` anchors to the subcommand; hook invocations read `/dev/null`; the default-delegation test now expects `ci-local --plan --plan-out <file>` then `pre-push`; the AREAS test asserts the plan resolution is not narrowed; 9 new tests (reused → 0, absent → 0, executing → 1 with reason, scope-only refuses too, unreadable plan blocks, failed resolution blocks, planner handed no store, other-branch record does not block, fixtures validate against `schema.validate_resolved_plan`)
        - added `.githooks/tests/fixtures/plan-wsl-{executing,reused,absent}.json`: schema-valid resolved plans in the `test_ci_local.py` shape
        - changed `.github/ci/README.md` and `docs/topics/ci-cd.md`: one sentence each stating the hook decides from the resolved plan's executions and (README) the repository identity; `CLAUDE.md` and `.claude/skills/rust-devops/ci-cd.md` contained no sentence describing the old order and were left alone
        - non-vacuity: the extended hook suite run against `git show HEAD:.githooks/pre-push` and against the pre-edit working-tree hook fails 7 of 25 on both: `default path calls 'just pre-push' with no selection`, `a reused prohibited cell does not block`, `an absent prohibited environment does not block`, `an executing prohibited cell blocks with its reason`, `an unreadable plan blocks`, `a failed plan resolution blocks`, `the plan resolution is handed no constraint store` (18 passed)
        - real-hook smoke on this branch (`RUSTY_BISCUIT_PRE_PUSH=scope-only`, real `just`): with a temp store forbidding wsl2-ubuntu the hook rendered the plan and exited 1 with "Push blocked by a recorded execution constraint: wsl2-ubuntu: smoke: do not rerun WSL (set by ken, expires 2099-01-01)" because this head's WSL cells execute; with no store it exited 0
        - gates: `env -u CDPATH ./.githooks/tests/test-pre-push.sh` 25 passed, 0 failed (16 before); `python3 scripts/ci/test_constraints.py` 31 OK (24 before); `test_ci_local.py` 21 OK; `test_resolved_plan.py` 36 OK; `python3 -m py_compile scripts/ci/*.py` OK; `shellcheck -s sh .githooks/pre-push` clean; `just test test-toolkit` 153 passed, 2 skipped; `just _lint test-toolkit` clean
- work completed for 'The hook rejects constraints already satisfied by evidence' at 18:59:42
- starting the work on 'Published receipts point to reports the hook does not retain' at 18:59:42
        - discovered: confirmed the finding end to end — with the publication guard satisfied (pushed head, clean tree, `origin/main`, fake `sniff`, real `jq`/`python3`) the pre-fix hook published a receipt whose `host.report_dir` was `~/.rusty-biscuit/ci-evidence/<9-char head>`, a path nothing had written to, while the staged reports died with the hook's EXIT trap
        - discovered: no evidence-root override variable existed anywhere in the repo (`grep` for `ci-evidence`, `EVIDENCE_DIR`, `EVIDENCE_ROOT`); the host's `~/.rusty-biscuit/ci-evidence/` holds PR #74's per-feature directories (`2026-09-07-faster-*-tests/{baseline,candidate,...}`), so there was no per-head layout to match; chose `<root>/<full head sha>/<environment>/` and introduced `BISCUIT_CI_EVIDENCE_DIR` for the root (default `$HOME/.rusty-biscuit/ci-evidence`; a relative override is anchored at the hook's cwd so the recorded path is always absolute)
        - discovered: `/usr/bin/jq` and `/usr/bin/python3` exist on this host, so the suite's `PATH=$tmpdir:/usr/bin:/bin` never blocked publication — only `PUSHES_HEAD=0` (no ref on stdin) and the absent `sniff` did; the new fixtures feed a ref line on stdin, add a fake `sniff`, and symlink the real `jq`/`python3` into the shim dir for hosts where they live elsewhere
        - discovered: the hook resolves `$REPO_ROOT` from `git rev-parse --show-toplevel`, so running it inside a temp repository requires `scripts/ci/*.py` to be present (and committed, for the clean-tree guard) in that repository; the fixture copies them from the checkout, which also makes the pre-fix non-vacuity run a one-directory substitution
        - discovered: the repository config has `commit.gpgsign=true`; fixture commits run with `-c commit.gpgsign=false` so the suite never touches the signing key
        - changed `.githooks/pre-push`: when a receipt would be published, the staged reports are copied (`cp -R`) to `$BISCUIT_CI_EVIDENCE_DIR/<head sha>/<environment>/` (that environment subdirectory is replaced, not merged) BEFORE `record-cells`, which now receives `--report-dir <that absolute path>`; if the copy fails no receipt is written, the staging directory is dropped from the EXIT trap so the reports remain readable, and stderr names both the intended location and the surviving staging path; the "Published"/"Recorded" lines now name the retained directory; header documents the variable and the fail-closed rule
        - changed `scripts/ci/local_evidence.py`: `assemble_receipt` records `report_dir` verbatim and raises `ValueError` when no host block is supplied and the value is empty (the `~/.rusty-biscuit/ci-evidence/<head9>` fallback is gone); `record_cells` raises `ValueError` naming `--report-dir` when it is empty and raises when any complete cell's `report` is not a file under it (a receipt may not name reports that are not there); the CLI `--report-dir` is `required=True`; `record_cross_check` is unchanged because it already passes the remote stage path in its host block
        - changed `scripts/ci/test_evidence_reuse.py`: every `record_cells` call passes `report_dir=` (the fixture's stage directory outlives the call, so naming it is truthful); 3 new tests in `ReceiptRecordingTests` — recorded `host.report_dir` is exactly what was passed and every cell report is a file under it, an empty/whitespace `report_dir` is refused, a directory that does not hold the reports is refused
        - changed `.githooks/tests/test-pre-push.sh`: the fake `just` sources an optional `$tmpdir/pre-push.hook` in its `pre-push` branch; new fixtures build a temp repo (`main` pushed to a bare `origin`, one feature commit as HEAD, CI scripts committed), a fake `sniff`, and a staging `pre-push` that writes the plan to `$BISCUIT_CI_PLAN_OUT` and one L1 report + `manifest.jsonl` to `$BISCUIT_CI_REPORTS_OUT`; 2 new tests — the receipt (read back from the `refs/notes/ci-local/macos-latest` note) names `<override root>/<head>/macos-latest`, which exists after the hook exited, with every `cells[].report` and `manifest.jsonl` readable inside it; an unwritable evidence root exits 0 (gates passed), attaches no note, prints no "Published", and explains on stderr naming the location
        - added `.githooks/tests/fixtures/plan-macos-executing.json`: the `plan-wsl-absent.json` shape with its `alpha` L1 cell on `macos-latest` (covered by the existing fixture-validity test)
        - changed `.github/ci/README.md`, `docs/topics/ci-cd.md`, `.claude/skills/rust-devops/ci-cd.md`, `.claude/skills/os/SKILL.md`: one sentence each on where retained reports live, the `BISCUIT_CI_EVIDENCE_DIR` root, and that a failed copy publishes no receipt
        - non-vacuity: a temp root carrying the pre-fix hook (this change's hunk inverted on the current working-tree hook) and the pre-fix `local_evidence.py` (fabricated default restored, checks removed) with the new suites: hook suite 25 passed / 2 failed — `a published receipt names retained, readable reports` (receipt named `~/.rusty-biscuit/ci-evidence/0781c966d`) and `reports that cannot be retained publish no receipt`; `ReceiptRecordingTests` `FAILED (failures=3)` — the empty-dir subtests and the non-retaining-dir test; the verbatim-recording test passes pre-fix by design (it pins the contract, the other two pin the defect)
        - gates: `env -u CDPATH ./.githooks/tests/test-pre-push.sh </dev/null` 27 passed, 0 failed (25 before); `python3 scripts/ci/test_evidence_reuse.py` 55 OK (52 before); `test_local_evidence.py` 5 OK; `test_schema.py` 37 OK; `python3 -m py_compile scripts/ci/*.py` OK; `shellcheck -s sh .githooks/pre-push` clean; `just test test-toolkit` 153 passed, 2 skipped; `just _lint test-toolkit` clean
- work completed for 'Published receipts point to reports the hook does not retain' at 19:09:08
- starting the work on 'Compile execution still uses Windows-only blanket target checks' at 19:09:08
        - impact: `node .gitnexus/run.cjs impact <symbol> --direction upstream` for `package_cells`, `matrix_record`, `declared_target_kinds` — each risk LOW, epistemic exact, 1 direct caller, 1 process (`main`); grep confirms the callers are `calculate_scope`, `legacy_scope_document`, and the two Python suites
        - discovered: `_wsl-ci.yml` builds its nextest archive from `${{ inputs.check-args }}`, so putting `--examples`/`--benches` into `check_args` would have baked example/bench binaries into the archive the WSL guest runs; renamed that input to `archive-args` and `_package-ci.yml` now passes `-p <package> <test-args>` (byte-identical to the old `check_args` value) so the selectors cannot reach the guest
        - discovered: check cells were `reusable=True`, so a whole-environment receipt (`accepted_environments`) marked the `windows-latest` check cell reused; check cells are now never reusable (a receipt records `L1`/`L2`/`browser` only — `local_evidence.RECORDABLE_GATES`)
        - discovered: `git show HEAD:scripts/ci/affected_scope.py` predates this session tree's earlier edits (no `area_slug`), so the suites cannot import it; the non-vacuity run used the pre-edit working-tree planner reconstructed by reversing this change's hunks onto a temp copy
        - changed `scripts/ci/affected_scope.py`: deleted `CHECK_ENVIRONMENT`; added `CHECK_SELECTORS` (`example → --examples`, `bench → --benches`), `native_environments()` (not archive-only, runner == name; shared by `package_cells` and `matrix_record`), `uncovered_target_kinds()`, and `check_arguments(package, target_kinds, features)` rendering `-p <pkg> <selectors> <features>`; `package_cells` schedules one `check` cell per native environment for the uncovered kinds (`reusable=False`, reason names the kinds and the environment, and ubuntu-latest's reason adds that it stands for the archive-only guest it builds for); `matrix_record` takes `target_kinds`, `check_args` uses `check_arguments`, `check_os` is every native environment whose check cell executes; the plan's package record uses `check_arguments` and `legacy_scope_document` forwards `entry["targets"]`; a package with no uncovered kind keeps `-p <pkg> <features>` (no check cell, and `_package-ci.yml` never runs the job)
        - changed `.github/workflows/_package-ci.yml`: check step is `cargo check ${{ inputs.check-args }}` (no `--all-targets`), renamed `cargo check (declared example/bench kinds)`; job header comment, `check-args` and `check-os` input descriptions rewritten (`check-os` default is now the three native environments); `wsl2` job passes `archive-args: -p ${{ inputs.package }} ${{ inputs.test-args }}`
        - changed `.github/workflows/_wsl-ci.yml`: `check-args` input renamed `archive-args` (both the `workflow_call` and `workflow_dispatch` declarations and the archive step)
        - changed `just/ci-local.just`: the unreachable check-only branch runs `cargo check ${check_args}` and its header line no longer quotes `--all-targets`; reachability untouched
        - changed docs: `.github/ci/README.md` § Compile-check, `docs/topics/ci-cd.md` (scope paragraph and the "does not guarantee" bullet, now about the WSL2 guest), `.claude/skills/rust-devops/ci-cd.md` (check-cell bullet); `CLAUDE.md`'s "Compile coverage is per target kind" bullet was already true and is unchanged; `ci-rollup`, `runner_loss.py`, `_area-ci.yml`, `ci-runners.md`, and the schema README needed no change (they derive cells from the plan or only use `check (windows-latest)` as a label example)
        - changed `scripts/ci/test_affected_scope.py`: `package()` fixture takes `targets`; `EstimateJobsTests` expects 3 check jobs; the forwarding contract asserts `cargo check ${{ inputs.check-args }}`, `archive-args: -p ${{ inputs.package }} ${{ inputs.test-args }}`, and that `_wsl-ci.yml` no longer reads `check-args`; new `CheckCellTests` (6: check on each native environment and never on wsl2-ubuntu; selectors exactly `--examples` / `--benches` / both / none with no `--all-targets`, `--lib`, `--bins`, `--tests`; L1-only kinds get no cell and no selector; a reused macOS L1 plus whole-environment acceptance leaves the macOS check executing; a prohibited Windows turns its check cell `prohibited`; `check_os` lists every executing check environment) and `CheckCellScopeTests` (2: synthetic workspace through `calculate_scope` and `legacy_scope_document` with macOS L1 reused and Windows prohibited → `check_os == ["ubuntu-latest", "macos-latest"]`)
        - changed `scripts/ci/test_resolved_plan.py` `TargetCoverageTests` (+4, real workspace): biscuit-speaks (examples only) gets check cells on exactly ubuntu/windows/macOS and its WSL L1 cell says `ubuntu-latest archive build`; every full-scope package's `check_args` carries exactly the selectors of its declared uncovered kinds and the workspace still holds all four shapes (none / examples / benches / both); macOS L1 reused keeps the macOS check executing and `check_os` at three; the workflow's `run: cargo check ${{ inputs.check-args }}` joined with biscuit-speaks's `check_args` yields `cargo check -p biscuit-speaks --examples --features playa` with no blanket flag
        - changed `tools/test-toolkit/tests/ci_workflow_contracts.rs`: the canonical-recipe test asserts `run: cargo check ${{ inputs.check-args }}`; new `the_check_command_adds_no_blanket_target_flag` (no non-comment line in any `_package-ci.yml` job carries `--all-targets`; the `wsl2` delegation passes `archive-args: -p <package> <test-args>` and never `inputs.check-args`; `_wsl-ci.yml`'s archive job reads `archive-args` alone); `BUILD_COMMANDS` entry is `cargo check ` so provisioning-order coverage survives the rename
        - cell counts (`just ci-local --plan --plan-out`, this head, no gate run): before 42 cells / 47 jobs / 3 check cells (biscuit-speaks, claudine, sniff — all windows-latest); after 48 cells / 53 jobs / 9 check cells (the same three packages × ubuntu-latest, windows-latest, macos-latest; none on wsl2-ubuntu); rendered `check_args`: `-p biscuit-speaks --examples --features playa`, `-p claudine --benches`, `-p sniff --examples --benches --features remote`
        - non-vacuity: the new/changed Python tests run against the reconstructed pre-edit planner: 16 test methods ran, 19 failures + 10 errors — every one of the 13 new methods and the updated estimate fixture fail (only the 3 pre-existing `TargetCoverageTests` pass); the Rust assertion's predicate replayed over `git show HEAD:.github/workflows/_package-ci.yml` finds two non-comment job lines carrying `--all-targets` (`- name: cargo check --all-targets`, `run: cargo check --all-targets ${{ inputs.check-args }}`) and no `archive-args` forwarding
        - gates: `python3 scripts/ci/test_affected_scope.py` 108 OK (100 before); `test_resolved_plan.py` 40 OK (36 before); `test_schema.py` 37 OK; `test_ci_local.py` 21 OK; `test_evidence_reuse.py` 55 OK; `test_runner_loss.py` 31 OK; `python3 -m py_compile scripts/ci/*.py` OK; `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` 183 passed; `--bin ci-plan` 9 passed; `just test test-toolkit` 154 passed, 2 skipped (153 before); `just _lint test-toolkit` clean; `actionlint .github/workflows/_package-ci.yml` exit 0 with only the 5 pre-existing `SC2086:info` items (plus the 1 pre-existing item in `_wsl-ci.yml`; no new findings)
- work completed for 'Compile execution still uses Windows-only blanket target checks' at 19:22:50
- starting the work on 'Workflow regression contracts are not executed by CI' at 19:22:50
        - discovered: `tools/test-toolkit` declares `[package.metadata.ci] gates = false` (promotion-pending, expiry 2026-10-31), so the `ci_workflow_contracts` binary was scheduled by no area job; `git show HEAD:.github/workflows/ci.yml` contains zero references to `ci_workflow_contracts`, and the `ci-tooling` leg ran only the eight Python suites plus the `ci-rollup` and `ci-plan` bins. `affected_scope.py` set `flags["ci_tooling"]` from `CI_TOOLING_PREFIXES = ("scripts/", ".github/ci/")` only: a `.github/workflows/*.yml` edit widened every package gate under the per-gate rule but did not flip `ci_tooling`, so a workflow-only change touching no package could schedule nothing
        - discovered: the suite needs no `sniff`. Its only host processes are `cargo metadata` (`package_policies`) and `just _tier_filter` (`nextest_filter`), both provisioned in `ci-tooling` via `rustup show` and `taiki-e/install-action@just`; no test carries `require_level!`, `available()`, or `#[ignore]`, so zero tests self-skip on a hosted Ubuntu runner (baseline run: 83 passed, 0 skipped). The Cargo test target is `ci_workflow_contracts` (kind `test`), confirmed via `cargo metadata`; the `ci` nextest profile's `junit = { path = "test-results.xml" }` resolves under the store dir and needs nothing extra for a `-p` invocation
        - completed: `.github/workflows/ci.yml` — new `Test the workflow contracts` step in `ci-tooling`, placed after the Python suites and before the rollup bin: `cargo nextest run -p test-toolkit --test ci_workflow_contracts` with the same `NEXTEST_TEST_THREADS` export as its neighbors (`NEXTEST_PROFILE: ci` is workflow-wide)
        - completed: `scripts/ci/affected_scope.py` — `CI_TOOLING_PREFIXES` gains `".github/workflows/"`, new `CI_TOOLING_PATHS = {"tools/test-toolkit/tests/ci_workflow_contracts.rs"}`, and the flag now checks prefix-or-path over the normalized file list; the block comment says why (workflow edits can select zero packages while the one suite inspecting workflows lives in a `gates = false` package)
        - completed: `scripts/ci/test_affected_scope.py` — three new `CiToolingFlagTests` methods: `test_workflow_changes_set_the_tooling_flag` (`ci.yml`, `_area-ci.yml`, `./release-plz.yml`), `test_the_workflow_contract_suite_sets_the_tooling_flag` (forward and backslash spellings), `test_other_test_toolkit_and_docs_changes_leave_the_tooling_flag_alone` (`tests/audio_spool.rs`, `src/lib.rs`, `docs/topics/ci-cd.md`, `.github/dependabot.yml`)
        - completed: `tools/test-toolkit/tests/ci_workflow_contracts.rs` — new `ci_tooling_leg_runs_the_workflow_contract_suite` asserts the `ci-tooling` job block contains `cargo nextest run -p test-toolkit --test ci_workflow_contracts` and that `affected_scope.py` carries the `CI_TOOLING_PATHS` entry; the existing `ci_tooling_changes_schedule_the_tooling_leg` literal updated to the three-prefix tuple
        - completed: docs — `.github/ci/README.md` reuse-boundary sentence now says the CI-tooling job also runs `ci_workflow_contracts`, and the "CI's own tooling" paragraph says the leg runs the R11 suite and that workflow/suite edits set the flag, with promotion still the durable fix; `docs/topics/ci-cd.md` names the suites the tooling leg runs. `.claude/skills/rust-devops/ci-cd.md` carries no claim about where the suite runs (its line 317 only says workflow changes require the contract suites), so it is unchanged; `rollout-2026-09-11.md` §5.1 left as the historical record
        - decision: `preflight` left alone and nothing added to the `just ci-local` self-test loop. `preflight` compiles no Rust (its steps are `rustup show`, `cargo metadata --no-deps`, `just check-canonical`, and Python suites), so the decision rule says do not add it there; the `ci-local` loop is Python-only by design and a cold `test-toolkit` test compile is not cheap, so the suite runs once, in `ci-tooling`
        - non-vacuity: the three new planner tests run against the pre-edit planner (my hunk reversed onto `/tmp/ci-pre/affected_scope.py`) yield `Ran 5 tests, FAILED (failures=5)` — every subtest of the two positive methods fails and only the negative method passes; the Rust guard's two assertion strings (`cargo nextest run -p test-toolkit --test ci_workflow_contracts`, `CI_TOOLING_PATHS`) each match zero lines in `git show HEAD:` of `ci.yml` and `affected_scope.py`
        - gates: `python3 scripts/ci/test_affected_scope.py` 111 OK (108 before); `test_resolved_plan.py` 40 OK; `test_ci_local.py` 21 OK; `python3 -m py_compile scripts/ci/*.py` OK; `NEXTEST_PROFILE=ci cargo nextest run -p test-toolkit --test ci_workflow_contracts` 84 passed, 0 skipped (83 before), 0.66 s after compile; `just test test-toolkit` 155 passed, 2 skipped (154 before); `just _lint test-toolkit` exit 0, 0 warnings; `actionlint .github/workflows/ci.yml` exit 0 with no output (same as HEAD's copy); rustfmt `--check` reports the same 20 pre-existing diffs in the contract file as at HEAD and none in the edited hunks
- work completed for 'Workflow regression contracts are not executed by CI' at 19:28:07
- starting the work on 'Authoritative local scope evidence is not implemented' at 19:28:07
        - discovered: `ci.yml` fires `push` only for `main` (`on: push: branches: [main]`); a feature-branch push is validated by a `pull_request` event whose base is the tip of `main`, so a scope receipt recording the branch's previous remote tip as `base` (the instructed "remote_sha when real" rule) could never hit on the common path — the spec's R1 ("merge base with origin/main") and R3 ("exact event comparison base") are only both satisfiable by an event-aware rule
        - decision (base identity rule): the base is the comparison base the CI event for THIS push will carry — when the remote ref carrying HEAD is `refs/heads/main` and `remote_sha` is a real local commit, `base = remote_sha` (the push event's `github.event.before`); for any other ref, or an all-zero `remote_sha`, `base = git merge-base origin/main HEAD` (equal to `pull_request.base.sha` until `main` advances, which the spec says must invalidate local scope). The hook prints one line naming the head, base, and kind. Deviation from the instructed rule recorded here deliberately; the hook test `a feature push records the PR base, not its previous tip` pins it
        - decision (evidence path): Option B — on a scope hit with accepted validation cells the planner is re-invoked once (full recompute with `--accepted-cells`) and its `{areas, packages}` selection is compared with the receipt's; drift fails the scope step with `::error::` rather than either side silently winning. Authority on the evidence path is therefore "compared, not handed off". Option A was rejected because `legacy_scope_document`/`matrix_record`/`policy_record` read policy fields the plan's package record does not carry (`l1_include_slow`, the policy-vocabulary `gates`, a non-gating package's `exclusion`), so re-deriving both documents from the plan alone needs a contract amendment, not ~80 lines
        - decision: the optional tie-in (require a v2 validation receipt's `scope_identity` to equal the scope receipt's plan identity) is NOT implemented: the hook's validation plan comes from `just pre-push` (worktree changes, merge-base base) and the scope plan from the committed set against the event base, so their identities legitimately differ; making it a requirement would reject valid receipts. Follow-up
        - completed: `scripts/ci/schema.py` — `SCOPE_RECEIPT_SCHEMA_VERSION = 1`, `SCOPE_RECEIPT_FIELDS` (`schema_version, base, head, tree, plan_schema_version, plan, scope`), `SCOPE_PROJECTION_FIELDS` (the `scope.json` keys `ci.yml` reads), sibling vocabulary `SCOPE_REJECTIONS` (`scope-missing, scope-schema, scope-head-mismatch, scope-tree-mismatch, scope-base-mismatch, scope-malformed`), `validate_scope_receipt` (structure: carried plan validates and names the receipt's base/head; projection carries every workflow key and the plan's packages), `_keys` takes a code; `contract.json` regenerated with a `scope_receipt` section
        - completed: `scripts/ci/local_evidence.py` — `SCOPE_NOTES_REF = refs/notes/ci-local/scope` (asserted not an environment), `record_scope` (refuses a non-ancestor base), `verify_scope` (event head only; R3 order schema → head → tree → base → structure), CLI `scope-record --plan --scope --base --head` and `scope-verify --base --head --plan-out --scope-out [--reason-out]` (hit: writes both canonical documents, exit 0; miss: coded reason, exit 3)
        - completed: `.githooks/pre-push` — captures the HEAD ref line's remote ref/sha; helpers `attach_note`/`push_note` shared by both receipts; `publish_scope_evidence` runs in every mode after the constraint check and before the gates/`scope-only` exit, invokes the planner exactly as `ci.yml` does (options, `--`, committed `git diff --name-only -z base HEAD` via `xargs -0`; empty diff handled), `scope-record`, note, push; CALCULATION vs PUBLICATION failures are named distinctly and never change the exit code; header updated
        - completed: `.github/workflows/ci.yml` scope step — `scope-verify` runs first for non-dispatch events with a real base; hit: carried documents are the plan/scope and the planner is not invoked for selection; miss: planner as before; summary row `| scope source | local (refs/notes/ci-local/scope @ <head9>) | CI fallback (<code: reason>) | CI (workflow_dispatch ignores local scope) | CI (no comparison base) |`; Option B comparison on the accepted-cells re-invocation
        - completed: tests — `scripts/ci/test_local_evidence.py` +15 (`ScopeReceiptTests`: exact hit, byte-stable, non-ancestor refused, each miss code, older commit's note never consulted, scope ref invisible to `verify_cells`, CLI hit/miss); `scripts/ci/test_schema.py` +8 (`ScopeReceiptValidationTests` + vocabulary disjointness/contract); `scripts/ci/test_ci_local.py` `WorkflowScopeStepTests` +9 (hit with the planner never invoked and the marker carried through, byte-identical plan, base/head/tree/schema/missing fallbacks with codes, dispatch never consults, Option B compare-equal reuse, drift fails loudly) — fixture now seeds three commits and a real-package source path, planner trampoline logs invocations; `.githooks/tests/test-pre-push.sh` +6 with committed stub `.githooks/tests/fixtures/affected_scope_stub.py` (the real planner needs the Cargo workspace it lives in, which the fixture repo lacks; `local_evidence.py` and the notes plumbing are real) and a `.gitignore` for `__pycache__/` in the fixture repo (the pre-gate `scope-record` import created one and dirtied the tree, withholding the validation receipt — the real repo already ignores it)
        - completed: docs — `.github/ci/README.md` (scope receipt vs validation receipt, base rule, miss codes, Option B), `docs/topics/ci-cd.md` (scope receipt paragraph + `scope-only` line), `.claude/skills/rust-devops/ci-cd.md` (live-as-of note, bypass-modes paragraph, mode table), `CLAUDE.md` (one clause), `.github/ci/schemas/README.md` (third document row + section), `fixes/2026-09-11-cicd-cleanup/plan.md` (the "narrowing" paragraph replaced), hook header
        - non-vacuity: `WorkflowScopeStepTests` against the reconstructed pre-edit `ci.yml` (scope-verify block, summary row, and drift check inverted) → `Ran 13, FAILED (failures=11)`: all 9 new methods fail (`test_a_matching_scope_receipt_is_the_plan_and_the_planner_never_runs`, `..._byte_identical_to_the_receipts`, `..._another_base_falls_back_with_its_code`, `..._missing_receipt_falls_back_with_its_code`, `..._another_head_or_tree_falls_back` (both subtests), `..._another_schema_version_falls_back`, `..._manual_run_never_consults_the_receipt`, `..._compared_planner_run`, `..._disagrees_with_the_planner_fails_the_step_loudly`) plus the extended `test_a_branch_creating_push_runs_the_full_scope` (scope source row absent); hook suite against the reconstructed pre-edit hook → `Passed: 27, Failed: 6`, exactly the six new tests
        - gates: `python3 scripts/ci/test_ci_local.py` 30 OK (21 before); `test_evidence_reuse.py` 55 OK; `test_local_evidence.py` 20 OK (5 before); `test_schema.py` 45 OK (37 before); `test_resolved_plan.py` 40 OK; `test_affected_scope.py` 111 OK; `test_constraints.py` 31 OK; `python3 -m py_compile scripts/ci/*.py` OK; `env -u CDPATH ./.githooks/tests/test-pre-push.sh </dev/null` Passed 33, Failed 0 (27 before); `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` 183 passed; `just test test-toolkit` 155 passed, 2 skipped; `just _lint test-toolkit` clean; `actionlint .github/workflows/ci.yml` clean; `shellcheck -s sh .githooks/pre-push` clean
        - blocked: nothing; GitNexus impact/detect-changes were not run for the Python/shell edits (no commit made here; the orchestrator's commit step should run `detect-changes --scope all`)
- work completed for 'Authoritative local scope evidence is not implemented' at 19:51:13
- starting the evaluation of 'The standalone global policy verdict remains' at 19:51:13
        - blocked: removing `ci-verdict` requires the merge-gate mechanism selected under Open Question 3 (blocker B3), which the spec says may not be implemented before Ken rules, and the review itself says "Do not remove the current check in isolation"
        - blocked: the selected mechanism must first be exercised in a scratch repository with ruleset writes (blocker B5); this session is non-interactive and holds no authorization for a push, workflow dispatch, scratch repository, or ruleset edit
        - discovered: the pending-contract wrappers `no_standalone_global_verdict_job_remains` and `the_verdict_consumers_are_rewired_when_the_job_goes` in `tools/test-toolkit/tests/ci_workflow_contracts.rs` still pass as pending, so the day the job is removed they fail and demand deletion; nothing further can be done without B3/B5
        - deferred
- work completed for 'The standalone global policy verdict remains' at 19:51:13 (deferred)
- starting the evaluation of 'Accepted-gap publication and hosted presentation verification are incomplete' at 19:51:13
        - blocked: the check-run conclusion (`cancelled` versus `neutral`) is Open Question 4 (blocker B4), unruled; the publisher's one behavioral choice is exactly that conclusion, and granting `checks: write` to a job is part of the mechanism being chosen
        - blocked: the hosted presentation fixture (throwaway branch plus a live run) is blocker B5 and needs a push this session cannot make
        - discovered: the `ACCEPTED GAP` machine-readable state, its owner/expiry/policy-link detail, and the rollup rule that it never derives from a GitHub conclusion are already implemented and tested (`ci-rollup` 183/183); only publication and live verification remain
        - deferred
- work completed for 'Accepted-gap publication and hosted presentation verification are incomplete' at 19:51:13 (deferred)
- starting the evaluation of 'Constraints do not survive an unconfigured fresh session' at 19:51:13
        - blocked: where the store lives is Open Question 2 (blocker B2); the spec forbids starting the affected section before Ken rules, and `constraints.default_directory()` is the single line that encodes the choice
        - completed as a side effect of finding 3: the hook no longer gates the store on `BISCUIT_CI_CONSTRAINTS_DIR` being set in shell; it always calls `constraints.py check`, which resolves the directory through `directory_from_environment()` (environment variable, else `default_directory()`), so the ruling needs only that one Python line and no hook change
        - deferred: the fresh-session discovery test (record in one session, plan/push in another with the variable unset) is meaningless until the default exists, so it is not written
- work completed for 'Constraints do not survive an unconfigured fresh session' at 19:51:13 (deferred)
- starting the combined verification pass over every impacted gate at 19:51:13
        - completed: every impacted gate re-run together after the last edit, all green: `test_schema.py` 45, `test_affected_scope.py` 111, `test_resolved_plan.py` 40, `test_evidence_reuse.py` 55, `test_local_evidence.py` 20, `test_constraints.py` 31, `test_ci_local.py` 30, `test_reuse_validation.py` 15, `test_runner_loss.py` 31, `py_compile` clean, hook suite 33/33, `shellcheck -s sh` clean, `actionlint` only the six pre-existing `SC2086:info` items, `ci-rollup` 183/183, `ci-plan` 9/9, `clippy` on both bins clean, `just test test-toolkit` 155 passed / 2 skipped, `just _lint test-toolkit` exit 0
        - completed: `just ci-local --plan --plan-out` against this head exits 0 in the new shape: 6 areas, 48 cells, 53 jobs, 9 `check` cells (three packages with example or bench targets, each on ubuntu, windows, and macOS, none on WSL), 0 prohibited cells, the lone v1 macOS receipt still refused as `v1-not-equivalence-eligible`
        - note: GitNexus `detect-changes` was not run because nothing is committed in this cycle; it must run before the commit that lands these edits
- combined verification pass completed at 19:53:38

### Successful Completion

The implementation of review cycle 1 has completed successfully in 1h 21m. During this implementation all 10 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 7 were fixed, 3 were deferred (see reasons below):

- **The standalone global policy verdict remains** — deferred. Removing `ci-verdict` needs the merge gate chosen under Open Question 3 (blocker B3) and its scratch-repository proof (blocker B5); the spec forbids implementing that section before Ken rules, the review forbids removing the check in isolation, and this non-interactive session holds no push, dispatch, or ruleset authorization.
- **Accepted-gap publication and hosted presentation verification are incomplete** — deferred. The check-run conclusion is Open Question 4 (blocker B4) and the live presentation fixture is blocker B5; the half that holds under every option (the distinct `ACCEPTED GAP` state and its detail) was already implemented and stays tested.
- **Constraints do not survive an unconfigured fresh session** — deferred. The store's location is Open Question 2 (blocker B2). Finding 3's fix already routes the hook through `directory_from_environment()`, so the ruling now costs one line in `constraints.default_directory()` and no hook change; the cross-session test is written once that default exists.

No finding was deferred for a performance-measurement reason, so `deferred_perf_measurement` is not set.

The working-tree files this fix now touches, as reported by `git status` (this includes files already modified by earlier phases before this cycle began; the files each finding actually edited are named in that finding's sub-bullets above):

        - `.claude/skills/os/ci-runners.md`
        - `.claude/skills/os/macos.md`
        - `.claude/skills/os/SKILL.md`
        - `.claude/skills/rust-devops/ci-cd.md`
        - `.claude/skills/sniff/remote-and-repository.md`
        - `.claude/skills/sniff/SKILL.md`
        - `.githooks/pre-push`
        - `.githooks/tests/fixtures/`
        - `.githooks/tests/test-pre-push.sh`
        - `.github/ci/ci-baseline.toml`
        - `.github/ci/environments.json`
        - `.github/ci/README.md`
        - `.github/ci/schemas/contract.json`
        - `.github/ci/schemas/README.md`
        - `.github/workflows/_area-ci.yml`
        - `.github/workflows/_package-ci.yml`
        - `.github/workflows/_wsl-ci.yml`
        - `.github/workflows/ci-infra-retry.yml`
        - `.github/workflows/ci.yml`
        - `.github/workflows/pr-health.yml`
        - `CLAUDE.md`
        - `docs/topics/ci-cd.md`
        - `just/ci-local.just`
        - `scripts/ci-rollup-tests.rs`
        - `scripts/ci-rollup.rs`
        - `scripts/ci/affected_scope.py`
        - `scripts/ci/constraints.py`
        - `scripts/ci/local_evidence.py`
        - `scripts/ci/runner_loss.py`
        - `scripts/ci/schema.py`
        - `scripts/ci/test_affected_scope.py`
        - `scripts/ci/test_ci_local.py`
        - `scripts/ci/test_constraints.py`
        - `scripts/ci/test_evidence_reuse.py`
        - `scripts/ci/test_local_evidence.py`
        - `scripts/ci/test_resolved_plan.py`
        - `scripts/ci/test_runner_loss.py`
        - `scripts/ci/test_schema.py`
        - `sniff/lib/src/filesystem/git/open.rs`
        - `sniff/lib/src/filesystem/git/remote_refresh.rs`
        - `sniff/lib/tests/git_parity.rs`
        - `tools/test-toolkit/tests/ci_workflow_contracts.rs`

## Implementation of Review Findings #2

> **started at:** 2026-09-11T20:20:54-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/fixes/2026-09-11-cicd-cleanup/review-2.md'
- this is iteration 2 of the review-to-implement cycle
- orchestration plan decided at 20:26:40 after reading the review, spec, `open-questions-and-blockers.md`, the rollout record, the hook, `just/ci-local.just`, `local_evidence.py`, the `ci.yml` scope step, `constraints.py`, and the shell fixture in `test_ci_local.py`:
        - implementable in this cycle (no ruling needed): finding 7 (workflow shell tests must resolve a Bash with `mapfile`), finding 3 (apply evidence to the carried plan instead of recomputing), finding 1 (trigger plan from the outgoing revision and committed manifests/policy), finding 2 (newest qualifying evidence per cell across every note on an environment ref), plus the AC14 drift the review notes in `.claude/skills/os/SKILL.md`
        - still blocked on unruled open questions, so expected to be deferred again: finding 4 (`ci-verdict` removal needs OQ3/B3 and the scratch-repository fixture B5), finding 5 (the gap publisher needs OQ4/B4 and the hosted fixture B5), finding 6 (the constraint store default needs OQ2/B2); `open-questions-and-blockers.md` is still `status: awaiting-ken` and the spec carries no `## Rulings` section
        - order chosen: 7 → 3 → 1 → 2 → AC14 doc, because 7 unblocks the extracted-shell fixture on this host (`/bin/bash` is 3.2 and lacks `mapfile`; `/opt/homebrew/bin/bash` is 5.3), 3 creates the "apply accepted cells to an existing plan" operation that 1's hook path then reuses, and 2 is independent of both
        - measured for finding 1's design: `git worktree add --detach` of the committed head costs 2.6 s on this host for 10,892 tracked files, `cargo metadata --offline` inside it 1.3 s, removal 0.5 s — cheap enough for a pre-push hook that only materializes the committed tree when the checkout is dirty
        - findings are worked serially by one subagent each; gates are run only in the impacted areas: `scripts/ci/*.py` suites, `.githooks/tests/test-pre-push.sh`, the `scripts/` Nextest bins, and `tools/test-toolkit`
        - host load at start: 16 logical cores, load average 9.19 (1 min); no finding needs a timing measurement, so this only affects wall time
- starting the work on 'Workflow shell tests do not declare their Bash requirement' at 20:26:40
        - discovered: GitNexus upstream impact on `WorkflowScopeStepTests` and on `scripts/ci/test_ci_local.py` returned `risk: UNKNOWN` with zero callers; `grep -rn` over `scripts/`, `.github/`, `just/`, `tools/` finds only the two script runners (`.github/workflows/ci.yml:435` and the self-test loop at `just/ci-local.just:298`), neither of which imports a symbol, so the file's internals are free to change
        - discovered: on this host `/bin/bash` reports `BASH_VERSINFO` `3 2` and `/opt/homebrew/bin/bash` `5 3`; `which -a bash` lists Homebrew first, which is the PATH-ordering accident that made the suite pass for the implementer and fail for the reviewer; `/usr/local/bin/bash` and `/usr/bin/bash` do not exist here; macOS 26 ships `/usr/bin/jq`
        - discovered: a `PATH=/usr/bin:/bin` demonstration cannot exercise the skip path on this host, because the well-known fallback list correctly finds `/opt/homebrew/bin/bash` and the class then runs — and fails only because the real planner needs `cargo` from `~/.cargo/bin`; that is a fixture-environment artifact, not the resolver, and it drove the override-is-exclusive decision below
        - changed `scripts/ci/test_ci_local.py`: added `bash_version` (probes `BASH_VERSINFO` major/minor, tolerates OSError, timeout, non-zero exit, and malformed output), `bash_runs_the_scope_step` (major >= 5, or 4 with minor >= 4, for `mapfile -d`), `bash_candidates` (`BISCUIT_TEST_BASH` override, then every `bash` on each `PATH` entry — `bash.exe` per `PATHEXT` on Windows — then `/opt/homebrew/bin/bash`, `/usr/local/bin/bash`, `/usr/bin/bash`, and `%ProgramFiles%\Git\bin\bash.exe`), `resolve_step_bash`, and the module constant `STEP_BASH` resolved once at import
        - changed `scripts/ci/test_ci_local.py`: an explicit `BISCUIT_TEST_BASH` is the only candidate considered — an incompatible override is reported as the prerequisite failure rather than quietly replaced by another Bash, since a silent fallback would hide the misconfiguration the override was set to fix
        - changed `scripts/ci/test_ci_local.py`: `WorkflowScopeStepTests` drops the `skipUnless(shutil.which("bash") ...)` decorator; `setUpClass` is the single decision point — with no compatible Bash (or no `jq`) it raises `unittest.SkipTest` off-CI and `AssertionError` when `CI` is set, both carrying `requires Bash >= 4.4 (\`mapfile -d\`) and jq; set BISCUIT_TEST_BASH to point at one (tried: ...)`; the step now runs under `[STEP_BASH, "-c", ...]` instead of bare `"bash"` (the only such call site in the file); class docstring documents the override
        - changed `scripts/ci/test_ci_local.py`: new `StepBashResolverTests` (4 tests) using real `#!/bin/sh` fake executables in a temp dir with `well_known=()` for hermeticity — `3 2`, `4 3`, and `exit 7` rejected, `4 4` accepted, first compatible in PATH order wins; override beats PATH and an incompatible override yields `None`; a missing PATH entry or missing override is skipped without error; candidate ordering pins override-exclusive and well-known-last (this last test also runs on Windows; the three script-backed ones skip there)
        - non-vacuity: `env -u CI BISCUIT_TEST_BASH=/bin/bash python3 scripts/ci/test_ci_local.py -v` → `setUpClass (__main__.WorkflowScopeStepTests) ... skipped 'requires Bash >= 4.4 (\`mapfile -d\`) and jq; set BISCUIT_TEST_BASH to point at one (tried: /bin/bash)'`, `Ran 21 tests`, `OK (skipped=1)`, exit 0
        - non-vacuity: `CI=true BISCUIT_TEST_BASH=/bin/bash PATH=/usr/bin:/bin python3 scripts/ci/test_ci_local.py -v` → `ERROR: setUpClass (__main__.WorkflowScopeStepTests)` / `AssertionError: requires Bash >= 4.4 (\`mapfile -d\`) and jq; set BISCUIT_TEST_BASH to point at one (tried: /bin/bash)`, `FAILED (errors=1, skipped=17)`, exit 1 (the 17 skips are the `just`-dependent classes under the restricted PATH); the same command with the full PATH gives `FAILED (errors=1)`, exit 1. Note unittest reports an `AssertionError` from `setUpClass` as `ERROR:` rather than `FAIL:`; the exit code and message are what the hosted job needs
        - gates: `python3 scripts/ci/test_ci_local.py -v` `Ran 34 tests` `OK` with all 13 `WorkflowScopeStepTests` and 4 `StepBashResolverTests` reported `ok`, 0 skipped (`STEP_BASH` resolved to `/opt/homebrew/Cellar/bash/5.3.15/bin/bash`); `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` `Ran 382 tests` `OK` (the review measured 378 with 11 failures); `python3 -m py_compile scripts/ci/*.py` OK; `just/ci-local.just:298` self-test loop and `.github/workflows/ci.yml:435` read and left unchanged — both run the file whole, so the new gate applies to them without edits; nothing outside `scripts/ci/test_ci_local.py` was touched
- work completed for 'Workflow shell tests do not declare their Bash requirement' at 20:35:01
- starting the work on 'Scope evidence is still recomputed when validation is reused' at 20:35:01
        - discovered: the hit-with-evidence path in `.github/workflows/ci.yml` re-ran the selection planner (`--accepted-cells … "${scope_args[@]}"`) and replaced both documents after comparing only area/package names; `just/ci-local.just --plan` had a second copy of the same two-pass evidence path
        - discovered: neither Rust reader denies unknown plan fields (`ci-rollup.rs` `ResolvedPlan` and `ci-plan.rs` `Plan` are plain `Deserialize`); `ci-rollup` pins `PLAN_SCHEMA_VERSION` and bails on a mismatch, `ci-plan` reads no version; `verify_scope` misses as `scope-schema` when `plan_schema_version != RESOLVED_PLAN_SCHEMA_VERSION`
        - discovered: GitNexus upstream impact for `legacy_scope_document`, `package_cells`, `calculate_scope`, `estimate_jobs`, `classify_preflight`, `matrix_record`, `policy_record` is LOW/exact (1 direct caller each); text grep across `scripts/ .github/ just/ tools/ .githooks/ .claude/skills/` confirmed `legacy_scope_document` has exactly two callers (`affected_scope.main`, `test_resolved_plan.py`) plus `test_affected_scope.py`
        - decision: plan schema bumped 1 → 2 with the new fields REQUIRED (`environments` on the plan, `l1_include_slow` on every package record, `exclusion` on exactly the `gates = []` records, `reusable` on every cell). Optional fields would let a version-1 receipt HIT and then leave the evidence path no honest way to finish except re-running selection (the defect) or failing the job; with the bump an old receipt misses as `scope-schema` and CI calculates scope itself, which the receipt contract permits. Cost was one Rust constant, three inline `ci-rollup-tests.rs` plan fixtures (now spelled `PLAN_SCHEMA_VERSION`), one `ci-plan-tests.rs` literal, and five hand-built Python plan fixtures plus the hook stub and its four `plan-*.json` fixtures
        - changed `scripts/ci/affected_scope.py`: `mark_reused`/`cell_evidence` are the one place a reused cell's shape is decided (used by `package_cells` and the new `apply_accepted_cells`); `outcome_fields` extracts the cell-derived plan fields from `calculate_scope`; `apply_accepted_cells(plan, accepted_cells, evidence_rejections, accepted_environments=None)` is pure, validates its input, deep-copies, reuses only `reusable` cells in state `pending`/`prohibited` (dropping `prohibition`), and recomputes the derived fields; every cell records `reusable`, every package record `l1_include_slow`, the non-gating record `exclusion`, the plan `environments`; `matrix_record(record, environments, gates, executing)` and `policy_record(record, gates)` read the plan's package record; `legacy_scope_document(plan)` takes only the plan; `--apply-to FILE` reads the plan, applies `--accepted-cells`/`--evidence-rejections`, and neither loads metadata, policy, nor `environments.json`; argparse rejects `--apply-to` with `--all`, files, or `--constraints`
        - changed `scripts/ci/schema.py`: `RESOLVED_PLAN_SCHEMA_VERSION = 2`; field lists and docstring extended; validator checks `l1_include_slow` is a bool, `exclusion` present iff `gates == []`, `environments` is a list of named records, `reusable` is a bool, and a reused cell is never `reusable: false`; `.github/ci/schemas/contract.json` regenerated via `python3 scripts/ci/schema.py`
        - changed `.github/workflows/ci.yml`: on accepted cells the step runs `affected_scope.py --apply-to resolved-plan.json --accepted-cells … --evidence-rejections … --plan-out resolved-plan.json` and takes its stdout as `scope.json`; the selection comparison and its `::error::` are gone; on a miss the one selection run stays and the same overlay applies evidence afterwards; step comments updated; `actionlint` clean
        - changed `just/ci-local.just`: the `--plan` second pass applies evidence with `--apply-to "${plan_file}" … --resolved-plan --plan-out "${plan_file}" > "${scope_file}"` instead of re-running selection (reads `plan_file`, never the stdout target, so the shell truncation of `scope_file` cannot race the read)
        - changed `scripts/ci-rollup.rs` (`PLAN_SCHEMA_VERSION = 2`), `scripts/ci-rollup-tests.rs`, `scripts/ci-plan-tests.rs`, `.githooks/tests/fixtures/affected_scope_stub.py` and the four `plan-*.json` fixtures: version-2 plans
        - changed `scripts/ci/test_affected_scope.py`: `ApplyFixture` workspace holds a `check` cell, a companion-suite L1 on the Node host, governed gaps, a prohibited cell evidence satisfies and one nothing satisfies, and a `gates = false` package; `ApplyAcceptedCellsTests` pins `canonical(calculate_scope(…, A, R)) == canonical(apply_accepted_cells(calculate_scope(…), A, R))`, the identity on an empty set, input non-mutation, an already-reused cell left as carried, refusal of a version-1 plan, and a JSON-round-tripped plan projecting without policy or environment table; `ApplyCliTests` runs the real CLI with a `cargo` stub that aborts (so `--apply-to` provably never reaches `cargo metadata`) and pins the argparse exclusion; `matrix_record` call sites now pass plan-shaped records via `plan_package()`, and the two feature-args tests assert `feature_args`/`check_arguments` directly
        - changed `scripts/ci/test_ci_local.py`: the two workaround tests are replaced by (a) hit + validation receipt ⇒ `scope_source` starts with `local (`, exactly one planner call and it carries `--apply-to resolved-plan.json`, the macOS L1 cell is `reuse`, and the written plan minus the evidence-derived fields is byte-identical to the receipt's; (b) the written `scope.json` equals `legacy_scope_document(written plan)` and the reused host has left `native_environments`; (d) miss + validation receipt ⇒ one selection call without `--accepted-cells` and one `--apply-to` call; the existing hit-without-evidence test additionally pins `receipt.scope == legacy_scope_document(receipt.plan)`; `StepRun` now reads `scope.json`
        - changed `.github/ci/README.md`, `.github/ci/schemas/README.md`, `.claude/skills/rust-devops/ci-cd.md`: the hit-with-evidence path is described as an overlay, never a compared re-run; the version-2 fields are enumerated. `.claude/skills/os/SKILL.md` and `docs/topics/ci-cd.md` never described the compare, so neither was touched (the `scope-only` paragraph belongs to another finding)
        - non-vacuity: `git show HEAD:.github/workflows/ci.yml > /tmp/ci-old.yml` and the three new workflow tests run with `test_ci_local.WORKFLOW` pointed at it → (a) `FAIL … AssertionError: '--apply-to resolved-plan.json' not found in '--accepted-cells accepted-cells.json --evidence-rejections evidence-rejections.json --plan-out resolved-plan.json --base … --head … -- README.md biscuit-hash/lib/src/lib.rs' : a planner call performed selection`; (d) `FAIL … AssertionError: 1 != 2` with two selection calls logged; (b) passes against the old workflow too, because the old flow also projected from the plan it (re)computed — it pins a different invariant, not the defect; `FAILED (failures=2)`
        - gates: `python3 scripts/ci/test_affected_scope.py` `Ran 118 tests` `OK`; `test_resolved_plan.py` `Ran 40 tests` `OK`; `test_schema.py` `Ran 45 tests` `OK`; `test_evidence_reuse.py` `Ran 55 tests` `OK`; `test_local_evidence.py` `Ran 20 tests` `OK`; `test_ci_local.py -v` `Ran 35 tests` `OK` with all 14 `WorkflowScopeStepTests` reported `ok` (0 skipped); `test_constraints.py` `Ran 31 tests` `OK`; `test_runner_loss.py` `Ran 31 tests` `OK`; `test_reuse_validation.py` `Ran 15 tests` `OK`; `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` `Ran 390 tests` `OK`; `python3 -m py_compile scripts/ci/*.py` OK; `env -u CDPATH ./.githooks/tests/test-pre-push.sh </dev/null` 33 PASS / 0 FAIL, exit 0 (one run before the stub/fixture patch showed `FAIL a failed scope publication is named` on `scope-schema: scope receipt carries a version 1 plan, this tool reads 2`, fixed by moving the stub and the four `plan-*.json` fixtures to version 2); `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` `Summary [0.649s] 183 tests run: 183 passed, 0 skipped`; `cargo nextest run --manifest-path scripts/Cargo.toml --bin ci-plan` (needs the default `local-tools` feature) `Summary [0.014s] 9 tests run: 9 passed, 0 skipped`; `cargo clippy … --no-default-features --bin ci-rollup -- -D warnings` exit 0; `cargo clippy … --bin ci-plan -- -D warnings` exit 0; `just test test-toolkit` `Summary [1.220s] 155 tests run: 155 passed, 2 skipped`; `just _lint test-toolkit` exit 0; `actionlint .github/workflows/ci.yml` clean; `just ci-local --plan --plan-out /tmp/plan-f3.json` exit 0 and `schema.validate_resolved_plan` prints `[]` (schema 2, 4 environments, 48 cells, every cell carries `reusable`)
- work completed for 'Scope evidence is still recomputed when validation is reused' at 20:59:46
- starting the work on 'The trigger constraint check can overlook committed work' at 20:59:46
        - discovered: GitNexus does not index the shell symbols (`publish_scope_evidence`, `calculate_committed_scope`, `make_fake_just` all `risk: UNKNOWN`, "not found"); `grep -rn` over `scripts/ .github/ just/ tools/ .githooks/ .claude/skills/ CLAUDE.md docs/` confirms the two hook functions exist only in `.githooks/pre-push` and the test helpers only in `.githooks/tests/test-pre-push.sh`; `tools/test-toolkit/tests/ci_workflow_contracts.rs` pins only that the hook contains `ci/constraints.py` (kept) and that `just/ci-local.just` contains no `cargo build`/`cargo run` (unchanged), plus the retired-phrase list for the docs (none reintroduced)
        - discovered: the planner's just-recipe classification (`affected_scope.py:904`) runs `git diff <base> -- <path>` with `cwd=ROOT`, i.e. against the WORKING TREE of the tree the script lives in — a second way the checkout leaked into the committed plan, closed by the same worktree
        - discovered: `local_evidence.py verify --cells` schema-validates the plan and turns an unreadable one into a `malformed-receipt` rejection with exit 0, so "an unreadable plan" now surfaces at the overlay stage (`--apply-to` cannot parse it), not at `constraints.py check`
        - discovered: HEAD's committed `scripts/ci/affected_scope.py` has no `--apply-to` and its `schema.py` is plan version 1 — finding 3's changes are uncommitted — so on THIS checkout, while dirty, the hook reviews HEAD's tooling and blocks at the overlay stage until those changes are committed (see the probe below); that is the designed behavior (committed tooling for the committed tree), transitional for exactly one commit
        - decision: the resolution stage (planner, `verify --cells`, `--apply-to`, and `scope-record`) runs the OUTGOING REVISION's own `scripts/ci/*.py` from `$TRIGGER_ROOT`, so the plan, the evidence overlay, and the receipt are one schema generation — the one CI will verify the receipt with after checking out that revision; `constraints.py check` stays on `$REPO_ROOT` (it reads only `cells[].{environment,gate,execution}` and the host-side store). The planner runs with cwd `$TRIGGER_ROOT` (subshell `cd`), the other stages with the hook's cwd; git notes and objects are shared across worktrees so cwd is immaterial to them
        - decision: `TRIGGER_ROOT` is the checkout only when the outgoing sha is HEAD and `git status --porcelain` is empty; otherwise `git worktree add --detach -q` of the outgoing sha under `mktemp -d`, removed by both EXIT traps through `remove_trigger_worktree` (`git worktree remove --force`, falling back to `rm -rf` + `git worktree prune`); `PYTHONDONTWRITEBYTECODE=1` is exported for the whole hook
        - decision: the reviewed revision is HEAD when the push carries it or when no ref line arrives (the fixture runs feed none; a real push always does); otherwise the last non-deleted local sha on stdin; a deletion-only push prints that it triggers no run and skips the review (the gates still run as before). A push that does not carry HEAD publishes no scope receipt (it binds HEAD), as today
        - decision: no comparison base (no `origin/main`) now blocks with exit 2 and a fetch hint — `ci-local --plan` exited 2 there before, so "fail closed" is the preserved behavior with a clearer message; every resolution-stage failure blocks with exit 2 naming the stage (`Trigger review failed: …`); a constraint refusal keeps `constraints.py`'s exit 1
        - decision: constraints are decided before the receipt is published, as today, so a blocked push leaves no scope note (pinned by the masked-change fixture); the receipt is recorded from the pre-overlay `$SCOPE_PLAN_FILE`/`$SCOPE_DOC_FILE`, content contract unchanged; `calculate_committed_scope` deleted; the "CALCULATION failed" message became "RECORDING failed" because the calculation now happens (and blocks) before it
        - decision: the cell renderer is duplicated from `ci-local.just` (`ci-plan` when built, else the same `jq` lines, else a one-line notice when `jq` is absent) — about ten lines — rather than shared, because the recipe's copy is bash-in-just with `{{ red }}` interpolation and the hook must stay POSIX `sh` without a `just` round-trip; the prohibited-cells block is not duplicated since the hook hands the planner no store, so `prohibited_cells` is always empty there and `constraints.py` prints the refusal
        - changed `.githooks/pre-push`: header rewritten for the trigger review; `TRIGGER_SHA`/`REF_LINES`/`LAST_PUSHED_SHA` from the stdin loop; base decision hoisted out of `publish_scope_evidence`; `TRIGGER_ROOT`/`TRIGGER_WORKTREE`; `block_trigger_review`, `plan_trigger_scope`, `render_trigger_plan`, `remove_trigger_worktree`; two new temp files (`ACCEPTED_FILE`, `REJECTED_FILE`); `publish_scope_evidence` only records/attaches/pushes; `just ci-local --plan` is no longer called; both EXIT traps remove the worktree; `shellcheck -s sh` clean
        - changed `.githooks/tests/fixtures/affected_scope_stub.py`: `TEST_PLANNER_PLAN=<fixture>` emits that plan with `base`/`head` rewritten (an unparseable fixture is passed through verbatim, modelling a planner that wrote garbage); `--apply-to` is a minimal overlay mirroring the real one's cell shape (`execution=reuse`, `origin`, `state=reused`, `evidence`, plus `accepted_evidence`/`evidence_rejections`); `preflight_reason` comes from `.github/ci/policy.json` in the stub's OWN root; every call logs a second line `root <ROOT> cwd <cwd>`; option parsing handles the valueless `--resolved-plan`/`--all`; the projection is derived from the plan
        - changed `.githooks/tests/test-pre-push.sh`: every test now runs in a fixture repository (the fake `just` answers only `pre-push`, anything else exits 99); `make_publishing_repo` takes an optional feature file/content and commits `pkg/alpha/src/lib.rs` and `.github/ci/policy.json` on `main`; `stage_fixture_tools` split out; `run_hook_with_ref_line` is the one runner (`run_hook_in_repo`, `run_hook`, `run_hook_with_env` layer on it; `__UNSET__` mode for the default-is-strict test); `assert_planner_ran`, `planner_root`, `assert_no_leftover_worktree`; `PRE_PUSH_HOOK_UNDER_TEST` selects the hook. Intent kept for all 33: `ci-local --plan` log assertions became planner-log assertions; "an unreadable plan blocks" asserts `Trigger review failed` (see discovery above); "a failed plan resolution blocks" and "a failed scope calculation is named; exit code unchanged" merged into "a failed plan resolution blocks and publishes no scope" (exit 2, stage named, no gate, no receipt) because the resolution IS the calculation now; "the plan resolution is handed no constraint store" asserts the planner argv lacks `--constraints`; "a failing warn run reaches the publication block" dirties the tree so the guard declines as before. New: masked committed change (exit 1, `macos-latest` named, `-- pkg/alpha/src/lib.rs` planned, no gate, no receipt, worktree announced and removed), dirty policy (receipt `plan`/`scope.preflight_reason` = committed value, planner root ≠ checkout), clean checkout planned in place, push not carrying HEAD reviews the pushed sha and publishes no receipt, deletion-only push not reviewed
        - changed `just/ci-local.just` (header only), `CLAUDE.md`, `.github/ci/README.md`, `.claude/skills/rust-devops/ci-cd.md`, `.claude/skills/os/SKILL.md` (the AC14 paragraph: scope-only DOES publish the scope receipt; plus the committed-tree wording), `docs/topics/ci-cd.md`: the hook reviews the outgoing revision's committed plan; `just ci-local --plan` previews the working tree; the two differ exactly when the checkout is dirty. The four `plan-*.json` fixtures show as modified from finding 3 and were not touched here. `constraints.py`'s refusal still says "Review the resolved plan with 'just ci-local --plan'" — left alone (shared with the recipe, and the hook has already rendered the plan above it)
        - non-vacuity: `git show HEAD:.githooks/pre-push > /tmp/old-pre-push` and a one-off copy of the suite (deleted afterwards) whose fake `just` models the old `ci-local --plan` working-tree short-circuit, run with `PRE_PUSH_HOOK_UNDER_TEST=/tmp/old-pre-push` on the two review fixtures → `FAIL a committed change masked by an unstaged revert is reviewed` with `expected exit 1, got 0` and stdout `ci-local: no changes; nothing to gate.` … `Published scope evidence` … `Pre-push validation passed.`; `FAIL an unstaged policy edit never reaches the plan or receipt` with `the scope receipt describes the working tree's policy, not the committed one: {"plan": "working-tree policy", "scope": "working-tree policy"}`; `Passed: 0 Failed: 2`; the same copy against the new hook `Passed: 2 Failed: 0`
        - gates: `env -u CDPATH ./.githooks/tests/test-pre-push.sh </dev/null` `Passed: 37` `Failed: 0`, exit 0, 29.8 s wall; `shellcheck -s sh .githooks/pre-push` clean; `shellcheck .githooks/tests/test-pre-push.sh` only the pre-existing SC2329 (function invoked via `run_test "$@"`, 51 in HEAD's copy too) and SC2016 (the two single-quoted guard strings) infos; `python3 scripts/ci/test_ci_local.py` `Ran 35 tests` `OK` (before and after the recipe-header edit); `test_local_evidence.py` `Ran 20 tests` `OK`; `test_evidence_reuse.py` `Ran 55 tests` `OK`; `test_constraints.py` `Ran 31 tests` `OK`; `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` `Ran 390 tests` `OK`; `python3 -m py_compile scripts/ci/*.py .githooks/tests/fixtures/*.py` OK; `just test test-toolkit` `Summary [1.171s] 155 tests run: 155 passed, 2 skipped`; `just _lint test-toolkit` exit 0
        - gates (end-to-end probe on this checkout, not pushed): `git clone --bare . /tmp/probe-origin.git`, `git remote add probe …`, README dirtied by one blank line, then `printf 'refs/heads/feat/unifi <HEAD> refs/heads/feat/unifi <origin/feat/unifi>\n' | RUSTY_BISCUIT_PRE_PUSH=scope-only BISCUIT_CI_CONSTRAINTS_DIR='' ./.githooks/pre-push probe /tmp/probe-origin.git` → stdout `Scope evidence base for 32ebd42a7: 05ab70065 (merge base with origin/main, …)` then `Reviewing the committed tree of 32ebd42a7 in a temporary worktree (the checkout has uncommitted changes).`; the committed planner and verifier ran, then HEAD's `affected_scope.py` refused `--apply-to` (`error: unrecognized arguments: --apply-to`) and the hook blocked: `Trigger review failed: the verified evidence could not be applied to the resolved plan`, exit 2 in 7 s; `git worktree list` 8 entries before and after, no `biscuit-pre-push-tree.*` left under `$TMPDIR`; `refs/notes/ci-local/scope` had 0 notes before and 0 after (nothing to remove); README restored with `git show HEAD:README.md > README.md` (`git status --porcelain README.md` empty); `probe` remote and the bare clone removed. Manual rerun of the planner stage alone in a detached worktree of HEAD: exit 0, plan `schema=1 base=05ab70065 head=32ebd42a7 cells=48 packages=9`, `verify --cells` exit 0 (0 accepted, 1 rejection), the worktree's `git status --porcelain` empty afterwards (no bytecode written), removal exit 0. The full path through the overlay is exercised by the 37-test suite; on this checkout it completes once finding 3's `scripts/ci` changes are committed
- work completed for 'The trigger constraint check can overlook committed work' at 21:22:58
        - orchestrator note: the end-to-end probe exposed a transitional trap worth Ken's attention — the hook now runs the COMMITTED tree's `scripts/ci` tooling, so until finding 3's `--apply-to` lands in a commit, a dirty-tree push from this branch blocks at the overlay stage with exit 2; a clean-tree push (in-place resolution) is unaffected once the working tree is committed
- starting the work on 'Newer partial package receipts hide valid older evidence' at 21:22:58
        - discovered: GitNexus upstream impact for `verify_cells`, `latest_note`, `_accept_legacy`, `candidate_commits` is `LOW` / `epistemic: exact` with one direct caller each (`main`); `grep -rn` over `scripts/ .github/ just/ tools/ .githooks/ .claude/skills/` confirms `verify_cells` is called only by the CLI `main` and the two Python suites, and `latest_note`/`candidate_commits` have no caller outside the module; `scripts/ci/local_evidence.py` is unmodified from HEAD, so `git show HEAD:` is a faithful pre-change copy
        - discovered: this checkout carries four `macos-latest` v1 notes on branch commits; the pre-change walk stopped at the first (`0156096ff`) and rejected it, and the pre-change verifier cost 3.45 s (three runs: 3.45 / 3.44 / 3.45 s) because `latest_note` issued one `git notes show` per candidate commit per ref (85 commits × 4 refs, almost all answering "no note")
        - decision: precedence contract, written in `verify_cells`'s `## Notes`: every note on an environment's ref between the merge base and the head is a candidate, walked newest commit first; a cell is resolved by the newest candidate whose cell for it qualifies (complete, environment- and revision-consistent, exact tree or gate-input-equivalent); once resolved, older candidates never change it, so an older pass never overrides a newer complete failure; a candidate that does not qualify is reported with its coded rejection naming its commit and does not stop older candidates from resolving the cell (a rejection describes one candidate, not the cell's outcome); a note that never mentions a wanted cell is not a refusal; a cell no candidate covers is reported nowhere and stays scheduled (kept as today); v1 notes take part in the same walk under their narrower rules and claim every still-unresolved wanted cell of their environment, so v1-on-head shadows older v2 and v2-on-head shadows older v1
        - decision: `latest_note` replaced by `candidate_notes(environment, commits) -> list[(commit, text)]` built from one `git notes --ref … list` per ref plus one `git cat-file -p` per hit, since the difference is material (3.45 s → 0.18 s) and both the hook and CI pay it; `candidate_commits` kept
        - changed `scripts/ci/local_evidence.py`: module docstring (every note on every ref, per-cell resolution, same-environment receipts combine across commits); `candidate_notes`; `verify_cells` walks every candidate per environment with `resolved` replacing `seen`, `schema.reusable_cells` rejections are suffixed with `(note on <sha>)` so a reader can tell which candidate they describe; `_accept_legacy` takes `resolved`, its docstring states the shadowing rule, and its `v1-not-equivalence-eligible` text now names the note's commit
        - changed `scripts/ci/test_evidence_reuse.py`: new `SuccessiveReceiptTests` (8 fixtures, real repositories): the review's reproduction (WSL alpha on C1, docs-only C2 with WSL beta ⇒ both accepted, alpha `prior-local` from C1, beta `local` from C2, no rejection), changed alpha inputs on C2 ⇒ beta accepted and one `gate-inputs-changed` naming C1, non-JSON note on C2 ⇒ `malformed-receipt` naming C2 and C1's two cells accepted, newer complete failure shadows an older pass (outcome `fail`, origin `local`, `evidence.commit` = C2), newer pass shadows an older failure (recency, not outcome), older v1 note on an exact-tree empty commit does not claim the cell a head v2 note resolved (alpha measured from the head, beta unrecorded from v1), v1 on the head not overridden by an older v2 (both cells unrecorded, `evidence.schema_version` 1, no `commit`), macOS on the head plus two non-overlapping WSL notes on older commits combine (four cells with their origins and commits pinned)
        - changed `.claude/skills/rust-devops/ci-cd.md`, `.github/ci/README.md`, `.claude/skills/os/SKILL.md`, `docs/topics/ci-cd.md`: the "reads every environment's notes ref" sentences now say every note on every ref between the merge base and the head, that same-environment receipts on successive commits combine per cell, and that each cell goes to the newest qualifying note. `CLAUDE.md`'s evidence bullet already reads "every ref … is read" and was left alone; `scripts/ci/test_local_evidence.py` and the `verify --cells` CLI needed no interface change (same signature, same output)
        - non-vacuity: `SuccessiveReceiptTests` run in a scratch copy of `scripts/ci` whose `local_evidence.py` is `git show HEAD:scripts/ci/local_evidence.py` → `Ran 8 tests`, `FAILED (failures=5)`: the reproduction `Lists differ: ['alpha/wsl2-ubuntu/L1', 'beta/wsl2-ubuntu/L1'] != ['beta/wsl2-ubuntu/L1']`; changed inputs `1 != 0 : []` (C1 was never read, so no rejection); malformed newer note `Lists differ: [...] != []`; older v1 vs head v2 `!= ['alpha/macos-latest/L1']` (beta never claimed); cross-environment `'beta/wsl2-ubuntu/L1': ('prior-local', …)` present but `alpha/wsl2-ubuntu/L1` missing. The three precedence pins (newer failure over older pass, newer pass over older failure, v1-on-head over older v2) pass on both implementations by construction — the old walk read only the newest note, which is also the winner under the new rule — so they guard the contract rather than the defect
        - gates: `python3 scripts/ci/test_evidence_reuse.py` `Ran 63 tests` `OK` (55 + 8); `test_local_evidence.py` `Ran 20 tests` `OK`; `test_schema.py` `Ran 45 tests` `OK`; `test_resolved_plan.py` `Ran 40 tests` `OK`; `test_affected_scope.py` `Ran 118 tests` `OK`; `test_ci_local.py -v` `Ran 35 tests` `OK` with all 14 `WorkflowScopeStepTests` reported `ok` (0 skipped); `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` `Ran 398 tests` `OK`; `python3 -m py_compile scripts/ci/*.py` OK; `env -u CDPATH ./.githooks/tests/test-pre-push.sh </dev/null` `Passed: 37` `Failed: 0`; `just test test-toolkit` `Summary [1.233s] 155 tests run: 155 passed, 2 skipped`; `just _lint test-toolkit` exit 0; `schema.py` untouched, so no contract regeneration and no `ci-rollup` run
        - gates (timing on this checkout, `verify --cells --plan /tmp/plan-f2.json --base origin/main --head HEAD`, 85 candidate commits × 4 refs): before 3.45 s real (3.45 / 3.44 / 3.45), after 0.18 s real (0.17 / 0.19 / 0.18); the post-change run reports both in-range v1 notes (`0156096ff`, `73bdd6d53`) as `v1-not-equivalence-eligible` where the old walk reported only the first, accepted set `[]` on both
- work completed for 'Newer partial package receipts hide valid older evidence' at 21:34:41
- starting the evaluation of 'The standalone global policy verdict remains' at 21:34:41
        - blocked: the merge gate is Open Question 3 (blocker B3) and its scratch-repository proof is blocker B5; `open-questions-and-blockers.md` is still `status: awaiting-ken`, the spec carries no `## Rulings` section, and the review itself says "Do not delete it independently of that transition"; this non-interactive session holds no push, dispatch, or ruleset authorization
        - deferred; nothing changed for this finding
- work completed for 'The standalone global policy verdict remains' at 21:34:41 (deferred)
- starting the evaluation of 'Accepted-gap publication and hosted presentation verification remain incomplete' at 21:34:41
        - blocked: the check-run conclusion is Open Question 4 (blocker B4) and the hosted fixture is blocker B5; the review says "Implement the publisher after the display decision"; the half that holds under every option (the distinct `ACCEPTED GAP` state and its explanatory detail) already exists and stays tested
        - deferred; nothing changed for this finding
- work completed for 'Accepted-gap publication and hosted presentation verification remain incomplete' at 21:34:41 (deferred)
- starting the evaluation of 'Constraints still disappear in an unconfigured fresh session' at 21:34:41
        - blocked: the store's location is Open Question 2 (blocker B2); the spec forbids implementing an affected section before Ken rules, and the review lists the choice among its `human_review_items`; the ruling still costs one line in `constraints.default_directory()` plus the cross-session fixture, and nothing in this cycle's hook rework changed that (the hook still resolves the store through `constraints.py`)
        - deferred; nothing changed for this finding
- work completed for 'Constraints still disappear in an unconfigured fresh session' at 21:34:41 (deferred)
- starting the combined verification pass over every impacted gate at 21:34:41
        - completed: every impacted gate re-run together after the last edit, all green: `python3 -m unittest discover -s scripts/ci` 398 tests OK (378 with 11 failures at review time), `py_compile` clean over `scripts/ci/*.py` and the hook fixtures, hook suite 37/37 in 30 s, `shellcheck -s sh .githooks/pre-push` clean, `actionlint .github/workflows/ci.yml` clean, `ci-rollup` 183/183, `ci-plan` 9/9, clippy `-D warnings` on both bins clean, `just test test-toolkit` 155 passed / 2 skipped, `just _lint test-toolkit` exit 0
        - completed: `just ci-local --plan --plan-out` against this checkout exits 0 in 1 s: schema 2, 6 areas, 48 cells, 0 prohibited, 0 accepted, 2 rejections (both in-range v1 macOS receipts refused as `v1-not-equivalence-eligible` — the per-cell walk now reports every candidate, where the old walk reported only the newest)
        - completed: no temporary worktree or scope note was left behind by the hook suite or the probes (`git worktree list` unchanged at 8; `refs/notes/ci-local/scope` lists nothing locally)
        - note: GitNexus `detect-changes` was not run because nothing is committed in this cycle; it must run before the commit that lands these edits, and the index is ~65 commits stale
- combined verification pass completed at 21:37:30

### Successful Completion

The implementation of review cycle 2 has completed successfully in 1h 16m. During this implementation all 7 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 4 were fixed, 3 were deferred (see reasons below):

- **The standalone global policy verdict remains** — deferred. Removing `ci-verdict` needs the merge gate chosen under Open Question 3 (blocker B3) and its scratch-repository proof (blocker B5); `open-questions-and-blockers.md` is still `status: awaiting-ken`, the review forbids removing the check independently of that transition, and this non-interactive session holds no push, dispatch, or ruleset authorization.
- **Accepted-gap publication and hosted presentation verification remain incomplete** — deferred. The check-run conclusion is Open Question 4 (blocker B4) and the hosted fixture is blocker B5; the review asks for the publisher only after the display decision. The half that holds under every option (the distinct `ACCEPTED GAP` state and its explanatory detail) already exists and stays tested.
- **Constraints still disappear in an unconfigured fresh session** — deferred. The store's location is Open Question 2 (blocker B2), which the review lists among its human-review items; the spec forbids implementing an affected section before Ken rules. The ruling still costs one line in `constraints.default_directory()` plus the cross-session fixture; this cycle's hook rework left that seam untouched.

The AC14 documentation drift the review noted in `.claude/skills/os/SKILL.md` (the claim that `scope-only` publishes no scope receipt) was corrected as part of the trigger-review finding.

No finding was deferred for a performance-measurement reason, so `deferred_perf_measurement` is not set.

One transitional trap for Ken, found by the end-to-end probe of the reworked hook: the hook now resolves the trigger plan with the COMMITTED tree's `scripts/ci` tooling. Until this cycle's `--apply-to` overlay is committed, a dirty-tree push from this branch blocks at the overlay stage (exit 2, "Trigger review failed"); a push from a clean tree, or any push once these edits are committed, is unaffected.

The files this cycle touched, as reported by `git status` (all 29 are this cycle's; the tree was clean at `32ebd42a7` when it began):

        - `.claude/skills/os/SKILL.md`
        - `.claude/skills/rust-devops/ci-cd.md`
        - `.githooks/pre-push`
        - `.githooks/tests/fixtures/affected_scope_stub.py`
        - `.githooks/tests/fixtures/plan-macos-executing.json`
        - `.githooks/tests/fixtures/plan-wsl-absent.json`
        - `.githooks/tests/fixtures/plan-wsl-executing.json`
        - `.githooks/tests/fixtures/plan-wsl-reused.json`
        - `.githooks/tests/test-pre-push.sh`
        - `.github/ci/README.md`
        - `.github/ci/schemas/contract.json`
        - `.github/ci/schemas/README.md`
        - `.github/workflows/ci.yml`
        - `CLAUDE.md`
        - `docs/topics/ci-cd.md`
        - `fixes/2026-09-11-cicd-cleanup/log.md`
        - `just/ci-local.just`
        - `scripts/ci-plan-tests.rs`
        - `scripts/ci-rollup-tests.rs`
        - `scripts/ci-rollup.rs`
        - `scripts/ci/affected_scope.py`
        - `scripts/ci/local_evidence.py`
        - `scripts/ci/schema.py`
        - `scripts/ci/test_affected_scope.py`
        - `scripts/ci/test_ci_local.py`
        - `scripts/ci/test_evidence_reuse.py`
        - `scripts/ci/test_local_evidence.py`
        - `scripts/ci/test_resolved_plan.py`
        - `scripts/ci/test_schema.py`

## Implementation of Review Findings #3

> **started at:** 2026-09-11T22:46:25-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/fixes/2026-09-11-cicd-cleanup/review-3.md'
- this is iteration 3 of the review-to-implement cycle
- orchestration plan decided at 22:52:00 after reading the review, spec, `open-questions-and-blockers.md`, the rollout record, iteration 2's log, the hook's ref loop and trigger-review block, `constraints.py`, and the shell harness:
        - the tree was clean at `e4279b59a` when this cycle began; iteration 2's edits are committed
        - implementable in this cycle (no ruling needed): finding 5 (the hook suite aborts under the default macOS Bash 3.2) and finding 1 (the trigger review reduces a push to one revision and checks constraints against the checkout's branch and `origin` rather than each pushed ref's branch and the actual remote)
        - still blocked on unruled open questions, so expected to be deferred again: finding 2 (`ci-verdict` removal needs OQ3/B3 and the scratch-repository fixture B5), finding 3 (the gap publisher needs OQ4/B4 and the hosted fixture B5), finding 4 (the constraint store default needs OQ2/B2); `open-questions-and-blockers.md` is still `status: awaiting-ken`, the spec carries no `## Rulings` section, and this non-interactive session holds no push, dispatch, or ruleset authorization
        - order chosen: 5 → 1, because 5 makes the harness runnable with the host's default `bash` (3.2.57) so that 1's new fixtures can be proven under both shells
        - gates are run only in the impacted areas: `.githooks/tests/test-pre-push.sh` under both `/bin/bash` and `/opt/homebrew/bin/bash`, `shellcheck` on the hook and harness, and the `scripts/ci/*.py` suites where touched
        - host load at start: 16 logical cores, load average 21.34 (1 min); these are logic suites, so timing is not a concern, but nothing in this cycle is a performance measurement
- starting the work on 'The revised hook test suite aborts under the default macOS Bash' at 22:48:04
        - discovered: `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh` (Bash 3.2.57) aborts with `line 224: env_args[@]: unbound variable` before the first test; the same suite under `/opt/homebrew/bin/bash` 5.3 passes 37/37
        - discovered: a sweep for Bash 4+ constructs (`mapfile`, `readarray`, `declare -A`, `${v,,}`, `${v^^}`, `|&`, `[[ -v`, empty-array `"${a[@]}"` under `set -u`) found exactly one breaker: `run_hook`'s `"${env_args[@]}"` when no areas override is given; `FAILED_TESTS` is only expanded when non-empty, `guards`/`commit` are literal non-empty arrays, `run_hook_with_ref_line`'s `env_args` always carries three entries, and `run_hook_with_env` forwards `"$@"`, which Bash 3.2 accepts when empty; `.githooks/pre-push` declares no arrays
        - changed `.githooks/tests/test-pre-push.sh`: `run_hook` now expands `${env_args[@]+"${env_args[@]}"}` with a one-line WHY at the idiom; header records that the suite must stay runnable under Bash 3.2 because the shebang selects `/bin/bash` on a stock Mac; the documented `Run directly:` command is unchanged
        - changed `.githooks/tests/test-pre-push.sh`: added `test_run_hook_forwards_no_selection_without_an_override`, a harness self-check that rebinds `HOOK` (dynamic scoping) to an `env`-dumping stub, calls `run_hook <tmpdir> warn` with no override, and asserts the stub ran, received `RUSTY_BISCUIT_PRE_PUSH=warn`, and received no `RUSTY_BISCUIT_PRE_PUSH_AREAS` at all; 37 → 38 tests
        - changed `.claude/skills/os/macos.md`: one bullet under "Host conditions that look like repo failures" recording the `/bin/bash` 3.2 empty-array trap and the portable idiom
        - non-vacuity: the new test injected into the committed harness (`git show HEAD:...`) and run under `/bin/bash` aborts at `line 224: env_args[@]: unbound variable`, 0 passed; under Bash 5.3 the same copy passes 1/1, confirming the defect is shell-specific rather than a broken test; a mutant of the fixed harness whose `run_hook` always forwards `RUSTY_BISCUIT_PRE_PUSH_AREAS=` fails only the new test (37 passed, 1 failed: "run_hook forwards no selection without an override"), so the no-selection assertion is load-bearing
        - gates: `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 38 passed / 0 failed; `env -u CDPATH /opt/homebrew/bin/bash ...` 38/0; the documented shebang command `env -u CDPATH ./.githooks/tests/test-pre-push.sh </dev/null` 38/0; `just test-pre-push-hook` 38/0; `shellcheck -x .githooks/tests/test-pre-push.sh` shows the same two codes as the committed copy (SC2016 ×2 unchanged, SC2329 info 58 → 59, the one addition being the new indirectly-invoked test function); `shellcheck .githooks/pre-push` clean, hook untouched
        - docs checked and left alone: `.github/ci/README.md`, `.claude/skills/rust-devops/*.md`, `docs/topics/ci-cd.md` (`bash .githooks/tests/test-pre-push.sh` remains correct), `justfile` recipe unchanged
- work completed for 'The revised hook test suite aborts under the default macOS Bash' at 22:54:39
- starting the work on 'Trigger review does not cover every pushed ref or its branch constraints' at 22:57:40
        - discovered: GitNexus (`node .gitnexus/run.cjs impact … --direction upstream`, index ~75 commits stale) resolves `Constraint.applies_to` at LOW risk with 5 upstream symbols (`constraints.load`, `constraints.main`, `affected_scope.read_prohibitions`, `affected_scope.main`); bare `parse_args`/`load`/`main` are ambiguous names and return `UNKNOWN`, so callers were confirmed by text search: the only CLI caller of `constraints.py check` is `.githooks/pre-push:286`, the only in-process caller of `load` is `affected_scope.read_prohibitions` (no branch argument), and `tools/test-toolkit/tests/ci_workflow_contracts.rs:2783` only requires the hook to contain `ci/constraints.py`
        - discovered: the committed hook reduces stdin to one `TRIGGER_SHA` (`.githooks/pre-push:95-110`) and identifies the constraint check by `git remote get-url origin` and `git rev-parse --abbrev-ref HEAD` (`:286-289`), ignoring `$2` (the remote URL Git hands every pre-push hook); the `while read` loop reads the hook's stdin directly, so a per-update body that spawns Git or Python would need its stdin redirected away from the remaining ref lines
        - discovered: the harness runner `run_hook_with_ref_line` prints its ref-line argument with `printf '%s\n'`, so a `$'a\nb'` argument already yields two stdin lines under Bash 3.2; only the remote name/URL (`origin "$tmpdir/origin.git"`) is hard-wired and needs a variant
        - design deviation: `constraints.load`/`Constraint.applies_to` take `branches: Sequence[str]` instead of a `branch: str` that may also be a list — a `str` is itself a `Sequence[str]`, so a mixed-type parameter would silently match single characters; the two `load(branch=…)` call sites in `test_constraints.py` are adapted, `affected_scope.read_prohibitions` passes no branch and is untouched
        - changed `.githooks/pre-push`: the stdin reduction to one `TRIGGER_SHA` is gone; stdin is captured to `REF_UPDATES_FILE` first (so nothing the review spawns can swallow unread lines) and `review_ref_update` runs once per line, in Git's order, with its stdin on `/dev/null`. Per update: a deletion or a non-`refs/heads/*` remote ref is named as triggering no run and skipped; otherwise a `Reviewing <local ref> -> <remote ref> (<sha>)` header, the existing base rule (remote `main` sha, else merge base with `origin/main` — kept even for another remote because pull requests open against origin, stated in the comment), a `Branch identity:` line, a `Remote:` line, the committed tree (in place only when the revision IS the clean checkout — `git status --porcelain` is read once up front — else a temporary detached worktree removed at the end of that update or by the EXIT traps), plan, `verify --cells`, `--apply-to`, render, and `constraints.py check --repository "$REMOTE_URL" --branch <remote branch> [--branch <local branch>]`. `REMOTE_URL` is `$2`, falling back to `git remote get-url "$1"`; `REMOTE_NAME` still drives the notes fetch/push. `block_trigger_review` names the update (exit 2); a refusal prints `Blocked while reviewing <update>.` (exit 1). The scope receipt is RECORDED while HEAD's tree is materialized (`record_scope_evidence`, after HEAD's update passes) and PUBLISHED only after every update has passed (`publish_scope_evidence`); `PUSHES_HEAD` keeps its old meaning (any non-deleted update carries HEAD) for the validation-receipt guard. Header comment rewritten; the wording `the push does not carry HEAD` became `<sha> is not HEAD`, and the deletion-only line became `This push updates no branch, so it triggers no run and there is no plan to review.`
        - changed `scripts/ci/constraints.py`: `--branch` is `action="append"`; `load(directory, repository, branches: Sequence[str])` and `Constraint.applies_to(repository, branches)` bind a `branch` record when it equals ANY supplied non-empty name (an empty list or empty names are no identity and exempt nothing); module docstring no longer says the repository is the `origin` URL and documents the repeatable `--branch`; `applies_to` docstring updated
        - changed `scripts/ci/test_constraints.py`: the two `load(branch=…)` call sites use `branches=[…]`; new `test_a_branch_scoped_constraint_binds_when_any_supplied_name_matches` (either order, empty names) and `test_a_repeated_branch_option_binds_a_record_under_any_of_the_names` (matching name first AND last, so a last-value-wins parser is distinguished from any-match)
        - changed `.githooks/tests/test-pre-push.sh`: `run_hook_as_remote <tmpdir> <mode> <remote name> <remote url> <ref lines> [env…]` is the one runner and `run_hook_with_ref_line` delegates to it with `origin`; newline-joined ref lines ride through the existing `printf '%s\n'`; new fixtures `add_other_branch` (committed `other` from `main` changing `pkg/alpha/src/lib.rs`, `feature` checked out again), `write_scoped_prohibition <dir> <name> <field> <value>` (reason names the scope), `planned_heads`, `reviewed_updates`, `reset_run_outputs`, `ZERO_SHA`; seven new tests — prohibition on the pushed (not checked-out) branch blocks; prohibition on the checked-out branch does not bind another branch's push; two branches each planned with their own path set and the second one's prohibition blocks with no scope note left; both orders reviewed and planned in Git's order and blocked in either position; repository identity is the remote being pushed to (second bare `upstream.git`, `$1=upstream $2=<url>`); renamed refspec `feature:other` binds under `other` and under `feature`, prints both names, exempts `main`; a deletion and a tag among branch updates are named and skipped while the branch update is planned and HEAD's scope still published. Two existing tests adapted to the new wording (`is not HEAD`; `deletion of refs/heads/feature` + `triggers no run and there is no plan to review`). Header comment gains the review-3 paragraph. Bash 3.2 idioms kept (no `mapfile`, no `declare -A`, `${arr[@]+"${arr[@]}"}` untouched)
        - non-vacuity: `PRE_PUSH_HOOK_UNDER_TEST=/tmp/old-hook` (`git show HEAD:.githooks/pre-push`, chmod +x) under `/bin/bash` → 36 passed / 9 failed: all seven new tests fail — "a prohibition on the pushed branch blocks" gets exit 0 with `Pre-push validation passed.` (the review's first reproduction), "a prohibition on the checked-out branch does not bind another push" gets exit 1 naming `branch feature`, "every pushed branch is planned with its own path set" gets exit 0 and publishes HEAD's scope, "ref updates are reviewed in the order Git supplies them" reports `reviewed as: ; planned: <one sha>` (the review's second reproduction: the second branch never planned), "the repository identity is the remote being pushed to" gets exit 0 under an upstream-scoped record, "a renamed refspec binds …" gets exit 0 under `branch: other`, "deletions and tags … skipped by name" finds no `deletion of refs/heads/stale`; the other two failures are the adapted-wording tests (`is not HEAD`, `deletion of refs/heads/feature`). Python against `git show HEAD:scripts/ci/constraints.py` (copied beside `schema.py` in a temp dir): 30 passed, 1 failed, 2 errors — `test_a_repeated_branch_option_binds_a_record_under_any_of_the_names` fails `0 == 0 : a record under the first name must still bind` (argparse kept only the last `--branch`), and both `branches=` tests error with `TypeError: load() got an unexpected keyword argument 'branches'` (the API deviation, not a behavior gap)
        - gates: `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 45 passed / 0 failed; `env -u CDPATH /opt/homebrew/bin/bash …` 45/0; `just test-pre-push-hook` 45/0; `python3 -m unittest scripts/ci/test_constraints.py` 33 OK; `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` 400 OK (95 s); `python3 -m py_compile scripts/ci/*.py` clean; `shellcheck .githooks/pre-push` (POSIX sh) clean; `shellcheck -x .githooks/tests/test-pre-push.sh` SC2016 ×3 (unchanged from the committed copy) and SC2329 info 59 → 73 (the new indirectly-invoked functions only); end-to-end probe on this checkout with the real planner — `printf 'refs/heads/feat/unifi <HEAD~1> refs/heads/probe-target <zeros>\n' | RUSTY_BISCUIT_PRE_PUSH=scope-only BISCUIT_CI_CONSTRAINTS_DIR='' ./.githooks/pre-push probe /tmp/does-not-exist.git` → header, `Branch identity: probe-target (pushed from feat/unifi; …)`, `Remote: probe (/tmp/does-not-exist.git)`, worktree announced, 53-job plan rendered by `ci-plan`, exit 0 in 3.7 s; `git worktree list` 8 before and after, `refs/notes/ci-local/scope` 0 notes before and after (a non-HEAD update records no scope), no `biscuit-pre-push-tree.*` left under `$TMPDIR`
        - docs synced: `CLAUDE.md` (Test Execution Constraints bullet), `.claude/skills/rust-devops/ci-cd.md` (constraint paragraph + scope-only sentence), `.claude/skills/os/SKILL.md`, `.github/ci/README.md` (hook paragraph; repository field is the remote's URL, not `origin`'s), `docs/topics/ci-cd.md` (Layer 1 paragraph), `just/ci-local.just` (comment: each PUSHED REVISION); the task brief also named an empty-string agent skill, which was noted and skipped
- work completed for 'Trigger review does not cover every pushed ref or its branch constraints' at 23:13:58

### Successful Completion

The implementation of review cycle 3 has completed successfully in 32m. During this implementation all 5 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 2 were fixed, 3 were deferred (see reasons below):

- **The standalone global policy verdict remains** — deferred. Removing `ci-verdict` needs the merge gate chosen under Open Question 3 (blocker B3) and its scratch-repository proof (blocker B5); `open-questions-and-blockers.md` is still `status: awaiting-ken`, the spec carries no `## Rulings` section, the review itself says to keep the current gate until the coordinated transition is ready, and this non-interactive session holds no push, dispatch, or ruleset authorization.
- **Accepted-gap publication and hosted presentation verification remain incomplete** — deferred. The check-run conclusion is Open Question 4 (blocker B4) and the hosted throwaway-branch/scratch-repository fixture is blocker B5; the review asks for the publisher only after the display choice, and the hosted experiment requires a push this session may not make. The half that holds under every option (the distinct `ACCEPTED GAP` state and its explanatory detail) already exists and stays tested.
- **Constraints still disappear in an unconfigured fresh session** — deferred. The store's location is Open Question 2 (blocker B2), listed again among this review's human-review items; the spec forbids implementing an affected section before Ken rules. The ruling still costs one line in `constraints.default_directory()` plus the cross-session fixture; this cycle's per-update hook rework left that seam untouched, and the repeatable `--branch` interface it added holds under all three OQ2 options.

The two fixed findings are the ones no ruling gates: the harness now runs under the host's default Bash 3.2 (38 tests, plus a self-check that the no-override path forwards no selection), and the hook reviews every branch update a push carries against that update's own base, committed tree, remote URL, and remote branch (plus the local branch when the refspec renames), before any note is published or any gate runs (45 hook tests, 400 `scripts/ci` tests).

No finding was deferred for a performance-measurement reason, so `deferred_perf_measurement` is not set.

Independent verification by the orchestrator after both subagents finished: `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 45 passed / 0 failed; `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` 400 OK; `python3 -m py_compile scripts/ci/*.py` clean; `shellcheck .githooks/pre-push` clean.

One behavior note for the reviewer: the committed hook reviewed HEAD when Git handed it no ref line at all; the reworked hook reviews only the updates it is given and says "this push updates no branch" for an empty list. Git always supplies the list on a real push, so only a harness that omits it sees the difference, and no document promised the old behavior.

The files this cycle touched, as reported by `git status` (all 12 are this cycle's; the tree was clean at `e4279b59a` when it began):

        - `.claude/skills/os/SKILL.md`
        - `.claude/skills/os/macos.md`
        - `.claude/skills/rust-devops/ci-cd.md`
        - `.githooks/pre-push`
        - `.githooks/tests/test-pre-push.sh`
        - `.github/ci/README.md`
        - `CLAUDE.md`
        - `docs/topics/ci-cd.md`
        - `fixes/2026-09-11-cicd-cleanup/log.md`
        - `just/ci-local.just`
        - `scripts/ci/constraints.py`
        - `scripts/ci/test_constraints.py`

## Implementation of Review Findings #4

> **started at:** 2026-09-11T23:40:13-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/fixes/2026-09-11-cicd-cleanup/review-4.md'
- this is iteration 4 of the review-to-implement cycle
- orchestration plan decided at 23:47:00 after reading the review, spec, `open-questions-and-blockers.md` (still `status: awaiting-ken`, spec still has no `## Rulings` section), iteration 3's log, the hook's trigger-review block, `ci.yml`'s triggers and scope step, `local_evidence.py`'s scope receipt code, and the shell harness:
        - the tree was clean at `a69c02c5c` when this cycle began; iteration 3's edits are committed
        - implementable in this cycle (no ruling needed): finding 1 (the trigger review assumes every non-main update is compared against `origin/main`, while `ci.yml` fires `push` only for `main` and `pull_request` with NO branch filter, computing scope from `pull_request.base.sha` — the base branch's tip at event time — so a stacked pull request or an advanced target branch is planned against the wrong base)
        - still blocked on unruled open questions, so expected to be deferred again: finding 2 (`ci-verdict` removal needs OQ3/B3 and the scratch-repository fixture B5), finding 3 (the gap publisher needs OQ4/B4 and the hosted fixture B5), finding 4 (the constraint store default needs OQ2/B2); all three are again listed among this review's `human_review_items`, and this non-interactive session holds no push, dispatch, or ruleset authorization
        - design facts fixed before dispatch: `pull_request.base.sha` is the base branch tip (two-dot diff in CI, so an advanced target over-selects and the hook must mirror that); a feature-branch push triggers no `push` run at all, only the open pull requests whose head it is; the scope receipt keeps its ancestor rule (`record_scope` refuses a base that is not an ancestor of the head, tested in `test_local_evidence.py`), so an advanced target is reviewed for constraints but records no receipt; pull requests are enumerated with `gh` on GitHub remotes only, a non-GitHub remote (the harness's bare repository) can hold none, and the harness stubs `gh` on its private PATH rather than adding a production seam
        - gates are run only in the impacted areas: `.githooks/tests/test-pre-push.sh` under `/bin/bash` and `/opt/homebrew/bin/bash`, `shellcheck` on the hook and harness, the `scripts/ci/*.py` suites where touched, and `just test test-toolkit` for the hook-text contract
        - host load at start: 16 logical cores; nothing in this cycle is a performance measurement
- starting the work on 'Trigger review uses the wrong comparison base for stacked pull requests' at 23:45:13
        - discovered: `ci.yml` fires `push` only for `main` and `pull_request` with no branch filter, and its scope step diffs `PR_BASE` (`pull_request.base.sha`, the target branch's tip at event time) two-dot against `PR_HEAD`; `local_evidence.py verify --cells` fails safe on an all-zero base (exit 0, one `missing-receipt` rejection), so a branch-creating push of `main` can be planned `--all` and still flow through the evidence overlay; the real planner's empty plan (empty path set) has `change_class: documentation`, `preflight_os: [ubuntu-latest]`, no package and no cell
        - discovered: the committed harness (`f45e405e2`) had clobbered iteration 3's Bash 3.2 fix from `19fb94c3e` — `run_hook` was back to a bare `"${env_args[@]}"`, so `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh` aborted at `line 239: env_args[@]: unbound variable` before the first test, and the `run_hook forwards no selection without an override` self-check was gone (44 `run_test` lines, not 45); both are restored in this change because the `/bin/bash` gate cannot run without them (`.claude/skills/os/macos.md`'s bullet from that commit is still in HEAD)
        - discovered: `gh` lives at `/opt/homebrew/bin/gh` (2.98.0, authenticated for github.com), so it is invisible under the harness's private `PATH=$tmpdir:/usr/bin:/bin` and a stub placed in `$tmpdir` is the only `gh` a test run sees; no document carries a verbatim "re-run failed jobs" caveat — the nearest is `CLAUDE.md`'s "jobs triggered indirectly by a push, dispatch, or retry" — so the new web-UI caveat is worded beside it
        - changed `.githooks/pre-push`: per branch update, `resolve_trigger_contexts` writes one `<base sha> <kind>` line per run the update triggers to `CONTEXTS_FILE` — `refs/heads/main`: the remote sha (fetched once from `$REMOTE_NAME` when absent locally, else fail closed), or the all-zero OID for a branch-creating push (planned `--all`, no receipt); any other branch on a github.com remote (`github_repo_of` reads `git@github.com:`, `ssh://`, `https://` forms into `GITHUB_REPO` and fails closed on a github.com URL it cannot read): `gh pr list --repo OWNER/REPO --head <remote branch> --state open --json number,baseRefName` parsed with `jq`, each base branch's CURRENT tip read with `git ls-remote $REMOTE_NAME refs/heads/<base>` and fetched once when absent (`remote_branch_tip`), `gh`/`jq` missing, `gh` failing (its stderr quoted), or an unresolvable base all `block_trigger_review` naming the command and `--no-verify`; zero open pull requests, or a non-GitHub remote: one PROVISIONAL context against the remote's `main` tip (fail closed when it cannot be resolved), worded "provisional: no open pull request …; a pull request opened against main before main advances will run this plan". `review_trigger_context` runs plan → `verify --cells` → `--apply-to` → render → `constraints.py check` per context with a `Comparison base for <sha>: <base> (<kind>)` line each, the first failure blocking; `plan_trigger_scope` plans `--all` for an all-zero base. `record_scope_evidence` runs for HEAD's FIRST context only and prints why no receipt is recorded when the base is all zeros or not an ancestor of HEAD (`git merge-base --is-ancestor`), and says the receipt binds the first base only when the update has several contexts; `record_scope`, `verify_scope`, and the receipt schema are untouched. Header: the scope-receipt base sentence corrected and a "Comparison bases" paragraph added; the in-function comment that claimed the merge base with `origin/main` is the pull-request base is gone. `REMOTE_URL` stays the constraint identity and `REMOTE_NAME` drives every ls-remote/fetch/notes transfer. POSIX `sh` throughout; `shellcheck` clean
        - changed `.githooks/tests/fixtures/affected_scope_stub.py`: an empty path set (a `--` with nothing after it and no `--all`) yields the real planner's empty plan (`empty_plan`, schema-validated) whatever `TEST_PLANNER_PLAN` names — needed to show that a child restoring its parent's change selects nothing against `main`
        - changed `.githooks/tests/test-pre-push.sh`: header gains the review-4 paragraph and the Bash 3.2 rule; `run_hook` restored to `${env_args[@]+"${env_args[@]}"}` with the self-check test back in the runner; scope-evidence comment block rewritten; new fixtures `GITHUB_URL` (`git@github.com:acme/widgets.git`, passed as `$2` while `origin` keeps pointing at the bare repository), `stage_fake_gh <tmpdir> [row…]` (a `sh` stub logging argv to `gh.log`, answering from `gh-reply.json`, failing like an unauthenticated `gh` when `gh-fail` exists), `make_stacked_repo` (the review's reproduction: `parent` from `main` changes `pkg/alpha/src/lib.rs` and is pushed; `child` from `parent` restores "base lib" and is HEAD), `run_stacked_push`, `assert_planned_with_no_paths`; eight new tests — stacked PR into `parent` planned `--base <parent tip> --head <child> -- pkg/alpha/src/lib.rs`, names `pull request #12 into parent`, asks `gh` `--repo acme/widgets --head child`, and is BLOCKED (exit 1) under a wsl2-ubuntu prohibition; the inverse PR into `main` gets an empty path set and passes; `main` advanced on the remote from a second clone with the tracking ref proven stale → planned against the new tip with `pkg/alpha/src/lib.rs work.txt`, "has advanced past the branch point" printed, no receipt; no open PR on a GitHub remote → provisional wording, constraint still enforced, receipt against `main`'s tip; two open PRs (`main`, `parent`) → two `Comparison base` lines, both bases planned, receipt binds the first (asserted by `.base/.head/.plan.base`), a prohibition only the second violates still blocks; `gh` absent → exit 2 naming `\`gh\` is not installed` and `--no-verify`, no plan resolved; `gh` failing → exit 2 quoting `\`gh pr list\` failed with: … gh auth login`; a non-GitHub remote never invokes the staged stub (no `gh.log`). Adapted: "a feature push records the PR base" and the renamed "a branch-creating push records the remote's main tip, says so" now assert the provisional wording (base still `main`'s tip); "a failed scope publication is named" makes the bare remote refuse only `refs/notes/ci-local/scope` through a `pre-receive` hook instead of pointing `origin` at a missing path, because an unreadable remote now fails closed before publication; "the repository identity is the remote being pushed to" pushes `main` to `upstream.git` first, since the provisional plan needs it there. 44 → 53 tests
        - non-vacuity: `PRE_PUSH_HOOK_UNDER_TEST=/tmp/old-hook` (`git show HEAD:.githooks/pre-push`) under `/bin/bash` → 43 passed / 10 failed: "a stacked pull request is planned against its target's tip" gets `expected exit 1, got 0` (the review's reproduction: the old hook planned against `origin/main`, found no changed path, and let the push through under the WSL prohibition); "an advanced target is read from the remote" plans against the STALE tracking ref instead of the remote's new tip; "two open pull requests from one head" prints one `Comparison base` line (`merge base with origin/main …`) instead of two; "a GitHub remote without gh" and "a failing gh" both get `expected exit 2, got 0` (the old hook never asked); "no open pull request on GitHub" never writes `gh.log`; "a pull request into main from a restoring child", "a non-GitHub remote never asks gh", and the two adapted tests fail only on the trigger wording, as expected
        - gates: `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 53 passed / 0 failed (the committed suite aborts under this shell, see above); `env -u CDPATH /opt/homebrew/bin/bash …` 53/0; `just test-pre-push-hook` 53/0; `shellcheck .githooks/pre-push` clean; `shellcheck -x .githooks/tests/test-pre-push.sh` SC2016 ×3 (same as the committed copy) and SC2329 info 72 → 85 (the new indirectly-invoked functions only); `just test test-toolkit` 155 passed / 2 skipped (the hook-text and AC14 documentation contracts included); no `scripts/ci/*.py` was touched, so that suite was not rerun
        - end-to-end probes on this checkout with the real planner, pushed sha `HEAD~1` so no receipt is recorded and nothing is pushed: (1) `printf 'refs/heads/feat/unifi <HEAD~1> refs/heads/probe-target <zeros>\n' | RUSTY_BISCUIT_PRE_PUSH=scope-only BISCUIT_CI_CONSTRAINTS_DIR='' ./.githooks/pre-push origin /tmp/does-not-exist.git` → `Remote: origin (/tmp/does-not-exist.git)`, worktree announced, `Comparison base … (provisional: no open pull request, since /tmp/does-not-exist.git is not a GitHub remote; …)` against `origin`'s `main` read by ls-remote, 55 cell lines rendered, exit 0 in 5 s; (2) the same with `$2=git@github.com:yankeeinlondon/rusty-biscuit.git` and the real `gh` (read-only `gh pr list`) → `Comparison base … (pull request #76 into main, the tip of main on origin)`, exit 0 in 5 s; `git worktree list` 8 before and after, `refs/notes/ci-local/scope` 0 notes before and after, no `biscuit-pre-push-tree.*` left under `$TMPDIR`
        - docs synced: `CLAUDE.md` (new Test Execution Constraints bullet: the base of every run the update triggers, and the web-UI pull request as a trigger no hook reviews), `.github/ci/README.md` (scope-receipt base sentence), `.claude/skills/rust-devops/ci-cd.md` (new paragraph after the per-update paragraph), `.claude/skills/os/SKILL.md` (scope-only sentence), `docs/topics/ci-cd.md` (Layer 1 base sentence), `just/ci-local.just` (the preview now differs from the hook by base as well as by tree); `tools/test-toolkit/tests/ci_workflow_contracts.rs` pins only `ci/constraints.py` in the hook and its retired/required doc phrases, all still satisfied; the task brief also named an empty-string agent skill, which was noted and skipped
- work completed for 'Trigger review uses the wrong comparison base for stacked pull requests' at 00:02:06
- orchestrator verification after the subagent finished (at 00:05:29): `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 53 passed / 0 failed; `env -u CDPATH /opt/homebrew/bin/bash …` 53/0; `shellcheck .githooks/pre-push` clean; harness shellcheck SC2016 ×3 unchanged and SC2329 72 → 85 (new indirectly-invoked functions only); `just test test-toolkit` 155 passed / 2 skipped; the claim that commit `f45e405e2` had clobbered iteration 3's Bash 3.2 fix was confirmed directly against `git show HEAD:.githooks/tests/test-pre-push.sh` (bare `"${env_args[@]}"` at line 240, 44 test functions), so review-4's "45 passed" figure was measured on a working tree that the later commit did not preserve; the hook diff was read against the agreed design (one context per triggerable run, fail-closed identity, labeled provisional plan, receipt ancestor rule untouched) and matches it
- 'The standalone global policy verdict remains' — evaluated at 00:05:29 and deferred (see Successful Completion)
- 'Accepted-gap publication and hosted presentation verification remain incomplete' — evaluated at 00:05:29 and deferred (see Successful Completion)
- 'Constraints still disappear in an unconfigured fresh session' — evaluated at 00:05:29 and deferred (see Successful Completion)

### Successful Completion

The implementation of review cycle 4 has completed successfully in 25m. During this implementation all 4 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 1 was fixed, 3 were deferred (see reasons below):

- **The standalone global policy verdict remains** — deferred for the third time. Removing `ci-verdict` needs the merge gate chosen under Open Question 3 (blocker B3) and its scratch-repository proof (blocker B5); `open-questions-and-blockers.md` is still `status: awaiting-ken`, the spec carries no `## Rulings` section, the review itself says to keep the current gate until the coordinated transition is ready, this review lists the choice among its `human_review_items`, and this non-interactive session holds no push, dispatch, or ruleset authorization.
- **Accepted-gap publication and hosted presentation verification remain incomplete** — deferred for the third time. The check-run conclusion is Open Question 4 (blocker B4) and the hosted throwaway-branch/scratch-repository fixture is blocker B5; the review asks for the publisher only after the display choice, and the hosted experiment requires a push this session may not make. The half that holds under every option (the distinct `ACCEPTED GAP` state and its explanatory detail) already exists and stays tested.
- **Constraints still disappear in an unconfigured fresh session** — deferred for the third time. The store's location is Open Question 2 (blocker B2), again among this review's `human_review_items`; the spec forbids implementing an affected section before Ken rules. The ruling still costs one line in `constraints.default_directory()` plus the cross-session fixture; nothing this cycle touched that seam, and the per-context hook review holds under all three OQ2 options.

The fixed finding is the one no ruling gates: the hook now resolves, for every branch update, the base of every run that update actually triggers — the remote's `main` for a push to `main`, the current remote tip of each open pull request's target branch (listed with `gh pr list` on GitHub remotes, fetched once when absent locally), or a clearly labeled provisional plan against the remote's `main` when no pull request is open — and plans, verifies, renders, and constraint-checks each one before permitting the push; an identity it cannot establish (`gh`/`jq` missing or failing, an unreadable github.com URL, an unfetchable base) fails closed naming the command. The scope receipt binds HEAD's first context and is withheld, with the reason printed, when the target has advanced past the branch point. 53 hook tests (44 committed; 8 new, 1 restored) pass under both shells, and the review's stacked-PR reproduction blocks under the old hook's exact conditions.

No finding was deferred for a performance-measurement reason, so `deferred_perf_measurement` is not set.

One repository-state note for the reviewer: commit `f45e405e2` (the cycle-3 commit) did not preserve cycle 3's Bash 3.2 harness fix from `19fb94c3e`, so the committed suite aborted under `/bin/bash` before this cycle; the idiom and its self-check test are restored here as part of the harness edit rather than as a separate change, because the `/bin/bash` gate could not run otherwise.

The files this cycle touched, as reported by `git status` (all 10 are this cycle's; the tree was clean at `a69c02c5c` when it began):

        - `.claude/skills/os/SKILL.md`
        - `.claude/skills/rust-devops/ci-cd.md`
        - `.githooks/pre-push`
        - `.githooks/tests/fixtures/affected_scope_stub.py`
        - `.githooks/tests/test-pre-push.sh`
        - `.github/ci/README.md`
        - `CLAUDE.md`
        - `docs/topics/ci-cd.md`
        - `fixes/2026-09-11-cicd-cleanup/log.md`
        - `just/ci-local.just`

## Implementation of Review Findings #5

> **started at:** 2026-09-12T01:27:50-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/fixes/2026-09-11-cicd-cleanup/review-5.md'
- this is iteration 5 of the review-to-implement cycle
- orchestration plan decided at 01:29:40 after reading the review, the spec (now carrying a `## Rulings` section for OQ1–OQ4, B0, D1, D2, B5, and the governing principle), the absorption audit, the scratch-repository fixture record, iteration 4's log, the hook's trigger-context code, `constraints.py`, `ci.yml`'s job map, `_area-ci.yml`, and the three pending workflow contracts:
        - the tree at `a8dbd1a61` carries only planning-document edits (spec rulings, review-5 pointers, open-questions status, prompt templates) plus the untracked audit and fixture record; no code differs from HEAD
        - every finding is now unblocked: finding 1 needed no ruling; finding 2 is ruled (OQ3 → Option B, the `ci-gate` fold, proven in the scratch repository); finding 3 is ruled (OQ4 → `neutral`, publisher job alone holds `checks: write`); finding 4 is ruled (OQ2 → Option B, `<home>/.rusty-biscuit/ci-constraints/<repo>/`, `Path.home()`, variable kept as override)
        - what this non-interactive session still cannot do: commit, push, dispatch a workflow, or edit the `protect-your-bacon` ruleset; the throwaway-branch half of finding 3 (hosted labels for nested areas and mixed cells) and the live required-context swap of finding 2 therefore remain owed to Ken after this cycle, and are reported as such rather than as done
        - order of work, serial, one subagent each: finding 1 (hook: simultaneous target/head updates), finding 4 (constraint store default), finding 2 (`ci-verdict` → `ci-gate` with consumer migration), finding 3 (accepted-gap publisher); findings 2 and 3 both edit `ci.yml` and the workflow contracts, so they are not parallelized
        - gates are run only in the impacted areas: `.githooks/tests/test-pre-push.sh` under `/bin/bash` and `/opt/homebrew/bin/bash`, `shellcheck`, the `scripts/ci/*.py` suites, `actionlint` on the touched workflows, `just test test-toolkit`, and the `scripts/` Nextest bins where touched
        - host load at start: 16 logical cores, load average 8.54 (1 min); nothing in this cycle is a performance measurement
- starting the work on 'Simultaneous PR head and target updates bypass the target-aware constraint check' at 01:30:00
        - discovered: `resolve_trigger_contexts` read every pull request's target with `remote_branch_tip` alone — the remote's CURRENT tip by `git ls-remote` — and never looked at `REF_UPDATES_FILE`, so a target branch travelling in the same push was compared at its old tip only; the review's reproduction (remote `main` and `parent` both at the base library, local `parent` changing `pkg/alpha/src/lib.rs`, `child` restoring it, PR #12 `child` → `parent`, a `branch: child` WSL prohibition) exits 0 in both ref orders because `child` against the old `parent` is an empty path set, while CI's `pull_request.base.sha` after both land is the incoming `parent` and selects the prohibited cell; the server applies a multi-ref push in an order the hook cannot know, so BOTH the current tip and the incoming revision are event states the run can see
        - discovered: `remote_branch_tip` was only ever called inside `$(...)`, so the `REMOTE_TIP_ERROR` it set on failure died with the subshell and both "could not be resolved: " messages ended empty; fixed in passing since both call sites were being rewritten (the function now assigns `REMOTE_TIP` instead of printing)
        - discovered: the harness's `stage_fake_gh` answered the same rows for every `--head`, so a push carrying `parent` and `child` would have given `parent` a pull request into itself; GitNexus does not index the hook's shell functions (`impact` → UNKNOWN for all four), callers confirmed by grep to be the hook alone
        - changed `.githooks/pre-push`: new `resolve_target_states <branch>` writes one `<sha> current|incoming` line per state to `TARGET_STATES_FILE` — the branch's current tip on `$REMOTE_NAME` (via `remote_branch_tip`) plus, when a line in `REF_UPDATES_FILE` writes `refs/heads/<branch>`, that update's local sha as the incoming state; one line when the two coincide, incoming only when the branch is created by this push (`remote_branch_tip` fails and the update's remote sha is all zeros), and a fail-closed reason in `TARGET_STATE_ERROR` when the current tip cannot be read or when the update DELETES the branch ("this push also deletes <branch> (refs/heads/<branch>), so a run into it may fire against the old tip before the deletion lands and has no base at all afterwards"). The pull-request loop calls it per base branch, blocks with "the base of pull request #N into B cannot be established: … push with --no-verify to skip this review entirely", prints "B is updated by this push too, so the run of pull request #N may see either its current tip or the incoming revision as its base; both are reviewed." when two states apply, and writes one context per state — the current one worded exactly as before (`the tip of B on origin`), the incoming one `the revision this push also writes to B`. The provisional path resolves `main` the same way (incoming wording: "main is updated by this push too, and a pull request opened against it once that lands will run this plan"). Deleted-target rule chosen: refuse, fail-closed — GitHub closes a pull request whose base is deleted, but the head update in the same push may fire a `pull_request` run against the old tip first, and neither state is one the hook can vouch for; the scope receipt rule is unchanged (HEAD's FIRST context, i.e. the current remote tip, so the run that sees the incoming target misses the receipt and CI calculates scope itself; the "binds its first comparison base only" line now ends "CI calculates scope itself for every other context"); header "Comparison bases" paragraph gains the simultaneous-update and deleted-target sentences; `TARGET_STATES_FILE` added to the mktemp/trap list; POSIX `sh`, `shellcheck` clean (the unused first `read` field is `_`)
        - changed `.githooks/tests/test-pre-push.sh`: header gains the review-5 paragraph; `stage_fake_gh` rows may carry a third `<head>` field, written to `gh-reply-<head>.json` and answered only for that `--head` (generic rows keep `gh-reply.json`; the `sh` stub scans its argv for `--head`); `make_stacked_repo <tmpdir> [pushed|behind]` — `behind` pushes `main:refs/heads/parent` so the parent's change is an incoming update — and now sets `MAIN_SHA`; new `simultaneous_ref_lines <parent first|child first>`; three new tests: "a target updated in the same push is reviewed at its incoming revision" (child-only `branch: child` prohibition, both orders in one stdin payload each, exit 1, the planner handed `--base $MAIN_SHA --head $CHILD_SHA --` with no paths AND `--base $PARENT_SHA --head $CHILD_SHA -- pkg/alpha/src/lib.rs`, the incoming state named, "do not rerun WSL for branch child", "Blocked while reviewing refs/heads/child -> refs/heads/child", no gate, no leftover worktree); "a simultaneous push with nothing prohibited passes in either order" (exit 0, exactly two `Comparison base for <child>` lines, both-states line, both state wordings, both child plans plus the parent's provisional `--base $MAIN_SHA --head $PARENT_SHA -- pkg/alpha/src/lib.rs`, receipt binds `{MAIN_SHA, CHILD_SHA}`); "a pull request into a branch this push deletes is refused" (`(delete)` line for `parent` before and after the child line, exit 2 naming `pull request #12 into parent`, `this push also deletes parent (refs/heads/parent)`, and `--no-verify`, no `planner.log`, deletion still "Not reviewing" when reached first). 53 → 56 tests; Bash 3.2-safe (no new arrays; `${a[@]+"${a[@]}"}` idiom untouched)
        - non-vacuity: `git show HEAD:.githooks/pre-push > /tmp/old-hook; PRE_PUSH_HOOK_UNDER_TEST=/tmp/old-hook env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh` → 53 passed / 3 failed, exactly the new three: the incoming-revision test gets `expected exit 1, got 0` on "parent first" (the old hook planned the child against the old parent, found no path, and let the push through under the child-only prohibition — the review's reproduction verbatim); the control fails on "expected the child's run to be reviewed in two states" showing one `Comparison base` line for the child (`the tip of parent on origin`); the deleted-target test gets `expected exit 2, got 0` with the old hook skipping the deletion, planning the child against the pushed parent, and publishing a scope receipt
        - gates: `env -u CDPATH GIT_TERMINAL_PROMPT=0 /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 56 passed / 0 failed; the same under `/opt/homebrew/bin/bash` (5.3.15) 56/0; `shellcheck .githooks/pre-push` clean; `shellcheck -x .githooks/tests/test-pre-push.sh` SC2016 ×3 (pre-existing) and SC2329 info 85 → 89 (the four new indirectly-invoked functions); `just test test-toolkit` 155 passed / 2 skipped, re-run after the doc edits; end-to-end probe on this checkout with the real planner and `ci-plan` renderer, pushed sha `HEAD~1` to `refs/heads/probe-target` on `origin` with `$2=/tmp/does-not-exist.git`, scope-only → provisional context against the remote's `main` (`c836cf00a`), 53-job plan rendered, exit 0 in 5 s, `git worktree list` 8 before and after, no scope note, the only `biscuit-pre-push*` entries under `$TMPDIR` dated 09-11 11:47 (before this session)
        - docs synced (only the now-incomplete "base of every run" wording, one clause each): `CLAUDE.md` Test Execution Constraints bullet, `.github/ci/README.md` scope-receipt paragraph (receipt binds the current tip), `.claude/skills/rust-devops/ci-cd.md` per-run paragraph, `.claude/skills/os/SKILL.md` scope-only sentence, `docs/topics/ci-cd.md` Layer 1 base sentence, `just/ci-local.just` preview comment; no contract pin in `ci_workflow_contracts.rs` names these phrases
        - orchestrator verification (at 01:45:10): `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 56 passed / 0 failed; `shellcheck .githooks/pre-push` clean; `git status` shows exactly the eight files the subagent named (hook, harness, six documents)
- work completed for 'Simultaneous PR head and target updates bypass the target-aware constraint check' at 01:45:10
- starting the work on 'Constraints still disappear in an unconfigured fresh session' at 01:45:10
        - discovered: `default_directory()` returned `""` and `load` globbed one flat directory, so with the variable unset every consumer read nothing — `check` (the hook), `environments`, and `just ci-local`, which forwarded `--constraints` only when a directory was named; the harness ran the hook under `env -i` with `HOME=$tmpdir/home` and no `USERPROFILE`, and `test_ci_local.py`'s recipe fixtures inherited the developer's real `HOME`, so any host default would have been read from the developer's own store; `repository_identity` can yield an absolute path (`/tmp/x/origin.git` → `/tmp/x/origin`, which `Path.joinpath` would let REPLACE the store root), a port colon (`github.com:2222/org/repo`), a Windows drive (`C:\Users\…` → `C/\Users\…`), and backslashes, so the identity is not usable as a path verbatim; GitNexus: `default_directory`, `directory_from_environment`, `read_prohibitions` LOW (one caller each), `load` ambiguous across 21 symbols → grep confirms the store's `load` is called only by `constraints.main` and `affected_scope.read_prohibitions`
        - design (simplest that keeps CI unable to read the store): the default is resolved ONLY by `constraints.py`'s own commands, which only the two trigger boundaries run — the hook's existing `check --repository "$REMOTE_URL"` now falls back to `default_directory(args.repository)` when neither `--directory` nor the variable is given, and `just ci-local` calls a new `constraints.py directory --repository "$(git remote get-url origin)" --directory "${constraints_dir}"` (explicit `--constraints` wins, else the variable, else the default) and hands the result to the planner's `--constraints` unconditionally; the planner keeps NO default (`read_prohibitions(None)` → `{}`), `ci.yml` never passes `--constraints` or runs `constraints.py` (contract `ci_never_reads_the_execution_constraint_store` still pins both), so Design Decision 8 holds by construction rather than by policy. Repository rule: `<home>/.rusty-biscuit/ci-constraints/` + the identity's `/`- or `\`-split segments with `<>:"|?*` and control characters replaced by `_` and `.`/`..`/empty segments dropped (`github.com/yankeeinlondon/rusty-biscuit`, `tmp/x/origin`, `C/Users/ken/origin`, `github.com_2222/org/repo`); an empty identity is the store ROOT and `load` now reads `rglob("*.json")` everywhere, so an unknown remote is bound by every repository's records (fail-closed) while a known one reads only its own directory; `<home>` is `Path.home()` (HOME on Unix, USERPROFILE on native Windows). The hook derives the repository from the remote being pushed to; `ci-local` has no push and uses `origin` (empty without one → root), stated in one comment at the recipe's resolution site. Repository/branch matching, expiry, and malformed-record blocking are untouched
        - changed `scripts/ci/constraints.py`: module docstring's OQ2 paragraph replaced by the ruled location, the `Path.home()`/`USERPROFILE` contract, and the CI-isolation contract; `directory_from_environment` removed (its one caller was the argparse default) in favor of `store_root()`, `store_segments()`, `default_directory(repository="")`, `resolve_directory(repository, explicit)`; `--directory` defaults to `""` and is resolved in `main`; new `directory` subcommand prints the resolved store; `load` uses `rglob` with a docstring sentence on why
        - changed `scripts/ci/affected_scope.py`: only the `read_prohibitions` docstring (it claimed the location was unruled); no behavior change
        - changed `just/ci-local.just`: header paragraph names the default store and the `directory` command; usage gains `--constraints <dir>`; the store is resolved right after the `python3` check and `--constraints` is always forwarded; `--plan` prints `Constraint store: <path>` before the cells so a reviewer sees which store decided the refusal
        - changed `.githooks/pre-push`: header comment only (the store's default and that the home is read by Python's `Path.home()`); the `check` call was already `--repository "$REMOTE_URL"`, which is the fallback's input, so no hook logic moved; `shellcheck` clean
        - changed `.githooks/tests/test-pre-push.sh`: `USERPROFILE=$tmpdir/home` beside `HOME` in `run_hook_as_remote` (with the reason); `write_prohibition`'s OQ2 comment replaced; new `test_a_record_in_the_default_store_binds_a_fresh_session` — a `github.com/acme/widgets/current.json` record under `$tmpdir/home/.rusty-biscuit/ci-constraints/` and a decoy under `github.com/other/repo/` (`repository`-scoped), the hook run against `$GITHUB_URL` (`git@github.com:acme/widgets.git`, so the directory derivation is a real identity rather than a temp path) with a stub `gh` answering no pull request and NO `BISCUIT_CI_CONSTRAINTS_DIR` (the `env -i` guarantee): exit 1 naming "do not rerun WSL for this branch", no gate run; then with the widgets record removed and only the decoy left, exit 0 and no "Push blocked". 56 → 57 tests, Bash 3.2-safe (no new arrays); the 20 existing variable-driven constraint tests are the override path's coverage and are unchanged
        - changed `scripts/ci/test_constraints.py`: `LocationTests` no longer pins the empty default; it relocates the home through `HOME`+`USERPROFILE` (`mock.patch.dict`, the seam a real session crosses, rather than patching `Path.home`) and pops the variable; 8 tests: known identity → `<home>/.rusty-biscuit/ci-constraints/github.com/yankeeinlondon/rusty-biscuit`; SSH/HTTPS/`ssh://`/bare spellings → one directory; empty identity → the root; filesystem, Windows-drive, and port identities stay under the root with portable segments; override wins (variable, then explicit over variable); `load` at the root reads both repositories' records and at the repository directory only its own; and the fresh-session case as a real subprocess — `check --repository …` in an environment without the variable exits non-zero naming the record, and `directory --repository …` prints the same path `default_directory` computes. 32 → 39 tests
        - changed `scripts/ci/test_ci_local.py`: `relocate_home()` sets `HOME`/`USERPROFILE` to a fixture-owned home and pops the variable in BOTH recipe fixtures (`run_recipe` and `run_plan`), and both copy the real `constraints.py` into the temp `scripts/ci` since the recipe now resolves the store unconditionally; the `--plan` stub planner (`STUB_PLANNER`) prints the fixture plan but resolves every cell in an environment forbidden by the store handed through `--constraints` to `prohibited` carrying that record's owner/reason/expiry (the one real-planner behavior the surface depends on), so a refusal's reason proves which store the recipe found; `run_plan` gains `origin` (a `git init` + `remote add origin` in the temp root) and `records` (store-relative paths written under the relocated home); new `test_plan_finds_a_record_in_the_default_store_without_the_variable`: origin `git@github.com:acme/widgets.git`, a record under `github.com/acme/widgets/` ("recorded in an earlier session") and a `repository`-scoped decoy under `github.com/other/repo/` → exit non-zero naming the first and not the second, `Constraint store:` line naming `github.com/acme/widgets`; the same records with origin `acme/other` → exit 0. 35 → 36 tests; the 8 pre-existing `--plan` contracts pass with the empty relocated home
        - non-vacuity: (1) `git show HEAD:scripts/ci/constraints.py` + the new `test_constraints.py` in a scratch dir → `LocationTests` FAILED (failures=1, errors=8): the fresh-session test fails `0 == 0 : the record was not found from a fresh session` (the review's defect verbatim) and the other seven error on the absent API (`default_directory() takes 0 positional arguments`, `no attribute 'resolve_directory'`); (2) `git archive HEAD scripts/ci just .github/workflows` + the new `test_ci_local.py` → `PlanSurfaceTests` 8 passed / 1 failed, exactly the new contract, `AssertionError: 0 == 0` (the old recipe forwarded no store, so the stub saw none); (3) the working-tree `.githooks/` with HEAD's `scripts/ci/` → 56 passed / 1 failed, exactly "a record in the default store binds a fresh session", `expected exit 1, got 0`
        - gates: `python3 -m py_compile scripts/ci/*.py` ok; `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` Ran 407 tests in 94.3 s, OK (399 before: test_constraints 32 → 39, test_ci_local 35 → 36); `env -u CDPATH GIT_TERMINAL_PROMPT=0 /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 57 passed / 0 failed; the same under `/opt/homebrew/bin/bash` (5.3.15) 57/0; `shellcheck .githooks/pre-push` clean; `shellcheck -x .githooks/tests/test-pre-push.sh` SC2016 ×3 (pre-existing) and SC2329 info 89 → 90 (the one new indirectly-invoked function); `just test test-toolkit` 155 passed / 2 skipped (the retired/required doc phrases and the CI-never-reads-the-store contract still hold); macOS is the only host exercised here — the Linux, native Windows (`Path.home()` → `USERPROFILE`, the reserved-character segments), and WSL2 legs are CI's (`ci-tooling` runs `test_constraints.py`, `pre-push-hook-tests.yml` the harness) and are not claimed
        - docs synced (only where the location was described or called unruled): `CLAUDE.md` Test Execution Constraints bullet (default directory, variable as override — `BISCUIT_CI_CONSTRAINTS_DIR` kept for the contract pin), `.github/ci/README.md` (the "still Open Question 2" paragraph replaced by the location, segment rule, home rule, root rule, and who resolves it), `.claude/skills/rust-devops/ci-cd.md` (the "unruled" sentence replaced), `.claude/skills/os/SKILL.md` (one clause), `docs/topics/ci-cd.md` (the Execution constraints paragraph), `.claude/skills/os/windows.md` (the Python `Path.home()`/`USERPROFILE` fact added to the existing home-directory trap, item 2 — it was not there), `just/ci-local.just` header, `.githooks/pre-push` header; `open-questions-and-blockers.md` and the spec were already updated by the ruling and are untouched here
        - orchestrator verification (at 02:02:30): `python3 -m unittest scripts/ci/test_constraints.py` 39 tests OK; `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 57 passed / 0 failed; the only two `constraints` mentions in `ci.yml` are the `test_constraints.py` suite invocations in `ci-tooling` and the self-test, so CI still never reads the store
- work completed for 'Constraints still disappear in an unconfigured fresh session' at 02:02:30
- starting the work on 'The standalone global policy verdict remains' at 02:03:00
        - discovered: `ci.yml` had eight top-level jobs — `validation`, `scope`, `preflight`, `area-ci`, `biscuit-tui-captured-stdout`, `ci-tooling`, `ci-verdict`, and the advisory `summary` — so the blocking set the fold must `need` is exactly the six the old verdict already waited on; nothing else at the top level can fail. Of what `ci-verdict` did, only three things were not already duplicated per area in `_area-ci.yml`'s `rollup` (`runner_loss.py attribute`, `ci-rollup rollup`, `ci-rollup verdict --baseline` all are): the reuse-validation CLEAR note, the "no package was scheduled" note, and the whole-run `ci-results` artifact. `ci-rollup summarize` already folds slices but renders Markdown only; `compare` took exactly one `--base` and one `--head`, while `Args::many` already collected repeated flags. GitNexus: `cmd_compare`, `load_rollup`, `render_comparison`, `is_non_producer` all LOW (one caller each); `repairTargets` UNKNOWN → grep confirms the watchdog is its only file
        - changed `.github/workflows/ci.yml`: `ci-verdict` (197 lines: checkout, toolchain, scope resolution, four artifact downloads, runner-loss attribution, `ci-rollup` build, rollup, `ci-results` upload, verdict, two CLEAR notes) replaced by `ci-gate` (`name: ci-gate`, `needs: [validation, scope, preflight, area-ci, biscuit-tui-captured-stdout, ci-tooling]`, `if: always()`, `ubuntu-latest`, `timeout-minutes: 2`, default permissions, no `continue-on-error`, no `uses:`), whose one `bash` step reads a `RESULTS: |` block of `<job>:${{ needs.<job>.result }}` lines and exits 1 naming every job whose result is not `success` or `skipped` (`ci-gate: blocked by scope preflight`), printing each job's result on stdout; the job comment carries the scratch-fixture facts (skipped accepted by design for unselected areas and reused validations, `failure`/`cancelled` block, MISSING lives in the per-area rollup because `needs` names static ids, `continue-on-error` turns a failure into `success` for the fold so no blocking job may carry it) and cites `fixtures/scratch-2026-09-12.md` once, plus the ruleset transition note. The `summary` job now runs `if: always()` (it was `reuse != 'true'`) and gained the two reporting notes as its first steps — "Reuse successful PR validation" (validation success + reuse, links `$VALIDATED_RUN`) and "No package was scheduled" (`reuse != 'true' && needs.scope.result == 'success' && has_packages != 'true'`, so it is silent on reuse and on a broken scope) — while the classify step is step-gated on `reuse != 'true'` and its text now says "The run's gate is `ci-gate`, not this line" and "no area rollup can see it; `ci-gate` blocks on its result directly"; two comments (`scope.json` upload, `ci-tooling` build) no longer name the verdict. The whole-run `ci-results` artifact is DROPPED, not re-published: the per-area `ci-results-<slug>` slices are the machine-readable results (spec §5 checklist), and giving the summary a folded document would have cost a `cargo build` plus fold logic in an advisory job for no consumer
        - changed `scripts/ci-rollup.rs`: `cmd_compare` reads `args.many("base")`/`many("head")` through a new `load_rollups(paths, flag)` — the first document, then each further slice's cells appended (a `{package, environment, tier}` already present is refused with "cell X is already present in an earlier `--base` slice"), scope/accepted_evidence/records concatenated, `run_id` from the first slice that has one, `areas` re-derived; no policy, no verdict; `USAGE` shows `--base <file>… --head <file>…` and states the fold rule; the two module comments that described the transitional whole-run verdict (`EXIT_*` docs, `policy_gap_findings`) now name the area rollups and the fold
        - changed `scripts/ci-rollup-tests.rs`: `compare_folds_repeated_base_and_head_slices_without_judging_them` (two base slices, two head slices, the regression only in the SECOND head slice → `EXIT_BLOCKED`, summary shows `base: run 100 · head: run 200` and no "not comparable"), `compare_refuses_a_cell_that_two_slices_both_carry`, `compare_still_requires_at_least_one_document_per_side`; one comment naming `ci-verdict` rewritten. 183 → 186 tests
        - changed `just/devops.just` (`ci-diff`): `latest_run_with_results` matches `^ci-results-`; both downloads are `gh run download <id> -p 'ci-results-*' -D <dir>`; a Bash 3.2-safe loop builds `compare_args+=(--base <slice>)` / `(--head <slice>)` over `<dir>/ci-results-*/ci-results-*.json` and hands them all to one `ci-rollup compare`; header comment states that there is no whole-run document and that the fold applies no policy; `just --list` parses, `just --dry-run ci-diff` renders, the evaluated recipe is `shellcheck` clean and `/bin/bash -n` clean (no `gh run download` was run)
        - changed `.claudine/scripts/ci-watchdog.ts`: header comment, `repairTargets` filter (with the WHY: the gate fails because a folded job did), the success lookup, and the not-green message all use `ci-gate`
        - changed `scripts/ci/runner_loss.py`: `NON_PRODUCER_JOBS = {"ci-gate", …}` with the comment rewritten (the fold fails whenever any producer did); `scripts/ci/test_runner_loss.py`: `VERDICT_JOB` → `GATE_JOB` named `ci-gate`, `test_the_gate_job_never_counts_as_a_failure`, the parse-to-nothing list names `ci-gate`
        - changed `tools/test-toolkit/tests/ci_workflow_contracts.rs`: `no_standalone_global_verdict_job_remains` and `the_verdict_consumers_are_rewired_when_the_job_goes` are ordinary assertions (the second now pins `-p 'ci-results-*'`, `compare_args+=(--base`/`--head`, the watchdog's `"ci-gate"`, and `NON_PRODUCER_JOBS = {"ci-gate"`; the first also asserts no job other than `ci-gate`'s block carries `ci-rollup verdict`); `ci_verdict_is_the_single_required_check` → `ci_gate_is_the_single_required_check` (job exists, `if: always()`, `needs` equals `GATED_JOBS` in order, folds each `needs.<job>.result`, no `continue-on-error`, no `permissions:`, no `uses:`, body names none of `ci-rollup`/`ci-baseline.toml`/`resolved-plan`/`download-artifact`/`--scope`/`cargo build`, contains `success|skipped)` and `exit 1`); new `every_top_level_job_is_either_folded_by_ci_gate_or_the_advisory_summary` (derived from `jobs(&ci)`, so a new top-level job cannot be added without deciding whether it blocks); `ci-gate` added to the gates-are-not-advisory loop; `post_merge_reuse_preserves_the_verdict…` → `…the_gate…` (gate folds `validation` under `always()`; summary needs `validation`, runs `if: always()`, links `$VALIDATED_RUN`, every later step gated on `reuse != 'true'`); `only_ci_verdict_makes_a_run_level_claim` → `only_ci_gate_…` (summary names `ci-gate`, not `ci-verdict`); `the_transitional_verdict_and_the_area_rollups_judge_the_same_inputs` → `the_area_rollup_judges_from_the_plan_policy_and_baseline`; `retired_packages_reach_verdict_through_package_evidence` checks `ci-gate`'s needs and the area rollup's artifact consumption; the AC14 corpus guard now requires `ci-gate` in `ci-cd.md`, `docs/topics/ci-cd.md`, `.github/ci/README.md`, and `CLAUDE.md` and forbids "still the single required context"; `pending_workflow_contract` kept for AC9's `checks: write` (untouched; the `only_the_advisory_summary_is_excluded_from_the_run_conclusion` exact-set contract still holds since the gate carries no `continue-on-error`). `scripts/ci/pending_contracts.py` is the decorator only and names no contract, so nothing there changes
        - changed `scripts/ci/test_ci_local.py`: `WorkflowGateStepTests` extracts the "Fold the blocking jobs' results" step with `workflow_step_script` and runs it under `STEP_BASH` (any Bash suffices; falls back to PATH `bash`) with `RESULTS` set: all six `success` → 0 with "every blocking job succeeded or was skipped"; `area-ci` and `biscuit-tui-captured-stdout` `skipped` → 0; `ci-tooling` `failure` → 1 with `ci-gate: blocked by ci-tooling` on stderr and `ci-tooling: failure (blocks)` on stdout; `area-ci` `cancelled` → 1 naming it; `scope` failure + `preflight` cancelled → "blocked by scope preflight" (every blocker named, not just the first); and `test_the_assertions_reject_a_fold_that_accepts_failure` runs the same failure input through the script with the accept clause widened to `success|skipped|failure)` and asserts exit 0. 36 → 42 tests
        - non-vacuity: (1) the widened-fold test above is in-suite and passes, i.e. the failure/cancelled assertions are load-bearing; (2) with HEAD's `ci.yml`, `just/devops.just`, `ci-watchdog.ts`, and `runner_loss.py` swapped in (my versions saved and restored byte-identical, `cmp` verified) the ten selected contracts ran 9 FAIL / 1 PASS: `no_standalone_global_verdict_job_remains` (line 2481, the job is declared), `ci_gate_is_the_single_required_check`, `the_area_rollup_judges…`, `post_merge_reuse…`, `retired_packages…` ("ci.yml must define the `ci-gate:` job"), `every_top_level_job…` (set mismatch), `the_verdict_consumers…` ("`just ci-diff` must download each run's per-area slices"), `only_ci_gate_makes_a_run_level_claim` ("must name ci-gate"), `advisory_jobs_cannot_fail…`; the one pass is the documentation guard, whose inputs were not swapped; (3) the single-file `compare` would have cleared the fold test's branch — the regression sits only in the second head slice, which `-n ci-results` never downloaded
        - gates: `actionlint .github/workflows/ci.yml .github/workflows/_area-ci.yml` clean (the five known `SC2086:info` in `_package-ci.yml` unchanged, nothing new); `just test test-toolkit` 156 passed / 2 skipped; `python3 -m unittest scripts/ci/test_ci_local.py scripts/ci/test_runner_loss.py` Ran 73 tests, OK (`test_pending_contracts.py` does not exist); `python3 -m py_compile scripts/ci/*.py` ok; `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` 186 passed; `shellcheck -s bash` on the extracted fold script exit 0; `just -f justfile --list` parses and `just --dry-run ci-diff` renders. A live run of the new workflow is a push this session cannot make: the fold's GitHub-side behavior rests on the scratch-repository record, not on a hosted run of `rusty-biscuit`, and no hosted verification is claimed
        - docs synced: `.github/ci/README.md` (reuse paragraph; the "single required check" section rewritten for `ci-gate` with the transition paragraph — ruleset 19747338 still names `ci-verdict`, the required check until 2026-09-12, every PR shows `ci-verdict — Expected` until Ken switches it after this change's own run is green, bypass actor untouched, `ci-diff`/watchdog consumers named), `.github/ci/ci-baseline.toml` header (verdict runs once per area; the gate never reads the file), `.claude/skills/rust-devops/ci-cd.md` (advisory bullet; "The merge gate" section rewritten — OQ3 ruled, Option B, the fold's semantics, the contract and step tests that pin it, the transition, the moved consumers; the "unruled" sentence is gone), `CLAUDE.md` CI Structure bullet (the gate is the policy-free `ci-gate` fold; the required-context swap is Ken's separate approval after this branch's run is green), `docs/topics/ci-cd.md` "The merge gate", `.claude/skills/rust-testing/nextest.md` reuse paragraph, `claudine/docs/rendezvous/local-ipc.md` (results preserved for the area's rollup), `.github/workflows/_area-ci.yml`, `_package-ci.yml`, `ci-infra-retry.yml` comments; `.claude/skills/os/SKILL.md`, `README.md`, `docs/testing-strategy.md`, `pr-health.yml`, `_wsl-ci.yml` never named `ci-verdict` and are untouched
        - left as history, deliberately: every hit under `fixes/`, `features/`, and `.claudine/` planning documents; `scripts/ci/test_resolved_plan.py:632` (a docstring explaining that the slices survive the removal); `.github/ci/ci-baseline.toml:68` (the path `fixes/2026-07-27-refactor/ci-verdict-job.md`); the transition notes in `ci.yml`'s gate comment, `CLAUDE.md`, `ci-cd.md`, `docs/topics/ci-cd.md`, and the README, which exist precisely to explain the `ci-verdict — Expected` state until the ruleset swap; the contract assertions that pin the retired name's absence
        - not done: the `protect-your-bacon` required-context swap (`ci-verdict` → `ci-gate`) — Ken's approval per Validation and Rollout step 6, after this branch's own run is green; until then no PR can merge. No hosted run of the new workflow. The memory note recording the 2026-09-09 ruling lives outside the repository and was not edited
        - orchestrator verification (at 02:16:00): `just test test-toolkit` 156 passed / 2 skipped; `actionlint .github/workflows/ci.yml .github/workflows/_area-ci.yml` clean; `grep ci-verdict` over the workflows, `runner_loss.py`, `devops.just`, and the watchdog finds only the two-line transition note in `ci.yml`'s gate comment; the `ci-gate` job read directly matches the ruling (six `needs`, `if: always()`, one bash step folding `needs.*.result`, `success|skipped` accepted, no permissions widening, no `continue-on-error`, no tool invocation)
- work completed for 'The standalone global policy verdict remains' at 02:16:00
- starting the work on 'Accepted-gap publication and hosted presentation verification remain incomplete' at 02:16:30
        - discovered: the plan already carries everything the check needs — `affected_scope.gap_record` puts `capability`, `governed`, `policy` (`.github/ci/environments.json`), `owner`, `reason`, `expiry`, and optional `closes` on every `accepted-gap` cell's `gap`, and `schema.py` refuses an accepted gap with no record — so the publisher is a pure projection of the plan; `_package-ci.yml` and `_wsl-ci.yml` declare no `permissions:` and use no token (`github.token`/`GH_TOKEN`/`run-id` absent outside comments), so a `permissions:` on `_area-ci.yml`'s `package-ci` is the whole confinement; the only caller of `pending_workflow_contract` was the AC9 test (GitNexus impact UNKNOWN for both symbols, confirmed by grep); `scripts/ci/pending_contracts.py` is still exercised by `test_schema.py`'s decorator self-tests and stays; `runner_loss.py` reads the jobs API, not check runs, so a synthetic check never reaches its parser; a real `--all` plan today has 36 accepted-gap cells across 6 areas (all L2 on `windows-latest`/`wsl2-ubuntu`), and 8+ areas carry none
        - changed `scripts/ci/publish_gaps.py` (new, stdlib): `--plan --area --head-sha --repo --server [--environments] [--today] [--out] [--dry-run]`; selects `cells[]` with `area == --area` and `state == "accepted-gap"`; per cell one `POST repos/<repo>/check-runs` payload — `name` `accepted gap: <area> / <package> / <gate> (<environment>)` (refused over 255 chars), `head_sha`, `status: completed`, `conclusion: neutral` (`CONCLUSION` constant; the literal `"cancelled"` appears nowhere), `details_url` = `<server>/<repo>/blob/<head>/.github/ci/environments.json#L<n>` where `n` is the capability key's line found by a block-bounded textual walk (the next `"name":` ends an environment's block; no line → bare file link; unreadable file → same), `output.title` `ACCEPTED GAP: <package> <gate> on <environment>`, `output.summary` a Markdown table (area, package, gate/tier, environment, capability, accepted by, expires) plus the reason, `output.text` with `## Policy entry` (dotted path + link + owner/expiry), `## Changing or revoking this acceptance` (edit the entry's fields; remove it or set `available: true` to revoke; absent/incomplete/expired is a blocking `POLICY GAP`), `## Coverage that closes this gap` (`closes` as a `tree/<head>/` link — the `_unscheduled` targets are directories — or an explicit "names no tracked work" sentence), `## What this check means` (presentation only, never alters the run conclusion, the rollup owns the outcome); fail closed with exit 2 on a cell whose `gap` is missing, `governed != true`, lacks owner/reason/expiry, has an unparseable expiry, or expired before `--today`/today (a refusal stops the whole area before anything is posted); exit 1 on an unreadable plan, a plan with no `cells` list, a missing `--repo`, or a failed `gh` call; exit 0 with `publish-gaps: area '<a>' carries no accepted gap; nothing to publish` when nothing is selected; posts via `gh api --method POST repos/<repo>/check-runs --input -` with the JSON on stdin (`GH_TOKEN` from the environment), one call per cell; `--dry-run` calls nothing, `--out` writes the payload list
        - changed `.github/workflows/_area-ci.yml`: new required boolean input `accepted-gaps`; new job `accepted-gaps` with NO `needs:`, no `name:` (static label under AC10), `if: inputs.accepted-gaps`, `runs-on: ubuntu-latest`, `timeout-minutes: 5`, `permissions: {contents: read, checks: write}`, steps checkout → `download-artifact@v7 name: ci-resolved-plan` → `publish_gaps.py --plan … --area "$AREA" --head-sha "$HEAD_SHA" --repo "$GITHUB_REPOSITORY" --server "$GITHUB_SERVER_URL"` with `HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}` (comment: the PR checks list is keyed on the head, not the merge commit); `package-ci` (`uses:`) gains `permissions: {contents: read}` with the cap-versus-grant comment; `rollup` untouched at `checks: read`. Deviation from the brief, stated: the plan download uses the plain in-run form rather than the rollup's `github-token`+`run-id` overlay, because that form needs `actions: read`, which the publisher deliberately does not hold; the plan is uploaded by the scope job before any area is called, so the in-run service sees it
        - changed `.github/workflows/ci.yml`: `area-ci` permissions `checks: read` → `checks: write` with the cap-not-grant comment; `scope` exposes a new `gap_areas` output, `jq -c '[.cells[] | select(.state == "accepted-gap") | .area] | unique' resolved-plan.json`, read from the PLAN on both the receipt-hit and calculated paths (a skip hint only — the publisher reselects from the same plan, so a wrong `true` publishes nothing); `area-ci` passes `accepted-gaps: ${{ contains(fromJSON(needs.scope.outputs.gap_areas), matrix.area) }}`; `preflight` and `ci-tooling` each gain `Test the accepted-gap publisher` → `python3 scripts/ci/test_publish_gaps.py` after the constraints step. Gating choice: gated by the input rather than always-run-with-no-op, because ~8 of the areas in a full plan carry no gap and an always-run job would spend a runner and add a green `accepted-gaps` entry under every one of them that published nothing; the jq stayed in the workflow rather than in `legacy_scope_document` because it is a one-expression projection with no selection logic and adding a key to the scope document would touch `SCOPE_PROJECTION_FIELDS`/the receipt contract for a hint
        - changed `just/ci-local.just` self-test loop and `scripts/ci/test_ci_local.py`'s stub-suite list: `test_publish_gaps.py` added between `test_constraints.py` and `test_runner_loss.py`
        - changed `scripts/ci/test_publish_gaps.py` (new, 19 tests): `PayloadTests` (only the area's two of three accepted cells selected, in plan order, pending cell never; every required field, `neutral`, title starts `ACCEPTED GAP`; summary carries area/package/gate/environment/owner/expiry/reason; text carries the revoke heading, `Remove the entry`, the `POLICY GAP` sentence, the dotted policy path, the `closes` tree link, and the "names no tracked work" fallback; `details_url` anchors `#L13` from a cut-down policy fixture and degrades to the bare file for an environment the fixture lacks and for no source at all; the line walk distinguishes `ubuntu-latest`'s boolean `tmux` on line 7 from Windows' object on line 13 and anchors nothing for an undeclared capability), `RefusalTests` (no record, ungoverned, missing owner, expired — with the same-day boundary still accepted — unparseable expiry, and a refusal stopping the whole area), `CommandTests` (`--dry-run --out` writes exactly `payloads()`'s list under `PATH=""`; no-gap area exits 0 with the exact note; refused cell exits 2 naming `alpha/windows-latest/L2 acceptance expired on 2020-01-01`; `{not json` and a cell-less document exit 1), `GhInvocationTests` (skipped on win32; a stub `gh` on a private `PATH` with a `sys.executable` shebang logs argv+stdin: two calls, each `api --method POST repos/yankeeinlondon/rusty-biscuit/check-runs --input -`, stdin equal to the dry-run payloads; a failing stub exits 1 naming the endpoint and the check and stops after one call; `--dry-run` never creates the log)
        - changed `tools/test-toolkit/tests/ci_workflow_contracts.rs`: `the_gap_publishing_job_holds_the_checks_write_permission_it_needs` (pending) → `the_gap_publisher_is_the_only_job_holding_checks_write` (ordinary) with new helpers `executable_lines` (comment-stripped block) and `jobs_containing(file, needle)`; asserts `ci.yml`'s only `checks: write` holder is `area-ci`, `_area-ci.yml`'s only holder is `accepted-gaps`, `_package-ci.yml`/`_wsl-ci.yml` declare no `checks:` at all, the publisher has no `needs:`, calls `python3 scripts/ci/publish_gaps.py --area`, uses `github.event.pull_request.head.sha || github.sha`, reads `ci-resolved-plan`, has no `name:`, holds no `actions:`/`pull-requests:`/`contents: write`, `rollup` stays `checks: read`, `package-ci` declares `permissions:`+`contents: read` and no `checks:`, no `_area-ci.yml` job's executable lines mention `check-runs` or `conclusion`, and the tool contains `CONCLUSION = "neutral"` and no `"cancelled"` literal; `pending_workflow_contract` deleted (no caller remained) and the section comment above it rewritten — `scripts/ci-rollup-tests.rs` keeps its own `pending_contract`, `pending_contracts.py` keeps its `test_schema.py` callers
        - non-vacuity: Python, on a copy of the module under `/tmp` — `CONCLUSION = "cancelled"` → 5 failures (`test_every_payload_carries_the_required_fields` ×2 subtests, dry-run payload equality, gh stdin equality, refusal message); `if expiry < today` → `if False` → 3 failures (expired refusal, refusal-stops-area, CLI exit 2); dropping the `area ==` filter → 4 failures + 2 errors (selection, no-gap note, dry-run payloads, gh calls; unpacking two-into-three). Rust, in place with a sha256 guard — the publisher's `checks: write` → `checks: read` fails `the_gap_publisher_is_the_only_job_holding_checks_write` at "exactly one job in _area-ci.yml may hold `checks: write`, and it is the publisher"; the workflow was restored byte-for-byte and the test passes again
        - gates: `python3 -m unittest scripts/ci/test_publish_gaps.py scripts/ci/test_ci_local.py` → `Ran 61 tests in 27.465s OK`; `python3 -m py_compile scripts/ci/*.py` clean; `actionlint .github/workflows/ci.yml .github/workflows/_area-ci.yml` clean; `just test test-toolkit` → `156 tests run: 156 passed, 2 skipped`; `just -f justfile --list >/dev/null` parses; additionally `python3 scripts/ci/test_runner_loss.py` → `Ran 31 tests OK` (the new job label `area-ci (<area>) / accepted-gaps` is neither a producer nor a non-producer to its parser); dry run of the real planner's `--all` plan for `claudine` produced 4 payloads (`claudine-cli`/`claudine-gen` × `windows-latest`/`wsl2-ubuntu`), the Windows `tmux` entry anchored to `environments.json#L38`
        - docs synced: `.github/ci/README.md` (`environments.json` section: the immediate `neutral` check per cell, what name/title/summary/text/`details_url` carry, the PR-head rule, the refusal, the `gap_areas` skip, and the `checks: write` cap/confinement); `CLAUDE.md` CI Structure bullet (one clause: published immediately as a `neutral` check run per cell by the only job holding `checks: write`); `.claude/skills/rust-devops/ci-cd.md` (new bullet under Governed policy gaps); `docs/topics/ci-cd.md` (new paragraph under Governed policy gaps); `.claude/skills/rust-testing/SKILL.md` (the Rust pending-contract bullet no longer names `ci_workflow_contracts.rs`); `.github/ci/schemas/README.md` untouched — it states only the plan's cross-field rule, not presentation
        - still owed (hosted): the throwaway-branch run on this repository with a reduced workflow — nobody in this session can push. Expected observations per B5 and `fixtures/scratch-2026-09-12.md`: (1) the four-level display of a nested area (`area-ci (claudine/rendezvous) / rendezvous-cli / test (ubuntu-latest)`) in the Actions graph and the Checks tab; (2) the labels of mixed reused, executing, gapped, failed, and unselected cells — a reused cell with no producer job yet a reported local-origin result, an executing cell, an `accepted-gaps` job that ran beside a skipped one, a failed selected area's red rollup, and an unselected area appearing nowhere; (3) the published check itself — `accepted gap: <area> / <package> / L2 (<environment>)` in the PR checks list attributed to `github-actions`, `neutral`, its title/summary rendered, `details_url` opening `environments.json` at the anchored line, the PR merge box staying at "All checks have passed", and the run conclusion unchanged (scratch Finding 3 proved the conclusion and CLEAN state on a scratch repository; the appearance under the real workflow, the in-run plan download from a job with no `actions: read`, and the `gap_areas` skip on a gap-free area are what the branch still has to show); (4) the required-context swap stays a separate approval (OQ3 step 6). `rollout-2026-09-11.md` stays `status: not-triggered`
        - orchestrator verification (at 02:38:42): `grep 'checks: write'` finds the grant on `ci.yml`'s `area-ci` (the cap) and on exactly one `_area-ci.yml` job, `accepted-gaps` (line 120), with `package-ci` and `rollup` read-only; `_area-ci.yml` has three jobs, `package-ci`, `accepted-gaps`, `rollup`
- work completed for 'Accepted-gap publication and hosted presentation verification remain incomplete' at 02:38:42
- orchestrator whole-cycle verification (at 02:38:42), every gate run again after the last subagent finished:
        - `python3 -m unittest discover -s scripts/ci -p 'test_*.py'`: first run 432 tests, 1 error — `test_affected_scope.WorkflowContractTests.test_verdict_download_unions_current_and_run_wide_artifacts` still split `ci.yml` on the retired verdict job's download steps (`IndexError`); finding 2's subagent had not run that suite. Fixed by the orchestrator: the test is now `test_area_rollup_download_unions_current_and_run_wide_artifacts` and reads the same four download steps from `_area-ci.yml`'s rollup, where they moved; rerun 432 tests OK (400 at the start of the cycle)
        - `env -u CDPATH /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` 57 passed / 0 failed; the same under `/opt/homebrew/bin/bash` 57 / 0 (53 at the start of the cycle)
        - `just test test-toolkit` 156 passed / 2 skipped (155 at the start; no `pending_workflow_contract` wrapper remains)
        - `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` 186 passed (183 at the start)
        - `actionlint` on `ci.yml`, `_area-ci.yml`, `ci-infra-retry.yml` clean; `shellcheck .githooks/pre-push` clean; `python3 -m py_compile scripts/ci/*.py` clean
        - `git status`: 35 modified files plus the two new `scripts/ci/publish_gaps.py` and `scripts/ci/test_publish_gaps.py`, in addition to the planning documents that were already dirty when the cycle began

### Successful Completion

The implementation of review cycle 5 has completed successfully in 70m 52s. During this implementation all 4 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 4 were fixed, 0 were deferred (see reasons below):

- No finding was deferred. Two findings carry a remainder that is NOT implementation and NOT this session's to perform, and each is recorded as owed rather than as done:
        - **The standalone global policy verdict remains** — the workflow half is complete (`ci-verdict` is gone, `ci-gate` is the policy-free fold, every consumer is migrated). The required-status-check context on ruleset `protect-your-bacon` (19747338) still names `ci-verdict`, and switching it to `ci-gate` is Ken's separate approval per Validation and Rollout step 6, after this branch's own run is green. Until then every pull request from this branch shows `ci-verdict — Expected` and cannot merge; `.github/ci/README.md` and `CLAUDE.md` say so.
        - **Accepted-gap publication and hosted presentation verification remain incomplete** — the publisher is complete and tested. The throwaway-branch fixture on this repository (nested-area display, mixed reused/executing/gapped/failed/unselected labels, and the published `neutral` check as GitHub renders it) needs a push this non-interactive session cannot make; `rollout-2026-09-11.md` stays `status: not-triggered`.

What each fixed finding now does:

- **Simultaneous PR head and target updates** — the hook resolves every pull request's target against the whole incoming ref-update set as well as the remote: when the push also updates the target, the run is reviewed at BOTH the current remote tip and the incoming revision (one `Comparison base` line each, either stdin order), and a pull request into a branch the push deletes is refused. The review's reproduction fails under the old hook and passes under the new one (3 new hook tests).
- **Constraints in a fresh session** — the store defaults to `<home>/.rusty-biscuit/ci-constraints/<repository identity>/` resolved with `Path.home()` (never `$HOME`), the variable stays an override, an unknown repository reads the whole store recursively (fail-closed), and only the hook and `just ci-local` resolve the default, so CI still cannot read it. Fresh-session behavior is proven for the hook, for `just ci-local --plan`, and by a real-subprocess Python test (8 new Python tests, 1 new hook test); Linux, native Windows, and WSL2 evidence is CI's.
- **The standalone global policy verdict** — `ci-gate` folds `needs.*.result` for the six blocking top-level jobs, accepting `success` and `skipped`; the reuse-validation and no-package notes moved to the advisory `summary`; the whole-run `ci-results` artifact is gone and `just ci-diff` compares the per-area slices through `ci-rollup compare` with repeated `--base`/`--head`; the watchdog and `runner_loss.py` name `ci-gate`. The two pending AC11 contracts are ordinary assertions and a behavioral test runs the extracted fold under Bash.
- **Accepted-gap publication** — `scripts/ci/publish_gaps.py` posts one `neutral` check run per accepted-gap cell from the `accepted-gaps` job in `_area-ci.yml`, which needs nothing, holds `checks: write` alone, and is skipped through the scope job's `gap_areas` output when an area has no gap; the check carries the `ACCEPTED GAP` marker, owner, expiry, reason, an anchored link to the policy entry, and the revoke/close instructions. The pending AC9 contract is an ordinary assertion (19 new Python tests).

No finding was deferred for a performance-measurement reason, so `deferred_perf_measurement` is not set.

The files this cycle touched, as reported by `git status` (the planning documents that were already dirty at `a8dbd1a61` — the two specs, `open-questions-and-blockers.md`, `review-5.md`, the two prompt templates, the audit, and the fixture record — are listed last and were not edited by this cycle except `review-5.md` and `log.md` for their metadata):

        - `.claude/skills/os/SKILL.md`
        - `.claude/skills/os/windows.md`
        - `.claude/skills/rust-devops/ci-cd.md`
        - `.claude/skills/rust-testing/SKILL.md`
        - `.claude/skills/rust-testing/nextest.md`
        - `.claudine/scripts/ci-watchdog.ts`
        - `.githooks/pre-push`
        - `.githooks/tests/test-pre-push.sh`
        - `.github/ci/README.md`
        - `.github/ci/ci-baseline.toml`
        - `.github/workflows/_area-ci.yml`
        - `.github/workflows/_package-ci.yml`
        - `.github/workflows/ci-infra-retry.yml`
        - `.github/workflows/ci.yml`
        - `CLAUDE.md`
        - `claudine/docs/rendezvous/local-ipc.md`
        - `docs/topics/ci-cd.md`
        - `fixes/2026-09-11-cicd-cleanup/log.md`
        - `fixes/2026-09-11-cicd-cleanup/review-5.md`
        - `just/ci-local.just`
        - `just/devops.just`
        - `scripts/ci-rollup-tests.rs`
        - `scripts/ci-rollup.rs`
        - `scripts/ci/affected_scope.py`
        - `scripts/ci/constraints.py`
        - `scripts/ci/publish_gaps.py` (new)
        - `scripts/ci/runner_loss.py`
        - `scripts/ci/test_affected_scope.py`
        - `scripts/ci/test_ci_local.py`
        - `scripts/ci/test_constraints.py`
        - `scripts/ci/test_publish_gaps.py` (new)
        - `scripts/ci/test_runner_loss.py`
        - `tools/test-toolkit/tests/ci_workflow_contracts.rs`
        - already dirty before the cycle: `fixes/2026-09-10-local-affected-scope/spec.md`, `fixes/2026-09-11-cicd-cleanup/spec.md`, `fixes/2026-09-11-cicd-cleanup/open-questions-and-blockers.md`, `prompts/_implement/implement-suggestions.md`, `prompts/_reviews/feature-review.md`, `fixes/2026-09-11-cicd-cleanup/absorption-audit-2026-09-12.md`, `fixes/2026-09-11-cicd-cleanup/fixtures/scratch-2026-09-12.md`

## Implementation of Review Findings #6

> **started at:** 2026-09-12T09:22:04-07:00

- this implementation is attempting to implement _all_ of the review findings found in '/Users/ken/.claudine/worktrees/rusty-biscuit/feat-unifi/fixes/2026-09-11-cicd-cleanup/review-6.md'
- this is iteration 6 of the review-to-implement cycle
- orchestration plan decided at 09:31:00 after reading the review, the spec's rulings, the absorption audit, the scratch fixture record, and the current hook, `ci.yml`, `_area-ci.yml`, `_package-ci.yml`, and `just/ci-local.just`:
        - findings are worked serially, one subagent each, in review order: 1 (baseline propagation), 2 (strict publication, D1), 3 (local reuse, W14/W1/W10), 4 (OQ1 dependent compile), 5 (verifier fallback), 6 (receipt fast path + summary), 8 (audit items and documentation)
        - finding 7 (hosted presentation fixture) and the hosted half of finding 1 need a commit and a push of a throwaway branch; this implementation prompt forbids committing, so both are expected to be deferred with their exact requirements recorded, while every L1 contract that can be proven locally is added
        - gates run only in the impacted areas: the `scripts/ci/*.py` suites, `.githooks/tests/test-pre-push.sh`, the `scripts/` Nextest bins (`ci-rollup`, `ci-plan`), `tools/test-toolkit` (`ci_workflow_contracts`), plus `shellcheck` and `actionlint`
        - host load at start: 16 logical cores, load average 6.07 (1 min); no finding in this review asks for a timing measurement
- starting the work on 'Raw producer failures bypass the area's baseline decision at the merge gate' at 09:31:30
        - discovery: every producer job (`check`, `test`, `lint`, `test-l2`, `test-browser` in `_package-ci.yml`; `wsl` in `_wsl-ci.yml`) records `result: ${{ job.status }}`; a failed gate command fails the JOB, `_area-ci.yml`'s `package-ci` (a `uses:` job) inherits it, `ci.yml`'s `area-ci` reads `failure`, and `ci-gate` blocks before the area's `rollup`/`verdict` (which already accepts an exact baseline entry) is consulted
        - discovery: `ci-rollup.rs` never needs a functional change — `state_from_status` maps a `failure` status to `FAIL` for status-only cells, `classify_state_from_evidence` downgrades a green JUnit to `FAIL` on a `failure` status, and the L1→L2/browser blame edge keys on `result` too; the defect is purely that the producer JOB concludes `failure`
        - discovery: `runner_loss.py` classifies only jobs with `conclusion == "failure"`, so a normalized producer (green job, red gate step) is invisible to it; only the module's contract needs pinning and its docstring updating
        - design decision: step-level normalization. Every gate-command step gets `id:` + `continue-on-error: true`; the `Record producer status` step folds `result` in shell as `failure` when `job.status` is `success` but any gate outcome is `failure` (else `job.status` unchanged, so setup, upload, and cancellation still fail the job, the area, and the gate), records the verbatim gate outcome as a new `gate` field, and emits a `::error title=…::` annotation so the green job still shows the failure. Rejected alternatives: job-level `continue-on-error` on `package-ci` (GitHub rejects it on a `uses:` job and it would hide setup failures); folding in `ci-rollup.rs` from a `gate` field alone (the artifact's `result` would then read `success` for a failed cell, and every other reader of `status.json` would have to learn the fold)
        - design decision: the companion-suite steps and the Zed-extension verification are gate commands too — a companion failure today fails the job exactly like a failed L1 — so they are normalized with the same fold; the contract test enumerates the exact normalized-step set so a setup or upload step can never be normalized by accident
        - actionlint baseline before edits: 6 findings (5 SC2086 in `_package-ci.yml` at :168 :259 :466 :626 :792, 1 in `_wsl-ci.yml` at :179), none on lines this finding touches
        - non-vacuity, workflow contracts (09:34): written BEFORE the workflow edits and run against the unmodified files — 5 of the 6 touched contracts fail: `a_failed_gate_command_is_normalized_so_its_area_owns_the_outcome` (no `check` gate step id), `only_gate_commands_are_normalized_never_setup_or_upload`, `every_producer_job_emits_an_explicit_status_artifact` (now demands `JOB_STATUS`), `wsl_provisioning_retries_with_a_delay_and_the_test_step_never_does` (now demands the normalized L1 step), and `the_status_fold_reports_a_failed_gate_on_a_green_job`, which executes each shipped status script under Bash and reproduces the defect itself: `check` with `job.status=success, gate=failure` wrote `"result":"success"`. `no_reusable_workflow_call_is_advisory` passes before and after (no `uses:` job carries `continue-on-error` today; it pins the constraint the design rejects)
        - the `ci-gate` needs-list contract is already exact (`ci_gate_is_the_single_required_check` asserts `needs == GATED_JOBS` in order and that `area-ci` is folded); no duplicate added
        - implemented (workflows): `_package-ci.yml` — header states the normalization contract once; `check`, `L1 tests`, `Companion suite homelab-frontend`, `Lint`, `Verify the Zed extension package`, `Companion suite homelab-frontend lint`, `L2 tests`, `Browser tests` carry `id:` + `continue-on-error: true`; every `Record producer status` step reads `JOB_STATUS: ${{ job.status }}` plus the gate outcomes, folds `result` (`failure` iff the job is `success` and a gate outcome is `failure`), prints `::error title=<package> <job> (<env>) failed::…`, and writes `"result":"$result"`; `_wsl-ci.yml` — `L1 tests from the archive` normalized the same way, folded from `steps.l1.outcome`, provisioning/staging `detail` logic untouched. Status JSON schema unchanged (no new field): only the meaning of `result` moved from "GitHub's job status" to "the cell's conclusion"
        - implemented (rollup): no functional change in `scripts/ci-rollup.rs`; `ProducerStatus.result` doc and the green-JUnit downgrade comment/message now say the STATUS reports the failure (the job no longer concludes it). Four tests appended to `scripts/ci-rollup-tests.rs`, each reading a shipped-shape `status.json` from disk through `read_producer_statuses`: a folded `check` failure is FAIL, blocks bare, blocks with a wrong-environment entry, is accepted by the exact `{package, environment, tier}` entry; the L1 shape likewise; a folded failure over a green JUnit downgrades to FAIL; exit 101/no report is MISSING and a baseline entry cannot excuse it, nor can it excuse a cell with neither report nor status
        - implemented (runner loss): `runner_loss.py` docstring records that a normalized producer is neither lost nor a veto; `test_a_normalized_gate_failure_is_neither_lost_nor_a_failure` pins it and that a runner lost DURING the normalized gate step is still attributed to `L1 tests` with the right cell. Consequence stated in the docstring: a lost runner in area A beside an UNACCEPTED failure in area B now reruns A's lost job (B's green producer is never re-executed); previously B's red job vetoed the retry
        - implemented (comments/docs): `ci.yml` `ci-gate` block, `_area-ci.yml` `package-ci` and `rollup` comments, `_package-ci.yml` trailing comment, `.github/ci/README.md` (`ci-gate` section and the `result` paragraph of the artifact contract), `.claude/skills/rust-devops/ci-cd.md` (one bullet under the presentation rules)
        - non-vacuity, rollup tests (09:39): with `state_from_status`'s `failure` arm mutated to `Pass` and the downgrade's `== "failure"` mutated to `== "never"`, `a_failed_check_command_on_a_green_job_is_judged_by_the_baseline_alone` and `a_folded_failure_status_downgrades_a_green_report_without_a_detail` fail; both mutations restored (file byte-identical). `a_failed_l1_command_on_a_green_job…` and `a_normalized_producer_with_no_report…` cannot fail against the previous rollup by construction: their FAIL/MISSING states come from the JUnit record, not the status — they pin the contract the workflow now depends on (an exact entry accepts a folded L1 failure; a missing report is never excused), and would catch a rollup change that made the baseline wider or MISSING baselinable
        - non-vacuity, runner loss (09:39): with the `conclusion != "failure"` guard relaxed to also consider a green job with a failed step, the new test fails (`other_failures == ['area-ci (messenger) / messenger / test (windows-latest)']`); restored byte-identical
        - verification: `cargo nextest run -p test-toolkit --test ci_workflow_contracts` 89/89 passed (was 84 + 4 new + 1 cfg(unix) fold execution; 0 failed); `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` 190/190 passed (186 + 4 new); `python3 scripts/ci/test_runner_loss.py` 32/32 OK (31 + 1); `cargo clippy … --bin ci-rollup --all-targets -- -D warnings` clean; actionlint on the four workflows: 6 findings before, 6 after — the same 5 SC2086 in `_package-ci.yml` (now at :182 :283 :500 :673 :849) and 1 in `_wsl-ci.yml` :179, none on edited lines; shellcheck runs inside actionlint on every `run:` block and reports nothing new
        - deferred: the hosted regression (baselined producer failure, the same failure unaccepted, a missing report, an equivalent reused failure) needs a commit and a push of a throwaway branch, which this session is forbidden to do. Its exact runbook — reduction to one package area, the four commits in order, the per-case job conclusions, annotations, artifacts, rollup findings, `ci-gate` conclusion, and merge-box reading — is appended to `rollout-2026-09-11.md` under "Hosted regression owed for the producer-normalization change". Case 4 additionally depends on review-6 finding 2 (strict-failure note publication) having landed
- work completed for 'Raw producer failures bypass the area's baseline decision at the merge gate' at 09:40:44
- starting the work on 'Complete strict failures still withhold remote validation evidence' at 09:41:10
- discovery: the withholding branch is `.githooks/pre-push` lines 647-650 (`if [ "$EXIT_CODE" -ne 0 ] && [ "$MODE" = "strict" ]` before `push_note`); the hook is `#!/bin/sh`, so the fix stays POSIX. The only source-grep pin of it is `test_a_blocked_strict_push_records_locally_but_publishes_nothing` (`test-pre-push.sh:741`); nothing under `scripts/ci/` reads the phrase.
- discovery: the docs already say strict and warn both publish (`.github/ci/README.md:157` says "both record one", `.claude/skills/rust-devops/ci-cd.md:105` and `docs/topics/ci-cd.md:78` say "both publish"); `.claude/skills/os/SKILL.md` and `CLAUDE.md` make no claim either way, so no doc edit is needed for D1.
- decision: the T2 fixture pushes the branch with `--no-verify` AFTER the blocked hook run (the D1 scenario), then verifies from a FRESH clone of the bare remote that fetched only `refs/notes/ci-local/*` — a note whose commit is absent from the remote cannot be verified by anyone, so the later push is part of what the test proves.
- discovery: a reused cell's shape is fixed by `affected_scope.py::mark_reused` — `execution: reuse`, `origin: local`, `state: reused`, `evidence.outcome: fail`; the T2 assertions use that shape (not the "execution omitted / state failed" wording in the task brief).
- hook: removed the strict-withholding branch in `.githooks/pre-push` (the `if [ "$EXIT_CODE" -ne 0 ] && [ "$MODE" = "strict" ]` arm before `push_note`); strict and warn now both `push_note` after `record-cells`, and the publication comment states why (D1: the failure blocks the branch, not the note, so the following `--no-verify` push lets CI reuse it). The "not published while the push is blocked" wording no longer exists in the hook. `shellcheck .githooks/pre-push` is clean; the hook stays `#!/bin/sh`.
- tests: removed `test_a_blocked_strict_push_records_locally_but_publishes_nothing` (a source grep for the deleted arm) from `.githooks/tests/test-pre-push.sh`; added `test_a_blocked_strict_failure_publishes_its_note_and_ci_reuses_the_failure` (T2) and `test_a_complete_warn_failure_publishes_its_note` (T1), with helpers `make_failing_staging_pre_push`, `run_failing_publishing_hook`, `remote_macos_receipt`, `assert_remote_receipt_is_a_complete_failure`
        - T2 drives a real fixture run under `strict`: a fake `just pre-push` stages an `alpha` L1 JUnit report with one `<failure>` and a manifest record with `exit_code: 1`, then exits 1. It asserts hook exit 1 and "Push blocked by strict mode"; that the branch did NOT reach the bare remote while `refs/notes/ci-local/macos-latest` did (note read from the remote's notes tree via `cat-file -p <ref>:<head>`, since the blocked commit is not there to `notes show` against) with `outcome: fail`, `completion: complete`; then pushes the branch `--no-verify`, clones the bare remote fresh, fetches `refs/notes/ci-local/*`, runs the committed `local_evidence.py verify --cells` there (accepted set: one cell, `outcome: fail`, `completion: complete`, `origin: local`), and overlays it with the REAL `affected_scope.py --apply-to --accepted-cells --resolved-plan` (cell `execution: reuse`, `origin: local`, `state: reused`, `evidence.outcome: fail`; no cell left executing)
        - T1 is the same fixture under `warn`: exit 0, "Pre-push validation failed" still printed, "Published 1 macos-latest cell(s)", and the same complete-failure receipt on the remote
- non-vacuity: the full suite run with `PRE_PUSH_HOOK_UNDER_TEST=/tmp/pre-push.original` (a byte copy of the hook before this edit) fails exactly T2 — 57 passed, 1 failed, on the "Published 1 macos-latest cell(s)" assertion, with the original's "Recorded 1 macos-latest cell(s) ... not published while the push is blocked" line in the captured stdout. T1 passes on the original too, as expected: the withholding arm only fired under strict, so T1 pins that warn publication is not lost by the edit
- verification: `env -u CDPATH GIT_TERMINAL_PROMPT=0 /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` against the edited hook: 58 passed, 0 failed (57 before: one grep test removed, two behavioral tests added); `shellcheck .githooks/pre-push` clean. Nothing under `scripts/ci/` was touched, so the Python suite was not rerun
- docs: no edit needed — `.claude/skills/rust-devops/ci-cd.md:105` and `docs/topics/ci-cd.md:78` already say strict and warn both publish; `.github/ci/README.md:157` says both "record one" (not wrong, left as is); `.claude/skills/os/SKILL.md` and `CLAUDE.md` make no claim on the point
- work completed for 'Complete strict failures still withhold remote validation evidence' at 09:47:35
- starting the work on 'Local validation replans and reruns cells already covered by accepted evidence' at 09:48:21
- discovery: the defect has two halves in code. (1) `just pre-push` → `just ci-local --l2` replans from the working tree against `git merge-base origin/main HEAD` (`just/ci-local.just:149-169`) and its evidence overlay is confined to `show_plan == 1` (`:178`), so the accepted set the hook already computed for HEAD's first trigger context (`PLAN_REVIEW_FILE`, `.githooks/pre-push:533-537`) never reaches the gates. (2) The publication block records against `$PLAN_FILE` (ci-local's replan) and `git merge-base origin/main "$HEAD_SHA"` (`:605`), so the receipt's `scope_identity` is `identity(canonical(replan))`, not the scope receipt's `plan`, and a stacked PR gets main's merge-base instead of the parent tip
- discovery: `record_cells` (`local_evidence.py:706`) records ONLY `staged_records(stage)` — the manifest records the tier recipes appended — never plan cells, so a cell the local run skipped is simply absent from the receipt (not `partial`/`interrupted`/missing); no filtering is needed, but a run that staged nothing raises `ValueError("the run staged no L1/L2/browser report")`, which today would surface as an uncaught traceback on stderr: the hook must name that case itself
- discovery: a reused cell's shape is `execution: reuse, state: reused, evidence.outcome: pass|fail` (`mark_reused`), and `verify_cells` accepts complete failures too, so the D2 assumption (rerun a cell whose newest prior evidence is a failure) is decidable from the fed plan alone: `execution == "reuse" and evidence.outcome == "fail"`
- discovery: equivalence needs `packages[].input_paths`; the shared fixture `plan-macos-executing.json` declares `null`, so a docs-only follow-up cannot reuse it (`gate-inputs-changed: ... declares no build closure`). The new hook fixtures need a plan that declares closures — and a package that declares none is exactly the natural "still executes on a docs-only commit" case, which gives commit B a receipt with a cell list to assert
- design decision: interface is `just ci-local --plan-in <path>` with `BISCUIT_CI_PLAN_IN` as the env form (the same shape as `BISCUIT_CI_SCOPE_OUT`/`BISCUIT_CI_PLAN_OUT`, which is how the hook already hands paths to the recipe through `just pre-push`). A fed plan skips the merge-base/diff/planner block entirely; the legacy scope shape comes from the existing `affected_scope.py --apply-to <plan>` entry (no accepted cells → pure projection, reads nothing from the checkout), so no second projection exists. Per-cell execution for this host's environment is read from the plan's `cells`: L1/L2 run iff `execution == "execute"` or `execution == "reuse" and evidence.outcome == "fail"`; `lint` runs as today (never recordable, hosted on ubuntu-latest, so a per-environment filter would never run it locally)
- design decision: the hook feeds the OVERLAID reviewed plan of HEAD's first context (what `record_scope_evidence` used) and records the receipt against the RAW plan of that context (`SCOPE_PLAN_FILE`, the `plan` the scope receipt carries) with `--base` = that context's base, so `scope_identity == identity(canonical(scope_receipt.plan))` (W1) and the base is the PR target tip when that is what CI compares with (W10). Feeding happens only when the checkout is clean and no `RUSTY_BISCUIT_PRE_PUSH_AREAS` override is set; otherwise the replan path stays for wider working-tree feedback and nothing is published
- implemented (recipe): `just/ci-local.just` gains `--plan-in <path>` / `BISCUIT_CI_PLAN_IN`. A fed plan replaces the merge-base/diff/planner block: it is copied to `plan_file` (so `--plan-out` is the fed plan byte-for-byte, rejections included), the legacy scope shape is `affected_scope.py --apply-to <plan>` with no accepted cells (the existing projection entry; reads nothing from the checkout), `--all`/selectors are refused alongside it, the evidence overlay stays skipped (`merge_base` is empty), and the host environment is detected for a fed plan as for `--l2`. The gate loop asks `cell_runs_here <pkg> L1|L2`, true without a plan and otherwise iff the plan's cell for this environment has `execution == "execute"` or `execution == "reuse" and evidence.outcome == "fail"` (D2 rerun); lint runs as before. Each cell is listed as `run`/`rerun`/`skip` with its reason before the gates start. Header comment rewritten (the "hook does NOT call this" and "no reason to consume evidence" sentences were both wrong)
- implemented (hook): `record_scope_evidence` also keeps HEAD's first-context plan once its base passed the ancestor checks — the raw `SCOPE_PLAN_FILE` as `HEAD_PLAN_FILE` (the receipt's plan; it is the scope receipt's `plan`) and the overlaid `PLAN_REVIEW_FILE` as `HEAD_REVIEW_FILE` (the run's plan) — and sets `HEAD_PLAN_BASE`. Before `just pre-push`: no override + clean checkout + `HEAD_PLAN_BASE` set → `export BISCUIT_CI_PLAN_IN="$HEAD_REVIEW_FILE"` and an announcement; each other case announces that it replans from the working tree and publishes nothing. The publication block is now a chain of guards that each set `WITHHELD` with a reason (`No validation receipt for <head>: …` on stderr) — HEAD not pushed, override, dirty tree, no reviewed ancestor base, no plan written, sniff/jq missing, an unmapped OS, no staged manifest (every recordable cell reused, or none planned here), or `record-cells` refusing — and `record-cells` runs with `--plan "$HEAD_PLAN_FILE" --base "$HEAD_PLAN_BASE"`; the literal `git merge-base origin/main` is gone. `record-cells` needs no change: it records staged manifest records only, so a reused cell is absent rather than incomplete
- discovery (fixed in passing, same mechanism): the hook exports `BISCUIT_CI_PLAN_OUT`, and `ci-local`'s self-test loop runs `test_ci_local.py`, whose `CiLocalTests` did not scrub that variable — the nested `--all` run overwrote the hook's `$PLAN_FILE` with a test fixture. The receipt is now recorded from the hook-owned `HEAD_PLAN_FILE` copy, so the witness file can no longer be the recorded plan; and `CiLocalTests`/`PlanSurfaceTests` now also scrub `BISCUIT_CI_PLAN_OUT`/`BISCUIT_CI_PLAN_IN`, since a fed plan would otherwise make the nested `--all` run exit 2 inside every real push
- tests (real recipe): `PlanFedRunTests` in `scripts/ci/test_ci_local.py` — `test_a_reused_passing_cell_is_skipped_and_the_planner_never_selects` (gate calls are exactly `_lint alpha`, `_lint beta`, `_test beta …`; a trampoline planner on PATH logs every call, refuses selection, and answers `--apply-to` with the real module, and the log holds exactly that one `--apply-to` call), `test_a_reused_failing_cell_is_rerun` (`_test alpha` runs too; `rerun  alpha/L1` printed), `test_the_environment_form_feeds_the_same_plan_and_writes_it_back_unchanged` (`BISCUIT_CI_PLAN_IN` form; `--plan-out` equals the fed plan)
- tests (real hook + real recipe): `.githooks/tests/test-pre-push.sh` — harness `stage_ci_local_harness` (a justfile importing the repository's `ci-local.just`, real `constraints.py`, empty self-test stubs, a `ci-local`-prefixed trampoline planner that refuses selection) and `make_ci_local_just` (fake `just` whose `pre-push` execs the real `just` on that harness and whose `_test <pkg>` stages a passing report, or a complete failure while `gate-fail-<pkg>` exists); new fixture `fixtures/plan-macos-two-packages.json` (alpha declares `input_paths: ["pkg/alpha"]`, beta declares none, both with lint + macOS L1). Tests: `test_a_docs_only_follow_up_skips_the_cells_prior_evidence_covers` (A: both run, receipt `[alpha/L1, beta/L1]`; B docs-only: no `_test alpha`, `_test beta` and `_lint alpha` run, `skip   alpha/L1` printed, one `ci-local --apply-to` planner call, receipt `[beta/L1]` against main's tip), `test_a_follow_up_changing_a_packages_inputs_reruns_its_cell` (B edits `pkg/alpha/src/lib.rs`: `_test alpha` reruns, `gate-inputs-changed: alpha/macos-latest/L1` named, receipt both cells), `test_a_cell_whose_prior_evidence_is_a_failure_is_rerun_and_superseded` (A under warn with a complete alpha failure; B docs-only reruns alpha with `rerun  alpha/L1`, and `verify --cells` against B's scope-receipt plan resolves alpha to B's pass with `evidence.commit == B`), `test_a_stacked_pull_request_records_its_target_tip_as_the_receipt_base` (fake `gh` PR #12 into `parent`: receipt `base == PARENT_SHA` ≠ `merge-base origin/main child`, and `scope_identity == schema.identity(schema.canonical(scope_receipt.plan))`). `test_publication_requires_a_clean_exact_tree_and_no_override` updated to the restructured guard spellings (plus the new `-z "$HEAD_PLAN_BASE"` clause). `CI_LOCAL_RECIPE_UNDER_TEST=<path>` added beside `PRE_PUSH_HOOK_UNDER_TEST` so the recipe half can be proven the same way
- non-vacuity (recipe): `PlanFedRunTests` run with `RECIPE` pointed at a byte copy of the pre-change recipe — 3 of 3 fail: the two flag-form tests with `ci-local: unknown flag --plan-in`, the env-form test with `ci-local: cannot resolve base ref 'origin/main'` (the original ignores `BISCUIT_CI_PLAN_IN` and replans)
- non-vacuity (hook): the suite under `PRE_PUSH_HOOK_UNDER_TEST=/tmp/pre-push.f3.original` (byte copy of the pre-change hook) — 57 passed, 5 failed: the four new tests (each `expected exit 0, got 2` with `ci-local: cannot resolve base ref 'origin/main'`, i.e. the original hook fed no plan and the real recipe replanned) and the guard-spelling test. Under `CI_LOCAL_RECIPE_UNDER_TEST=/tmp/ci-local.f3.original.just` (new hook, pre-change recipe) — 58 passed, 4 failed: the four new tests, same replan message (the original recipe ignores the fed plan). Both copies were taken before any edit and are outside the repository
- verification: `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` → Ran 436 tests, OK (432 before this cycle + finding 1's addition + 3 here); `env -u CDPATH GIT_TERMINAL_PROMPT=0 /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null` → Passed: 62, Failed: 0 (58 after finding 2 + 4); `shellcheck .githooks/pre-push` clean (the hook stays `#!/bin/sh`); `just --list` parses the root justfile with the edited import; a manual two-commit run through the fixture prints `skip   alpha/L1  (reused: prior-local pass on <A>)` on the follow-up and publishes `1 macos-latest cell(s)` for it
- docs: `.github/ci/README.md` (validation-receipt paragraph: fed plan, reviewed base, scope receipt's plan identity, cells that ran; withheld receipts are announced), `.claude/skills/rust-devops/ci-cd.md` (the "receipt's `base` is the branch's merge base" paragraph, plus the local-gates reuse paragraph), `docs/topics/ci-cd.md` (Layer 1 sentence and the receipt's fields). Left alone as outside this finding: the root `justfile`'s `pre-push` doc comment still says the hook "compile-checks direct reverse dependencies", which was retired earlier
- edge noted, not changed: a cell reused from a FAILURE is rerun locally even if its environment is the one a constraint prohibits — `mark_reused` drops the prohibition, and the constraint protects the hosted trigger, not the developer's own machine; the rerun is the D2 behavior
- no Rust symbol was edited, so no GitNexus impact run was warranted; nothing was committed
- work completed for 'Local validation replans and reruns cells already covered by accepted evidence' at 10:15:39
- starting the work on 'The approved dependent compile step is absent' at 10:16:29
- discovery: the plan contract already reserves an OPTIONAL package field for exactly this — `dependent_seam` (`schema.py:225`, `.github/ci/schemas/contract.json:47`, `schemas/README.md:74` "describing downstream packages compiled inside this package's own check"); it is emitted nowhere today. Cells have no such slot, so the cell side needs one new optional field, which means `contract.json` is regenerated (`python3 scripts/ci/schema.py`; `test_schema.py` pins byte-stability). No schema version bump: both fields are optional and `validate_resolved_plan` rejects only UNKNOWN keys
- discovery: `check_os` in `matrix_record` and `gates` on the package record are both derived from the owned cells (`"check" in gates and runs(env, "check")`), so giving a no-example/no-bench package one ubuntu check cell makes `check` flow into `gates` and `check_os == ["ubuntu-latest"]` with no further projection change; the check job's `if: contains(fromJSON(inputs.gates), 'check')` then fires on its own
- discovery: the workflow-contract fold test (`the_status_fold_reports_a_failed_gate_on_a_green_job`) resolves every `${{ inputs.* }}` it does not know to the string `success`, so a new `inputs.dependents` JSON-array env var needs its own arm there (`[]`) or the real status script would fail on `fromjson`
- discovery: `direct_dependents` includes `gates = false` members (biscuit-visualized is one — a governed "CI launches nothing for this package" exclusion); compiling such a package as a dependent would make every change to its dependencies red for a package CI is told not to build. Decision: exclude non-gating members from the dependent list, with the reason at the exclusion
- design decision (planner): the reserved optional package field `dependent_seam` is what carries the ruling — `{"dependents": [sorted names], "check_args": "-p b -p c --lib --bins --tests"}`, present only when at least one dependent is attributed — and the `ubuntu-latest` check cell carries the same names as optional `dependents` (new `CELL_FIELDS` entry; `contract.json` regenerated, schema version unchanged). Own targets and the seam stay separate: `check_args` is untouched, the matrix projection adds flat `dependents` / `dependents_check_args`. `DEPENDENTS_ENVIRONMENT = "ubuntu-latest"` and `DEPENDENT_SELECTORS = {lib: --lib, bin: --bins, test: --tests}` sit beside `CHECK_SELECTORS` with the why: the seam is the changed package's public API, consumed by a dependent's lib/bin/test targets (what its own L1 would compile); its example/bench kinds are deliberately not selected; selectors come from the dependents' DECLARED kinds (union, each present only when some dependent declares it), never `--all-targets`; no feature flags (default features; declared-feature coverage is the dependent's own L1 when it is selected). One `cargo check` for all dependents, since a target filter with several `-p` compiles whatever matches
- design decision (planner): attribution walks `reverse_dependency_map` (extracted from `direct_dependents`, which now calls it) per source package: workspace members only, not in the selected set (so a source-changed dependent is excluded and `--all` attributes nothing), and gating. `reverse_dependencies` reporting is untouched (still every unselected direct dependent, gating or not). `package_cells(..., dependents=)` gives a package with no uncovered kind ONE `ubuntu-latest` check cell (`target_kinds: []`, `compile_coverage_from: check`, `reusable: false`, reason naming the count and names); with uncovered kinds every native cell exists as before and only the ubuntu one carries `dependents`. `gates` and `check_os` are derived from the owned cells, so `check` reaches the matrix and `check_os == ["ubuntu-latest"]` with no other projection change
- schema: `validate_resolved_plan` now also rejects a `dependent_seam` naming a package that holds a record (the AC1 rule, applied to the compiled seam), a malformed seam (object with non-empty `dependents` and non-empty `check_args`), and `dependents` on a non-check cell
- existing tests corrected (their premise changed): `test_source_change_selects_the_source_package_alone` and `test_non_source_input_does_not_widen_a_source_change` (alpha-core now owns a `check` gate for beta-app's seam — the second now also asserts equality with the unwidened scope, which is what "does not widen" means); `test_an_area_whose_every_cell_is_reused_still_owns_a_result_slice` moved its fixture from `playa/lib` to `playa/cli` (a leaf: the playa library now owns a never-reusable check cell for four dependents, so "every cell reused" is unreachable for it)
- tests added (planner, hermetic): `test_affected_scope.DependentSeamTests` (7 tests on synthetic metadata: A with dependents B, C where C also depends on D; gates=false E excluded from the seam but still reported; no-uncovered → one Linux check cell; bench kinds → three cells, seam on Linux only; selected dependent excluded; `--all` empty; matrix fields; selectors by declared kinds; schema rejection) and `test_affected_scope.DependentSeamFixtureTests` (a REAL two-crate Cargo workspace in a temp dir — `alpha` exports `exported()`, `beta` calls `alpha::not_exported()` — fed to the real `calculate_scope` via `load_metadata(root)`; asserts beta is alpha's dependent, no beta area/record/cell, then runs the generated commands with `CARGO_TARGET_DIR` inside the temp dir and `--offline`: `cargo check -p alpha` exits 0, `cargo check -p beta --bins` exits non-zero naming beta; skips only when `cargo` is absent); `test_resolved_plan.DependentSeamTests` (3 tests on the real workspace: darkmatter's seam holds dmls and darkmatter-cli, none scheduled, `-p` per dependent, Linux-only carriage, matrix projection, full scope empty)
- non-vacuity (planner, 10:31): with the attribution line mutated to `seam = None` (the pre-ruling behavior: nothing attributed), the 12 new tests give 3 failures + 6 errors; the 3 that still pass are the two `--all`-is-empty pins and the direct unit test of `dependent_seam` (the mutant did not remove the function). Restored byte-identical (`cmp` clean). The broken consumer's compile genuinely fails: first error line `error[E0425]: cannot find function `not_exported` in crate `alpha``, then `error: could not compile `beta` (bin "beta") due to 1 previous error`
- implemented (workflows): `_package-ci.yml` declares `dependents-check-args` (string, required) and `dependents` (JSON array string, required); the `check` job gains ONE step `Compile unchanged dependents` (`id: dependents`, `continue-on-error: true`, `if: matrix.os == 'ubuntu-latest' && inputs.dependents-check-args != ''`, `run: cargo check ${{ inputs.dependents-check-args }}`) after the own-kinds step, whose command is unchanged (for a package with no uncovered kind `check_args` is `-p <pkg>`, and its lib is compiled for the dependents anyway). `Record producer status` reads `steps.dependents.outcome` and `inputs.dependents`, folds either failed half into `result=failure` with a `detail` naming which half (own kinds / dependents / both), annotates with `::error`, and writes the JSON with `jq` (dependent names in a JSON array; `detail` present only when non-empty). `_area-ci.yml` forwards `dependents-check-args: matrix.dependents_check_args` and `dependents: toJSON(matrix.dependents)`. The check job's `if:` already fires from `gates`, which the planner derives from the owned cells. Stale `check-os` description and check-job header comment corrected in the same file
- implemented (rollup): `ProducerStatus.dependents: Vec<String>` (serde default) and `Cell.dependents` (skipped when empty); `status_cells` copies the names onto the cell in both the planned-gate and unplanned branches and appends `dependents_note` ("also compiled N dependent(s): a, b") to the reasons whatever the state, so a blocking check cell's "why" carries the names beside the producer's which-half `detail`; `render_grid` adds a "### Compiled dependents" table (`cell | state | also compiled`) after the accepted-gap table. JUnit-backed cells never carry dependents. GitNexus impact before editing: `status_cells` MEDIUM (8 direct callers, all in `scripts/`), `state_from_status` LOW, `ProducerStatus` LOW — no HIGH/CRITICAL
- tests added (workflow contracts): `unchanged_dependents_are_compiled_inside_the_changed_packages_linux_check` (inputs declared, `_area-ci.yml` forwards both matrix fields, the `dependents` step exists with the exact run line and no `--all-targets`, its condition skips on an empty argument string, the planner's `DEPENDENTS_ENVIRONMENT` literal read from `affected_scope.py` equals `ubuntu-latest` and appears in the step condition as `matrix.os == '…'`, the status step carries `inputs.dependents` and writes `dependents`/`detail`); `GATE_STEPS` pinned set extended with `("check:", "dependents", "Compile unchanged dependents")` so the normalization, exact-normalized-set, and real-bash fold tests all cover it, `PRODUCERS` in the fold test extended with the dependents outcome as the varying gate, and the fold's env resolver maps `inputs.dependents` to `[]`; the matrix-forwarding test lists `dependents_check_args` and `dependents`
- tests added (rollup): `a_check_cell_reports_the_dependents_it_also_compiled` (a real `status.json` with `dependents` read through `read_producer_statuses`; PASS cell keyed on the changed package, names on the cell and in reasons, "### Compiled dependents" row rendered, no block) and `a_failed_dependents_half_is_a_fail_cell_on_the_changed_packages_area` (`result: failure` + the which-half `detail`: FAIL cell on `darkmatter/ubuntu-latest/check` in area `darkmatter`, no consumer cell, `cell-failed` block whose subject is the changed package and whose detail names `dmls`, FAIL row in the table); helper `write_status`
- non-vacuity (rollup, 10:36): with `dependents_note` short-circuited to `None` and the unplanned branch's `dependents` copy replaced by `Vec::new()`, both new tests fail; restored byte-identical (`cmp` clean). Non-vacuity (contracts): with the step condition spelled `macos-latest`, the new test fails on "the step condition must name the planner's dependents environment"; with the step's `continue-on-error` removed, `a_failed_gate_command_is_normalized…` and `only_gate_commands_are_normalized…` both fail; restored byte-identical
- docs corrected (only the sentences now wrong): `just/ci-local.just` header (check cells also exist on ubuntu for dependents; local runs still execute no check cell), root `justfile` `pre-push` doc comment (no longer claims to compile-check reverse dependencies), `.github/ci/README.md` (AC1 paragraph, `gates` derivation, "Compile-check" section), `.github/ci/ci-baseline.toml` header, `.github/ci/schemas/README.md` (`reverse_dependencies` bullet), `.claude/skills/rust-devops/ci-cd.md` (two bullets; the "OQ1 is unruled" sentence), `docs/topics/ci-cd.md` (two paragraphs), `CLAUDE.md` CI Structure bullet ("a check cell exists only where example or bench targets are declared" was now false). The stale planner comment that called OQ1 open now states the ruling
- verification (10:37): `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` → Ran 448 tests, OK (436 + 12: 7 `DependentSeamTests` + 1 `DependentSeamFixtureTests` in `test_affected_scope.py`, 3 `DependentSeamTests` in `test_resolved_plan.py`, plus `test_schema`'s contract check re-passing on the regenerated `contract.json` — 12 new test functions); `cargo nextest run -p test-toolkit --test ci_workflow_contracts` → 90 passed (89 + 1); `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup` → 192 passed (190 + 2); `cargo clippy … --bin ci-rollup --all-targets -- -D warnings` exit 0, zero warnings; `actionlint` on ci.yml/_area-ci.yml/_package-ci.yml/_wsl-ci.yml → 6 findings, the same 5 SC2086 in `_package-ci.yml` (now :190 :323 :540 :713 :889) and 1 in `_wsl-ci.yml` :179, none on edited lines
- deferred / out of scope by the ruling: hub-crate duration (darkmatter attributes 10 dependents, playa 4) is measured in the first real hosted run, not here; no `[package.metadata.ci]` declared-dependent list; no local execution of the dependents half (cost decision); `.github/ci/README.md:117`'s "consumer checks" wording is review-6 finding 7's. Nothing was committed
- work completed for 'The approved dependent compile step is absent' at 10:38:15
- starting the work on 'Validation-verifier failure aborts scope instead of retaining normal execution' at 10:38:50
- discovery: the defect is exactly as probed — under `set -euo pipefail` the `verify --cells` call (`ci.yml` scope step) and the `--apply-to` overlay were bare statements, so a verifier exit N ended the step with N; re-running the reviewer's probe through the harness against a byte copy of the original workflow gives `exit 42 | outputs written: [] | summary bytes: 0`. The overlay had a second hazard: `--apply-to resolved-plan.json --plan-out resolved-plan.json` writes the plan IN PLACE with a plain `write_text` (`affected_scope.py:2458`), so a crash mid-write would hand the fan-out a half-written plan, not merely no overlay
- design decision (workflow): the verifier and the overlay are each captured with `cmd || status=$?` (the `||` list is what keeps errexit from firing; `$?` there is the real exit code, which `if ! cmd` would have negated). Three failure classes set one `evidence_failure` string: `verifier failed (exit N)`, `verifier output is not a JSON list` (`jq -e 'type == "array"'` on the captured stdout, so an exit-0 verifier printing garbage or nothing is a failure, not evidence), and `overlay failed (exit N)`. The overlay now writes `--plan-out applied-plan.json` and the step `mv`s it over `resolved-plan.json` only on exit 0, so `resolved-plan.json` is the receipt's plan or this run's calculation, untouched, until the overlay has succeeded. On any failure: `accepted-cells.json` is rewritten to `[]` (nothing downstream may read a partial accepted set; finding 6's rejection overlay sees an empty list, not a crash), `applied-plan.json` is removed, one `::warning title=CI scope::local evidence <failure>; the plan runs unmodified and no cell is reused` annotation is printed, and the "CI scope" summary table gets a row `| evidence processing | <failure>; no cell reused |` directly under `scope source` (present only on failure, so finding 6's evidence rows have no duplicate to reconcile). Step comments describe the fallback
- design decision (no new rejection code): `evidence_rejections` only ever reaches the plan through the planner's `--apply-to` (`affected_scope.py`), and the finding's own contract is that the written plan equals the pre-overlay plan byte for byte. Injecting a coded entry would mean either a second overlay call (the very tool that just failed, in the overlay case) or editing the plan with `jq` outside the schema-validated writer, and `schema.REJECTIONS` says adding a code is a contract change (`contract.json` regenerates). Chosen: summary row plus `::warning::`, no plan mutation, no schema change. The hook's constraint enforcement is untouched
- tests added (`WorkflowScopeStepTests`, `scripts/ci/test_ci_local.py`): `test_a_crashing_verifier_retains_the_plan_and_reuses_nothing` (verifier exits 42 with a validation receipt that WOULD have been accepted: step exits 0, every matrix output present, plan bytes equal the scope receipt's plan, `accepted_evidence == []`, no `execution == "reuse"`, projection re-derived from that plan, zero planner calls, summary row and warning name the verifier and 42), `test_a_verifier_printing_garbage_is_a_failure_not_evidence` (exit 0 with `not json {` on stdout: same retention, row names the JSON-list check), `test_a_crashing_overlay_retains_the_plan_and_reuses_nothing` (real verifier accepts the macOS L1 cell; the planner's `--apply-to` clobbers its `--plan-out` target with `{"corrupt": true}` and exits 7: exactly one `--apply-to` planner call, plan bytes still the receipt's — which proves the `applied-plan.json` indirection is load-bearing — nothing reused, row and warning name the overlay and 7). Harness: `run_step` gained `verifier_failure` (digit string = exit code, `"garbage"` = non-JSON stdout) and `overlay_failure`; the faults live in the test-generated trampolines (`TEST_VERIFIER_FAILURE` fires only on `verify`, so `scope-verify`/`record-cells`/`scope-record` stay real; `TEST_OVERLAY_FAILURE` fires only when `--apply-to` is present), no production code was touched for testability. `CI_WORKFLOW_UNDER_TEST=<path>` added beside the hook suite's `PRE_PUSH_HOOK_UNDER_TEST` so the step suite can be pointed at another copy of the workflow. The existing happy-path tests (`test_evidence_on_a_scope_hit_is_applied_to_the_carried_plan_without_selection`, `…_scope_miss_is_applied_after_one_selection_run`, `…_projection_on_an_evidence_hit…`) still assert `["reuse"]`, so the guard silenced no real overlay
- non-vacuity: the three new tests run with `CI_WORKFLOW_UNDER_TEST=/tmp/ci.f5.original.yml` (a byte copy taken before any edit, outside the repository) — 3 of 3 fail, each on the step's exit code: `0 != 42`, `0 != 1` (`jq: parse error: Invalid numeric literal` from the unguarded `jq -r length` on the garbage output, then the harness's `JSONDecodeError` on the missing plan), `0 != 7`; the probe above shows the original writes no matrix output and an empty summary in the same run
- verification: `python3 scripts/ci/test_ci_local.py` → Ran 48 tests, OK (45 + 3); `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` → Ran 451 tests, OK (448 + 3); `actionlint .github/workflows/ci.yml` → exit 0, no findings (0 before, 0 after). The step stays strict-mode-safe: every guarded command sits in an `||` list, `(( ))` only inside `if`, and `evidence_failure` is initialized before the `workflow_dispatch` branch so `set -u` cannot trip on the summary
- left to finding 6 by instruction: `rustup show` before the receipt check, the summary's matching-environment/reused-cell rows, and overlaying `evidence_rejections` when the accepted list is empty (the fallback only guarantees that list is `[]` and readable). No Rust symbol was edited, so no GitNexus impact run was warranted; nothing was committed
- work completed for 'Validation-verifier failure aborts scope instead of retaining normal execution' at 10:48:22
- starting the work on 'The receipt fast path still initializes Rust and omits required diagnostics' at 10:48:46
- discovery: `rustup show` is its own step (`Set up the pinned Rust toolchain`, `ci.yml:88-89`) ahead of `Calculate package and area scope`, so it runs on every scope job, hit or miss. The only Rust consumer on the job is the planner's SELECTION (`cargo metadata` inside `affected_scope.py` when `--apply-to` is absent); `scope-verify`, `verify --cells`, `--apply-to`, `jq`, and the summary are Python and jq. `affected_scope.py --apply-to` with an EMPTY accepted list and a rejections file exits 0 and the result differs from the input plan in exactly `evidence_rejections` (probed on a scratch plan), so the all-rejected overlay needs no planner change
- discovery: `verify --cells` does not expose which notes refs it walked; it walks every `refs/notes/ci-local/<environment>` for `ENVIRONMENTS`. The refs present in the checkout after the fetch step (`git for-each-ref refs/notes/ci-local/`, minus `scope`) are exactly the ones it consults, so the summary's "consulted" list comes from there and the "matched" list from the reused cells' `environment` in the written plan. Rejections describe CANDIDATES (a note, or one cell of it), not the plan's cells, per the verifier's own docstring, so the retained row renders the coded reasons (text before the first `:`) with counts; the full strings stay in the plan artifact
- design decision (toolchain): option (a) — `rustup show` moves INTO the calculate step, on the receipt-miss branch only, immediately before the selection run; the separate step is deleted from the scope job. `workflow_dispatch` and a branch-creating push take the same miss branch, so they still materialize the toolchain. The contract test `required_ci_honors_the_toolchain_file_without_stable_override` needs `ci.yml` to CONTAIN `rustup show`, which it still does; `test_runner_loss.py`'s `Set up the pinned Rust toolchain` stage case stays real via `_package-ci.yml`
- design decision (harness): a single-step extraction cannot fail the hit test against the ORIGINAL workflow (its `rustup show` sits in a step the harness never ran), so `run_step` now runs EVERY `run:` step of the `scope` job in order, one `bash -c` per step as GitHub does, honoring `continue-on-error` (the notes fetch fails harmlessly in the fixture, which has no `origin`); a `rustup` stub first on PATH and the planner trampoline append to ONE shared call log, so order is provable and a hit's counter is 0 only if no step in the job invoked it. `workflow_step_script` stays for the gate suite
- implemented (workflow, `ci.yml` scope job): the `Set up the pinned Rust toolchain` step is removed from the scope job (a comment at its old position says why); `rustup show` now sits on the receipt-MISS branch of the calculate step, immediately before `affected_scope.py` selection — the one consumer of `cargo metadata` — so `workflow_dispatch` and a branch-creating push still set it up and a hit never does. The overlay condition is now "accepted list non-empty OR `evidence-rejections.json` is a non-empty JSON list", so an all-rejected verification still publishes `evidence_rejections` through `--apply-to` (empty accepted list); a verification that saw no evidence at all still leaves the plan and the planner untouched, and finding 5's fallback (which rewrites the accepted list to `[]` and never reaches the overlay) composes unchanged. Four summary rows added directly under `scope source`/`evidence processing`, all read from the WRITTEN `resolved-plan.json` with `jq`: `validation environments` (`consulted: <refs under refs/notes/ci-local/ minus scope>; matched: <environments of reused cells>`, `none` with no refs, `n/a (workflow_dispatch runs no verifier)` on a manual run), `reused passing cells` and `reused failing cells` (`N: pkg/env/gate, …` or `none`, split on `.evidence.outcome`), `cells retained (evidence incomplete or rejected)` (`N rejection(s): code (k), …` or `none`). Honesty note recorded in the step comment: the verifier does not report the refs it walked, so "consulted" is the fetched refs it walks by construction, and rejections describe candidate notes, not plan cells, so they are counted by code with the full strings left in the plan artifact
- implemented (harness, `scripts/ci/test_ci_local.py`): `workflow_job_run_steps(workflow, job)` parses every `run:` step of a job (one-liner or block, `continue-on-error`, `uses:` skipped) under the same fixed-layout reading as `workflow_step_script`, which stays for the gate suite; `WorkflowScopeStepTests` now runs the scope job's run steps through `Calculate package and area scope` in order, one `bash -c` per step, stopping on a failing non-`continue-on-error` step (the notes fetch fails harmlessly: the fixture has no `origin`). A `rustup` stub first on PATH and the planner trampoline both append to one shared `tool-calls.log` (`TEST_CALL_LOG` replaces `TEST_PLANNER_LOG` in this suite only; the ci-local recipe suite and the hook suite keep theirs); `StepRun` exposes `calls`, `planner_calls` (prefix-stripped, so every existing assertion is unchanged), `rustup_calls`, and `summary_row(field)`. `publish_validation_receipt` gained `environment`, `outcome` (a failure names its failing test so the cell records `complete`), and `mutate`; `run_step` gained `evidence=[{…}]` beside the existing `validation_receipt=True`
- tests added (`WorkflowScopeStepTests`, L1): `test_a_receipt_miss_sets_up_the_toolchain_once_before_selection` (shared log is exactly `rustup show` then one non-`--apply-to` planner call), `test_a_hit_with_accepted_evidence_sets_up_no_toolchain_and_reports_the_reuse` (`rustup_calls == []`, one `--apply-to` call, rows `consulted: macos-latest; matched: macos-latest` / `1: biscuit-hash/macos-latest/L1` / `none` / `none`), `test_all_rejected_evidence_still_publishes_its_rejections` (receipt mutated to `completion: interrupted` → plan `evidence_rejections` is one `incomplete-run: the macos-latest run is interrupted…`, `accepted_evidence == []`, the L1 cell still `execute`, one `--apply-to` call, retained row `1 rejection(s): incomplete-run (1)`), `test_reused_passing_and_failing_cells_are_listed_separately` (macOS pass + ubuntu fail receipts: both cells `reuse`, rows list them apart, `matched: macos-latest, ubuntu-latest`). Extended: the existing hit test asserts `rustup_calls == []`; the manual-run test asserts `["rustup show"]` and the `n/a` environments row; `assert_unmodified_plan_and_nothing_reused` (finding 5's three fallback tests) asserts the four rows read `consulted: macos-latest; matched: none` / `none` / `none` / `none`
- non-vacuity: the 21-test suite under `CI_WORKFLOW_UNDER_TEST=/tmp/ci.f6.original.yml` (byte copy taken before any edit, outside the repository) — 8 fail: both hit tests on `[] != ['rustup show']` (the original's separate toolchain step fires, which the multi-step harness now sees), the all-rejected test on `1 != 0` rejections (the original overlays only with a non-empty accepted list), and five on a missing summary row (`'' != …`: the manual-run, mixed-reuse, and finding 5's three fallback tests). `test_a_receipt_miss_sets_up_the_toolchain_once_before_selection` passes on the original as well, deliberately: the miss path's toolchain-then-selection order is unchanged, and that test guards that the move dropped nothing from it
- verification: `python3 scripts/ci/test_ci_local.py` → Ran 52 tests, OK (48 + 4); `python3 -m unittest discover -s scripts/ci -p 'test_*.py'` → Ran 455 tests, OK (451 + 4); `actionlint .github/workflows/ci.yml` → exit 0, no findings (0 before, 0 after); `cargo nextest run -p test-toolkit --test ci_workflow_contracts` → 90 passed (90 before; `required_ci_honors_the_toolchain_file_without_stable_override` still finds `rustup show` in `ci.yml`, and `test_runner_loss.py`'s `Set up the pinned Rust toolchain` stage case stays real through `_package-ci.yml`)
- docs: `.github/ci/README.md` (scope-verify paragraph: toolchain only on a miss, the four summary rows and where they come from), `.claude/skills/rust-devops/ci-cd.md` (the "Live as of 2026-09-11" bullet), `docs/topics/ci-cd.md` (the scope-receipt paragraph). No document said the scope job always sets up Rust, so nothing was corrected, only extended
- deferred: none. No Rust symbol was edited, so no GitNexus impact run was warranted; nothing was committed
- work completed for 'The receipt fast path still initializes Rust and omits required diagnostics' at 11:03:12
- starting the work on 'Real workflow presentation remains unverified at the required boundary' at 11:03:53
- deferred, with reason: the hosted fixture itself. Ruling B5 authorizes the throwaway branch, but this implementation session is forbidden to commit, push, or open a pull request, and a hosted fixture is exactly a commit, a push, and a pull request — B5 authorizes the fixture, the session does not authorize the git operations. No hosted observation is claimed for AC2/4/8/9/10/12; the finding stays open at the hosted boundary. The producer-normalization regression (finding 1, rollout final section) shares the same blocker and the same branch
- written: `fixes/2026-09-11-cicd-cleanup/fixtures/throwaway-branch-plan-2026-09-12.md` (`kind: fixture-plan`, `status: awaiting-push-authorization`) — the observations owed mapped to AC2/AC10, AC4, AC9, AC8/AC12, and the cross-referenced normalization cases; the reduction; the push sequence; the recording template (run id, PR, commit/case, check name as displayed, conclusion, `ci-gate`, merge box, plus per-observation pass/fail lines) to be saved as `fixtures/throwaway-branch-<date>.md`; the prerequisites (the four shipped workflows and the scripts they call, `gh` auth, clean checkout, empty constraint store, branch name `fixture/ci-presentation-<date>`, deletion after capture)
- discovery (planner, run read-only against the candidate file set): `claudine/rendezvous/core/src/lib.rs` + `worktree/cli/src/main.rs` + `.github/ci/ci-baseline.toml` resolves to two areas, 15 cells, 2 of them `accepted-gap` (`worktree-cli` L2 on windows-latest and wsl2-ubuntu, governed `tmux`); `rendezvous-core` is the leanest nested-area package (lib only, 88 L1 tests, no example/bench) and its ubuntu check cell carries the three OQ1 dependents (`claudine-cli`, `rendezvous-client`, `rendezvous-daemon`); `worktree-cli` is the smallest gap-bearing package (75 L1 + 2 L2 tests)
- discovery (cost facts the reduction cannot remove): there is NO per-package `wsl = false` — `affected_scope.py` schedules `wsl2-ubuntu` L1 for every tested package (`"wsl": testing and …`), so the rollout final section's "`wsl = false`" premise names a manifest key that does not exist; `.github/ci/` is a `CI_TOOLING_PREFIXES` entry, so the baseline edit selects the `ci-tooling` job; `preflight_os` is all three runners. Recorded in the plan as the price of the baseline case rather than hidden
- discovery (recipe corrections, recorded in the plan and flagged in rollout §8): pinning the failure with `#[cfg(target_os = "linux")]` would also fail `wsl2-ubuntu` (the guest runs the Linux archive) and case 3's `compile_error!` would break the WSL archive build — a setup failure the normalization deliberately does not excuse; the plan pins it on `BISCUIT_CI_ENVIRONMENT == "ubuntu-latest"` at runtime (stamped per leg in `_package-ci.yml`/`_wsl-ci.yml`, unset in the archive job and locally) and uses `rendezvous-core`'s existing `build.rs` for case 3. Case 4 as written cannot be produced from this macOS host (the failure never fires here); the plan makes the assertion also fail when the variable is unset and adds a `macos-latest` baseline entry, pushing with `RUSTY_BISCUIT_PRE_PUSH=warn` (D1 publication is in the working tree's hook)
- discovery (push sequence): with no pull request open the hook plans a provisional run against remote `main`, and `feat/unifi`'s workflow/`scripts/ci/` changes are global inputs, so the local gates would try to run the full workspace; the plan therefore pushes a docs-only commit in `scope-only` mode, opens the draft PR against `feat/unifi` (run 0: nothing selected, static-name skip), and only then pushes the case-1 tree from a clean checkout so the hook runs the two packages' gates here and publishes `refs/notes/ci-local/macos-latest` and `refs/notes/ci-local/scope` (run 1: the reused cells). Five runs total, none full-scope
- updated: `rollout-2026-09-11.md` — one paragraph at the top of §8 naming the two remaining hosted proofs, their single blocker, and the plan file; frontmatter `status: not-triggered` unchanged and still true. `fixtures/scratch-2026-09-12.md` — one sentence at the end of its final section pointing at the plan
- drift noted, not edited (outside this finding's small-edit brief): rollout §8 step 7 still says "Confirm `ci-verdict` reports"; the gate is `ci-gate` since 2026-09-12
- no test was run, no workflow or code file was edited, nothing was committed
- work completed for 'Real workflow presentation remains unverified at the required boundary' at 11:15:02
- steering received at 11:16 from Ken's other session (`feat-unifi-8f`): finish finding 6, then stop without starting findings 7 or 8, close the cycle normally with 7 and 8 recorded as deferred, and write a handoff section
        - finding 6 had already completed (11:03:12) and finding 7's subagent had already finished (11:15:02) when the message arrived; finding 7 produced planning documents only (no code, no workflow edit, no hosted run), which is exactly the deferred state the steering asks for, so nothing is undone
        - finding 8 was not started
- verification totals as they stand at 11:19, run by the orchestrator over the whole working tree:
        - `python3 -m unittest discover -s scripts/ci -p 'test_*.py'`: 455 tests, OK (432 at the start of the cycle)
        - `env -u CDPATH GIT_TERMINAL_PROMPT=0 /bin/bash ./.githooks/tests/test-pre-push.sh </dev/null`: 62 passed, 0 failed (57 at the start)
        - `cargo nextest run -p test-toolkit --test ci_workflow_contracts`: 90 passed (84 at the start)
        - `cargo nextest run --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup`: 192 passed (186 at the start)
        - `cargo nextest run --manifest-path scripts/Cargo.toml --bin ci-plan`: 9 passed
        - `cargo clippy --manifest-path scripts/Cargo.toml --no-default-features --bin ci-rollup --all-targets -- -D warnings`: clean
        - `shellcheck .githooks/pre-push`: clean
        - `actionlint` on `ci.yml`, `_area-ci.yml`, `_package-ci.yml`, `_wsl-ci.yml`, `ci-infra-retry.yml`: 6 findings, all pre-existing SC2086 (`_package-ci.yml:193,326,543,716,892`; `_wsl-ci.yml:179`), none introduced by this cycle (W13, finding 8)

### Successful Completion

The implementation of review cycle 6 has completed successfully in 1 hour 58 minutes (09:22:04 to 11:20). During this implementation all 8 review findings were evaluated to see if they could be fixed as a part of this implementation cycle: 6 were fixed, 2 were deferred (see reasons below):

- **Finding 7, "Real workflow presentation remains unverified at the required boundary" — DEFERRED.** The B5 reduced hosted fixture is a commit and a push of a throwaway branch of this repository, which triggers a `ci` run on this branch. That needs Ken's explicit authorization under the branch's no-extra-runs position, and this implementation session is forbidden to commit, push, or trigger CI. What it still owes, each named in `fixes/2026-09-11-cicd-cleanup/fixtures/throwaway-branch-plan-2026-09-12.md` with its acceptance criterion: the nested-area labels through the four-level reusable-workflow chain (AC2, AC10); a reused cell with no producer job but a visible completed result in the area rollup grid (AC4); gap and no-gap publisher routing, `neutral` check runs landing on the pull-request head, and the plan artifact download without `actions: read` (AC9); the mixed reused/executing/failed/unselected presentation and the `ci-gate` result (AC8, AC12); and the four producer-normalization cases finding 1 owes (baselined producer failure accepted, the same failure unaccepted, a missing report, an equivalent reused failure), recorded in the final section of `rollout-2026-09-11.md`. Only the plan, the recording template, and the reduction were written this cycle; no hosted observation is claimed.
- **Finding 8, "Absorbed audit work and active documentation remain unresolved" — DEFERRED.** Ken is routing the audit-disposition and documentation sweep to a separate implementer (steering at 11:16), so it was not started. Still open, by path: W2 (`scripts/ci/local_evidence.py:474` `record_scope` rejects a non-ancestor base; `.githooks/tests/test-pre-push.sh:1680` `test_an_advanced_target_branch_is_read_from_the_remote_and_records_no_receipt` pins the withheld receipt); W6 (`local_evidence.py:483` still writes the legacy `scope` projection beside `plan`); W7 (lint cells default to `reusable: true` in `affected_scope.py` `package_cells`, unlike `check`); W8 (`.githooks/pre-push` has only EXIT traps, no interruption policy, and no test proves an interrupted run cannot publish complete evidence); W9 (`RECORDABLE_GATES` at `local_evidence.py:62` still names `browser` with no recorded decision); W11 (`.githooks/pre-push:118` still says `Expected: off, warn, or strict`); W12 (`local_evidence.py:983` `verified_environment` and the four `test_today_*` tests in `scripts/ci/test_evidence_reuse.py:307,531,538,852`); W13 (the six SC2086 findings listed above); T3 (behavioral area-override test asserting no validation note), T4 (validation receipt under `workflow_dispatch` at the step level), T5 (malformed scope receipt at the step level); R10/AC14 (`README.md:117` still says the hook compile-checks direct reverse dependencies, `README.md:129` still says `off` skips validation entirely, `docs/testing-strategy.md` "What is in flight" at line 672 and the retired-model paragraphs near 601 and 639). W1, W3, W4, W5, W10, W14 and T1, T2 were closed by findings 2, 3, 5, and 6 of this cycle.

No finding was deferred for a performance-measurement reason, so `deferred_perf_measurement` is not set.

The files this cycle touched (planning documents that were already dirty at `a8dbd1a61` and untracked files from other sessions, such as `process-review.md` and `prompts/_agent-skills.md`, are excluded):

        - `.githooks/pre-push`
        - `.githooks/tests/test-pre-push.sh`
        - `.githooks/tests/fixtures/plan-macos-two-packages.json` (new)
        - `.github/ci/README.md`
        - `.github/ci/ci-baseline.toml`
        - `.github/ci/schemas/contract.json`
        - `.github/ci/schemas/README.md`
        - `.github/workflows/ci.yml`
        - `.github/workflows/_area-ci.yml`
        - `.github/workflows/_package-ci.yml`
        - `.github/workflows/_wsl-ci.yml`
        - `.claude/skills/rust-devops/ci-cd.md`
        - `CLAUDE.md` (one clause about the check cell)
        - `docs/topics/ci-cd.md`
        - `just/ci-local.just`
        - `justfile` (the `pre-push` recipe doc comment)
        - `scripts/ci-rollup.rs`
        - `scripts/ci-rollup-tests.rs`
        - `scripts/ci/affected_scope.py`
        - `scripts/ci/schema.py`
        - `scripts/ci/runner_loss.py`
        - `scripts/ci/test_affected_scope.py`
        - `scripts/ci/test_ci_local.py`
        - `scripts/ci/test_resolved_plan.py`
        - `scripts/ci/test_runner_loss.py`
        - `tools/test-toolkit/tests/ci_workflow_contracts.rs`
        - `fixes/2026-09-11-cicd-cleanup/fixtures/throwaway-branch-plan-2026-09-12.md` (new)
        - `fixes/2026-09-11-cicd-cleanup/fixtures/scratch-2026-09-12.md`
        - `fixes/2026-09-11-cicd-cleanup/rollout-2026-09-11.md`
        - `fixes/2026-09-11-cicd-cleanup/review-6.md` and `log.md` (metadata)

### Handoff

- **Implemented and verified (findings 1 through 6)**, totals as of 11:19 listed above. Each finding's non-vacuity proof is recorded in its own bullets earlier in this section.
- **Left partially done or deliberately deferred inside findings 1 through 6:**
        - finding 1: the hosted four-case regression (runbook: `rollout-2026-09-11.md`, final section); its case 4 depends on finding 2's strict-failure note publication, which is now in the tree. A behavioral consequence to know: `scripts/ci/runner_loss.py` `decide` now reruns a lost job beside an area whose producer is green with a failed gate step, because a normalized producer no longer vetoes the retry (pinned by the new test in `test_runner_loss.py`).
        - finding 3: `just/ci-local.just` `cell_runs_here` reruns a cell reused from a FAILURE even when its environment is constraint-prohibited, because `affected_scope.py` `mark_reused` drops the prohibition; the constraint protects the hosted trigger, not the local rerun. Recorded, not changed.
        - finding 4: by the OQ1 ruling, hub-crate duration (darkmatter attributes 10 dependents, playa 4) is measured in the first real run; no `[package.metadata.ci]` declared-dependent list; the dependents half is not executed locally by `ci-local`.
        - finding 5: no new `evidence_rejections` code for a verifier crash; the fallback is a `::warning::` plus the `evidence processing` summary row, because injecting a code would need a second `--apply-to` call from the tool that just failed.
        - finding 6: the `validation environments` summary row derives the consulted set from the fetched `refs/notes/ci-local/*` refs, because `local_evidence.py verify` does not report which refs it walked; rejections are counted by code because they describe candidate notes, not plan cells.
- **What findings 7 and 8 still require** is itemized in the deferral bullets above; the fixture plan names the package pair (`rendezvous-core` plus `worktree-cli`) and the push sequence, and records three planning facts: no per-package `wsl = false` exists, a baseline edit selects `ci-tooling`, and the rollout's `cfg`-gated failure recipe would also hit the WSL leg and cannot be reproduced from a macOS host for case 4.
- **Facts the next implementer needs:**
        - Producer status JSON is unchanged in shape; only the MEANING of `result` moved from "job status" to "the cell's conclusion" (`failure` when the job is `success` and a gate outcome is `failure`; `job.status` otherwise). `ci_workflow_contracts.rs` pins the exact set of normalized steps (`GATE_STEPS`) and executes every shipped status script under Bash across six status/outcome combinations, so adding a gate step means extending that set.
        - `ci-local` reads `--plan-in`/`BISCUIT_CI_PLAN_IN`; the hook exports it only on a clean checkout with no `RUSTY_BISCUIT_PRE_PUSH_AREAS` override, from `HEAD_REVIEW_FILE` (first context of the head update), and records the receipt from `HEAD_PLAN_FILE` with `HEAD_PLAN_BASE`. `CiLocalTests`/`PlanSurfaceTests` scrub `BISCUIT_CI_PLAN_IN`/`BISCUIT_CI_PLAN_OUT` because the recipe's self-test loop runs `test_ci_local.py` inside every push.
        - Three suite override knobs exist for non-vacuity runs: `PRE_PUSH_HOOK_UNDER_TEST` and `CI_LOCAL_RECIPE_UNDER_TEST` (hook suite) and `CI_WORKFLOW_UNDER_TEST` (`WorkflowScopeStepTests`, which now runs every `run:` step of the `scope` job in order with a `rustup` stub counting invocations).
        - The planner's package record carries `dependent_seam = {dependents, check_args}` and the `ubuntu-latest` check cell carries `dependents`; `schema.py` rejects a seam naming a package that holds its own record. `.github/ci/schemas/contract.json` was regenerated without a version bump (optional fields only).
        - The GitNexus index was 94 commits behind HEAD at review time; impact on Python, YAML, and shell edits resolves UNKNOWN, so each subagent confirmed callers by text search instead.
