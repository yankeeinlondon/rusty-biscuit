## Best Practices

- when rendering to the terminal always use `biscuit-terminal` [components](@darkmatter/docs/components/) when possible!
    - the `Prose` component allows a very simple syntax to render colors, OSC8 links, and more
    - the `Table` component allows high quality table rendering in the terminal
    - the `UnorderedList` component allows 
