# Tabby

> Provides a tabbed HTML page which with tabs which navigate between a set of related content.

## Modules

1. Library

    A Rust library which provides an API surface to create and open a Tabby HTML page.

2. CLI

    A CLI application which will create and open your configured tabby interface.


