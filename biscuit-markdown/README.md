# Biscuit Markdown

A library for rendering basic markdown syntax to either:

1. the terminal
2. the browser
3. an AST representation

> Note: this was designed for pure Markdown rendering which has none of the additional features of Darkmatter
