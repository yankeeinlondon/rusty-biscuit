# biscuit-location

We need to build a new package area called `biscuit-location` who's primary focus is to:

1. Try to use the host's GPS capabilities (if present)
2. Use a local database to resolve an IP address to a geography
3. Allow bare longitude, latitude coordinates to be converted into a useful 
