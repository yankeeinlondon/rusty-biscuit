# Biscuit Location

## Features

Provides location based services to programmatic callers via it's library and via the terminal via it's CLI, including:

- IP to Location services (using local Maxmind DB)
- Using the GPS on the host device
- Converting lat/long to city, state, etc.
- Calculate the distance between two locations
- Generate a link to static Google Map of a location
