# Link Validation

The Link Validation operation is used to ensure that all linked references -- both _hyperlinks_ and _image references_ -- are validated. This operation, unlike most others, does not typically mutate the page's content in any way but instead acts as a binary checker on whether the page "passes" or "fails" and when a failure occurs it exits with an error.


