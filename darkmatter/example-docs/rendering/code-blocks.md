---
style:
    page:
        top-margin: 1
        left-margin: 2ch
        right-margin: 2ch
    
---
# Blocks Test

This Markdown has a series of code blocks to test that functionality.

## Rust

```rust
pub struct FooBar {
    foo: String,
    bar: u32
}
```

## Typescript

```ts
type FooBar = {
    foo: string;
    bar: number;
}
```

## YAML

```yaml max-width=50% title="Who loves ya Baby"
$schema:
    # to foo, or not to foo
    foo: string
    bar: number
```


---

## Summary

| Prop | Analysis |
|:----:|:-----    |
| `foo` | very talky, can't get a word in edgewise | 
| `bar` | hyper focused on numbers, hard to talk to |
| `foobar` | huh? mix them together? that's a terrible idea |
