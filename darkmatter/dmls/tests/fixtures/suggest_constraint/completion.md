---
$schema:
  color: string(suggest(red, green, blue))
  settings: "{ mode: string(suggest(fast, slow)) }"
  tags: string(suggest(alpha, beta))[]
  ratios: number(suggest(0.25, 0.5, 1))[]
color: gr
settings:
  mode: sl
tags:
  - al
ratios: [0., ]
---

# Completion positions
