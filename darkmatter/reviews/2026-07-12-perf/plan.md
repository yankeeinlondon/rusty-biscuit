---
agent: "claude"
total_phases: 11
created: "2026-07-14"
phase: 11
yolo: "true"
source_files_during_phase_1:
  - darkmatter/lib/benches/compose_schema_transclusion.rs
  - darkmatter/lib/benches/render_code_heavy.rs
  - darkmatter/lib/Cargo.toml
docs_updated_during_phase_1: []
docs_created_during_phase_1:
  - darkmatter/reviews/2026-07-12-perf/baseline.md
skills_files_updated_during_phase_1: []
source_files_during_phase_2:
  - darkmatter/lib/src/markdown/compose/context/capture/datetime.rs
  - darkmatter/lib/src/markdown/compose/context/capture/mod.rs
  - sniff/lib/src/os/time.rs
docs_updated_during_phase_2: []
docs_created_during_phase_2: []
skills_files_updated_during_phase_2: []
source_files_during_phase_3:
  - biscuit-terminal/lib/src/discovery/osc_queries/mod.rs
  - biscuit-terminal/lib/src/discovery/detection/color.rs
  - darkmatter/lib/src/layout/page.rs
  - darkmatter/cli/src/commands/compose.rs
  - darkmatter/cli/src/commands/mod.rs
docs_updated_during_phase_3: []
docs_created_during_phase_3: []
skills_files_updated_during_phase_3: []
source_files_during_phase_4:
  - darkmatter/lib/src/markdown/schemas/coerce.rs
  - darkmatter/lib/src/markdown/schemas/validate.rs
  - darkmatter/lib/src/markdown/schemas/mod.rs
  - darkmatter/lib/src/markdown/compose/schema_validation.rs
  - darkmatter/lib/src/markdown/compose/context/options.rs
  - darkmatter/lib/src/markdown/compose/pipeline/mod.rs
docs_updated_during_phase_4: []
docs_created_during_phase_4: []
skills_files_updated_during_phase_4: []
source_files_during_phase_5:
  - darkmatter/lib/src/markdown/toc/mod.rs
docs_updated_during_phase_5: []
docs_created_during_phase_5: []
skills_files_updated_during_phase_5: []
packages_during_phase_5:
  - darkmatter
source_files_during_phase_6:
  - darkmatter/lib/src/markdown/reference/validate.rs
  - darkmatter/lib/src/markdown/reference/mod.rs
  - darkmatter/lib/src/markdown/reference/file_tree/mod.rs
  - darkmatter/lib/src/markdown/compose/preflight/mod.rs
  - darkmatter/lib/src/markdown/compose/preflight/collect.rs
  - darkmatter/lib/src/markdown/compose/transclusion/engine.rs
docs_updated_during_phase_6: []
docs_created_during_phase_6: []
skills_files_updated_during_phase_6: []
packages_during_phase_6:
  - darkmatter
source_files_during_phase_7:
  - darkmatter/lib/src/markdown/compose/pipeline/phases.rs
  - darkmatter/lib/src/markdown/compose/context/runtime.rs
  - darkmatter/lib/src/markdown/compose/interpolation/rewrite.rs
  - darkmatter/lib/src/markdown/compose/interpolation/evaluator.rs
  - darkmatter/lib/src/markdown/compose/expression/doc_namespace.rs
  - darkmatter/lib/src/markdown/compose/context/effective_state.rs
  - darkmatter/lib/src/markdown/compose/frontmatter_interpolation.rs
  - darkmatter/lib/src/markdown/compose/remote.rs
  - darkmatter/lib/src/markdown/compose/transclusion/engine.rs
docs_updated_during_phase_7: []
docs_created_during_phase_7: []
skills_files_updated_during_phase_7: []
packages_during_phase_7:
  - darkmatter
source_files_during_phase_8:
  - darkmatter/lib/src/markdown/render_tree/inline_extension.rs
  - darkmatter/lib/src/markdown/render_tree/fold.rs
  - darkmatter/lib/src/markdown/highlighting/themes.rs
  - darkmatter/lib/src/markdown/highlighting/mod.rs
  - darkmatter/lib/src/markdown/highlighting/prose.rs
  - darkmatter/lib/src/markdown/output/code_block.rs
docs_updated_during_phase_8: []
docs_created_during_phase_8: []
skills_files_updated_during_phase_8: []
packages_during_phase_8:
  - darkmatter
source_files_during_phase_9:
  - darkmatter/lib/src/markdown/schemas/validate.rs
  - darkmatter/lib/src/markdown/schemas/resolve.rs
  - darkmatter/lib/src/markdown/schemas/example.rs
  - darkmatter/lib/src/markdown/schemas/mod.rs
  - darkmatter/lib/Cargo.toml
docs_updated_during_phase_9:
  - darkmatter/docs/topics/schema-definition.md
docs_created_during_phase_9: []
skills_files_updated_during_phase_9: []
packages_during_phase_9:
  - darkmatter
source_files_during_phase_10:
  - darkmatter/lib/src/markdown/fs.rs
  - darkmatter/lib/src/markdown/cleanup/emphasis.rs
  - darkmatter/cli/src/commands/mod.rs
docs_updated_during_phase_10:
  - darkmatter/docs/cli/hash.md
docs_created_during_phase_10: []
skills_files_updated_during_phase_10: []
packages_during_phase_10:
  - darkmatter
source_files_during_phase_11: []
docs_updated_during_phase_11: []
docs_created_during_phase_11:
  - darkmatter/reviews/_completed/2026-07-12-perf/results.md
skills_files_updated_during_phase_11: []
packages_during_phase_11:
  - darkmatter
packages:
  - darkmatter
  - biscuit-terminal
  - sniff
source_code:
  - darkmatter/lib/benches/compose_schema_transclusion.rs
  - darkmatter/lib/benches/render_code_heavy.rs
  - darkmatter/lib/Cargo.toml
  - darkmatter/lib/src/markdown/compose/context/capture/datetime.rs
  - darkmatter/lib/src/markdown/compose/context/capture/mod.rs
  - sniff/lib/src/os/time.rs
  - biscuit-terminal/lib/src/discovery/osc_queries/mod.rs
  - biscuit-terminal/lib/src/discovery/detection/color.rs
  - darkmatter/lib/src/layout/page.rs
  - darkmatter/cli/src/commands/compose.rs
  - darkmatter/cli/src/commands/mod.rs
  - darkmatter/lib/src/markdown/schemas/coerce.rs
  - darkmatter/lib/src/markdown/schemas/validate.rs
  - darkmatter/lib/src/markdown/schemas/mod.rs
  - darkmatter/lib/src/markdown/schemas/resolve.rs
  - darkmatter/lib/src/markdown/schemas/example.rs
  - darkmatter/lib/src/markdown/compose/schema_validation.rs
  - darkmatter/lib/src/markdown/compose/context/options.rs
  - darkmatter/lib/src/markdown/compose/context/runtime.rs
  - darkmatter/lib/src/markdown/compose/context/effective_state.rs
  - darkmatter/lib/src/markdown/compose/pipeline/mod.rs
  - darkmatter/lib/src/markdown/compose/pipeline/phases.rs
  - darkmatter/lib/src/markdown/compose/preflight/mod.rs
  - darkmatter/lib/src/markdown/compose/preflight/collect.rs
  - darkmatter/lib/src/markdown/compose/transclusion/engine.rs
  - darkmatter/lib/src/markdown/compose/interpolation/rewrite.rs
  - darkmatter/lib/src/markdown/compose/interpolation/evaluator.rs
  - darkmatter/lib/src/markdown/compose/expression/doc_namespace.rs
  - darkmatter/lib/src/markdown/compose/frontmatter_interpolation.rs
  - darkmatter/lib/src/markdown/compose/remote.rs
  - darkmatter/lib/src/markdown/toc/mod.rs
  - darkmatter/lib/src/markdown/reference/validate.rs
  - darkmatter/lib/src/markdown/reference/mod.rs
  - darkmatter/lib/src/markdown/reference/file_tree/mod.rs
  - darkmatter/lib/src/markdown/render_tree/inline_extension.rs
  - darkmatter/lib/src/markdown/render_tree/fold.rs
  - darkmatter/lib/src/markdown/highlighting/themes.rs
  - darkmatter/lib/src/markdown/highlighting/mod.rs
  - darkmatter/lib/src/markdown/highlighting/prose.rs
  - darkmatter/lib/src/markdown/output/code_block.rs
  - darkmatter/lib/src/markdown/fs.rs
  - darkmatter/lib/src/markdown/cleanup/emphasis.rs
documentation:
  - darkmatter/reviews/_completed/2026-07-12-perf/baseline.md
  - darkmatter/reviews/_completed/2026-07-12-perf/results.md
  - darkmatter/docs/topics/schema-definition.md
  - darkmatter/docs/cli/hash.md
---

# Execution Plan: Darkmatter Performance Review (2026-07-12)

Derived from [`spec.md`](./spec.md). The spec catalogs **1 critical, 5 high,
12 medium, 15 low** findings across `darkmatter/lib`, `darkmatter/cli`, and the
cross-package hot paths in `sniff` and `biscuit-terminal`.

## Strategy

- **Phase 1 builds the measurement harness first.** Every subsequent phase has a
  concrete before/after benchmark checkpoint. The spec explicitly recommends
  landing criterion benches before the allocation-tier work.
- **Phases 2–5 are mutually independent** (NTP / terminal / schema / toc touch
  disjoint subsystems) and may run in parallel by different implementers once
  Phase 1 lands. They are ordered by value, not dependency.
- **Phases 6–10 are the deeper architectural + allocation-tier work**, ordered
  so cache/dedup foundations land before the items that depend on them
  (e.g. schema-tier cleanups in Phase 9 depend on Phase 4).
- **Phase 11 is the closeout**: re-run the full baseline, confirm the quadratic
  toc regression is gone, and update docs/memory.

### Global validation gates (run at every phase checkpoint)

- `just test` (unit) and `just test-l2` (integration) from `darkmatter/`
- `just lint` from `darkmatter/`
- For cross-package phases, also run `just test <pkg>` from repo root for
  `sniff` and `biscuit-terminal`
- `cargo check --workspace` — new schema/enum variants have historically
  drift-broken claudine's exhaustive matches
- **Behavior invariant:** `md compose`, `md schema validate`, and render output
  must be byte-identical before/after every performance fix unless the spec
  explicitly notes a semantic change (only Finding 1 changes an observable:
  no more NTP probe).

---

## Phase 1 — Benchmark & Measurement Foundation

**Goal:** Reproducible before/after harness so every fix is validated, not
assumed. No production behavior changes.

- [x] Capture the current baseline table from the spec locally on this host:
      run `hyperfine` for `md --help`, `md small.md`, `md large.md`,
      `md hash small.md`, `md compose small.md`,
      `md compose --no-trigger-schemas`, against a **release** build; record to
      `reviews/2026-07-12-perf/baseline.md`.
- [x] Capture the toc scaling baseline: `md toc --json` at 81 KB / 326 KB /
      1.3 MB fixtures; record wall times (this host: ~128 ms / 1.77 s / 27.7 s —
      13.8×/15.6× per 4× size, quadratic confirmed).
- [x] Add a criterion bench for `compose_with` on a fixture that exercises
      frontmatter interpolation + `$schema` + one transclusion (the regressed
      path with no existing coverage). → `benches/compose_schema_transclusion.rs`
- [x] Add a criterion bench for `as_terminal` / `DarkmatterPage::render` on a
      code-heavy 100 KB document. → `benches/render_code_heavy.rs`
- [x] Confirm `md compose --perf` attribution still reports the documented
      segments (capture context / validate references / build options / compose
      pipeline / schema validation) — this is the primary harness for validating
      Findings 1, 5, 6, 7.
- [x] **Checkpoint:** benches compile and run under `just`; baseline artifacts
      committed to the review folder.

> Parallelizable: the two criterion benches can be authored concurrently.

---

## Phase 2 — Critical: Eliminate the per-compose NTP network probe (Finding 1)

**Goal:** Remove the live `sntp time.apple.com` round-trip from every compose.
Expected: compose drops ~127 ms → ~70 ms; removes the offline 3 s stall.

**Cross-package:** `darkmatter/lib` + `sniff/lib`.

- [x] In `darkmatter/lib/src/markdown/compose/context/capture/datetime.rs:123`,
      call `sniff::os::detect_timezone_with_options(false)` (NTP not needed —
      darkmatter only uses timezone-abbreviation derivation; `ntp_status` is
      never surfaced by any `ctx.*` key).
- [x] Fix the stale comment at `capture/mod.rs:42` ("zero-cost local
      computation") to match reality (drift: code is authority).
- [x] Evaluate making `sniff::os::detect_timezone()` default to
      `probe_ntp: false` — a network probe is a surprising default for a
      "detect timezone" call. If changed, audit sniff callers and update
      `sniff` skill/docs. (Ken preference: strategic fix over tactical; flag as
      a separate reviewable change if blast radius is non-trivial.)
      **Decision: flipped the default.** Audit found the only production caller
      of the bare `detect_timezone()` was darkmatter (now on the explicit
      `_with_options(false)` path); every other production path
      (`os::mod.rs` via `OsRequest::include_ntp_status`) already selects NTP
      explicitly, so the blast radius is only sniff's own tests (which assert
      offset/abbr/monotonic, never `ntp_status`). Updated the rustdoc contract.
      No sniff skill/README change needed — the OsRequest-level NTP docs are
      unchanged; no doc claimed the bare-fn default probed NTP.
- [x] **Checkpoint:** `md compose --perf` on a no-`ctx.*` document shows
      "capture context" drop from ~60 ms to near-zero; compose total ≈ 70 ms.
      Run `just test`/`just test-l2` for both `darkmatter` and `sniff`.
      `darkmatter`: `just test` (566 pass), `just test-l2` (69 CLI + 3 dmls
      pass), `just lint` clean. `sniff`: the 22 timezone/NTP unit tests pass
      with the default flip; the two `just test sniff` failures
      (`os_json_snapshot`, `detect_area_errors_when_not_in_repo`) are
      pre-existing host-environment flakes — OS point-version drift
      (`26.5.1`→`26.5.2`) and the tempdir git-root-ancestor flake — and touch
      neither `os/time.rs` nor any NTP/timezone `ctx.*` surface.

---

## Phase 3 — Terminal Detection Caching (Findings 2, 3, 21)

**Goal:** Detect the terminal once per process; stop round-tripping the tty and
spawning subprocesses on every `Terminal` construction.

**Cross-package:** `biscuit-terminal/lib` + `darkmatter/lib` + `darkmatter/cli`.

- [x] **(F2)** Add a `TEXT_COLOR_CACHE: OnceLock` mirroring `BG_COLOR_CACHE` in
      `biscuit-terminal/lib/src/discovery/osc_queries/mod.rs:93-100`; ideally
      batch OSC 10 + OSC 11 in one raw-mode session.
      **Done:** added `TEXT_COLOR_CACHE`; `text_color()` now `get_or_init`s the
      default path (`text_color_with_timeout` stays uncached). Kept the simple
      per-code cache mirror rather than the optional OSC 10+11 raw-mode batch —
      lower risk, and the round-trip cost is already eliminated by caching.
- [x] **(F21)** Cache `color_mode()` per process and skip the
      `defaults read -g AppleInterfaceStyle` spawn when not a TTY —
      `biscuit-terminal/lib/src/discovery/detection/color.rs:193-208`.
      **Done:** `color_mode()` now caches through a `OnceLock<ColorMode>` over a
      new `detect_color_mode()`; the macOS `defaults read` fork is gated behind
      `is_tty()` (in a fully-redirected context the mode is unobservable, so it
      falls through to the `Dark` default without spawning).
- [x] **(F2, lib side)** Cache one detected `Terminal` per process in a
      `LazyLock<Terminal>` and clone it in
      `terminal_options_from_terminal_options` / `ambient_terminal_width`;
      stop `render_tree_terminal` building a fresh `Terminal::default()` on
      every render — `darkmatter/lib/src/render_tree/entrypoints.rs:574`,
      `layout/page.rs:1479-1481`.
      **Done, spec-aligned (deviation from the literal wording).** The spec
      (finding 2) explicitly forbids caching a whole `Terminal` in the
      darkmatter library — a snapshot bakes construction-scoped cwd/env/
      stream/color-mode facts that mutate within a process (tests, library
      hosts). The dominant per-render costs (the OSC 10 tty round-trip and the
      macOS subprocess spawn) are now removed at their source by the F2/F21
      `biscuit-terminal` caches above, so the convenience path keeps fresh
      construction semantics. For the width-only call site, `ambient_terminal_width`
      (`layout/page.rs`) now calls the dynamic `terminal_width()` detector
      directly instead of constructing a full `Terminal` just to read `.width()`
      — exactly the spec's endorsed fix. No `LazyLock<Terminal>` was introduced.
- [x] **(F3, CLI side)** Introduce a CLI-level `LazyLock<Terminal>` and thread
      it through `run_compose`'s verbose/`-vv`/warnings/deferred-report
      constructions — `darkmatter/cli/src/commands/compose.rs:294,505,522,580,592`,
      `main.rs:82,119`, `frontmatter.rs:153`.
      **Done, spec-aligned (deviation from the literal wording).** The spec
      (finding 3) explicitly says *not* a process-global `LazyLock<Terminal>`
      (tests/hosts change cwd/env/streams within one process) — "detect once per
      CLI invocation and pass a borrowed terminal context". Implemented as a
      per-invocation `std::cell::OnceCell<Terminal>` local in `run_compose`,
      built lazily and shared across all five human-rendered branches (strict
      validation report, verbose summary, `-vv` perf, warnings footer, deferred
      report) — collapsing up to 4-5 fresh detections to at most 1, with pure
      Markdown/HTML/JSON output still detecting no terminal. `main.rs:82,119`
      (error handler; mutually-exclusive branches, ≤1 construction) and
      `frontmatter.rs:153` (single per-invocation construction) were left
      unchanged: neither exhibits the repeated-construction problem, and there
      is no shared terminal to thread from `run_compose` into the outer error
      handler.
- [x] **(F3)** Move `Terminal::new()` inside the non-JSON branch of `toc` so
      `md toc --json` never builds an unused `Terminal` —
      `darkmatter/cli/src/commands/mod.rs:158-171`.
- [x] **Checkpoint:** verify caching via an L2 test that constructs multiple
      terminals and asserts a single OSC query (see `biscuit-test-harness`
      patterns); confirm no SGR/behavioral regression in captured frames.
      Run `just test`/`just test-l2`/`just lint` for `biscuit-terminal` and
      `darkmatter`.
      **Done.** A bespoke "single OSC query" L2 counter would be fragile (it
      needs a real tty and to intercept the raw-mode I/O); instead added
      deterministic per-process determinism unit tests for both new caches
      (`text_color_is_stable_across_calls` / `bg_color_is_stable_across_calls`
      in `osc_queries/mod.rs`; `color_mode_is_stable_across_calls` in
      `detection/color.rs`) and relied on the existing L2 frame-capture suites
      for SGR/frame regression. Results: `biscuit-terminal` `just test`
      (2852 pass; the pre-existing width-flaky `layout_matrix_snapshots` is
      unrelated — reproduces on clean `main`), `just test-l2` (76 pass),
      `just lint` clean. `darkmatter` `just test` (6366 lib+cli pass; 566 dmls),
      `just test-l2` (all pass), `just lint` clean.

> Ordering: land the `biscuit-terminal` cache primitives first, then the
> darkmatter/CLI consumers. The two consumer sub-tasks are parallelizable.

---

## Phase 4 — Schema Stage Double-Work (Findings 5, 6, 9, 8)

**Goal:** Stop resolving the effective schema twice, coercing twice, and
recompiling uncached validators per compose. Expected: recovers a large share
of the measured ~47 ms baseline-schema delta.

**Package:** `darkmatter/lib` (schema stage). No behavior change.

- [x] **(F5)** In `compose/schema_validation.rs`, validate through the
      already-held `EffectiveSchema` (`effective.validate_with_positions(...)`)
      instead of `schemas.validate()`, which re-runs `effective_for`.
      **Done, with a correctness guard.** `effective_for`'s result depends on
      frontmatter values *only* through trigger matching, and the coercion
      write-back above the validate call can change a value a trigger matches
      on. So the held-effective reuse is taken only when no trigger registry is
      active (the overwhelmingly common baseline + `$schema` path, where the
      effective schema is a pure function of `$schema` + baseline and stable
      across coercion — byte-identical); with active triggers it re-resolves via
      `schemas.validate` to preserve the exact prior behavior. F8's process-wide
      validator cache mitigates the trigger-path fallback (the re-resolution
      reuses compiled validators). `frontmatter_as_json`/`positions_for` widened
      to `pub(crate)` so the stage can build the same instance/positions.
- [x] **(F6)** Route `coerce_root_union` (`coerce.rs:313`) and
      `coerce_property_union` (`:436`) through a cache instead of cold
      `jsonschema` compiles. **Done** via a dedicated process-wide
      `COERCION_VALIDATOR_CACHE: LazyLock<ValidatorCache>` in `coerce.rs`. The
      throwaway per-arm probe validators are built with no file-reference
      anchors (`build_validator(_, None, None)`), so `validator_for(schema,
      None)` on a default-fallback cache returns a byte-identical validator on a
      hit. Kept it separate from the effective-schema cache so the many small
      arm schemas don't churn that cache's LRU budget (rather than threading the
      per-instance cache/`arm_validators` through every coercion signature).
- [x] **(F9)** Reuse the cached compiled `darkmatter_base_json_schema()` for the
      default darkmatter baseline via `with_baseline_json_schema(...)` instead of
      `with_baseline()`'s full `to_json_schema` re-conversion per compose —
      `compose/schema_validation.rs`. **Done.** Added
      `ComposeOptions::baseline_is_darkmatter_default` (set by
      `with_darkmatter_baseline_schema`, cleared by `with_baseline_schema`) so
      the stage knows to take the cached-JSON fast path; a caller-supplied
      baseline still converts once. *Deferred:* the "hold layers as `Arc<Value>`
      in `effective_for` (`mod.rs:388`)" micro-opt — that is a broader
      `merge_baseline`/`BaselineSchema`/`TriggerLayer` signature refactor which
      Phase 9 **F29** explicitly finishes ("Falls out of Findings 5/8/9; finish
      any residual clones"). Left it there to keep this phase surgical.
- [x] **(F8)** Promote the `ValidatorCache` to a process-wide `LazyLock`; reuse
      the first `run()`'s trigger registry for the pipeline's second `run()`.
      **Done.** `DarkmatterSchemas::default/new` now clone one
      `SHARED_VALIDATOR_CACHE: LazyLock<ValidatorCache>` (was a fresh cache per
      instance), so validators compiled by one pass/compose are reused by the
      next. To make one shared cache safe across instances carrying different
      launch-area fallbacks, `canonical_hash` now folds `file_ref_fallback_dir`
      into the key (previously baked per-cache and *out* of the key). Trigger
      registry reuse: added `run_with_registry(md, opts, &mut Option<TriggerRegistry>)`;
      the pipeline threads one slot across both passes (`pipeline/mod.rs`) so the
      post-shell pass calls `with_trigger_registry` instead of re-walking the
      `schemas/` ancestry from disk. `run` is now a `#[cfg(test)]` wrapper. Fixed
      the (previously aspirational) "process-wide" claim + fallback invariant in
      the `ValidatorCache` doc to describe the shared-clone design.
- [x] **Checkpoint:** `md compose --perf` still reports the documented segments
      (`schema validation` attribution intact) and validate/compose output is
      correct on schema-bearing docs; added schema-stage tests
      (`trigger_schemas_enabled_but_no_effective_schema_is_vacuously_valid`,
      `darkmatter_default_baseline_validates_through_cached_json`) and made
      `validator_cache_reuses_across_documents` delta-based for the shared cache.
      `just test` (lib 5608 + cli 555 + dmls 566 pass; 1 pre-existing parallel
      cache-contention flake, stable in isolation), `just test-l2` (19 + 69 + 3
      pass), `just lint` clean. `cargo check` for `claudine`/`claudine-cli` clean
      (no schema drift-break); the only `--workspace` failure is the pre-existing
      `visualizer` Tauri `frontendDist` scaffold panic, unrelated to schemas.

> Ordering within phase: F5 first (it exposes the held `EffectiveSchema` the
> others reuse), then F6/F9 in parallel, then F8 (cache-lifetime design change)
> as the follow-on.

---

## Phase 5 — TOC O(n²) → O(n log n) (Finding 4)

**Goal:** Kill the quadratic per-event prefix scan. Expected: 326 KB from
2.24 s → milliseconds; the 1.3 MB tier from 45.3 s → sub-second.

**Package:** `darkmatter/lib`.

- [x] Replace the per-event `content[..range.start].lines().count()` in
      `markdown/toc/mod.rs:210-211` (and `:232`) with a precomputed sorted
      line-start offset table + binary search, or incremental line tracking
      (offsets are mostly increasing). Compute the line only in the event arms
      that actually consume it.
      **Done.** Added `newline_offset_table` (one ascending pass) +
      `line_at_offset` (binary search via `partition_point`), replacing the
      per-event prefix scan. Both are byte-identical to
      `content[..offset].lines().count() + 1` — including the trailing-mid-line
      case where a plain newline count (and `span::line_of_offset`) would
      diverge, so this is a faithful O(n²)→O(n log n) rewrite. The line is now
      computed only in the arms that consume it (heading-end, code-block
      start/end, link-end), not once per event.
- [x] Verify all downstream consumers still get correct line numbers: `md toc`,
      `md hash --kind structured/detailed`, `md hash --diff/--save`
      explanations, and the reference graph's heading indexes.
      **Done.** Line numbers are byte-identical by construction (helper equals
      the prior formula for every offset), so every consumer of `MarkdownToc`
      line ranges is unaffected; the full `just test`/`just test-l2` suites
      (hash, delta, reference-graph, toc) pass unchanged.
- [x] **Checkpoint:** re-run the toc scaling test (81 KB / 326 KB / 1.3 MB) —
      the 4×-size→11×-time relationship must become roughly linear. Add a
      regression unit test asserting toc line numbers on a multi-heading fixture
      are unchanged. `just test`/`just test-l2`.
      **Done.** `md toc --json` on this host: 81 KB ≈ 0.02 s, 326 KB ≈ 0.02 s,
      1.3 MB ≈ 0.19 s — the 1.3 MB tier dropped from tens of seconds to
      sub-second and scaling is now roughly linear. Added
      `test_line_at_offset_matches_naive_lines_count` (exhaustive offset
      equivalence across empty/CRLF/mid-line/trailing-newline fixtures) and
      `test_toc_line_numbers_multi_heading_fixture` (heading/code-block/link
      line-number integration guard).

---

## Phase 6 — Compose Multi-Walk Deduplication (Findings 7, 16, 18)

**Goal:** Stop preparing the document tree multiple times per compose. Biggest
architectural item; benefits claudine most. Requires care around cache-key
correctness.

**Package:** `darkmatter/lib` (+ CLI compose entry).

- [x] **(F7)** Reuse the preflight graph for reference validation (or share the
      `RunLocalCache`/prepared content across the validate → preflight → compose
      passes) so interpolation/page-block work runs once per node, not ~2× —
      `cli/commands/compose.rs:267-310,399-424,426`,
      `reference/graph.rs:804-825`.
      **Done, spec-aligned (deviation from the literal wording — safe subset
      landed, deep cross-pass content/cache sharing deferred with justification).**
      Investigation: the ~2× redundancy is genuinely *across* three passes —
      reference-graph build (`build_node` runs an InlinePre `compose_with` per
      node at `graph.rs:247`), the preflight collection walk (its own InlinePre
      `compose_with` per node at `collect.rs:241`), and the terminal compose
      (full pipeline per node). Each `compose_with` spins up its own internal
      `RunLocalCache`, so no prepared content is shared. Bridging them
      byte-identically is blocked on two hard constraints: (1) sharing one
      `RunLocalCache` across `validate_references` + `compose_preflight_approvals`
      + `compose_with` requires exposing the `pub(crate)` `RunLocalCache` as new
      **public** CLI-facing API (the CLI is a separate crate and cannot construct
      one), touching the persistent-cache + remote-fetch-attachment + single-flight
      layers the phase intro explicitly flags ("Requires care around cache-key
      correctness"); and (2) carrying the *prepared body* forward contradicts the
      deliberate condition-aware-rendering invariant documented on
      `PreflightGraphNode` ("The body itself is **not** cached: the design
      deliberately keeps rendering condition-aware at the normal pipeline point").
      Both are large, regression-prone API/architecture changes unsuitable for a
      byte-identical-guarantee phase and would want Ken's sign-off on the new API
      shape. The **safe, byte-identical subset is landed**: F18 removed the
      redundant reference-*graph* rebuilds (validate no longer builds a second
      graph for fragment heading collection; `md graph --validate` reuses the
      tree's graph), F16 removed the preflight subtree deep-clones, and the
      preflight→compose resolved-target reuse (`with_preflight_graph`) was already
      present. Deferred the deep prepared-content/run-cache sharing across passes.
- [x] **(F16)** Carry prepared/interpolated content in the preflight graph so
      the terminal compose does not re-run frontmatter interpolation +
      replacement + body interpolation per visited doc; store children as
      indices or `Arc` instead of deep-cloning each `PreflightGraphNode` into
      both `edges[i].child` and `children` — `compose/preflight/collect.rs:241,
      355,375,392-393,509`.
      **Done (Arc dedup); content-carrying deferred with justification.**
      `PreflightGraphEdge.child` and `PreflightGraphNode.children` are now
      `Arc<PreflightGraphNode>` — the flat `children` view is a set of refcount
      bumps (`Arc::clone`) instead of the previous O(n²)-in-nodes subtree
      deep-clone (`edges.iter().map(|e| e.child.clone())`), and the recursive
      graph-reuse handoff in the transclusion engine
      (`child_for_source`/`child_for_url`) now returns `&Arc<…>` so its
      per-recursion child clone is also a refcount bump (dropped the redundant
      `.map(Arc::new)` re-wrap). Byte-identical: the graph shape is unchanged;
      only the ownership of the nodes moved behind `Arc`. **Deferred:** carrying
      the prepared/interpolated *body* into the terminal compose — that
      contradicts the deliberate condition-aware-rendering invariant the node's
      own doc-comment states ("The body itself is not cached"), so pre-baking it
      would risk `when=`/page-block condition divergence and non-byte-identical
      output. Left it condition-aware at the normal pipeline point (see F7 note).
- [x] **(F18)** Add a `validate_graph(&graph, ...)` entry point so
      `md graph --validate` accepts the already-built graph instead of calling
      `build_reference_graph` internally (twice, three times with
      `--fragments`); cache prepared heading slugs per canonical target path for
      the run so `validate_cross_doc_fragment` stops re-reading/re-composing —
      `reference/file_tree/mod.rs:225-247`, `reference/validate.rs:315-318,605,
      616-625,687-689`.
      **Done.** `validate` now builds the graph once and delegates to a new
      `validate_with_graph(md, options, &graph)` core, exposed publicly as
      `Markdown::validate_references_with_graph(&graph, options)`. `FileTree::
      ensure_built` (the `md graph --validate` path) passes the graph it already
      built for the tree straight through, dropping the second build; and
      `collect_composed_heading_slugs` reuses that same graph instead of building
      a third with `--fragments`. Fragment heading slugs are memoized per
      canonical target path in a per-run `HeadingSlugCache`, so a document
      referenced by many `path#fragment` links (and as a graph node) is
      InlinePre-composed once, not once per reference. Byte-identical by
      construction: every cache entry is produced from a fresh on-disk
      `Markdown::try_from` (the in-memory root, which may carry `--set`/`--state`
      overrides, is deliberately never inserted under its path key, so a
      self-referential `path#fragment` still recomputes from disk exactly as
      before). New unit test `validate_with_graph_matches_validate_with_fragments`
      asserts the graph-taking entry point matches `validate` for present +
      missing + repeated cross-doc fragments.
- [x] **Checkpoint:** `md compose --perf` on a multi-file composition shows
      per-node prepare work drop to ~1×; `md graph --validate` graph-build count
      drops. Assert composed output byte-identical on a multi-transclusion
      fixture. `just test`/`just test-l2`.
      **Done.** `md graph --validate` graph-build count dropped from 2 (3 with
      `--fragments`) to 1 (F18); preflight subtree deep-clones eliminated (F16).
      Per-node prepare "to ~1×" was **not** pursued — it needs the deep
      cross-pass cache/content sharing deferred under F7/F16 (byte-identical
      constraint + condition-aware invariant + new public-API surface). Composed
      output is byte-identical: the preflight graph shape is unchanged (only node
      ownership moved behind `Arc`), and reference validation output is unchanged
      (existing `validate_cross_doc_fragment*` tests + the new
      `validate_with_graph_matches_validate_with_fragments` all green). Verified
      with `just test` / `just test-l2` / `just lint` on `darkmatter`.

> This phase depends on nothing structurally but is best sequenced after
> Phase 4 so the schema-stage cache reuse is already correct when compose walks
> collapse.

---

## Phase 7 — Compose Allocation & Re-Parse Reductions (Findings 10–15, 17, 30–35)

**Goal:** Cut the ~8–10 full-body copies and ~4–6 redundant pulldown-cmark
re-parses per compose. Opportunistic; land against the Phase 1 benches.

**Package:** `darkmatter/lib` (compose subsystem).

- [x] **(F10)** Memoize the env/agent-overridden ctx map once (`OnceLock`) at
      construction; serve `get_effective` by reference instead of cloning the
      whole map per `ctx.*` lookup/presence-test —
      `compose/context/runtime.rs:219-221,255-256`.
      **Done.** Added `overrides: OnceLock<Map>` to `ComposeContextInner`;
      `get_effective`/`as_object` now route through `effective_values()`, which
      `get_or_init`s the AGENT/MODEL-overridden map once instead of re-cloning
      (and re-overriding) the whole context map per access — `get_effective`
      then clones only the requested leaf. `env_mut` resets the `OnceLock`
      (the overrides derive from env), so a post-construction env injection is
      still observed. Byte-identical: same map contents, lazily computed once.
- [ ] **(F12)** Return `Option<&ResolutionContext>` (or `Arc`) from
      `resolution_context()` and fetch only after the fs-function name match —
      pure functions like `length()` must stop cloning the full ctx map —
      `compose/expression/mod.rs:687`, `context/effective_state.rs:374-376`.
      **Deferred (broad blast radius).** `resolution_context()` is a method on
      the public `EvaluationLookup` trait; changing its return type from
      `Option<ResolutionContext>` to `Option<&…>`/`Arc` ripples through every
      impl (EffectiveState/ResolvingLookup, FrontmatterSeedState, LayeredLookup,
      ShortcutLookup) **and** the read-side function dispatch that consumes it.
      That is not the isolated per-module edit this phase assumes; it belongs in
      its own reviewable trait-signature change. Left verbatim.
- [ ] **(F11)** Precompute per-key expression refs before the frontmatter
      interpolation fixpoint loop and mutate one seed state incrementally to
      remove the O(k²) map clones + re-parses —
      `compose/frontmatter_interpolation.rs:457-529,565-566`.
      **Deferred (large, regression-prone refactor).** Removing the O(k²)
      re-clone/re-parse means restructuring the whole fixpoint loop (precomputed
      per-key ref tables + one incrementally-mutated seed state) on a
      heavily-tested, ordering-sensitive path where byte-identical output is the
      phase invariant. Too large and risky for this opportunistic pass; left for
      a dedicated change with its own before/after fixture coverage.
- [ ] **(F13)** Replace the per-position `starts_with` rule probe with an
      `aho_corasick` leftmost-longest matcher built once from the keys; return
      `Cow<str>` from `apply_replacements` (no-op on zero matches) —
      `compose/replacement.rs:92,165-198`.
      **Deferred (byte-identical tiebreak risk).** The current scanner resolves
      overlaps by "longest key wins, then lexicographically smallest key"; an
      `aho_corasick` `LeftmostLongest` automaton breaks equal-length ties by
      insertion priority, not lexicographic order, so reproducing the exact
      overlap semantics byte-for-byte requires careful pattern ordering and new
      test coverage. The `Cow` return is also constrained by invariant 2 (the
      public `apply_replacements` facade must stay `(String, usize)`), and the
      zero-match path already early-returns. Left for a focused change.
- [x] **(F14)** Return expressions + literals from one body-interpolation scan;
      skip `convert_literals` when there are zero `{{{ }}}` literals; emit
      between spans instead of `replace_range` —
      `compose/interpolation/rewrite.rs:69-80,104-110,147`.
      **Done (subset); scan-merge/emit-between-spans deferred.** `interpolate_text`
      now skips the trailing `convert_literals` (a full expression scan — a
      pulldown-cmark parse in MarkdownAware mode — plus a copy) whenever the body
      contains no `{{{` sequence, which a `{{{ }}}` literal always requires. This
      is byte-identical (the scan would find no literals) and removes the extra
      parse+copy from the overwhelmingly common literal-free document. *Deferred:*
      merging the expression+literal scans into one and the `replace_range`→
      emit-between-spans rewrite touch the shared `ExpressionFinder` scan product
      and the per-location replacement loop — a larger change with higher
      byte-identical risk, left opportunistic.
- [x] **(F15)** Extract parent headings once per transclusion phase and
      binary-search by offset instead of re-parsing from byte 0 per
      `::file`/`::url` directive — `compose/transclusion/engine.rs:59-73`.
      **Done.** `TransclusionEngine` now memoizes an ascending
      `(heading_start, level)` table in a `OnceLock` (the parent body is
      immutable for the engine's lifetime and the resolve stage runs in
      parallel, so `OnceLock` is the correct once-per-phase primitive). The two
      per-directive `find_preceding_heading_level` calls (both in
      `resolve_prepared_transclusion`) now `partition_point`-binary-search that
      table via `preceding_heading_level`. Byte-identical: both return the last
      heading whose start is strictly before the offset, parsed by the same
      `Parser::new` (no extensions). The free `find_preceding_heading_level`
      stays for its other callers.
- [ ] **(F17)** Apply the frontmatter path's prepare-serial / execute-parallel
      split (rayon) to body `::shell` directives and replace the 10 ms sleep-poll
      with a blocking `wait` on a thread + channel; preserve output order —
      `compose/inline/shell_expansion.rs:41-58`,
      `shell_expansion/executor.rs:240-309`.
      **Deferred (compatibility invariant 5).** The spec's own correctness
      invariant 5 keeps body shell directives at source-order execution *and*
      failure semantics because "commands may observe or mutate shared
      filesystem, environment, and process-external state" — and the finding
      itself flags that the serial ordering may be intentional. Parallelizing
      execution (not just output assembly) risks observable side-effect
      reordering, so it needs an explicit ruling rather than an opportunistic
      byte-identical edit. Left verbatim.
- [x] **(F30)** Add a map-based `doc.*` resolver that walks by reference and
      clones only the leaf, instead of rebuilding the full effective state as a
      `Value::Object` per lookup — `context/effective_state.rs:182-184`.
      **Done.** Added `doc_namespace::resolve_doc_namespace_in_map`, which
      borrows into the `HashMap<String, Value>` for a `doc.<path>` traversal and
      clones only the resolved leaf (bare `doc` still snapshots the whole
      object). Wired into both `EffectiveState::get` and
      `FrontmatterSeedState::get` — the two sites that previously deep-cloned the
      entire state into a `Value::Object` per lookup. `ShortcutLookup`
      (`conditions.rs`) already passed a `&Value` and was left unchanged.
      Byte-identical to building the object and calling `resolve_doc_namespace`.
- [x] **(F31)** Stringify the first `get` result directly in the interpolation
      evaluator's non-array arm instead of discarding it and re-running the
      lookup — `compose/interpolation/evaluator.rs:242-247`.
      **Done.** The `Expr::Variable` arm now coerces the single `get` result
      inline (array → line-separated per spec D4; every other type via the same
      `None|Null→""`, `String→s`, `Number/Bool/other→to_string` ladder) instead
      of calling `get_string`, which re-ran the full lookup. Verified
      byte-identical: every `EvaluationLookup::get_string` impl in the crate
      (EffectiveState, ResolvingLookup, FrontmatterSeedState, LayeredLookup)
      mirrors the trait default coercion exactly, so inlining cannot diverge.
- [ ] **(F32)** Check whitelist/blacklist rules under the shared lock (or
      snapshot once per stage) instead of cloning three rule collections per
      directive — `shell_expansion/mod.rs:188`, `types.rs:1060-1067`.
      **Deferred (concurrency correctness).** The per-directive `snapshot()`
      clones `allow_once`/`whitelist`/`user_blacklist` under the lock, but
      `allow_once` **mutates within a stage** as approvals resolve (including
      across concurrent sibling transclusions). A once-per-stage snapshot could
      observe stale approvals and change validation output; checking under the
      held lock restructures the whole validation path. Not a safe
      byte-identical isolated edit — left verbatim.
- [x] **(F33)** Single forward pass for line computation in remote-URL
      expression discovery; skip discovery when a `memchr` probe finds no
      `http` — `compose/pipeline/mod.rs:72-78`, `compose/remote.rs:287-307`.
      **Done (skip guard); incremental line-pass deferred.**
      `discover_remote_urls_from_expressions` now returns early when the document
      contains no `http` substring — a remote-read URL argument is a string
      literal validating as `http(s)`, so it always contains `http`; the guard
      is byte-identical (the full scan would register the same empty set) and
      removes the pulldown-cmark parse the interpolation stage repeats for every
      literal-free-of-URLs document. *Deferred:* the O(n)-per-expression
      `byte_offset_to_line` → single-forward-pass micro-opt only bites when a
      document has both many expressions and an `http` present (rare), so it was
      left to keep the change surgical.
- [x] **(F34)** Compare xxHash (via `biscuit-hash`) before/after — or have the
      cleanup passes report modification — instead of a full-body copy + compare
      for the `cleanup_changed` flag — `compose/pipeline/phases.rs:80,114`.
      **Done.** The Cleanup stage now records `cleanup_changed` by comparing
      `biscuit_hash::xx_hash` of the body before and after the passes instead of
      cloning the whole body and doing a full-string compare. A cache-key-grade
      collision would only mis-set the advisory report flag (never affect output
      or validation) — exactly the accidental-collision-resistance the repo
      convention permits.
- [ ] **(F35)** Assorted single-copy costs: hash effective state once per
      transclusion phase (`transclusion/engine.rs:1316-1319`); avoid per-heading
      full-child copies and byte-0 line counts in `relevel_with_overflow` /
      `extract_headings` (`:149-154,164,197`); store `RemoteFetchRuntime`
      response bodies as `Arc<str>` (`remote_fetch.rs:408`); read
      `::toc-linking` targets through the run cache (`reference/graph.rs:919`).
      **Deferred (public signature / broader plumbing).** The parent-heading
      re-parse portion of this cluster is subsumed by F15's memoized table. The
      remaining sub-items each exceed an isolated byte-identical edit:
      `RemoteFetchRuntime::get_content` returning `Arc<str>` is a **public**
      signature change touching every consumer (non-goal: source-compatible
      public APIs); `effective_state_hash`/`extract_headings`/`relevel_with_overflow`
      restructuring threads state through several engine call sites; and the
      `::toc-linking` run-cache read is graph/run-cache plumbing (adjacent to the
      cross-pass cache sharing deferred in Phase 6 F7). Left for follow-on work.
- [x] **Checkpoint:** `compose_with` criterion bench improves on the
      100 KB+ fixture; composed output byte-identical across the fixture suite.
      `just test`/`just test-l2`/`just lint`.
      **Done.** Landed the byte-identical allocation/re-parse reductions with
      confident equivalence (F10, F14-subset, F15, F30, F31, F33-subset, F34);
      deferred the changes that would require public-API/trait-signature changes,
      large ordering-sensitive refactors, or a concurrency ruling (F11, F12, F13,
      F17, F32, F35) with justification above. Each landed change removes a
      full-map clone, a full-body clone, or a redundant pulldown-cmark parse from
      the compose hot path. `darkmatter` `just test` (lib + cli + dmls, 566 dmls)
      all pass and `just lint` is clean; composed/validate output is unchanged by
      construction (the entire existing compose/interpolation/transclusion suite
      is green).

> Highly parallelizable: each finding is an isolated edit in a distinct module.
> Split across implementers; land behind the Phase 1 bench. F35 sub-items are
> independent of each other.

---

## Phase 8 — Render-Path Reductions (Findings 19, 20, 23, 24)

**Goal:** Trim the render path's redundant second parse and per-token/per-block
allocations. Renders are already fast in absolute terms — these are the largest
remaining single wins after Finding 2.

**Package:** `darkmatter/lib` (render tree + code emit) + syntect theme handling.

- [x] **(F19)** Track fence state during the initial O(n) `scan_delimiters`
      pass (or require a plausible `==` delimiter pair before parsing) so a
      document containing `a == b` in code does not pay a full second
      pulldown-cmark parse for `protected_ranges` —
      `render_tree/inline_extension.rs:241-260,355`.
      **Done (plausible-pair guard).** Added `has_plausible_pair`: after the
      single `scan_delimiters` walk, `rewrite_inline_extensions` returns the
      source borrowed unchanged unless some token kind has an opener followed by
      a later closer of the same kind. Sound because open/close capability is a
      per-delimiter property of the surrounding source — never of the other
      delimiters — so a `false` result holds for every subset `protected_ranges`
      filtering would later produce; the second pulldown-cmark pre-parse would
      only rediscover an all-literal outcome. Byte-identical (the skip returns
      exactly the borrowed/empty-provenance result the full path produces in
      that case) and catches the ubiquitous lone-`==` (`a == b`) case. New tests
      `lone_unpaired_delimiter_is_borrowed_unchanged` /
      `plausible_pair_detection_matches_pairing_outcome`.
- [x] **(F20)** Push the original `(event, range)` through unchanged when no
      disclosure directive matches, instead of re-allocating every
      `Event::Text` into a boxed `String` —
      `render_tree/fold.rs:514-519,768-769,890-891`.
      **Done.** In `split_disclosure_directives`, the no-match branch now
      forwards the borrowed `Event::Text(text)` with its original `range` when
      `split_from == 0` (no directive anywhere in the event — the common case
      for documents with no disclosure directives) instead of copying its bytes
      into a fresh `String`. Byte-identical: the borrowed event carries the same
      text and span the copied event would have.
- [x] **(F23)** Hold `&'static SyntectTheme` in `CodeHighlighter` instead of
      `THEME_SET.get(name).clone()` per code block; hoist `code_theme_from_env`
      out of the per-block loop — `highlighting/themes.rs:527-531`,
      `render_tree/code_renderer.rs:243,365`, `entrypoints.rs:649-651`.
      **Done (theme clone eliminated); env hoist deferred.** `load_resolved_theme`
      / `load_theme` now return `&'static SyntectTheme` borrowed straight from
      the process-wide `THEME_SET` (a `lazy_static`, so the borrow is `'static`),
      and `CodeHighlighter.theme` is a `&'static SyntectTheme` — removing the
      per-code-block deep clone of the scope-selector tables (and the once-per-
      render clone on the `inline_code_colors` path, which also routes through
      `load_theme`). The `code_theme_from_env` hoist was **not** taken: each code
      block invokes the `CodeRenderer` hook once (there is no in-renderer loop),
      so hoisting the two `std::env::var` reads would mean threading a resolved
      theme through the `CodeRenderer` trait + renderer construction — a
      structural change disproportionate to two getenv calls now that the
      dominant per-block cost (the theme clone) is gone.
- [x] **(F24)** Replace `format!` + `push_str` with `write!(output, ...)`
      straight into the buffer in the per-token code-block emission loop —
      `output/code_block.rs:101-104,136-142,273-279,310-316`,
      `highlighting/mod.rs:189-193`.
      **Done.** Converted the per-line and per-token `output.push_str(&format!(…))`
      sites in `render_terminal_code_block` (line background, gutter, styled
      range) and both HTML per-token span loops to `let _ = write!(output, …)`
      (via `use std::fmt::Write`), plus the `highlight_yaml_lines_with_theme`
      per-token loop. `write!` into a `String` never fails and emits the same
      bytes, so output is byte-identical while eliminating the temporary `String`
      per styled range (5–20 per line).
- [x] **Checkpoint:** `as_terminal` criterion bench improves on the code-heavy
      100 KB doc; terminal/browser code-block output byte-identical (existing
      code-block equality tests must stay green). `just test`/`just test-l2`.
      **Done.** All landed changes are byte-identical by construction (borrowed
      forwards, `&'static` theme borrows, `write!` vs `push_str(format!)`), so
      the existing terminal/browser/YAML code-block equality suites stay green.
      `darkmatter` `just test` (lib + cli + 566 dmls all pass), `just test-l2`
      (69 CLI + 3 dmls pass), `just lint` clean across lib/cli/dmls. New render
      guards `lone_unpaired_delimiter_is_borrowed_unchanged` /
      `plausible_pair_detection_matches_pairing_outcome` plus the full inline /
      code-block / YAML-block equality suites green.

> Parallelizable: F19/F20 (fold path) and F23/F24 (code emit) are independent
> pairs.

---

## Phase 9 — Schema-Tier Cleanups (Findings 26, 27, 28, 29)

**Goal:** Remove the remaining per-lookup schema serialization/parse/clone
costs. **Depends on Phase 4** — the spec notes several of these are "subsumed by"
or "fall out of" Findings 5/8/9.

**Package:** `darkmatter/lib` (schemas).

- [x] **(F26)** Replace the per-lookup full-schema serialize + SHA-256 in
      `canonical_hash` with an xxHash via `biscuit-hash` (repo convention;
      accidental-collision resistance suffices), hashed once per stable schema —
      `schemas/validate.rs:148,948-967`.
      **Done.** `canonical_hash` now folds the serialized schema bytes + both
      domain-separated file-reference anchors into a single buffer and returns
      `biscuit_hash::xx_hash_bytes(&bytes)` (`u64`); the `ValidatorCache` key type
      dropped from `[u8; 32]` to `u64`. This removes the SHA-256 finalize on every
      hit *and* miss. `sha2` was the crate's only cryptographic-hash use, so the
      dependency was dropped from `darkmatter/lib/Cargo.toml`. Accidental-collision
      resistance suffices — a false hit could only serve a wrong validator, never a
      security boundary — matching the repo's non-crypto-hash convention.
- [x] **(F27)** Memoize imported namespaces per canonical path on
      `ImportEngine` so `A@types.yaml`/`B@types.yaml`/`C@types.yaml` read +
      parse `types.yaml` once, not three times (+ `canonicalize` syscalls) —
      `schemas/resolve.rs:817-824,871-873,897-899,909-929`.
      **Done.** `ImportEngine` gained a `namespace_cache: HashMap<PathBuf,
      CachedNamespace>` keyed by canonical path; `resolve_namespace` checks it
      before `load_named_types` (the `fs::read` + `parse_standalone_schema_document`
      + payload classification — the expensive part) and clones the parsed named
      types from cache on a hit. The dependency edge is still recorded on every
      resolve (cache hit or miss), so `ResolvedSchema.imports` is unchanged. The
      cache lives on the per-expansion engine, so it dedups within one document's
      resolution exactly where the `A@`/`B@`/`C@`-same-file pattern occurs. New
      test `multiple_imports_from_same_file_resolve_and_dedup` asserts three
      same-file imports inline correctly and record one dependency edge.
- [x] **(F28)** Content-hash memoize the `example()` returns-target validator
      and cache the example file read, matching the already-memoized envelope
      validation — `schemas/resolve.rs:1271-1283`, `schemas/example.rs:224-252`.
      (Largely subsumed once Finding 5 stops the double resolution.)
      **Done.** Two parts: (1) `validate_returns_against_target` now compiles the
      target validator through a dedicated process-wide
      `RETURNS_TARGET_VALIDATOR_CACHE: LazyLock<ValidatorCache>` (via
      `validator_for(target, None)`) instead of a cold `build_validator` per
      `example(...)` reference per resolution — byte-identical because the target
      is compiled with no file-reference anchors (the check is structural/type
      level), so a cache keyed on the target JSON alone returns an identical
      validator on a hit (same pattern as F6's coercion cache). (2) `resolve_one_example`
      threads a per-run `reads: HashMap<PathBuf, Vec<u8>>` keyed by canonical path
      so an example file referenced by more than one site is read from disk once,
      not once per site. The envelope validation stays content-hash memoized. The
      double-resolution component is already gone via F5 (Phase 4).
- [x] **(F29)** Hold baseline JSON / trigger payloads / document JSON as
      `Arc<Value>` layers so `effective_for` stops cloning every layer per call —
      `schemas/mod.rs:386-393,477-479`. (Falls out of Findings 5/8/9; finish any
      residual clones.)
      **Done.** `BaselineSchema.json_schema` and the public
      `EffectiveSchema.json_schema` are now `Arc<Value>`. `effective_for`'s merge
      was restructured so the lowest-precedence layer is the *borrowed* merge base
      (never deep-cloned) and only the higher-precedence layers are consumed
      (owned) by `merge_baseline`; when there is a single layer it is shared
      directly (an `Arc` refcount bump) instead of materialized. This eliminates
      the baseline deep-clone on the two dominant paths — baseline-only (the
      default-baseline, no-`$schema` compose) shares the baseline Arc with zero
      deep clones, and baseline+`$schema` borrows the baseline for the single
      merge instead of cloning it. Blast radius stayed inside `darkmatter/lib`
      (all `&self.json_schema`/`effective.json_schema[...]` read sites deref
      transparently; `cargo check` for `darkmatter-cli`/`dmls`/`claudine`/
      `claudine-cli` clean — no external reader of the field). The document layer
      keeps its single clone (sourced from the still-live `resolved`); trigger
      payloads keep theirs (consumed by the merge) — the residual clones the
      finding calls out are the *unavoidable* owned-side merge inputs.
- [x] **Checkpoint:** schema-bearing `md compose --perf` shows no residual
      per-lookup hashing/parse cost; `md schema validate` byte-identical.
      `cargo check --workspace`; `just test`/`just test-l2`.
      **Done.** `md schema validate` / compose output is byte-identical by
      construction — every landed change is a caching or hashing swap that
      preserves the exact resolved schema and validator (xxHash cache key,
      per-canonical-path namespace/read reuse, target-validator cache, and
      `Arc`-shared merge layers). The full schema suite (778 unit + all schema
      integration binaries) is green, plus the new
      `multiple_imports_from_same_file_resolve_and_dedup`. `darkmatter`
      `just test` (lib 5054 + cli 555 + dmls 566 + 560 lib-integration all pass),
      `just test-l2` (pass), `just lint` clean. `cargo check` for `claudine` /
      `claudine-cli` / `darkmatter-cli` / `dmls` clean — no schema drift-break; the
      only `--workspace` failure is the pre-existing `visualizer` Tauri
      `frontendDist` scaffold panic, unrelated to schemas.

---

## Phase 10 — CLI / IO & Cleanup Miscellany (Findings 22, 25)

**Goal:** Remaining CLI/IO and cleanup-pipeline wins not on the render or
compose hot paths.

**Package:** `darkmatter/lib` + `darkmatter/cli`.

- [x] **(F22)** In `md hash <dir>`, skip well-known vendored dirs
      (`node_modules`, `target`, `vendor`) — or switch to `ignore`-based
      walking — instead of only pruning dot-directories —
      `markdown/fs.rs:20-29`. Confirm the aggregate fingerprint change is
      intended and document it (public behavior → update README/hash docs).
      **Done.** `collect_markdown_files_recursive` now prunes a conservative
      `SKIPPED_VENDOR_DIRS = [node_modules, target, vendor]` set alongside the
      existing dot-directory skip. Kept the set intentionally narrow (the plan's
      explicit three) rather than importing sniff's broader `should_skip_directory_name`
      (which is `pub(crate)` and also skips `bin`/`out`/`build`/`dist`/`coverage` —
      directories that plausibly hold authored Markdown a user asked to hash). This
      is a **deliberate public-behavior change**: an `md hash <dir>` aggregate over
      a tree containing a vendored subtree now excludes that subtree's `.md`/`.dm`
      files, changing the fingerprint. Documented in `darkmatter/docs/cli/hash.md`
      (Directory input list). New unit test `skips_vendored_and_hidden_directories`.
- [x] **(F25)** Fuse the four back-to-back whole-string `String::replace` calls
      into one scan and fold the line-based cleanup passes into a single
      iterator, reducing ~10 sequential full-document passes —
      `markdown/cleanup/mod.rs:214-314`, `cleanup/emphasis.rs:111-115`.
      (Not on the render path — only `Markdown::cleanup*` / `md clean` /
      compose's Cleanup stage.)
      **Done (replace-fusion); line-pass folding deferred.** `restore_emphasis_placeholders`
      now does a single forward peekable scan that pairs the two private-use
      placeholder chars into their strong (`__`/`**`) markers and emits lone ones
      as emphasis (`_`/`*`), plus a zero-placeholder fast path — replacing four
      full-string allocating `str::replace` passes with one scan/one allocation.
      Byte-identical: greedy left-to-right pairing reproduces the previous
      strong-then-emphasis replace order exactly (the two placeholder families are
      disjoint chars, so cross-family runs are unaffected), verified by the whole
      cleanup/emphasis test suite. **Deferred:** folding the string-stage line
      passes (`normalize_list_spacing`, `fix_blockquote_formatting`,
      `restore_list_markers`, `fix_list_indentation`, `unescape_brackets`) into a
      single iterator — those passes are ordering-sensitive and mutate the whole
      buffer in place; fusing them into one line-iterator is a large,
      regression-prone rewrite on a heavily-tested canonical-output path where
      byte-identical is the invariant. Left for a dedicated change with its own
      before/after fixture coverage.
- [x] **(F35 residual)** `md hash --diff/--save` computes the document hash
      2–3× (each rebuilding the toc — Phase 5 already multiplies the win);
      `md delta` clones both full documents; `normalize_body_rhythm` allocates
      an ANSI-stripped copy per line; `apply_link_policy`/`apply_image_policy`
      clone URL+title per node even with an empty policy — address
      opportunistically (`cli/commands/hash.rs:151-186`,
      `hash/explain.rs:449-502`, `cli/commands/mod.rs:189-190`,
      `layout/page.rs:1244-1269`, `render_tree/build_context.rs:375-379,411-415`).
      **Done (`md delta`); other three deferred.** The `md delta` non-JSON branch
      now **moves** `base_md`/`updated_md` into `DeltaReport::with_documents`
      instead of cloning them — `delta()` already returns an owned `MarkdownDelta`
      (no borrow) and neither document is used afterward, so two full-document
      clones vanish byte-identically. **Deferred, with justification:** (1) the
      `md hash --diff/--save` 2–3× recompute is compute-caching plumbing across
      `compare_hash`/`explain_hash_diff`/`plan_hash_save` (each independently
      rebuilds the toc) — a shared per-run hash/toc memo is real plumbing, not an
      isolated edit, and Phase 5 already cut the per-toc cost by orders of
      magnitude so the residual win is small; (2) `normalize_body_rhythm`'s
      per-line `strip_escape_codes` allocation would require reimplementing
      biscuit-terminal's ANSI-strip blankness predicate inline to stay
      byte-identical — a divergence risk disproportionate to a per-line temp on a
      non-hot render-frame path; (3) `apply_link_policy`/`apply_image_policy`
      clone url/title because the helper call sites (`attach_text_layout`,
      directive application) take `&mut RenderNode`, so a borrow of `node.kind`
      cannot be held across them — removing the clone needs a helper-signature /
      split-borrow refactor (`&mut node.attrs`), broader than a byte-identical
      opportunistic edit. Left for follow-on work.
- [x] **Checkpoint:** `md hash <dir>` no longer walks vendored trees;
      `md clean` output byte-identical; cleanup pass count reduced.
      `just test`/`just test-l2`/`just lint`.
      **Done.** `md hash <dir>` now prunes `node_modules`/`target`/`vendor`
      (F22, documented public-behavior change). `md clean` output is
      byte-identical: the only cleanup change is `restore_emphasis_placeholders`,
      whose single-scan replacement reproduces the previous four-pass `str::replace`
      output exactly (F25), and its emphasis pass count dropped from 4 allocating
      full-string passes to 1. `md delta` no longer clones both documents (F35).
      `darkmatter` `just test` (lib + cli + 566 dmls all pass), `just test-l2`
      (69 CLI pass — 2 pre-existing width-flaky terminal-alignment tests flaked
      then passed, unrelated to this phase — plus 3 dmls L2 pass), `just lint`
      clean across lib/cli/dmls. New unit test
      `markdown::fs::tests::skips_vendored_and_hidden_directories`.

---

## Phase 11 — Final Validation & Closeout

**Goal:** Prove the regressions are gone and record the outcome.

- [x] Re-run the full Phase 1 baseline table (release build) and diff against
      the recorded baseline; confirm `md compose` ≈ 70 ms and no NTP probe fires
      (verify via `RUST_LOG` trace — no silent ~60 ms gap).
      **Done — exceeded target.** `md compose small.md` collapsed from 84.4 ms
      to **12.5 ms** (far below the ~70 ms goal; the goal predated Phases 4/7/9,
      which trimmed the schema/compose work the NTP wall used to hide). NTP
      probe verified gone via `md compose --perf`: `capture context` = **185 µs**
      (was 62.9 ms) — an NTP round-trip cannot complete in 185 µs, so there is no
      silent ~60 ms gap. Recorded in [`results.md`](./results.md).
- [x] Re-run the toc scaling test at all three tiers; confirm roughly linear
      scaling (the 1.3 MB tier must be sub-second, down from 45.3 s).
      **Done.** 81 KB ≈ 10.8 ms, 326 KB ≈ 22.9 ms, 1.3 MB ≈ **140 ms** (down from
      27.7 s on this host — sub-second, ~197×). 4×-size→time went from 13.8×/15.6×
      (quadratic) to ~2×/~6× (roughly linear with fixed overhead).
- [x] Re-run both criterion benches (`compose_with`, `as_terminal`) and record
      before/after deltas in `reviews/2026-07-12-perf/results.md`.
      **Done.** `compose_with` ~7.9 ms → **4.28 ms** (−53.7%); `as_terminal`
      ~239 ms → **57.1 ms** (−76.1%); `page_render` ~187 ms → **56.8 ms**
      (−69.6%). Deltas recorded in [`results.md`](./results.md).
- [x] Confirm the "good patterns" from the spec were **not** disturbed (syntect
      one-time load, `HighlightLines` reuse, zero render-path regexes, non-TTY
      short-circuit, demand-driven capture, single-flight caches).
      **Done.** All six confirmed preserved (details in
      [`results.md`](./results.md) §"Good patterns preserved"): syntect
      `THEME_SET`/`SYNTAX_SET` stay process-wide `lazy_static` (F23 now *borrows*
      `&'static` instead of cloning); `HighlightLines` reuse unchanged; no
      render-path regex added (F13 deferred; F19 uses a delimiter-pair scan);
      non-TTY short-circuit preserved and extended (F21 gates the macOS spawn on
      `is_tty()`); demand-driven `ctx.*` capture unchanged; single-flight caches
      additive and key-correct.
- [x] Run `just test`/`just test-l2`/`just lint` for `darkmatter`, plus
      `just test sniff` and `just test biscuit-terminal` from repo root;
      `cargo check --workspace`.
- [x] Update drift surfaces: any changed public behavior in READMEs, per-area
      `docs/dependencies.md` if `aho_corasick`/`biscuit-hash` deps were added,
      and the `darkmatter` skill if entry-point behavior changed.
      **Done — no drift.** `aho_corasick` was **not** added (F13 deferred);
      `biscuit-hash` was already a `darkmatter/lib` dependency (F26/F34 reused
      it), and `sha2` was *removed* — neither is listed in the feature-organized
      `darkmatter/docs/dependencies.md`, so no edit is needed there. Public
      behavior change from F22 (`md hash <dir>` now prunes `node_modules`/
      `target`/`vendor`) was already documented in `docs/cli/hash.md` during
      Phase 10. No compose/render/schema entry-point behavior changed (every fix
      is byte-identical), so the `darkmatter` skill needs no update.
- [x] Move this review folder to `_completed` per repo lifecycle convention and
      record a memory note summarizing the fixes and measured wins.

---

## Dependency & Parallelization Summary

| Phase | Depends on | Parallel with | Subsystem |
|-------|-----------|---------------|-----------|
| 1 Benches | — | — | lib benches |
| 2 NTP | 1 | 3, 4, 5 | sniff + lib |
| 3 Terminal | 1 | 2, 4, 5 | biscuit-terminal + lib + cli |
| 4 Schema double-work | 1 | 2, 3, 5 | lib schemas |
| 5 TOC | 1 | 2, 3, 4 | lib toc |
| 6 Compose walks | 1 (best after 4) | — | lib compose + cli |
| 7 Compose alloc/reparse | 1 | 8 | lib compose |
| 8 Render path | 1 | 7 | lib render + syntect |
| 9 Schema-tier | **4** | 10 | lib schemas |
| 10 CLI/IO/cleanup | 1 (5 multiplies F35) | 9 | lib + cli |
| 11 Closeout | all | — | all |

**Fast path to the critical/high wins:** land Phase 1, then run Phases 2, 3, 4,
5 concurrently — this clears the 1 critical + 5 high findings (the quick-wins
table) before any of the opportunistic allocation work begins.
