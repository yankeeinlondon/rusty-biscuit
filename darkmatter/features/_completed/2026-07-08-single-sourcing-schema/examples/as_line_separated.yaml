kind: example
parameters:
    - list:
        - a
        - b
        - c
invocation: "as_line_separated(list)"
returns: |-
    a
    b
    c
description: |-
    `as_line_separated(list)` joins a list into a newline-separated string.
