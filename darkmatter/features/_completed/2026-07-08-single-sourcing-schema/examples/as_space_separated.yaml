kind: example
parameters:
    - list:
        - a
        - b
        - c
invocation: "as_space_separated(list)"
returns: "a b c"
description: |-
    `as_space_separated(list)` joins a list into a single space-separated line.
