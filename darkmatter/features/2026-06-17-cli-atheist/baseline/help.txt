    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.33s
     Running `/Users/ken/.claudine/worktrees/rusty-biscuit/darkmatter/target/debug/md --help`
Command-line interface for the darkmatter markdown renderer.

Use `md --help` to see all available options.

Usage: md [OPTIONS] [INPUT] [COMMAND]

Commands:
  render      Render a markdown document (same as default behavior without a subcommand)
  clean       Clean up markdown formatting
  compose     Compose a document through the compose pipeline
  toc         Show markdown table of contents
  delta       Compare two markdown documents
  get         Get frontmatter properties from a markdown document
  set         Set a frontmatter property on a markdown document
  rm          Remove one or more frontmatter properties from a markdown document
  edit        Open a markdown file in your preferred editor
  validate    Validate references in a markdown document
  hash        Hash a markdown document's frontmatter and body
  schema      Schema authoring and validation commands
  graph       Visualize a markdown file's dependency graph
  code-block  Render a single code block from a file or literal content

Arguments:
  [INPUT]
          Input file path (reads from stdin if not provided, use "-" for explicit stdin)

Options:
      --theme <THEME>
          Theme for prose content (kebab-case name)

      --code-theme <CODE_THEME>
          Theme for code blocks (overrides derived theme)

      --code-block <MODE>
          Choose the code block theme variant relative to the page color mode: `inverse` (default,
          dark page -> light panel), `dark`, `light`, or `same`

          Possible values:
          - inverse: Opposite variant from the page (default): dark page -> light panel
          - dark:    Always the dark variant
          - light:   Always the light variant
          - same:    Same variant as the page
          
          [default: inverse]

      --list-themes
          List available themes

      --output <OUTPUT>
          Output format for top-level render mode (when no subcommand given)

          Possible values:
          - auto:          Auto: terminal rendering on TTY, markdown text on non-TTY
          - markdown:      Markdown text output
          - markdown-plus: Markdown output enhanced with disclosure blocks rendered as HTML
            details/summary elements. Only the browser render target produces these elements; on
            other targets this falls back to plain Markdown
          - html:          HTML output
          - json:          AST JSON output
          
          [default: auto]

      --show
          Open selected output in the default app using a temp file

      --save
          Shorthand for `clean --save` with top-level [INPUT]

      --mermaid
          Render mermaid diagrams to terminal as images. Falls back to code blocks if terminal
          doesn't support images

  -m, --margin <N>
          Margin on all sides (cells)

      --mx <N>
          Horizontal margin (left + right)

      --my <N>
          Vertical margin (top + bottom)

      --mt <N>
          Top margin
          
          [aliases: --margin-top]

      --mb <N>
          Bottom margin
          
          [aliases: --margin-bottom]

      --ml <N>
          Left margin
          
          [aliases: --margin-left]

      --mr <N>
          Right margin
          
          [aliases: --margin-right]

      --padding <N>
          Padding on all sides (cells)

      --px <N>
          Horizontal padding (left + right)

      --py <N>
          Vertical padding (top + bottom)

      --pt <N>
          Top padding
          
          [aliases: --padding-top]

      --pb <N>
          Bottom padding
          
          [aliases: --padding-bottom]

      --pl <N>
          Left padding
          
          [aliases: --padding-left]

      --pr <N>
          Right padding
          
          [aliases: --padding-right]

      --page-bg <STYLE>
          Page background style

          Possible values:
          - transparent: Transparent (default)
          - subtle:      Slightly off-background fill
          - pronounced:  High-contrast inverse fill
          
          [aliases: --page-background]

      --page-bg-color <COLOR>
          Explicit page background color (e.g. `#1e1e23` or `30,30,35`).
          
          Overrides the computed `PageBackground` color when set. Accepts a hex string (`#RGB` /
          `#RRGGBB`) or a comma-separated `R,G,B` triple in the 0-255 range. Tailwind palette names
          (`red-500`, `slate-50`, etc.) and CSS special keywords (`transparent`, `inherit`,
          `currentColor`) are also accepted.

      --max-width <N>
          Max content width in columns (0 rejected)

      --width <N>
          Intentionally unsupported width shorthand.
          
          The terminal-cell width is determined from the captured `Terminal` and the configured
          `--max-width`; a plain `--width` flag would shadow those for no reason. The flag is
          rejected with a clear error so callers know to use `--max-width` instead.

      --line-numbers [<BOOL>]
          Include line numbers in code blocks
          
          Accepts `--line-numbers` (defaults to `true`) or `--line-numbers <true|false>` for
          explicit control.
          
          [possible values: true, false]

      --alignment <ALIGN>
          Default alignment for all components

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-images <ALIGN>
          Image alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-lists <ALIGN>
          List alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-ul <ALIGN>
          Unordered list alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-ol <ALIGN>
          Ordered list alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-li <ALIGN>
          List item alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-block-quotes <ALIGN>
          Block quote alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-tables <ALIGN>
          Table alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --align-code-blocks <ALIGN>
          Code block alignment

          Possible values:
          - left:   Left-aligned
          - center: Centered
          - right:  Right-aligned

      --fill <FILL>
          Default fill for all components

      --fill-images <FILL>
          Image fill

      --fill-lists <FILL>
          List fill

      --fill-ul <FILL>
          Unordered list fill

      --fill-ol <FILL>
          Ordered list fill

      --fill-li <FILL>
          List item fill

      --fill-block-quotes <FILL>
          Block quote fill

      --fill-tables <FILL>
          Table fill

      --fill-code-blocks <FILL>
          Code block fill

      --strict-style
          Promote schema-validation warnings (unknown / deprecated keys) to errors

  -v, --verbose...
          Increase verbosity for styled user-facing output (-v summary, -vv detailed)

      --completions <SHELL>
          Generate shell completions for the specified shell
          
          [possible values: bash, elvish, fish, powershell, zsh]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version
