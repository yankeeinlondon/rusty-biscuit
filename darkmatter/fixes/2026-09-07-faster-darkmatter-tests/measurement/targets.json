{
  "note": "Every Phase 8 L1 timeout/readiness/concurrency contract runs in both five-run candidate L1 populations, yielding ten representative-load executions. Every available L2 terminal contract runs in ten canonical suite-load repetitions. L3 focus-dependent contracts are recorded separately as pending.",
  "targets": [
    {
      "binary": "darkmatter-cli::compose_remote_caching",
      "contract": "loopback HTTP accept/read/shutdown bounds, request count/content, cache freshness, refresh, TTL, consent, and error behavior"
    },
    {
      "binary": "dmls::stdio_subprocess",
      "contract": "native child protocol completion plus bounded kill-and-reap on unwind/cancellation"
    },
    {
      "binary": "dmls::lsp_session",
      "contract": "protocol-response synchronization and server-thread join across normal and unwind paths"
    },
    {
      "binary": "darkmatter::level2_render_tree_terminal",
      "contract": "real-terminal sentinel and final-frame readiness with bounded polling"
    },
    {
      "binary": "darkmatter-cli::level2_code_block_styling",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "darkmatter-cli::level2_disclosure_blocks",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "darkmatter-cli::level2_errors",
      "contract": "error rendering waits for the final settled terminal condition"
    },
    {
      "binary": "darkmatter-cli::level2_frontmatter_images",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "darkmatter-cli::level2_frontmatter_tables",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "darkmatter-cli::level2_horizontal_rules",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "darkmatter-cli::level2_layout_dimensions",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "darkmatter-cli::level2_ordered_lists",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "darkmatter-cli::level2_schema_about",
      "contract": "schema rendering waits for the final settled terminal condition"
    },
    {
      "binary": "darkmatter-cli::level2_schema_validate",
      "contract": "shared real-terminal helper observes a prompt and two settled frames"
    },
    {
      "binary": "dmls::level2_editor_neovim",
      "contract": "Neovim clear/repaint waits for final SGR state with a deadline"
    }
  ]
}
