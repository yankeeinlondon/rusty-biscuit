### Plan

Create a detailed plan from:

- Functional Specification: {{spec}}
- Technical Design Document: {{design}}
- When the plan is complete evaluate:
    - what packages in the monorepo will be impacted by the implementation of the plan? Set the `packages_impacted` frontmatter property of @{{package_area}}/features/{{feature}}/feature.md to this list of packages.
    - what package areas in the monorepo will be impacted by the implementation of the plan? Set the `package_areas_impacted` frontmatter property of @{{package_area}}/features/{{feature}}/feature.md to this list of package areas.

Save the plan to @{{package_area}}/features/{{feature}}/plan.md
