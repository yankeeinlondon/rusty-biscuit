# Blast Radius

Many Markdown documents in this repo will include a frontmatter property called "blast_radius" which is a YAML list of source code files which -- when changed -- may indicate that the document has become "stale".
