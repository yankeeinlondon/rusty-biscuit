# The `getModel(kind,desc)` utility function

The shared library for **deckhand** includes a handy utility function **getModel(kind,desc)** which:

- takes the "kind" of model which provides an enumeration of `ModelKind` which represents the sophistication of the task you are using the 
